import {
  $e,
  Ce,
  Gs,
  Jo,
  Kn,
  Ms,
  Nn,
  Nr,
  Ou,
  Qn,
  Un,
  Vs,
  Wo,
  Xo,
  Yr,
  ac,
  bi,
  bu,
  cc,
  cu,
  dc,
  du,
  fc,
  fu,
  gr,
  gu,
  hc,
  hu,
  ic,
  iu,
  lc,
  lu,
  mu,
  nc,
  oc,
  pc,
  pu,
  qn,
  sc,
  uc,
  uu,
  vu,
  xr,
  ye,
  yu,
  zo,
  zr
} from "./chunk-SH2REVY5.js";
import {
  S
} from "./chunk-32LN6IFQ.js";
import {
  $,
  Bs,
  Ga,
  It,
  Ma,
  Ps,
  Rs,
  Vs as Vs2,
  ar,
  dt,
  g,
  kt,
  qe,
  rr,
  we
} from "./chunk-JYLCFJRO.js";
import {
  f
} from "./chunk-T457C7XC.js";
import "./chunk-GKNUOLVO.js";
import "./chunk-ONELSXMF.js";
import "./chunk-K6XZ6KXP.js";
import "./chunk-3JE72L2J.js";
import "./chunk-KLBCLHEP.js";
import "./chunk-IWISGUXJ.js";
import "./chunk-R54EFXNU.js";
import "./chunk-JNHOR6SI.js";
import "./chunk-72E3F2OW.js";
import "./chunk-JHJKCYTU.js";
import "./chunk-LL7TNUZB.js";
import "./chunk-YPKTU6G3.js";
import "./chunk-YPKBKVIN.js";
import "./chunk-QGJIGGMO.js";
import "./chunk-FXNFNHXZ.js";
import "./chunk-VCPSW7YA.js";
import "./chunk-DW5YFC3V.js";
import "./chunk-IXRQDBTI.js";
import "./chunk-TCGE25WV.js";
import "./chunk-BL6D5YOI.js";
import "./chunk-3R46EL5G.js";
import "./chunk-3JZST3TF.js";
import "./chunk-Z5AKTONR.js";
import "./chunk-KJLHI4KE.js";
import "./chunk-5CMMY7LK.js";
import "./chunk-XTV2HAPF.js";
import "./chunk-YY6SLAPL.js";
import "./chunk-S73QDKPN.js";
import "./chunk-G3PMV62Z.js";

// node_modules/zudoku/lib/processors/traverse.js
var u = (y, s, c = []) => {
  const r = s(y, c);
  if (typeof r != "object" || r === null)
    return r;
  const t = Array.isArray(r) ? [] : {};
  for (const [o, e] of Object.entries(r)) {
    const l = [...c, o];
    Array.isArray(e) ? t[o] = e.map(
      (n, f2) => typeof n == "object" && n != null ? u(n, s, [...l, f2.toString()]) : n
    ) : typeof e == "object" && e != null ? t[o] = u(e, s, l) : t[o] = e;
  }
  return t;
};

// node_modules/zudoku/lib/createServer-BPz8ZCrd.js
function zo2(t, e) {
  for (var n = 0; n < e.length; n++) {
    const r = e[n];
    if (typeof r != "string" && !Array.isArray(r)) {
      for (const i in r)
        if (i !== "default" && !(i in t)) {
          const s = Object.getOwnPropertyDescriptor(r, i);
          s && Object.defineProperty(t, i, s.get ? s : {
            enumerable: true,
            get: () => r[i]
          });
        }
    }
  }
  return Object.freeze(Object.defineProperty(t, Symbol.toStringTag, { value: "Module" }));
}
var ns = Object.freeze({
  major: 16,
  minor: 11,
  patch: 0,
  preReleaseTag: null
});
function Jo2(t) {
  return typeof t?.then == "function";
}
var A;
(function(t) {
  t.QUERY = "QUERY", t.MUTATION = "MUTATION", t.SUBSCRIPTION = "SUBSCRIPTION", t.FIELD = "FIELD", t.FRAGMENT_DEFINITION = "FRAGMENT_DEFINITION", t.FRAGMENT_SPREAD = "FRAGMENT_SPREAD", t.INLINE_FRAGMENT = "INLINE_FRAGMENT", t.VARIABLE_DEFINITION = "VARIABLE_DEFINITION", t.SCHEMA = "SCHEMA", t.SCALAR = "SCALAR", t.OBJECT = "OBJECT", t.FIELD_DEFINITION = "FIELD_DEFINITION", t.ARGUMENT_DEFINITION = "ARGUMENT_DEFINITION", t.INTERFACE = "INTERFACE", t.UNION = "UNION", t.ENUM = "ENUM", t.ENUM_VALUE = "ENUM_VALUE", t.INPUT_OBJECT = "INPUT_OBJECT", t.INPUT_FIELD_DEFINITION = "INPUT_FIELD_DEFINITION";
})(A || (A = {}));
function Yo(t, e) {
  const n = new Xo2(t, e), r = n.parseDocument();
  return Object.defineProperty(r, "tokenCount", {
    enumerable: false,
    value: n.tokenCount
  }), r;
}
var Xo2 = class {
  constructor(e, n = {}) {
    const r = ar(e) ? e : new kt(e);
    this._lexer = new Vs2(r), this._options = n, this._tokenCounter = 0;
  }
  get tokenCount() {
    return this._tokenCounter;
  }
  /**
   * Converts a name lex token into a name parse node.
   */
  parseName() {
    const e = this.expectToken(g.NAME);
    return this.node(e, {
      kind: ye.NAME,
      value: e.value
    });
  }
  // Implements the parsing rules in the Document section.
  /**
   * Document : Definition+
   */
  parseDocument() {
    return this.node(this._lexer.token, {
      kind: ye.DOCUMENT,
      definitions: this.many(
        g.SOF,
        this.parseDefinition,
        g.EOF
      )
    });
  }
  /**
   * Definition :
   *   - ExecutableDefinition
   *   - TypeSystemDefinition
   *   - TypeSystemExtension
   *
   * ExecutableDefinition :
   *   - OperationDefinition
   *   - FragmentDefinition
   *
   * TypeSystemDefinition :
   *   - SchemaDefinition
   *   - TypeDefinition
   *   - DirectiveDefinition
   *
   * TypeDefinition :
   *   - ScalarTypeDefinition
   *   - ObjectTypeDefinition
   *   - InterfaceTypeDefinition
   *   - UnionTypeDefinition
   *   - EnumTypeDefinition
   *   - InputObjectTypeDefinition
   */
  parseDefinition() {
    if (this.peek(g.BRACE_L))
      return this.parseOperationDefinition();
    const e = this.peekDescription(), n = e ? this._lexer.lookahead() : this._lexer.token;
    if (n.kind === g.NAME) {
      switch (n.value) {
        case "schema":
          return this.parseSchemaDefinition();
        case "scalar":
          return this.parseScalarTypeDefinition();
        case "type":
          return this.parseObjectTypeDefinition();
        case "interface":
          return this.parseInterfaceTypeDefinition();
        case "union":
          return this.parseUnionTypeDefinition();
        case "enum":
          return this.parseEnumTypeDefinition();
        case "input":
          return this.parseInputObjectTypeDefinition();
        case "directive":
          return this.parseDirectiveDefinition();
      }
      if (e)
        throw $(
          this._lexer.source,
          this._lexer.token.start,
          "Unexpected description, descriptions are supported only on type definitions."
        );
      switch (n.value) {
        case "query":
        case "mutation":
        case "subscription":
          return this.parseOperationDefinition();
        case "fragment":
          return this.parseFragmentDefinition();
        case "extend":
          return this.parseTypeSystemExtension();
      }
    }
    throw this.unexpected(n);
  }
  // Implements the parsing rules in the Operations section.
  /**
   * OperationDefinition :
   *  - SelectionSet
   *  - OperationType Name? VariableDefinitions? Directives? SelectionSet
   */
  parseOperationDefinition() {
    const e = this._lexer.token;
    if (this.peek(g.BRACE_L))
      return this.node(e, {
        kind: ye.OPERATION_DEFINITION,
        operation: dt.QUERY,
        name: void 0,
        variableDefinitions: [],
        directives: [],
        selectionSet: this.parseSelectionSet()
      });
    const n = this.parseOperationType();
    let r;
    return this.peek(g.NAME) && (r = this.parseName()), this.node(e, {
      kind: ye.OPERATION_DEFINITION,
      operation: n,
      name: r,
      variableDefinitions: this.parseVariableDefinitions(),
      directives: this.parseDirectives(false),
      selectionSet: this.parseSelectionSet()
    });
  }
  /**
   * OperationType : one of query mutation subscription
   */
  parseOperationType() {
    const e = this.expectToken(g.NAME);
    switch (e.value) {
      case "query":
        return dt.QUERY;
      case "mutation":
        return dt.MUTATION;
      case "subscription":
        return dt.SUBSCRIPTION;
    }
    throw this.unexpected(e);
  }
  /**
   * VariableDefinitions : ( VariableDefinition+ )
   */
  parseVariableDefinitions() {
    return this.optionalMany(
      g.PAREN_L,
      this.parseVariableDefinition,
      g.PAREN_R
    );
  }
  /**
   * VariableDefinition : Variable : Type DefaultValue? Directives[Const]?
   */
  parseVariableDefinition() {
    return this.node(this._lexer.token, {
      kind: ye.VARIABLE_DEFINITION,
      variable: this.parseVariable(),
      type: (this.expectToken(g.COLON), this.parseTypeReference()),
      defaultValue: this.expectOptionalToken(g.EQUALS) ? this.parseConstValueLiteral() : void 0,
      directives: this.parseConstDirectives()
    });
  }
  /**
   * Variable : $ Name
   */
  parseVariable() {
    const e = this._lexer.token;
    return this.expectToken(g.DOLLAR), this.node(e, {
      kind: ye.VARIABLE,
      name: this.parseName()
    });
  }
  /**
   * ```
   * SelectionSet : { Selection+ }
   * ```
   */
  parseSelectionSet() {
    return this.node(this._lexer.token, {
      kind: ye.SELECTION_SET,
      selections: this.many(
        g.BRACE_L,
        this.parseSelection,
        g.BRACE_R
      )
    });
  }
  /**
   * Selection :
   *   - Field
   *   - FragmentSpread
   *   - InlineFragment
   */
  parseSelection() {
    return this.peek(g.SPREAD) ? this.parseFragment() : this.parseField();
  }
  /**
   * Field : Alias? Name Arguments? Directives? SelectionSet?
   *
   * Alias : Name :
   */
  parseField() {
    const e = this._lexer.token, n = this.parseName();
    let r, i;
    return this.expectOptionalToken(g.COLON) ? (r = n, i = this.parseName()) : i = n, this.node(e, {
      kind: ye.FIELD,
      alias: r,
      name: i,
      arguments: this.parseArguments(false),
      directives: this.parseDirectives(false),
      selectionSet: this.peek(g.BRACE_L) ? this.parseSelectionSet() : void 0
    });
  }
  /**
   * Arguments[Const] : ( Argument[?Const]+ )
   */
  parseArguments(e) {
    const n = e ? this.parseConstArgument : this.parseArgument;
    return this.optionalMany(g.PAREN_L, n, g.PAREN_R);
  }
  /**
   * Argument[Const] : Name : Value[?Const]
   */
  parseArgument(e = false) {
    const n = this._lexer.token, r = this.parseName();
    return this.expectToken(g.COLON), this.node(n, {
      kind: ye.ARGUMENT,
      name: r,
      value: this.parseValueLiteral(e)
    });
  }
  parseConstArgument() {
    return this.parseArgument(true);
  }
  // Implements the parsing rules in the Fragments section.
  /**
   * Corresponds to both FragmentSpread and InlineFragment in the spec.
   *
   * FragmentSpread : ... FragmentName Directives?
   *
   * InlineFragment : ... TypeCondition? Directives? SelectionSet
   */
  parseFragment() {
    const e = this._lexer.token;
    this.expectToken(g.SPREAD);
    const n = this.expectOptionalKeyword("on");
    return !n && this.peek(g.NAME) ? this.node(e, {
      kind: ye.FRAGMENT_SPREAD,
      name: this.parseFragmentName(),
      directives: this.parseDirectives(false)
    }) : this.node(e, {
      kind: ye.INLINE_FRAGMENT,
      typeCondition: n ? this.parseNamedType() : void 0,
      directives: this.parseDirectives(false),
      selectionSet: this.parseSelectionSet()
    });
  }
  /**
   * FragmentDefinition :
   *   - fragment FragmentName on TypeCondition Directives? SelectionSet
   *
   * TypeCondition : NamedType
   */
  parseFragmentDefinition() {
    const e = this._lexer.token;
    return this.expectKeyword("fragment"), this._options.allowLegacyFragmentVariables === true ? this.node(e, {
      kind: ye.FRAGMENT_DEFINITION,
      name: this.parseFragmentName(),
      variableDefinitions: this.parseVariableDefinitions(),
      typeCondition: (this.expectKeyword("on"), this.parseNamedType()),
      directives: this.parseDirectives(false),
      selectionSet: this.parseSelectionSet()
    }) : this.node(e, {
      kind: ye.FRAGMENT_DEFINITION,
      name: this.parseFragmentName(),
      typeCondition: (this.expectKeyword("on"), this.parseNamedType()),
      directives: this.parseDirectives(false),
      selectionSet: this.parseSelectionSet()
    });
  }
  /**
   * FragmentName : Name but not `on`
   */
  parseFragmentName() {
    if (this._lexer.token.value === "on")
      throw this.unexpected();
    return this.parseName();
  }
  // Implements the parsing rules in the Values section.
  /**
   * Value[Const] :
   *   - [~Const] Variable
   *   - IntValue
   *   - FloatValue
   *   - StringValue
   *   - BooleanValue
   *   - NullValue
   *   - EnumValue
   *   - ListValue[?Const]
   *   - ObjectValue[?Const]
   *
   * BooleanValue : one of `true` `false`
   *
   * NullValue : `null`
   *
   * EnumValue : Name but not `true`, `false` or `null`
   */
  parseValueLiteral(e) {
    const n = this._lexer.token;
    switch (n.kind) {
      case g.BRACKET_L:
        return this.parseList(e);
      case g.BRACE_L:
        return this.parseObject(e);
      case g.INT:
        return this.advanceLexer(), this.node(n, {
          kind: ye.INT,
          value: n.value
        });
      case g.FLOAT:
        return this.advanceLexer(), this.node(n, {
          kind: ye.FLOAT,
          value: n.value
        });
      case g.STRING:
      case g.BLOCK_STRING:
        return this.parseStringLiteral();
      case g.NAME:
        switch (this.advanceLexer(), n.value) {
          case "true":
            return this.node(n, {
              kind: ye.BOOLEAN,
              value: true
            });
          case "false":
            return this.node(n, {
              kind: ye.BOOLEAN,
              value: false
            });
          case "null":
            return this.node(n, {
              kind: ye.NULL
            });
          default:
            return this.node(n, {
              kind: ye.ENUM,
              value: n.value
            });
        }
      case g.DOLLAR:
        if (e)
          if (this.expectToken(g.DOLLAR), this._lexer.token.kind === g.NAME) {
            const r = this._lexer.token.value;
            throw $(
              this._lexer.source,
              n.start,
              `Unexpected variable "$${r}" in constant value.`
            );
          } else
            throw this.unexpected(n);
        return this.parseVariable();
      default:
        throw this.unexpected();
    }
  }
  parseConstValueLiteral() {
    return this.parseValueLiteral(true);
  }
  parseStringLiteral() {
    const e = this._lexer.token;
    return this.advanceLexer(), this.node(e, {
      kind: ye.STRING,
      value: e.value,
      block: e.kind === g.BLOCK_STRING
    });
  }
  /**
   * ListValue[Const] :
   *   - [ ]
   *   - [ Value[?Const]+ ]
   */
  parseList(e) {
    const n = () => this.parseValueLiteral(e);
    return this.node(this._lexer.token, {
      kind: ye.LIST,
      values: this.any(g.BRACKET_L, n, g.BRACKET_R)
    });
  }
  /**
   * ```
   * ObjectValue[Const] :
   *   - { }
   *   - { ObjectField[?Const]+ }
   * ```
   */
  parseObject(e) {
    const n = () => this.parseObjectField(e);
    return this.node(this._lexer.token, {
      kind: ye.OBJECT,
      fields: this.any(g.BRACE_L, n, g.BRACE_R)
    });
  }
  /**
   * ObjectField[Const] : Name : Value[?Const]
   */
  parseObjectField(e) {
    const n = this._lexer.token, r = this.parseName();
    return this.expectToken(g.COLON), this.node(n, {
      kind: ye.OBJECT_FIELD,
      name: r,
      value: this.parseValueLiteral(e)
    });
  }
  // Implements the parsing rules in the Directives section.
  /**
   * Directives[Const] : Directive[?Const]+
   */
  parseDirectives(e) {
    const n = [];
    for (; this.peek(g.AT); )
      n.push(this.parseDirective(e));
    return n;
  }
  parseConstDirectives() {
    return this.parseDirectives(true);
  }
  /**
   * ```
   * Directive[Const] : @ Name Arguments[?Const]?
   * ```
   */
  parseDirective(e) {
    const n = this._lexer.token;
    return this.expectToken(g.AT), this.node(n, {
      kind: ye.DIRECTIVE,
      name: this.parseName(),
      arguments: this.parseArguments(e)
    });
  }
  // Implements the parsing rules in the Types section.
  /**
   * Type :
   *   - NamedType
   *   - ListType
   *   - NonNullType
   */
  parseTypeReference() {
    const e = this._lexer.token;
    let n;
    if (this.expectOptionalToken(g.BRACKET_L)) {
      const r = this.parseTypeReference();
      this.expectToken(g.BRACKET_R), n = this.node(e, {
        kind: ye.LIST_TYPE,
        type: r
      });
    } else
      n = this.parseNamedType();
    return this.expectOptionalToken(g.BANG) ? this.node(e, {
      kind: ye.NON_NULL_TYPE,
      type: n
    }) : n;
  }
  /**
   * NamedType : Name
   */
  parseNamedType() {
    return this.node(this._lexer.token, {
      kind: ye.NAMED_TYPE,
      name: this.parseName()
    });
  }
  // Implements the parsing rules in the Type Definition section.
  peekDescription() {
    return this.peek(g.STRING) || this.peek(g.BLOCK_STRING);
  }
  /**
   * Description : StringValue
   */
  parseDescription() {
    if (this.peekDescription())
      return this.parseStringLiteral();
  }
  /**
   * ```
   * SchemaDefinition : Description? schema Directives[Const]? { OperationTypeDefinition+ }
   * ```
   */
  parseSchemaDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("schema");
    const r = this.parseConstDirectives(), i = this.many(
      g.BRACE_L,
      this.parseOperationTypeDefinition,
      g.BRACE_R
    );
    return this.node(e, {
      kind: ye.SCHEMA_DEFINITION,
      description: n,
      directives: r,
      operationTypes: i
    });
  }
  /**
   * OperationTypeDefinition : OperationType : NamedType
   */
  parseOperationTypeDefinition() {
    const e = this._lexer.token, n = this.parseOperationType();
    this.expectToken(g.COLON);
    const r = this.parseNamedType();
    return this.node(e, {
      kind: ye.OPERATION_TYPE_DEFINITION,
      operation: n,
      type: r
    });
  }
  /**
   * ScalarTypeDefinition : Description? scalar Name Directives[Const]?
   */
  parseScalarTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("scalar");
    const r = this.parseName(), i = this.parseConstDirectives();
    return this.node(e, {
      kind: ye.SCALAR_TYPE_DEFINITION,
      description: n,
      name: r,
      directives: i
    });
  }
  /**
   * ObjectTypeDefinition :
   *   Description?
   *   type Name ImplementsInterfaces? Directives[Const]? FieldsDefinition?
   */
  parseObjectTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("type");
    const r = this.parseName(), i = this.parseImplementsInterfaces(), s = this.parseConstDirectives(), o = this.parseFieldsDefinition();
    return this.node(e, {
      kind: ye.OBJECT_TYPE_DEFINITION,
      description: n,
      name: r,
      interfaces: i,
      directives: s,
      fields: o
    });
  }
  /**
   * ImplementsInterfaces :
   *   - implements `&`? NamedType
   *   - ImplementsInterfaces & NamedType
   */
  parseImplementsInterfaces() {
    return this.expectOptionalKeyword("implements") ? this.delimitedMany(g.AMP, this.parseNamedType) : [];
  }
  /**
   * ```
   * FieldsDefinition : { FieldDefinition+ }
   * ```
   */
  parseFieldsDefinition() {
    return this.optionalMany(
      g.BRACE_L,
      this.parseFieldDefinition,
      g.BRACE_R
    );
  }
  /**
   * FieldDefinition :
   *   - Description? Name ArgumentsDefinition? : Type Directives[Const]?
   */
  parseFieldDefinition() {
    const e = this._lexer.token, n = this.parseDescription(), r = this.parseName(), i = this.parseArgumentDefs();
    this.expectToken(g.COLON);
    const s = this.parseTypeReference(), o = this.parseConstDirectives();
    return this.node(e, {
      kind: ye.FIELD_DEFINITION,
      description: n,
      name: r,
      arguments: i,
      type: s,
      directives: o
    });
  }
  /**
   * ArgumentsDefinition : ( InputValueDefinition+ )
   */
  parseArgumentDefs() {
    return this.optionalMany(
      g.PAREN_L,
      this.parseInputValueDef,
      g.PAREN_R
    );
  }
  /**
   * InputValueDefinition :
   *   - Description? Name : Type DefaultValue? Directives[Const]?
   */
  parseInputValueDef() {
    const e = this._lexer.token, n = this.parseDescription(), r = this.parseName();
    this.expectToken(g.COLON);
    const i = this.parseTypeReference();
    let s;
    this.expectOptionalToken(g.EQUALS) && (s = this.parseConstValueLiteral());
    const o = this.parseConstDirectives();
    return this.node(e, {
      kind: ye.INPUT_VALUE_DEFINITION,
      description: n,
      name: r,
      type: i,
      defaultValue: s,
      directives: o
    });
  }
  /**
   * InterfaceTypeDefinition :
   *   - Description? interface Name Directives[Const]? FieldsDefinition?
   */
  parseInterfaceTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("interface");
    const r = this.parseName(), i = this.parseImplementsInterfaces(), s = this.parseConstDirectives(), o = this.parseFieldsDefinition();
    return this.node(e, {
      kind: ye.INTERFACE_TYPE_DEFINITION,
      description: n,
      name: r,
      interfaces: i,
      directives: s,
      fields: o
    });
  }
  /**
   * UnionTypeDefinition :
   *   - Description? union Name Directives[Const]? UnionMemberTypes?
   */
  parseUnionTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("union");
    const r = this.parseName(), i = this.parseConstDirectives(), s = this.parseUnionMemberTypes();
    return this.node(e, {
      kind: ye.UNION_TYPE_DEFINITION,
      description: n,
      name: r,
      directives: i,
      types: s
    });
  }
  /**
   * UnionMemberTypes :
   *   - = `|`? NamedType
   *   - UnionMemberTypes | NamedType
   */
  parseUnionMemberTypes() {
    return this.expectOptionalToken(g.EQUALS) ? this.delimitedMany(g.PIPE, this.parseNamedType) : [];
  }
  /**
   * EnumTypeDefinition :
   *   - Description? enum Name Directives[Const]? EnumValuesDefinition?
   */
  parseEnumTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("enum");
    const r = this.parseName(), i = this.parseConstDirectives(), s = this.parseEnumValuesDefinition();
    return this.node(e, {
      kind: ye.ENUM_TYPE_DEFINITION,
      description: n,
      name: r,
      directives: i,
      values: s
    });
  }
  /**
   * ```
   * EnumValuesDefinition : { EnumValueDefinition+ }
   * ```
   */
  parseEnumValuesDefinition() {
    return this.optionalMany(
      g.BRACE_L,
      this.parseEnumValueDefinition,
      g.BRACE_R
    );
  }
  /**
   * EnumValueDefinition : Description? EnumValue Directives[Const]?
   */
  parseEnumValueDefinition() {
    const e = this._lexer.token, n = this.parseDescription(), r = this.parseEnumValueName(), i = this.parseConstDirectives();
    return this.node(e, {
      kind: ye.ENUM_VALUE_DEFINITION,
      description: n,
      name: r,
      directives: i
    });
  }
  /**
   * EnumValue : Name but not `true`, `false` or `null`
   */
  parseEnumValueName() {
    if (this._lexer.token.value === "true" || this._lexer.token.value === "false" || this._lexer.token.value === "null")
      throw $(
        this._lexer.source,
        this._lexer.token.start,
        `${on(
          this._lexer.token
        )} is reserved and cannot be used for an enum value.`
      );
    return this.parseName();
  }
  /**
   * InputObjectTypeDefinition :
   *   - Description? input Name Directives[Const]? InputFieldsDefinition?
   */
  parseInputObjectTypeDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("input");
    const r = this.parseName(), i = this.parseConstDirectives(), s = this.parseInputFieldsDefinition();
    return this.node(e, {
      kind: ye.INPUT_OBJECT_TYPE_DEFINITION,
      description: n,
      name: r,
      directives: i,
      fields: s
    });
  }
  /**
   * ```
   * InputFieldsDefinition : { InputValueDefinition+ }
   * ```
   */
  parseInputFieldsDefinition() {
    return this.optionalMany(
      g.BRACE_L,
      this.parseInputValueDef,
      g.BRACE_R
    );
  }
  /**
   * TypeSystemExtension :
   *   - SchemaExtension
   *   - TypeExtension
   *
   * TypeExtension :
   *   - ScalarTypeExtension
   *   - ObjectTypeExtension
   *   - InterfaceTypeExtension
   *   - UnionTypeExtension
   *   - EnumTypeExtension
   *   - InputObjectTypeDefinition
   */
  parseTypeSystemExtension() {
    const e = this._lexer.lookahead();
    if (e.kind === g.NAME)
      switch (e.value) {
        case "schema":
          return this.parseSchemaExtension();
        case "scalar":
          return this.parseScalarTypeExtension();
        case "type":
          return this.parseObjectTypeExtension();
        case "interface":
          return this.parseInterfaceTypeExtension();
        case "union":
          return this.parseUnionTypeExtension();
        case "enum":
          return this.parseEnumTypeExtension();
        case "input":
          return this.parseInputObjectTypeExtension();
      }
    throw this.unexpected(e);
  }
  /**
   * ```
   * SchemaExtension :
   *  - extend schema Directives[Const]? { OperationTypeDefinition+ }
   *  - extend schema Directives[Const]
   * ```
   */
  parseSchemaExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("schema");
    const n = this.parseConstDirectives(), r = this.optionalMany(
      g.BRACE_L,
      this.parseOperationTypeDefinition,
      g.BRACE_R
    );
    if (n.length === 0 && r.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: ye.SCHEMA_EXTENSION,
      directives: n,
      operationTypes: r
    });
  }
  /**
   * ScalarTypeExtension :
   *   - extend scalar Name Directives[Const]
   */
  parseScalarTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("scalar");
    const n = this.parseName(), r = this.parseConstDirectives();
    if (r.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: ye.SCALAR_TYPE_EXTENSION,
      name: n,
      directives: r
    });
  }
  /**
   * ObjectTypeExtension :
   *  - extend type Name ImplementsInterfaces? Directives[Const]? FieldsDefinition
   *  - extend type Name ImplementsInterfaces? Directives[Const]
   *  - extend type Name ImplementsInterfaces
   */
  parseObjectTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("type");
    const n = this.parseName(), r = this.parseImplementsInterfaces(), i = this.parseConstDirectives(), s = this.parseFieldsDefinition();
    if (r.length === 0 && i.length === 0 && s.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: ye.OBJECT_TYPE_EXTENSION,
      name: n,
      interfaces: r,
      directives: i,
      fields: s
    });
  }
  /**
   * InterfaceTypeExtension :
   *  - extend interface Name ImplementsInterfaces? Directives[Const]? FieldsDefinition
   *  - extend interface Name ImplementsInterfaces? Directives[Const]
   *  - extend interface Name ImplementsInterfaces
   */
  parseInterfaceTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("interface");
    const n = this.parseName(), r = this.parseImplementsInterfaces(), i = this.parseConstDirectives(), s = this.parseFieldsDefinition();
    if (r.length === 0 && i.length === 0 && s.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: ye.INTERFACE_TYPE_EXTENSION,
      name: n,
      interfaces: r,
      directives: i,
      fields: s
    });
  }
  /**
   * UnionTypeExtension :
   *   - extend union Name Directives[Const]? UnionMemberTypes
   *   - extend union Name Directives[Const]
   */
  parseUnionTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("union");
    const n = this.parseName(), r = this.parseConstDirectives(), i = this.parseUnionMemberTypes();
    if (r.length === 0 && i.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: ye.UNION_TYPE_EXTENSION,
      name: n,
      directives: r,
      types: i
    });
  }
  /**
   * EnumTypeExtension :
   *   - extend enum Name Directives[Const]? EnumValuesDefinition
   *   - extend enum Name Directives[Const]
   */
  parseEnumTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("enum");
    const n = this.parseName(), r = this.parseConstDirectives(), i = this.parseEnumValuesDefinition();
    if (r.length === 0 && i.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: ye.ENUM_TYPE_EXTENSION,
      name: n,
      directives: r,
      values: i
    });
  }
  /**
   * InputObjectTypeExtension :
   *   - extend input Name Directives[Const]? InputFieldsDefinition
   *   - extend input Name Directives[Const]
   */
  parseInputObjectTypeExtension() {
    const e = this._lexer.token;
    this.expectKeyword("extend"), this.expectKeyword("input");
    const n = this.parseName(), r = this.parseConstDirectives(), i = this.parseInputFieldsDefinition();
    if (r.length === 0 && i.length === 0)
      throw this.unexpected();
    return this.node(e, {
      kind: ye.INPUT_OBJECT_TYPE_EXTENSION,
      name: n,
      directives: r,
      fields: i
    });
  }
  /**
   * ```
   * DirectiveDefinition :
   *   - Description? directive @ Name ArgumentsDefinition? `repeatable`? on DirectiveLocations
   * ```
   */
  parseDirectiveDefinition() {
    const e = this._lexer.token, n = this.parseDescription();
    this.expectKeyword("directive"), this.expectToken(g.AT);
    const r = this.parseName(), i = this.parseArgumentDefs(), s = this.expectOptionalKeyword("repeatable");
    this.expectKeyword("on");
    const o = this.parseDirectiveLocations();
    return this.node(e, {
      kind: ye.DIRECTIVE_DEFINITION,
      description: n,
      name: r,
      arguments: i,
      repeatable: s,
      locations: o
    });
  }
  /**
   * DirectiveLocations :
   *   - `|`? DirectiveLocation
   *   - DirectiveLocations | DirectiveLocation
   */
  parseDirectiveLocations() {
    return this.delimitedMany(g.PIPE, this.parseDirectiveLocation);
  }
  /*
   * DirectiveLocation :
   *   - ExecutableDirectiveLocation
   *   - TypeSystemDirectiveLocation
   *
   * ExecutableDirectiveLocation : one of
   *   `QUERY`
   *   `MUTATION`
   *   `SUBSCRIPTION`
   *   `FIELD`
   *   `FRAGMENT_DEFINITION`
   *   `FRAGMENT_SPREAD`
   *   `INLINE_FRAGMENT`
   *
   * TypeSystemDirectiveLocation : one of
   *   `SCHEMA`
   *   `SCALAR`
   *   `OBJECT`
   *   `FIELD_DEFINITION`
   *   `ARGUMENT_DEFINITION`
   *   `INTERFACE`
   *   `UNION`
   *   `ENUM`
   *   `ENUM_VALUE`
   *   `INPUT_OBJECT`
   *   `INPUT_FIELD_DEFINITION`
   */
  parseDirectiveLocation() {
    const e = this._lexer.token, n = this.parseName();
    if (Object.prototype.hasOwnProperty.call(A, n.value))
      return n;
    throw this.unexpected(e);
  }
  // Core parsing utility functions
  /**
   * Returns a node that, if configured to do so, sets a "loc" field as a
   * location object, used to identify the place in the source that created a
   * given parsed object.
   */
  node(e, n) {
    return this._options.noLocation !== true && (n.loc = new Ma(
      e,
      this._lexer.lastToken,
      this._lexer.source
    )), n;
  }
  /**
   * Determines if the next token is of a given kind
   */
  peek(e) {
    return this._lexer.token.kind === e;
  }
  /**
   * If the next token is of the given kind, return that token after advancing the lexer.
   * Otherwise, do not change the parser state and throw an error.
   */
  expectToken(e) {
    const n = this._lexer.token;
    if (n.kind === e)
      return this.advanceLexer(), n;
    throw $(
      this._lexer.source,
      n.start,
      `Expected ${rs(e)}, found ${on(n)}.`
    );
  }
  /**
   * If the next token is of the given kind, return "true" after advancing the lexer.
   * Otherwise, do not change the parser state and return "false".
   */
  expectOptionalToken(e) {
    return this._lexer.token.kind === e ? (this.advanceLexer(), true) : false;
  }
  /**
   * If the next token is a given keyword, advance the lexer.
   * Otherwise, do not change the parser state and throw an error.
   */
  expectKeyword(e) {
    const n = this._lexer.token;
    if (n.kind === g.NAME && n.value === e)
      this.advanceLexer();
    else
      throw $(
        this._lexer.source,
        n.start,
        `Expected "${e}", found ${on(n)}.`
      );
  }
  /**
   * If the next token is a given keyword, return "true" after advancing the lexer.
   * Otherwise, do not change the parser state and return "false".
   */
  expectOptionalKeyword(e) {
    const n = this._lexer.token;
    return n.kind === g.NAME && n.value === e ? (this.advanceLexer(), true) : false;
  }
  /**
   * Helper function for creating an error when an unexpected lexed token is encountered.
   */
  unexpected(e) {
    const n = e ?? this._lexer.token;
    return $(
      this._lexer.source,
      n.start,
      `Unexpected ${on(n)}.`
    );
  }
  /**
   * Returns a possibly empty list of parse nodes, determined by the parseFn.
   * This list begins with a lex token of openKind and ends with a lex token of closeKind.
   * Advances the parser to the next lex token after the closing token.
   */
  any(e, n, r) {
    this.expectToken(e);
    const i = [];
    for (; !this.expectOptionalToken(r); )
      i.push(n.call(this));
    return i;
  }
  /**
   * Returns a list of parse nodes, determined by the parseFn.
   * It can be empty only if open token is missing otherwise it will always return non-empty list
   * that begins with a lex token of openKind and ends with a lex token of closeKind.
   * Advances the parser to the next lex token after the closing token.
   */
  optionalMany(e, n, r) {
    if (this.expectOptionalToken(e)) {
      const i = [];
      do
        i.push(n.call(this));
      while (!this.expectOptionalToken(r));
      return i;
    }
    return [];
  }
  /**
   * Returns a non-empty list of parse nodes, determined by the parseFn.
   * This list begins with a lex token of openKind and ends with a lex token of closeKind.
   * Advances the parser to the next lex token after the closing token.
   */
  many(e, n, r) {
    this.expectToken(e);
    const i = [];
    do
      i.push(n.call(this));
    while (!this.expectOptionalToken(r));
    return i;
  }
  /**
   * Returns a non-empty list of parse nodes, determined by the parseFn.
   * This list may begin with a lex token of delimiterKind followed by items separated by lex tokens of tokenKind.
   * Advances the parser to the next lex token after last item in the list.
   */
  delimitedMany(e, n) {
    this.expectOptionalToken(e);
    const r = [];
    do
      r.push(n.call(this));
    while (this.expectOptionalToken(e));
    return r;
  }
  advanceLexer() {
    const { maxTokens: e } = this._options, n = this._lexer.advance();
    if (n.kind !== g.EOF && (++this._tokenCounter, e !== void 0 && this._tokenCounter > e))
      throw $(
        this._lexer.source,
        n.start,
        `Document contains more that ${e} tokens. Parsing aborted.`
      );
  }
};
function on(t) {
  const e = t.value;
  return rs(t.kind) + (e != null ? ` "${e}"` : "");
}
function rs(t) {
  return Bs(t) ? `"${t}"` : t;
}
function hr(t, e) {
  return t === e ? true : Nn(t) && Nn(e) || Ms(t) && Ms(e) ? hr(t.ofType, e.ofType) : false;
}
function Tt(t, e, n) {
  return e === n ? true : Nn(n) ? Nn(e) ? Tt(t, e.ofType, n.ofType) : false : Nn(e) ? Tt(t, e.ofType, n) : Ms(n) ? Ms(e) ? Tt(t, e.ofType, n.ofType) : false : Ms(e) ? false : oc(n) && (Qn(e) || Nr(e)) && t.isSubType(n, e);
}
function Kr(t, e, n) {
  return e === n ? true : oc(e) ? oc(n) ? t.getPossibleTypes(e).some((r) => t.isSubType(n, r)) : t.isSubType(e, n) : oc(n) ? t.isSubType(n, e) : false;
}
var Xn = 2147483647;
var Zn = -2147483648;
var jn = new zr({
  name: "Int",
  description: "The `Int` scalar type represents non-fractional signed whole numeric values. Int can represent values between -(2^31) and 2^31 - 1.",
  serialize(t) {
    const e = en(t);
    if (typeof e == "boolean")
      return e ? 1 : 0;
    let n = e;
    if (typeof e == "string" && e !== "" && (n = Number(e)), typeof n != "number" || !Number.isInteger(n))
      throw new qe(
        `Int cannot represent non-integer value: ${It(e)}`
      );
    if (n > Xn || n < Zn)
      throw new qe(
        "Int cannot represent non 32-bit signed integer value: " + It(e)
      );
    return n;
  },
  parseValue(t) {
    if (typeof t != "number" || !Number.isInteger(t))
      throw new qe(
        `Int cannot represent non-integer value: ${It(t)}`
      );
    if (t > Xn || t < Zn)
      throw new qe(
        `Int cannot represent non 32-bit signed integer value: ${t}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== ye.INT)
      throw new qe(
        `Int cannot represent non-integer value: ${gr(t)}`,
        {
          nodes: t
        }
      );
    const e = parseInt(t.value, 10);
    if (e > Xn || e < Zn)
      throw new qe(
        `Int cannot represent non 32-bit signed integer value: ${t.value}`,
        {
          nodes: t
        }
      );
    return e;
  }
});
var Ir = new zr({
  name: "Float",
  description: "The `Float` scalar type represents signed double-precision fractional values as specified by [IEEE 754](https://en.wikipedia.org/wiki/IEEE_floating_point).",
  serialize(t) {
    const e = en(t);
    if (typeof e == "boolean")
      return e ? 1 : 0;
    let n = e;
    if (typeof e == "string" && e !== "" && (n = Number(e)), typeof n != "number" || !Number.isFinite(n))
      throw new qe(
        `Float cannot represent non numeric value: ${It(e)}`
      );
    return n;
  },
  parseValue(t) {
    if (typeof t != "number" || !Number.isFinite(t))
      throw new qe(
        `Float cannot represent non numeric value: ${It(t)}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== ye.FLOAT && t.kind !== ye.INT)
      throw new qe(
        `Float cannot represent non numeric value: ${gr(t)}`,
        t
      );
    return parseFloat(t.value);
  }
});
var V = new zr({
  name: "String",
  description: "The `String` scalar type represents textual data, represented as UTF-8 character sequences. The String type is most often used by GraphQL to represent free-form human-readable text.",
  serialize(t) {
    const e = en(t);
    if (typeof e == "string")
      return e;
    if (typeof e == "boolean")
      return e ? "true" : "false";
    if (typeof e == "number" && Number.isFinite(e))
      return e.toString();
    throw new qe(
      `String cannot represent value: ${It(t)}`
    );
  },
  parseValue(t) {
    if (typeof t != "string")
      throw new qe(
        `String cannot represent a non string value: ${It(t)}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== ye.STRING)
      throw new qe(
        `String cannot represent a non string value: ${gr(t)}`,
        {
          nodes: t
        }
      );
    return t.value;
  }
});
var X = new zr({
  name: "Boolean",
  description: "The `Boolean` scalar type represents `true` or `false`.",
  serialize(t) {
    const e = en(t);
    if (typeof e == "boolean")
      return e;
    if (Number.isFinite(e))
      return e !== 0;
    throw new qe(
      `Boolean cannot represent a non boolean value: ${It(e)}`
    );
  },
  parseValue(t) {
    if (typeof t != "boolean")
      throw new qe(
        `Boolean cannot represent a non boolean value: ${It(t)}`
      );
    return t;
  },
  parseLiteral(t) {
    if (t.kind !== ye.BOOLEAN)
      throw new qe(
        `Boolean cannot represent a non boolean value: ${gr(t)}`,
        {
          nodes: t
        }
      );
    return t.value;
  }
});
var Mn = new zr({
  name: "ID",
  description: 'The `ID` scalar type represents a unique identifier, often used to refetch an object or as key for a cache. The ID type appears in a JSON response as a String; however, it is not intended to be human-readable. When expected as an input type, any string (such as `"4"`) or integer (such as `4`) input value will be accepted as an ID.',
  serialize(t) {
    const e = en(t);
    if (typeof e == "string")
      return e;
    if (Number.isInteger(e))
      return String(e);
    throw new qe(
      `ID cannot represent value: ${It(t)}`
    );
  },
  parseValue(t) {
    if (typeof t == "string")
      return t;
    if (typeof t == "number" && Number.isInteger(t))
      return t.toString();
    throw new qe(`ID cannot represent value: ${It(t)}`);
  },
  parseLiteral(t) {
    if (t.kind !== ye.STRING && t.kind !== ye.INT)
      throw new qe(
        "ID cannot represent a non-string and non-integer value: " + gr(t),
        {
          nodes: t
        }
      );
    return t.value;
  }
});
var Zo = Object.freeze([
  V,
  jn,
  Ir,
  X,
  Mn
]);
function en(t) {
  if (Rs(t)) {
    if (typeof t.valueOf == "function") {
      const e = t.valueOf();
      if (!Rs(e))
        return e;
    }
    if (typeof t.toJSON == "function")
      return t.toJSON();
  }
  return t;
}
function is(t) {
  return rr(t, Le);
}
var Le = class {
  constructor(e) {
    var n, r;
    this.name = Ce(e.name), this.description = e.description, this.locations = e.locations, this.isRepeatable = (n = e.isRepeatable) !== null && n !== void 0 ? n : false, this.extensions = $e(e.extensions), this.astNode = e.astNode, Array.isArray(e.locations) || we(false, `@${e.name} locations must be an Array.`);
    const i = (r = e.args) !== null && r !== void 0 ? r : {};
    Rs(i) && !Array.isArray(i) || we(
      false,
      `@${e.name} args must be an object with argument names as keys.`
    ), this.args = mu(i);
  }
  get [Symbol.toStringTag]() {
    return "GraphQLDirective";
  }
  toConfig() {
    return {
      name: this.name,
      description: this.description,
      locations: this.locations,
      args: yu(this.args),
      isRepeatable: this.isRepeatable,
      extensions: this.extensions,
      astNode: this.astNode
    };
  }
  toString() {
    return "@" + this.name;
  }
  toJSON() {
    return this.toString();
  }
};
var Or = new Le({
  name: "include",
  description: "Directs the executor to include this field or fragment only when the `if` argument is true.",
  locations: [
    A.FIELD,
    A.FRAGMENT_SPREAD,
    A.INLINE_FRAGMENT
  ],
  args: {
    if: {
      type: new pu(X),
      description: "Included when true."
    }
  }
});
var _r = new Le({
  name: "skip",
  description: "Directs the executor to skip this field or fragment when the `if` argument is true.",
  locations: [
    A.FIELD,
    A.FRAGMENT_SPREAD,
    A.INLINE_FRAGMENT
  ],
  args: {
    if: {
      type: new pu(X),
      description: "Skipped when true."
    }
  }
});
var ea = "No longer supported";
var ss = new Le({
  name: "deprecated",
  description: "Marks an element of a GraphQL schema as no longer supported.",
  locations: [
    A.FIELD_DEFINITION,
    A.ARGUMENT_DEFINITION,
    A.INPUT_FIELD_DEFINITION,
    A.ENUM_VALUE
  ],
  args: {
    reason: {
      type: V,
      description: "Explains why this element was deprecated, usually also including a suggestion for how to access supported similar data. Formatted using the Markdown syntax, as specified by [CommonMark](https://commonmark.org/).",
      defaultValue: ea
    }
  }
});
var ta = new Le({
  name: "specifiedBy",
  description: "Exposes a URL that specifies the behavior of this scalar.",
  locations: [A.SCALAR],
  args: {
    url: {
      type: new pu(V),
      description: "The URL that specifies the behavior of this scalar."
    }
  }
});
var na = new Le({
  name: "oneOf",
  description: "Indicates exactly one field must be supplied and this field must not be `null`.",
  locations: [A.INPUT_OBJECT],
  args: {}
});
var tn = Object.freeze([
  Or,
  _r,
  ss,
  ta,
  na
]);
function os(t) {
  return typeof t == "object" && typeof t?.[Symbol.iterator] == "function";
}
function Mt(t, e) {
  if (Nn(e)) {
    const n = Mt(t, e.ofType);
    return n?.kind === ye.NULL ? null : n;
  }
  if (t === null)
    return {
      kind: ye.NULL
    };
  if (t === void 0)
    return null;
  if (Ms(e)) {
    const n = e.ofType;
    if (os(t)) {
      const r = [];
      for (const i of t) {
        const s = Mt(i, n);
        s != null && r.push(s);
      }
      return {
        kind: ye.LIST,
        values: r
      };
    }
    return Mt(t, n);
  }
  if (Yr(e)) {
    if (!Rs(t))
      return null;
    const n = [];
    for (const r of Object.values(e.getFields())) {
      const i = Mt(t[r.name], r.type);
      i && n.push({
        kind: ye.OBJECT_FIELD,
        name: {
          kind: ye.NAME,
          value: r.name
        },
        value: i
      });
    }
    return {
      kind: ye.OBJECT,
      fields: n
    };
  }
  if (ac(e)) {
    const n = e.serialize(t);
    if (n == null)
      return null;
    if (typeof n == "boolean")
      return {
        kind: ye.BOOLEAN,
        value: n
      };
    if (typeof n == "number" && Number.isFinite(n)) {
      const r = String(n);
      return Wr.test(r) ? {
        kind: ye.INT,
        value: r
      } : {
        kind: ye.FLOAT,
        value: r
      };
    }
    if (typeof n == "string")
      return Kn(e) ? {
        kind: ye.ENUM,
        value: n
      } : e === Mn && Wr.test(n) ? {
        kind: ye.INT,
        value: n
      } : {
        kind: ye.STRING,
        value: n
      };
    throw new TypeError(`Cannot convert value to AST: ${It(n)}.`);
  }
  Ps(false, "Unexpected input type: " + It(e));
}
var Wr = /^-?(?:0|[1-9][0-9]*)$/;
var Nr2 = new fu({
  name: "__Schema",
  description: "A GraphQL Schema defines the capabilities of a GraphQL server. It exposes all available types and directives on the server, as well as the entry points for query, mutation, and subscription operations.",
  fields: () => ({
    description: {
      type: V,
      resolve: (t) => t.description
    },
    types: {
      description: "A list of all types supported by this server.",
      type: new pu(new lu(new pu(Ie))),
      resolve(t) {
        return Object.values(t.getTypeMap());
      }
    },
    queryType: {
      description: "The type that query operations will be rooted at.",
      type: new pu(Ie),
      resolve: (t) => t.getQueryType()
    },
    mutationType: {
      description: "If this server supports mutation, the type that mutation operations will be rooted at.",
      type: Ie,
      resolve: (t) => t.getMutationType()
    },
    subscriptionType: {
      description: "If this server support subscription, the type that subscription operations will be rooted at.",
      type: Ie,
      resolve: (t) => t.getSubscriptionType()
    },
    directives: {
      description: "A list of all directives supported by this server.",
      type: new pu(
        new lu(new pu(as))
      ),
      resolve: (t) => t.getDirectives()
    }
  })
});
var as = new fu({
  name: "__Directive",
  description: `A Directive provides a way to describe alternate runtime execution and type validation behavior in a GraphQL document.

In some cases, you need to provide options to alter GraphQL's execution behavior in ways field arguments will not suffice, such as conditionally including or skipping a field. Directives provide this by describing additional information to the executor.`,
  fields: () => ({
    name: {
      type: new pu(V),
      resolve: (t) => t.name
    },
    description: {
      type: V,
      resolve: (t) => t.description
    },
    isRepeatable: {
      type: new pu(X),
      resolve: (t) => t.isRepeatable
    },
    locations: {
      type: new pu(
        new lu(new pu(ls))
      ),
      resolve: (t) => t.locations
    },
    args: {
      type: new pu(
        new lu(new pu(Un2))
      ),
      args: {
        includeDeprecated: {
          type: X,
          defaultValue: false
        }
      },
      resolve(t, { includeDeprecated: e }) {
        return e ? t.args : t.args.filter((n) => n.deprecationReason == null);
      }
    }
  })
});
var ls = new bu({
  name: "__DirectiveLocation",
  description: "A Directive can be adjacent to many parts of the GraphQL language, a __DirectiveLocation describes one such possible adjacencies.",
  values: {
    QUERY: {
      value: A.QUERY,
      description: "Location adjacent to a query operation."
    },
    MUTATION: {
      value: A.MUTATION,
      description: "Location adjacent to a mutation operation."
    },
    SUBSCRIPTION: {
      value: A.SUBSCRIPTION,
      description: "Location adjacent to a subscription operation."
    },
    FIELD: {
      value: A.FIELD,
      description: "Location adjacent to a field."
    },
    FRAGMENT_DEFINITION: {
      value: A.FRAGMENT_DEFINITION,
      description: "Location adjacent to a fragment definition."
    },
    FRAGMENT_SPREAD: {
      value: A.FRAGMENT_SPREAD,
      description: "Location adjacent to a fragment spread."
    },
    INLINE_FRAGMENT: {
      value: A.INLINE_FRAGMENT,
      description: "Location adjacent to an inline fragment."
    },
    VARIABLE_DEFINITION: {
      value: A.VARIABLE_DEFINITION,
      description: "Location adjacent to a variable definition."
    },
    SCHEMA: {
      value: A.SCHEMA,
      description: "Location adjacent to a schema definition."
    },
    SCALAR: {
      value: A.SCALAR,
      description: "Location adjacent to a scalar definition."
    },
    OBJECT: {
      value: A.OBJECT,
      description: "Location adjacent to an object type definition."
    },
    FIELD_DEFINITION: {
      value: A.FIELD_DEFINITION,
      description: "Location adjacent to a field definition."
    },
    ARGUMENT_DEFINITION: {
      value: A.ARGUMENT_DEFINITION,
      description: "Location adjacent to an argument definition."
    },
    INTERFACE: {
      value: A.INTERFACE,
      description: "Location adjacent to an interface definition."
    },
    UNION: {
      value: A.UNION,
      description: "Location adjacent to a union definition."
    },
    ENUM: {
      value: A.ENUM,
      description: "Location adjacent to an enum definition."
    },
    ENUM_VALUE: {
      value: A.ENUM_VALUE,
      description: "Location adjacent to an enum value definition."
    },
    INPUT_OBJECT: {
      value: A.INPUT_OBJECT,
      description: "Location adjacent to an input object type definition."
    },
    INPUT_FIELD_DEFINITION: {
      value: A.INPUT_FIELD_DEFINITION,
      description: "Location adjacent to an input object field definition."
    }
  }
});
var Ie = new fu({
  name: "__Type",
  description: "The fundamental unit of any GraphQL Schema is the type. There are many kinds of types in GraphQL as represented by the `__TypeKind` enum.\n\nDepending on the kind of a type, certain fields describe information about that type. Scalar types provide no information beyond a name, description and optional `specifiedByURL`, while Enum types provide their values. Object and Interface types provide the fields they describe. Abstract types, Union and Interface, provide the Object types possible at runtime. List and NonNull types compose other types.",
  fields: () => ({
    kind: {
      type: new pu(fs),
      resolve(t) {
        if (Un(t))
          return z.SCALAR;
        if (Nr(t))
          return z.OBJECT;
        if (Qn(t))
          return z.INTERFACE;
        if (qn(t))
          return z.UNION;
        if (Kn(t))
          return z.ENUM;
        if (Yr(t))
          return z.INPUT_OBJECT;
        if (Ms(t))
          return z.LIST;
        if (Nn(t))
          return z.NON_NULL;
        Ps(false, `Unexpected type: "${It(t)}".`);
      }
    },
    name: {
      type: V,
      resolve: (t) => "name" in t ? t.name : void 0
    },
    description: {
      type: V,
      resolve: (t) => (
        /* c8 ignore next */
        "description" in t ? t.description : void 0
      )
    },
    specifiedByURL: {
      type: V,
      resolve: (t) => "specifiedByURL" in t ? t.specifiedByURL : void 0
    },
    fields: {
      type: new lu(new pu(us)),
      args: {
        includeDeprecated: {
          type: X,
          defaultValue: false
        }
      },
      resolve(t, { includeDeprecated: e }) {
        if (Nr(t) || Qn(t)) {
          const n = Object.values(t.getFields());
          return e ? n : n.filter((r) => r.deprecationReason == null);
        }
      }
    },
    interfaces: {
      type: new lu(new pu(Ie)),
      resolve(t) {
        if (Nr(t) || Qn(t))
          return t.getInterfaces();
      }
    },
    possibleTypes: {
      type: new lu(new pu(Ie)),
      resolve(t, e, n, { schema: r }) {
        if (oc(t))
          return r.getPossibleTypes(t);
      }
    },
    enumValues: {
      type: new lu(new pu(cs)),
      args: {
        includeDeprecated: {
          type: X,
          defaultValue: false
        }
      },
      resolve(t, { includeDeprecated: e }) {
        if (Kn(t)) {
          const n = t.getValues();
          return e ? n : n.filter((r) => r.deprecationReason == null);
        }
      }
    },
    inputFields: {
      type: new lu(new pu(Un2)),
      args: {
        includeDeprecated: {
          type: X,
          defaultValue: false
        }
      },
      resolve(t, { includeDeprecated: e }) {
        if (Yr(t)) {
          const n = Object.values(t.getFields());
          return e ? n : n.filter((r) => r.deprecationReason == null);
        }
      }
    },
    ofType: {
      type: Ie,
      resolve: (t) => "ofType" in t ? t.ofType : void 0
    },
    isOneOf: {
      type: X,
      resolve: (t) => {
        if (Yr(t))
          return t.isOneOf;
      }
    }
  })
});
var us = new fu({
  name: "__Field",
  description: "Object and Interface types are described by a list of Fields, each of which has a name, potentially a list of arguments, and a return type.",
  fields: () => ({
    name: {
      type: new pu(V),
      resolve: (t) => t.name
    },
    description: {
      type: V,
      resolve: (t) => t.description
    },
    args: {
      type: new pu(
        new lu(new pu(Un2))
      ),
      args: {
        includeDeprecated: {
          type: X,
          defaultValue: false
        }
      },
      resolve(t, { includeDeprecated: e }) {
        return e ? t.args : t.args.filter((n) => n.deprecationReason == null);
      }
    },
    type: {
      type: new pu(Ie),
      resolve: (t) => t.type
    },
    isDeprecated: {
      type: new pu(X),
      resolve: (t) => t.deprecationReason != null
    },
    deprecationReason: {
      type: V,
      resolve: (t) => t.deprecationReason
    }
  })
});
var Un2 = new fu({
  name: "__InputValue",
  description: "Arguments provided to Fields or Directives and the input fields of an InputObject are represented as Input Values which describe their type and optionally a default value.",
  fields: () => ({
    name: {
      type: new pu(V),
      resolve: (t) => t.name
    },
    description: {
      type: V,
      resolve: (t) => t.description
    },
    type: {
      type: new pu(Ie),
      resolve: (t) => t.type
    },
    defaultValue: {
      type: V,
      description: "A GraphQL-formatted string representing the default value for this input value.",
      resolve(t) {
        const { type: e, defaultValue: n } = t, r = Mt(n, e);
        return r ? gr(r) : null;
      }
    },
    isDeprecated: {
      type: new pu(X),
      resolve: (t) => t.deprecationReason != null
    },
    deprecationReason: {
      type: V,
      resolve: (t) => t.deprecationReason
    }
  })
});
var cs = new fu({
  name: "__EnumValue",
  description: "One possible value for a given Enum. Enum values are unique values, not a placeholder for a string or numeric value. However an Enum value is returned in a JSON response as a string.",
  fields: () => ({
    name: {
      type: new pu(V),
      resolve: (t) => t.name
    },
    description: {
      type: V,
      resolve: (t) => t.description
    },
    isDeprecated: {
      type: new pu(X),
      resolve: (t) => t.deprecationReason != null
    },
    deprecationReason: {
      type: V,
      resolve: (t) => t.deprecationReason
    }
  })
});
var z;
(function(t) {
  t.SCALAR = "SCALAR", t.OBJECT = "OBJECT", t.INTERFACE = "INTERFACE", t.UNION = "UNION", t.ENUM = "ENUM", t.INPUT_OBJECT = "INPUT_OBJECT", t.LIST = "LIST", t.NON_NULL = "NON_NULL";
})(z || (z = {}));
var fs = new bu({
  name: "__TypeKind",
  description: "An enum describing what kind of type a given `__Type` is.",
  values: {
    SCALAR: {
      value: z.SCALAR,
      description: "Indicates this type is a scalar."
    },
    OBJECT: {
      value: z.OBJECT,
      description: "Indicates this type is an object. `fields` and `interfaces` are valid fields."
    },
    INTERFACE: {
      value: z.INTERFACE,
      description: "Indicates this type is an interface. `fields`, `interfaces`, and `possibleTypes` are valid fields."
    },
    UNION: {
      value: z.UNION,
      description: "Indicates this type is a union. `possibleTypes` is a valid field."
    },
    ENUM: {
      value: z.ENUM,
      description: "Indicates this type is an enum. `enumValues` is a valid field."
    },
    INPUT_OBJECT: {
      value: z.INPUT_OBJECT,
      description: "Indicates this type is an input object. `inputFields` is a valid field."
    },
    LIST: {
      value: z.LIST,
      description: "Indicates this type is a list. `ofType` is a valid field."
    },
    NON_NULL: {
      value: z.NON_NULL,
      description: "Indicates this type is a non-null. `ofType` is a valid field."
    }
  }
});
var En = {
  name: "__schema",
  type: new pu(Nr2),
  description: "Access the current type schema of this server.",
  args: [],
  resolve: (t, e, n, { schema: r }) => r,
  deprecationReason: void 0,
  extensions: /* @__PURE__ */ Object.create(null),
  astNode: void 0
};
var wn = {
  name: "__type",
  type: Ie,
  description: "Request the type information of a single type.",
  args: [
    {
      name: "name",
      description: void 0,
      type: new pu(V),
      defaultValue: void 0,
      deprecationReason: void 0,
      extensions: /* @__PURE__ */ Object.create(null),
      astNode: void 0
    }
  ],
  resolve: (t, { name: e }, n, { schema: r }) => r.getType(e),
  deprecationReason: void 0,
  extensions: /* @__PURE__ */ Object.create(null),
  astNode: void 0
};
var Sn = {
  name: "__typename",
  type: new pu(V),
  description: "The name of the current Object type at runtime.",
  args: [],
  resolve: (t, e, n, { parentType: r }) => r.name,
  deprecationReason: void 0,
  extensions: /* @__PURE__ */ Object.create(null),
  astNode: void 0
};
var ds = Object.freeze([
  Nr2,
  as,
  ls,
  Ie,
  us,
  Un2,
  cs,
  fs
]);
function ps(t) {
  return ds.some(({ name: e }) => t.name === e);
}
function hs(t) {
  return rr(t, Rr);
}
function ra(t) {
  if (!hs(t))
    throw new Error(`Expected ${It(t)} to be a GraphQL schema.`);
  return t;
}
var Rr = class {
  // Used as a cache for validateSchema().
  constructor(e) {
    var n, r;
    this.__validationErrors = e.assumeValid === true ? [] : void 0, Rs(e) || we(false, "Must provide configuration object."), !e.types || Array.isArray(e.types) || we(
      false,
      `"types" must be Array if provided but got: ${It(e.types)}.`
    ), !e.directives || Array.isArray(e.directives) || we(
      false,
      `"directives" must be Array if provided but got: ${It(e.directives)}.`
    ), this.description = e.description, this.extensions = $e(e.extensions), this.astNode = e.astNode, this.extensionASTNodes = (n = e.extensionASTNodes) !== null && n !== void 0 ? n : [], this._queryType = e.query, this._mutationType = e.mutation, this._subscriptionType = e.subscription, this._directives = (r = e.directives) !== null && r !== void 0 ? r : tn;
    const i = new Set(e.types);
    if (e.types != null)
      for (const s of e.types)
        i.delete(s), Ee(s, i);
    this._queryType != null && Ee(this._queryType, i), this._mutationType != null && Ee(this._mutationType, i), this._subscriptionType != null && Ee(this._subscriptionType, i);
    for (const s of this._directives)
      if (is(s))
        for (const o of s.args)
          Ee(o.type, i);
    Ee(Nr2, i), this._typeMap = /* @__PURE__ */ Object.create(null), this._subTypeMap = /* @__PURE__ */ Object.create(null), this._implementationsMap = /* @__PURE__ */ Object.create(null);
    for (const s of i) {
      if (s == null)
        continue;
      const o = s.name;
      if (o || we(
        false,
        "One of the provided types for building the Schema is missing a name."
      ), this._typeMap[o] !== void 0)
        throw new Error(
          `Schema must contain uniquely named types but contains multiple types named "${o}".`
        );
      if (this._typeMap[o] = s, Qn(s)) {
        for (const a of s.getInterfaces())
          if (Qn(a)) {
            let l = this._implementationsMap[a.name];
            l === void 0 && (l = this._implementationsMap[a.name] = {
              objects: [],
              interfaces: []
            }), l.interfaces.push(s);
          }
      } else if (Nr(s)) {
        for (const a of s.getInterfaces())
          if (Qn(a)) {
            let l = this._implementationsMap[a.name];
            l === void 0 && (l = this._implementationsMap[a.name] = {
              objects: [],
              interfaces: []
            }), l.objects.push(s);
          }
      }
    }
  }
  get [Symbol.toStringTag]() {
    return "GraphQLSchema";
  }
  getQueryType() {
    return this._queryType;
  }
  getMutationType() {
    return this._mutationType;
  }
  getSubscriptionType() {
    return this._subscriptionType;
  }
  getRootType(e) {
    switch (e) {
      case dt.QUERY:
        return this.getQueryType();
      case dt.MUTATION:
        return this.getMutationType();
      case dt.SUBSCRIPTION:
        return this.getSubscriptionType();
    }
  }
  getTypeMap() {
    return this._typeMap;
  }
  getType(e) {
    return this.getTypeMap()[e];
  }
  getPossibleTypes(e) {
    return qn(e) ? e.getTypes() : this.getImplementations(e).objects;
  }
  getImplementations(e) {
    const n = this._implementationsMap[e.name];
    return n ?? {
      objects: [],
      interfaces: []
    };
  }
  isSubType(e, n) {
    let r = this._subTypeMap[e.name];
    if (r === void 0) {
      if (r = /* @__PURE__ */ Object.create(null), qn(e))
        for (const i of e.getTypes())
          r[i.name] = true;
      else {
        const i = this.getImplementations(e);
        for (const s of i.objects)
          r[s.name] = true;
        for (const s of i.interfaces)
          r[s.name] = true;
      }
      this._subTypeMap[e.name] = r;
    }
    return r[n.name] !== void 0;
  }
  getDirectives() {
    return this._directives;
  }
  getDirective(e) {
    return this.getDirectives().find((n) => n.name === e);
  }
  toConfig() {
    return {
      description: this.description,
      query: this.getQueryType(),
      mutation: this.getMutationType(),
      subscription: this.getSubscriptionType(),
      types: Object.values(this.getTypeMap()),
      directives: this.getDirectives(),
      extensions: this.extensions,
      astNode: this.astNode,
      extensionASTNodes: this.extensionASTNodes,
      assumeValid: this.__validationErrors !== void 0
    };
  }
};
function Ee(t, e) {
  const n = lc(t);
  if (!e.has(n)) {
    if (e.add(n), qn(n))
      for (const r of n.getTypes())
        Ee(r, e);
    else if (Nr(n) || Qn(n)) {
      for (const r of n.getInterfaces())
        Ee(r, e);
      for (const r of Object.values(n.getFields())) {
        Ee(r.type, e);
        for (const i of r.args)
          Ee(i.type, e);
      }
    } else if (Yr(n))
      for (const r of Object.values(n.getFields()))
        Ee(r.type, e);
  }
  return e;
}
function ia(t) {
  if (ra(t), t.__validationErrors)
    return t.__validationErrors;
  const e = new sa(t);
  oa(e), aa(e), la(e);
  const n = e.getErrors();
  return t.__validationErrors = n, n;
}
function gs(t) {
  const e = ia(t);
  if (e.length !== 0)
    throw new Error(e.map((n) => n.message).join(`

`));
}
var sa = class {
  constructor(e) {
    this._errors = [], this.schema = e;
  }
  reportError(e, n) {
    const r = Array.isArray(n) ? n.filter(Boolean) : n;
    this._errors.push(
      new qe(e, {
        nodes: r
      })
    );
  }
  getErrors() {
    return this._errors;
  }
};
function oa(t) {
  const e = t.schema, n = e.getQueryType();
  if (!n)
    t.reportError("Query root type must be provided.", e.astNode);
  else if (!Nr(n)) {
    var r;
    t.reportError(
      `Query root type must be Object type, it cannot be ${It(
        n
      )}.`,
      (r = er(
        e,
        dt.QUERY
      )) !== null && r !== void 0 ? r : n.astNode
    );
  }
  const i = e.getMutationType();
  if (i && !Nr(i)) {
    var s;
    t.reportError(
      `Mutation root type must be Object type if provided, it cannot be ${It(i)}.`,
      (s = er(
        e,
        dt.MUTATION
      )) !== null && s !== void 0 ? s : i.astNode
    );
  }
  const o = e.getSubscriptionType();
  if (o && !Nr(o)) {
    var a;
    t.reportError(
      `Subscription root type must be Object type if provided, it cannot be ${It(o)}.`,
      (a = er(
        e,
        dt.SUBSCRIPTION
      )) !== null && a !== void 0 ? a : o.astNode
    );
  }
}
function er(t, e) {
  var n;
  return (n = [t.astNode, ...t.extensionASTNodes].flatMap(
    // FIXME: https://github.com/graphql/graphql-js/issues/2203
    (r) => {
      var i;
      return (
        /* c8 ignore next */
        (i = r?.operationTypes) !== null && i !== void 0 ? i : []
      );
    }
  ).find((r) => r.operation === e)) === null || n === void 0 ? void 0 : n.type;
}
function aa(t) {
  for (const n of t.schema.getDirectives()) {
    if (!is(n)) {
      t.reportError(
        `Expected directive but got: ${It(n)}.`,
        n?.astNode
      );
      continue;
    }
    st(t, n), n.locations.length === 0 && t.reportError(
      `Directive @${n.name} must include 1 or more locations.`,
      n.astNode
    );
    for (const r of n.args)
      if (st(t, r), uu(r.type) || t.reportError(
        `The type of @${n.name}(${r.name}:) must be Input Type but got: ${It(r.type)}.`,
        r.astNode
      ), pc(r) && r.deprecationReason != null) {
        var e;
        t.reportError(
          `Required argument @${n.name}(${r.name}:) cannot be deprecated.`,
          [
            kr(r.astNode),
            (e = r.astNode) === null || e === void 0 ? void 0 : e.type
          ]
        );
      }
  }
}
function st(t, e) {
  e.name.startsWith("__") && t.reportError(
    `Name "${e.name}" must not begin with "__", which is reserved by GraphQL introspection.`,
    e.astNode
  );
}
function la(t) {
  const e = ga(t), n = t.schema.getTypeMap();
  for (const r of Object.values(n)) {
    if (!cc(r)) {
      t.reportError(
        `Expected GraphQL named type but got: ${It(r)}.`,
        r.astNode
      );
      continue;
    }
    ps(r) || st(t, r), Nr(r) || Qn(r) ? (zr2(t, r), Jr(t, r)) : qn(r) ? fa(t, r) : Kn(r) ? da(t, r) : Yr(r) && (pa(t, r), e(r));
  }
}
function zr2(t, e) {
  const n = Object.values(e.getFields());
  n.length === 0 && t.reportError(`Type ${e.name} must define one or more fields.`, [
    e.astNode,
    ...e.extensionASTNodes
  ]);
  for (const o of n) {
    if (st(t, o), !cu(o.type)) {
      var r;
      t.reportError(
        `The type of ${e.name}.${o.name} must be Output Type but got: ${It(o.type)}.`,
        (r = o.astNode) === null || r === void 0 ? void 0 : r.type
      );
    }
    for (const a of o.args) {
      const l = a.name;
      if (st(t, a), !uu(a.type)) {
        var i;
        t.reportError(
          `The type of ${e.name}.${o.name}(${l}:) must be Input Type but got: ${It(a.type)}.`,
          (i = a.astNode) === null || i === void 0 ? void 0 : i.type
        );
      }
      if (pc(a) && a.deprecationReason != null) {
        var s;
        t.reportError(
          `Required argument ${e.name}.${o.name}(${l}:) cannot be deprecated.`,
          [
            kr(a.astNode),
            (s = a.astNode) === null || s === void 0 ? void 0 : s.type
          ]
        );
      }
    }
  }
}
function Jr(t, e) {
  const n = /* @__PURE__ */ Object.create(null);
  for (const r of e.getInterfaces()) {
    if (!Qn(r)) {
      t.reportError(
        `Type ${It(e)} must only implement Interface types, it cannot implement ${It(r)}.`,
        Gt(e, r)
      );
      continue;
    }
    if (e === r) {
      t.reportError(
        `Type ${e.name} cannot implement itself because it would create a circular reference.`,
        Gt(e, r)
      );
      continue;
    }
    if (n[r.name]) {
      t.reportError(
        `Type ${e.name} can only implement ${r.name} once.`,
        Gt(e, r)
      );
      continue;
    }
    n[r.name] = true, ca(t, e, r), ua(t, e, r);
  }
}
function ua(t, e, n) {
  const r = e.getFields();
  for (const l of Object.values(n.getFields())) {
    const u2 = l.name, c = r[u2];
    if (!c) {
      t.reportError(
        `Interface field ${n.name}.${u2} expected but ${e.name} does not provide it.`,
        [l.astNode, e.astNode, ...e.extensionASTNodes]
      );
      continue;
    }
    if (!Tt(t.schema, c.type, l.type)) {
      var i, s;
      t.reportError(
        `Interface field ${n.name}.${u2} expects type ${It(l.type)} but ${e.name}.${u2} is type ${It(c.type)}.`,
        [
          (i = l.astNode) === null || i === void 0 ? void 0 : i.type,
          (s = c.astNode) === null || s === void 0 ? void 0 : s.type
        ]
      );
    }
    for (const f2 of l.args) {
      const d = f2.name, h = c.args.find((g2) => g2.name === d);
      if (!h) {
        t.reportError(
          `Interface field argument ${n.name}.${u2}(${d}:) expected but ${e.name}.${u2} does not provide it.`,
          [f2.astNode, c.astNode]
        );
        continue;
      }
      if (!hr(f2.type, h.type)) {
        var o, a;
        t.reportError(
          `Interface field argument ${n.name}.${u2}(${d}:) expects type ${It(f2.type)} but ${e.name}.${u2}(${d}:) is type ${It(h.type)}.`,
          [
            (o = f2.astNode) === null || o === void 0 ? void 0 : o.type,
            (a = h.astNode) === null || a === void 0 ? void 0 : a.type
          ]
        );
      }
    }
    for (const f2 of c.args) {
      const d = f2.name;
      !l.args.find((g2) => g2.name === d) && pc(f2) && t.reportError(
        `Object field ${e.name}.${u2} includes required argument ${d} that is missing from the Interface field ${n.name}.${u2}.`,
        [f2.astNode, l.astNode]
      );
    }
  }
}
function ca(t, e, n) {
  const r = e.getInterfaces();
  for (const i of n.getInterfaces())
    r.includes(i) || t.reportError(
      i === e ? `Type ${e.name} cannot implement ${n.name} because it would create a circular reference.` : `Type ${e.name} must implement ${i.name} because it is implemented by ${n.name}.`,
      [
        ...Gt(n, i),
        ...Gt(e, n)
      ]
    );
}
function fa(t, e) {
  const n = e.getTypes();
  n.length === 0 && t.reportError(
    `Union type ${e.name} must define one or more member types.`,
    [e.astNode, ...e.extensionASTNodes]
  );
  const r = /* @__PURE__ */ Object.create(null);
  for (const i of n) {
    if (r[i.name]) {
      t.reportError(
        `Union type ${e.name} can only include type ${i.name} once.`,
        Yr2(e, i.name)
      );
      continue;
    }
    r[i.name] = true, Nr(i) || t.reportError(
      `Union type ${e.name} can only include Object types, it cannot include ${It(i)}.`,
      Yr2(e, String(i))
    );
  }
}
function da(t, e) {
  const n = e.getValues();
  n.length === 0 && t.reportError(
    `Enum type ${e.name} must define one or more values.`,
    [e.astNode, ...e.extensionASTNodes]
  );
  for (const r of n)
    st(t, r);
}
function pa(t, e) {
  const n = Object.values(e.getFields());
  n.length === 0 && t.reportError(
    `Input Object type ${e.name} must define one or more fields.`,
    [e.astNode, ...e.extensionASTNodes]
  );
  for (const s of n) {
    if (st(t, s), !uu(s.type)) {
      var r;
      t.reportError(
        `The type of ${e.name}.${s.name} must be Input Type but got: ${It(s.type)}.`,
        (r = s.astNode) === null || r === void 0 ? void 0 : r.type
      );
    }
    if (dc(s) && s.deprecationReason != null) {
      var i;
      t.reportError(
        `Required input field ${e.name}.${s.name} cannot be deprecated.`,
        [
          kr(s.astNode),
          (i = s.astNode) === null || i === void 0 ? void 0 : i.type
        ]
      );
    }
    e.isOneOf && ha(e, s, t);
  }
}
function ha(t, e, n) {
  if (Nn(e.type)) {
    var r;
    n.reportError(
      `OneOf input field ${t.name}.${e.name} must be nullable.`,
      (r = e.astNode) === null || r === void 0 ? void 0 : r.type
    );
  }
  e.defaultValue !== void 0 && n.reportError(
    `OneOf input field ${t.name}.${e.name} cannot have a default value.`,
    e.astNode
  );
}
function ga(t) {
  const e = /* @__PURE__ */ Object.create(null), n = [], r = /* @__PURE__ */ Object.create(null);
  return i;
  function i(s) {
    if (e[s.name])
      return;
    e[s.name] = true, r[s.name] = n.length;
    const o = Object.values(s.getFields());
    for (const a of o)
      if (Nn(a.type) && Yr(a.type.ofType)) {
        const l = a.type.ofType, u2 = r[l.name];
        if (n.push(a), u2 === void 0)
          i(l);
        else {
          const c = n.slice(u2), f2 = c.map((d) => d.name).join(".");
          t.reportError(
            `Cannot reference Input Object "${l.name}" within itself through a series of non-null fields: "${f2}".`,
            c.map((d) => d.astNode)
          );
        }
        n.pop();
      }
    r[s.name] = void 0;
  }
}
function Gt(t, e) {
  const { astNode: n, extensionASTNodes: r } = t;
  return (n != null ? [n, ...r] : r).flatMap((s) => {
    var o;
    return (
      /* c8 ignore next */
      (o = s.interfaces) !== null && o !== void 0 ? o : []
    );
  }).filter((s) => s.name.value === e.name);
}
function Yr2(t, e) {
  const { astNode: n, extensionASTNodes: r } = t;
  return (n != null ? [n, ...r] : r).flatMap((s) => {
    var o;
    return (
      /* c8 ignore next */
      (o = s.types) !== null && o !== void 0 ? o : []
    );
  }).filter((s) => s.name.value === e);
}
function kr(t) {
  var e;
  return t == null || (e = t.directives) === null || e === void 0 ? void 0 : e.find(
    (n) => n.name.value === ss.name
  );
}
function fe(t, e) {
  switch (e.kind) {
    case ye.LIST_TYPE: {
      const n = fe(t, e.type);
      return n && new lu(n);
    }
    case ye.NON_NULL_TYPE: {
      const n = fe(t, e.type);
      return n && new pu(n);
    }
    case ye.NAMED_TYPE:
      return t.getType(e.name.value);
  }
}
var ms = class {
  constructor(e, n, r) {
    this._schema = e, this._typeStack = [], this._parentTypeStack = [], this._inputTypeStack = [], this._fieldDefStack = [], this._defaultValueStack = [], this._directive = null, this._argument = null, this._enumValue = null, this._getFieldDef = r ?? ma, n && (uu(n) && this._inputTypeStack.push(n), sc(n) && this._parentTypeStack.push(n), cu(n) && this._typeStack.push(n));
  }
  get [Symbol.toStringTag]() {
    return "TypeInfo";
  }
  getType() {
    if (this._typeStack.length > 0)
      return this._typeStack[this._typeStack.length - 1];
  }
  getParentType() {
    if (this._parentTypeStack.length > 0)
      return this._parentTypeStack[this._parentTypeStack.length - 1];
  }
  getInputType() {
    if (this._inputTypeStack.length > 0)
      return this._inputTypeStack[this._inputTypeStack.length - 1];
  }
  getParentInputType() {
    if (this._inputTypeStack.length > 1)
      return this._inputTypeStack[this._inputTypeStack.length - 2];
  }
  getFieldDef() {
    if (this._fieldDefStack.length > 0)
      return this._fieldDefStack[this._fieldDefStack.length - 1];
  }
  getDefaultValue() {
    if (this._defaultValueStack.length > 0)
      return this._defaultValueStack[this._defaultValueStack.length - 1];
  }
  getDirective() {
    return this._directive;
  }
  getArgument() {
    return this._argument;
  }
  getEnumValue() {
    return this._enumValue;
  }
  enter(e) {
    const n = this._schema;
    switch (e.kind) {
      case ye.SELECTION_SET: {
        const i = lc(this.getType());
        this._parentTypeStack.push(
          sc(i) ? i : void 0
        );
        break;
      }
      case ye.FIELD: {
        const i = this.getParentType();
        let s, o;
        i && (s = this._getFieldDef(n, i, e), s && (o = s.type)), this._fieldDefStack.push(s), this._typeStack.push(cu(o) ? o : void 0);
        break;
      }
      case ye.DIRECTIVE:
        this._directive = n.getDirective(e.name.value);
        break;
      case ye.OPERATION_DEFINITION: {
        const i = n.getRootType(e.operation);
        this._typeStack.push(Nr(i) ? i : void 0);
        break;
      }
      case ye.INLINE_FRAGMENT:
      case ye.FRAGMENT_DEFINITION: {
        const i = e.typeCondition, s = i ? fe(n, i) : lc(this.getType());
        this._typeStack.push(cu(s) ? s : void 0);
        break;
      }
      case ye.VARIABLE_DEFINITION: {
        const i = fe(n, e.type);
        this._inputTypeStack.push(
          uu(i) ? i : void 0
        );
        break;
      }
      case ye.ARGUMENT: {
        var r;
        let i, s;
        const o = (r = this.getDirective()) !== null && r !== void 0 ? r : this.getFieldDef();
        o && (i = o.args.find(
          (a) => a.name === e.name.value
        ), i && (s = i.type)), this._argument = i, this._defaultValueStack.push(i ? i.defaultValue : void 0), this._inputTypeStack.push(uu(s) ? s : void 0);
        break;
      }
      case ye.LIST: {
        const i = uc(this.getInputType()), s = Ms(i) ? i.ofType : i;
        this._defaultValueStack.push(void 0), this._inputTypeStack.push(uu(s) ? s : void 0);
        break;
      }
      case ye.OBJECT_FIELD: {
        const i = lc(this.getInputType());
        let s, o;
        Yr(i) && (o = i.getFields()[e.name.value], o && (s = o.type)), this._defaultValueStack.push(
          o ? o.defaultValue : void 0
        ), this._inputTypeStack.push(
          uu(s) ? s : void 0
        );
        break;
      }
      case ye.ENUM: {
        const i = lc(this.getInputType());
        let s;
        Kn(i) && (s = i.getValue(e.value)), this._enumValue = s;
        break;
      }
    }
  }
  leave(e) {
    switch (e.kind) {
      case ye.SELECTION_SET:
        this._parentTypeStack.pop();
        break;
      case ye.FIELD:
        this._fieldDefStack.pop(), this._typeStack.pop();
        break;
      case ye.DIRECTIVE:
        this._directive = null;
        break;
      case ye.OPERATION_DEFINITION:
      case ye.INLINE_FRAGMENT:
      case ye.FRAGMENT_DEFINITION:
        this._typeStack.pop();
        break;
      case ye.VARIABLE_DEFINITION:
        this._inputTypeStack.pop();
        break;
      case ye.ARGUMENT:
        this._argument = null, this._defaultValueStack.pop(), this._inputTypeStack.pop();
        break;
      case ye.LIST:
      case ye.OBJECT_FIELD:
        this._defaultValueStack.pop(), this._inputTypeStack.pop();
        break;
      case ye.ENUM:
        this._enumValue = null;
        break;
    }
  }
};
function ma(t, e, n) {
  const r = n.name.value;
  if (r === En.name && t.getQueryType() === e)
    return En;
  if (r === wn.name && t.getQueryType() === e)
    return wn;
  if (r === Sn.name && sc(e))
    return Sn;
  if (Nr(e) || Qn(e))
    return e.getFields()[r];
}
function ys(t, e) {
  return {
    enter(...n) {
      const r = n[0];
      t.enter(r);
      const i = Vs(e, r.kind).enter;
      if (i) {
        const s = i.apply(e, n);
        return s !== void 0 && (t.leave(r), Ga(s) && t.enter(s)), s;
      }
    },
    leave(...n) {
      const r = n[0], i = Vs(e, r.kind).leave;
      let s;
      return i && (s = i.apply(e, n)), t.leave(r), s;
    }
  };
}
function ya(t) {
  return t.kind === ye.OPERATION_DEFINITION || t.kind === ye.FRAGMENT_DEFINITION;
}
function ba(t) {
  return t.kind === ye.SCHEMA_DEFINITION || Ar(t) || t.kind === ye.DIRECTIVE_DEFINITION;
}
function Ar(t) {
  return t.kind === ye.SCALAR_TYPE_DEFINITION || t.kind === ye.OBJECT_TYPE_DEFINITION || t.kind === ye.INTERFACE_TYPE_DEFINITION || t.kind === ye.UNION_TYPE_DEFINITION || t.kind === ye.ENUM_TYPE_DEFINITION || t.kind === ye.INPUT_OBJECT_TYPE_DEFINITION;
}
function va(t) {
  return t.kind === ye.SCHEMA_EXTENSION || bs(t);
}
function bs(t) {
  return t.kind === ye.SCALAR_TYPE_EXTENSION || t.kind === ye.OBJECT_TYPE_EXTENSION || t.kind === ye.INTERFACE_TYPE_EXTENSION || t.kind === ye.UNION_TYPE_EXTENSION || t.kind === ye.ENUM_TYPE_EXTENSION || t.kind === ye.INPUT_OBJECT_TYPE_EXTENSION;
}
function Ta(t) {
  return {
    Document(e) {
      for (const n of e.definitions)
        if (!ya(n)) {
          const r = n.kind === ye.SCHEMA_DEFINITION || n.kind === ye.SCHEMA_EXTENSION ? "schema" : '"' + n.name.value + '"';
          t.reportError(
            new qe(`The ${r} definition is not executable.`, {
              nodes: n
            })
          );
        }
      return false;
    }
  };
}
function Ea(t) {
  return {
    Field(e) {
      const n = t.getParentType();
      if (n && !t.getFieldDef()) {
        const i = t.getSchema(), s = e.name.value;
        let o = Jo(
          "to use an inline fragment on",
          wa(i, n, s)
        );
        o === "" && (o = Jo(Sa(n, s))), t.reportError(
          new qe(
            `Cannot query field "${s}" on type "${n.name}".` + o,
            {
              nodes: e
            }
          )
        );
      }
    }
  };
}
function wa(t, e, n) {
  if (!oc(e))
    return [];
  const r = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ Object.create(null);
  for (const o of t.getPossibleTypes(e))
    if (o.getFields()[n]) {
      r.add(o), i[o.name] = 1;
      for (const a of o.getInterfaces()) {
        var s;
        a.getFields()[n] && (r.add(a), i[a.name] = ((s = i[a.name]) !== null && s !== void 0 ? s : 0) + 1);
      }
    }
  return [...r].sort((o, a) => {
    const l = i[a.name] - i[o.name];
    return l !== 0 ? l : Qn(o) && t.isSubType(o, a) ? -1 : Qn(a) && t.isSubType(a, o) ? 1 : Xo(o.name, a.name);
  }).map((o) => o.name);
}
function Sa(t, e) {
  if (Nr(t) || Qn(t)) {
    const n = Object.keys(t.getFields());
    return Wo(e, n);
  }
  return [];
}
function Ia(t) {
  return {
    InlineFragment(e) {
      const n = e.typeCondition;
      if (n) {
        const r = fe(t.getSchema(), n);
        if (r && !sc(r)) {
          const i = gr(n);
          t.reportError(
            new qe(
              `Fragment cannot condition on non composite type "${i}".`,
              {
                nodes: n
              }
            )
          );
        }
      }
    },
    FragmentDefinition(e) {
      const n = fe(t.getSchema(), e.typeCondition);
      if (n && !sc(n)) {
        const r = gr(e.typeCondition);
        t.reportError(
          new qe(
            `Fragment "${e.name.value}" cannot condition on non composite type "${r}".`,
            {
              nodes: e.typeCondition
            }
          )
        );
      }
    }
  };
}
function Oa(t) {
  return {
    // eslint-disable-next-line new-cap
    ..._a(t),
    Argument(e) {
      const n = t.getArgument(), r = t.getFieldDef(), i = t.getParentType();
      if (!n && r && i) {
        const s = e.name.value, o = r.args.map((l) => l.name), a = Wo(s, o);
        t.reportError(
          new qe(
            `Unknown argument "${s}" on field "${i.name}.${r.name}".` + Jo(a),
            {
              nodes: e
            }
          )
        );
      }
    }
  };
}
function _a(t) {
  const e = /* @__PURE__ */ Object.create(null), n = t.getSchema(), r = n ? n.getDirectives() : tn;
  for (const o of r)
    e[o.name] = o.args.map((a) => a.name);
  const i = t.getDocument().definitions;
  for (const o of i)
    if (o.kind === ye.DIRECTIVE_DEFINITION) {
      var s;
      const a = (s = o.arguments) !== null && s !== void 0 ? s : [];
      e[o.name.value] = a.map((l) => l.name.value);
    }
  return {
    Directive(o) {
      const a = o.name.value, l = e[a];
      if (o.arguments && l)
        for (const u2 of o.arguments) {
          const c = u2.name.value;
          if (!l.includes(c)) {
            const f2 = Wo(c, l);
            t.reportError(
              new qe(
                `Unknown argument "${c}" on directive "@${a}".` + Jo(f2),
                {
                  nodes: u2
                }
              )
            );
          }
        }
      return false;
    }
  };
}
function Na(t) {
  const e = /* @__PURE__ */ Object.create(null), n = t.getSchema(), r = n ? n.getDirectives() : tn;
  for (const s of r)
    e[s.name] = s.locations;
  const i = t.getDocument().definitions;
  for (const s of i)
    s.kind === ye.DIRECTIVE_DEFINITION && (e[s.name.value] = s.locations.map((o) => o.value));
  return {
    Directive(s, o, a, l, u2) {
      const c = s.name.value, f2 = e[c];
      if (!f2) {
        t.reportError(
          new qe(`Unknown directive "@${c}".`, {
            nodes: s
          })
        );
        return;
      }
      const d = Ra(u2);
      d && !f2.includes(d) && t.reportError(
        new qe(
          `Directive "@${c}" may not be used on ${d}.`,
          {
            nodes: s
          }
        )
      );
    }
  };
}
function Ra(t) {
  const e = t[t.length - 1];
  switch ("kind" in e || Ps(false), e.kind) {
    case ye.OPERATION_DEFINITION:
      return ka(e.operation);
    case ye.FIELD:
      return A.FIELD;
    case ye.FRAGMENT_SPREAD:
      return A.FRAGMENT_SPREAD;
    case ye.INLINE_FRAGMENT:
      return A.INLINE_FRAGMENT;
    case ye.FRAGMENT_DEFINITION:
      return A.FRAGMENT_DEFINITION;
    case ye.VARIABLE_DEFINITION:
      return A.VARIABLE_DEFINITION;
    case ye.SCHEMA_DEFINITION:
    case ye.SCHEMA_EXTENSION:
      return A.SCHEMA;
    case ye.SCALAR_TYPE_DEFINITION:
    case ye.SCALAR_TYPE_EXTENSION:
      return A.SCALAR;
    case ye.OBJECT_TYPE_DEFINITION:
    case ye.OBJECT_TYPE_EXTENSION:
      return A.OBJECT;
    case ye.FIELD_DEFINITION:
      return A.FIELD_DEFINITION;
    case ye.INTERFACE_TYPE_DEFINITION:
    case ye.INTERFACE_TYPE_EXTENSION:
      return A.INTERFACE;
    case ye.UNION_TYPE_DEFINITION:
    case ye.UNION_TYPE_EXTENSION:
      return A.UNION;
    case ye.ENUM_TYPE_DEFINITION:
    case ye.ENUM_TYPE_EXTENSION:
      return A.ENUM;
    case ye.ENUM_VALUE_DEFINITION:
      return A.ENUM_VALUE;
    case ye.INPUT_OBJECT_TYPE_DEFINITION:
    case ye.INPUT_OBJECT_TYPE_EXTENSION:
      return A.INPUT_OBJECT;
    case ye.INPUT_VALUE_DEFINITION: {
      const n = t[t.length - 3];
      return "kind" in n || Ps(false), n.kind === ye.INPUT_OBJECT_TYPE_DEFINITION ? A.INPUT_FIELD_DEFINITION : A.ARGUMENT_DEFINITION;
    }
    // Not reachable, all possible types have been considered.
    /* c8 ignore next */
    default:
      Ps(false, "Unexpected kind: " + It(e.kind));
  }
}
function ka(t) {
  switch (t) {
    case dt.QUERY:
      return A.QUERY;
    case dt.MUTATION:
      return A.MUTATION;
    case dt.SUBSCRIPTION:
      return A.SUBSCRIPTION;
  }
}
function Aa(t) {
  return {
    FragmentSpread(e) {
      const n = e.name.value;
      t.getFragment(n) || t.reportError(
        new qe(`Unknown fragment "${n}".`, {
          nodes: e.name
        })
      );
    }
  };
}
function Fa(t) {
  const e = t.getSchema(), n = e ? e.getTypeMap() : /* @__PURE__ */ Object.create(null), r = /* @__PURE__ */ Object.create(null);
  for (const s of t.getDocument().definitions)
    Ar(s) && (r[s.name.value] = true);
  const i = [
    ...Object.keys(n),
    ...Object.keys(r)
  ];
  return {
    NamedType(s, o, a, l, u2) {
      const c = s.name.value;
      if (!n[c] && !r[c]) {
        var f2;
        const d = (f2 = u2[2]) !== null && f2 !== void 0 ? f2 : a, h = d != null && Ca(d);
        if (h && Xr.includes(c))
          return;
        const g2 = Wo(
          c,
          h ? Xr.concat(i) : i
        );
        t.reportError(
          new qe(
            `Unknown type "${c}".` + Jo(g2),
            {
              nodes: s
            }
          )
        );
      }
    }
  };
}
var Xr = [...Zo, ...ds].map(
  (t) => t.name
);
function Ca(t) {
  return "kind" in t && (ba(t) || va(t));
}
function xa(t) {
  let e = 0;
  return {
    Document(n) {
      e = n.definitions.filter(
        (r) => r.kind === ye.OPERATION_DEFINITION
      ).length;
    },
    OperationDefinition(n) {
      !n.name && e > 1 && t.reportError(
        new qe(
          "This anonymous operation must be the only defined operation.",
          {
            nodes: n
          }
        )
      );
    }
  };
}
var Da = 3;
function La(t) {
  function e(n, r = /* @__PURE__ */ Object.create(null), i = 0) {
    if (n.kind === ye.FRAGMENT_SPREAD) {
      const s = n.name.value;
      if (r[s] === true)
        return false;
      const o = t.getFragment(s);
      if (!o)
        return false;
      try {
        return r[s] = true, e(o, r, i);
      } finally {
        r[s] = void 0;
      }
    }
    if (n.kind === ye.FIELD && // check all introspection lists
    (n.name.value === "fields" || n.name.value === "interfaces" || n.name.value === "possibleTypes" || n.name.value === "inputFields") && (i++, i >= Da))
      return true;
    if ("selectionSet" in n && n.selectionSet) {
      for (const s of n.selectionSet.selections)
        if (e(s, r, i))
          return true;
    }
    return false;
  }
  return {
    Field(n) {
      if ((n.name.value === "__schema" || n.name.value === "__type") && e(n))
        return t.reportError(
          new qe("Maximum introspection depth exceeded", {
            nodes: [n]
          })
        ), false;
    }
  };
}
function $a(t) {
  const e = /* @__PURE__ */ Object.create(null), n = [], r = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: () => false,
    FragmentDefinition(s) {
      return i(s), false;
    }
  };
  function i(s) {
    if (e[s.name.value])
      return;
    const o = s.name.value;
    e[o] = true;
    const a = t.getFragmentSpreads(s.selectionSet);
    if (a.length !== 0) {
      r[o] = n.length;
      for (const l of a) {
        const u2 = l.name.value, c = r[u2];
        if (n.push(l), c === void 0) {
          const f2 = t.getFragment(u2);
          f2 && i(f2);
        } else {
          const f2 = n.slice(c), d = f2.slice(0, -1).map((h) => '"' + h.name.value + '"').join(", ");
          t.reportError(
            new qe(
              `Cannot spread fragment "${u2}" within itself` + (d !== "" ? ` via ${d}.` : "."),
              {
                nodes: f2
              }
            )
          );
        }
        n.pop();
      }
      r[o] = void 0;
    }
  }
}
function Pa(t) {
  let e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: {
      enter() {
        e = /* @__PURE__ */ Object.create(null);
      },
      leave(n) {
        const r = t.getRecursiveVariableUsages(n);
        for (const { node: i } of r) {
          const s = i.name.value;
          e[s] !== true && t.reportError(
            new qe(
              n.name ? `Variable "$${s}" is not defined by operation "${n.name.value}".` : `Variable "$${s}" is not defined.`,
              {
                nodes: [i, n]
              }
            )
          );
        }
      }
    },
    VariableDefinition(n) {
      e[n.variable.name.value] = true;
    }
  };
}
function ja(t) {
  const e = [], n = [];
  return {
    OperationDefinition(r) {
      return e.push(r), false;
    },
    FragmentDefinition(r) {
      return n.push(r), false;
    },
    Document: {
      leave() {
        const r = /* @__PURE__ */ Object.create(null);
        for (const i of e)
          for (const s of t.getRecursivelyReferencedFragments(
            i
          ))
            r[s.name.value] = true;
        for (const i of n) {
          const s = i.name.value;
          r[s] !== true && t.reportError(
            new qe(`Fragment "${s}" is never used.`, {
              nodes: i
            })
          );
        }
      }
    }
  };
}
function Ma2(t) {
  let e = [];
  return {
    OperationDefinition: {
      enter() {
        e = [];
      },
      leave(n) {
        const r = /* @__PURE__ */ Object.create(null), i = t.getRecursiveVariableUsages(n);
        for (const { node: s } of i)
          r[s.name.value] = true;
        for (const s of e) {
          const o = s.variable.name.value;
          r[o] !== true && t.reportError(
            new qe(
              n.name ? `Variable "$${o}" is never used in operation "${n.name.value}".` : `Variable "$${o}" is never used.`,
              {
                nodes: s
              }
            )
          );
        }
      }
    },
    VariableDefinition(n) {
      e.push(n);
    }
  };
}
function Fr(t) {
  switch (t.kind) {
    case ye.OBJECT:
      return { ...t, fields: Ua(t.fields) };
    case ye.LIST:
      return { ...t, values: t.values.map(Fr) };
    case ye.INT:
    case ye.FLOAT:
    case ye.STRING:
    case ye.BOOLEAN:
    case ye.NULL:
    case ye.ENUM:
    case ye.VARIABLE:
      return t;
  }
}
function Ua(t) {
  return t.map((e) => ({
    ...e,
    value: Fr(e.value)
  })).sort(
    (e, n) => Xo(e.name.value, n.name.value)
  );
}
function vs(t) {
  return Array.isArray(t) ? t.map(
    ([e, n]) => `subfields "${e}" conflict because ` + vs(n)
  ).join(" and ") : t;
}
function Va(t) {
  const e = new ws(), n = new Ka(), r = /* @__PURE__ */ new Map();
  return {
    SelectionSet(i) {
      const s = qa(
        t,
        r,
        e,
        n,
        t.getParentType(),
        i
      );
      for (const [[o, a], l, u2] of s) {
        const c = vs(a);
        t.reportError(
          new qe(
            `Fields "${o}" conflict because ${c}. Use different aliases on the fields to fetch both if this was intentional.`,
            {
              nodes: l.concat(u2)
            }
          )
        );
      }
    }
  };
}
function qa(t, e, n, r, i, s) {
  const o = [], [a, l] = _n(
    t,
    e,
    i,
    s
  );
  if (Ga2(
    t,
    o,
    e,
    n,
    r,
    a
  ), l.length !== 0)
    for (let u2 = 0; u2 < l.length; u2++) {
      In(
        t,
        o,
        e,
        n,
        r,
        false,
        a,
        l[u2]
      );
      for (let c = u2 + 1; c < l.length; c++)
        On(
          t,
          o,
          e,
          n,
          r,
          false,
          l[u2],
          l[c]
        );
    }
  return o;
}
function In(t, e, n, r, i, s, o, a) {
  if (r.has(
    o,
    a,
    s
  ))
    return;
  r.add(
    o,
    a,
    s
  );
  const l = t.getFragment(a);
  if (!l)
    return;
  const [u2, c] = mr(
    t,
    n,
    l
  );
  if (o !== u2) {
    Cr(
      t,
      e,
      n,
      r,
      i,
      s,
      o,
      u2
    );
    for (const f2 of c)
      In(
        t,
        e,
        n,
        r,
        i,
        s,
        o,
        f2
      );
  }
}
function On(t, e, n, r, i, s, o, a) {
  if (o === a || i.has(
    o,
    a,
    s
  ))
    return;
  i.add(o, a, s);
  const l = t.getFragment(o), u2 = t.getFragment(a);
  if (!l || !u2)
    return;
  const [c, f2] = mr(
    t,
    n,
    l
  ), [d, h] = mr(
    t,
    n,
    u2
  );
  Cr(
    t,
    e,
    n,
    r,
    i,
    s,
    c,
    d
  );
  for (const g2 of h)
    On(
      t,
      e,
      n,
      r,
      i,
      s,
      o,
      g2
    );
  for (const g2 of f2)
    On(
      t,
      e,
      n,
      r,
      i,
      s,
      g2,
      a
    );
}
function Ba(t, e, n, r, i, s, o, a, l) {
  const u2 = [], [c, f2] = _n(
    t,
    e,
    s,
    o
  ), [d, h] = _n(
    t,
    e,
    a,
    l
  );
  Cr(
    t,
    u2,
    e,
    n,
    r,
    i,
    c,
    d
  );
  for (const g2 of h)
    In(
      t,
      u2,
      e,
      n,
      r,
      i,
      c,
      g2
    );
  for (const g2 of f2)
    In(
      t,
      u2,
      e,
      n,
      r,
      i,
      d,
      g2
    );
  for (const g2 of f2)
    for (const w of h)
      On(
        t,
        u2,
        e,
        n,
        r,
        i,
        g2,
        w
      );
  return u2;
}
function Ga2(t, e, n, r, i, s) {
  for (const [o, a] of Object.entries(s))
    if (a.length > 1)
      for (let l = 0; l < a.length; l++)
        for (let u2 = l + 1; u2 < a.length; u2++) {
          const c = Ts(
            t,
            n,
            r,
            i,
            false,
            // within one collection is never mutually exclusive
            o,
            a[l],
            a[u2]
          );
          c && e.push(c);
        }
}
function Cr(t, e, n, r, i, s, o, a) {
  for (const [l, u2] of Object.entries(o)) {
    const c = a[l];
    if (c)
      for (const f2 of u2)
        for (const d of c) {
          const h = Ts(
            t,
            n,
            r,
            i,
            s,
            l,
            f2,
            d
          );
          h && e.push(h);
        }
  }
}
function Ts(t, e, n, r, i, s, o, a) {
  const [l, u2, c] = o, [f2, d, h] = a, g2 = i || l !== f2 && Nr(l) && Nr(f2);
  if (!g2) {
    const E = u2.name.value, _ = d.name.value;
    if (E !== _)
      return [
        [s, `"${E}" and "${_}" are different fields`],
        [u2],
        [d]
      ];
    if (!Qa(u2, d))
      return [
        [s, "they have differing arguments"],
        [u2],
        [d]
      ];
  }
  const w = c?.type, k = h?.type;
  if (w && k && gr2(w, k))
    return [
      [
        s,
        `they return conflicting types "${It(w)}" and "${It(
          k
        )}"`
      ],
      [u2],
      [d]
    ];
  const T = u2.selectionSet, x = d.selectionSet;
  if (T && x) {
    const E = Ba(
      t,
      e,
      n,
      r,
      g2,
      lc(w),
      T,
      lc(k),
      x
    );
    return Ha(E, s, u2, d);
  }
}
function Qa(t, e) {
  const n = t.arguments, r = e.arguments;
  if (n === void 0 || n.length === 0)
    return r === void 0 || r.length === 0;
  if (r === void 0 || r.length === 0 || n.length !== r.length)
    return false;
  const i = new Map(r.map(({ name: s, value: o }) => [s.value, o]));
  return n.every((s) => {
    const o = s.value, a = i.get(s.name.value);
    return a === void 0 ? false : Zr(o) === Zr(a);
  });
}
function Zr(t) {
  return gr(Fr(t));
}
function gr2(t, e) {
  return Ms(t) ? Ms(e) ? gr2(t.ofType, e.ofType) : true : Ms(e) ? true : Nn(t) ? Nn(e) ? gr2(t.ofType, e.ofType) : true : Nn(e) ? true : ac(t) || ac(e) ? t !== e : false;
}
function _n(t, e, n, r) {
  const i = e.get(r);
  if (i)
    return i;
  const s = /* @__PURE__ */ Object.create(null), o = /* @__PURE__ */ Object.create(null);
  Es(
    t,
    n,
    r,
    s,
    o
  );
  const a = [s, Object.keys(o)];
  return e.set(r, a), a;
}
function mr(t, e, n) {
  const r = e.get(n.selectionSet);
  if (r)
    return r;
  const i = fe(t.getSchema(), n.typeCondition);
  return _n(
    t,
    e,
    i,
    n.selectionSet
  );
}
function Es(t, e, n, r, i) {
  for (const s of n.selections)
    switch (s.kind) {
      case ye.FIELD: {
        const o = s.name.value;
        let a;
        (Nr(e) || Qn(e)) && (a = e.getFields()[o]);
        const l = s.alias ? s.alias.value : o;
        r[l] || (r[l] = []), r[l].push([e, s, a]);
        break;
      }
      case ye.FRAGMENT_SPREAD:
        i[s.name.value] = true;
        break;
      case ye.INLINE_FRAGMENT: {
        const o = s.typeCondition, a = o ? fe(t.getSchema(), o) : e;
        Es(
          t,
          a,
          s.selectionSet,
          r,
          i
        );
        break;
      }
    }
}
function Ha(t, e, n, r) {
  if (t.length > 0)
    return [
      [e, t.map(([i]) => i)],
      [n, ...t.map(([, i]) => i).flat()],
      [r, ...t.map(([, , i]) => i).flat()]
    ];
}
var ws = class {
  constructor() {
    this._data = /* @__PURE__ */ new Map();
  }
  has(e, n, r) {
    var i;
    const s = (i = this._data.get(e)) === null || i === void 0 ? void 0 : i.get(n);
    return s === void 0 ? false : r ? true : r === s;
  }
  add(e, n, r) {
    const i = this._data.get(e);
    i === void 0 ? this._data.set(e, /* @__PURE__ */ new Map([[n, r]])) : i.set(n, r);
  }
};
var Ka = class {
  constructor() {
    this._orderedPairSet = new ws();
  }
  has(e, n, r) {
    return e < n ? this._orderedPairSet.has(e, n, r) : this._orderedPairSet.has(n, e, r);
  }
  add(e, n, r) {
    e < n ? this._orderedPairSet.add(e, n, r) : this._orderedPairSet.add(n, e, r);
  }
};
function Wa(t) {
  return {
    InlineFragment(e) {
      const n = t.getType(), r = t.getParentType();
      if (sc(n) && sc(r) && !Kr(t.getSchema(), n, r)) {
        const i = It(r), s = It(n);
        t.reportError(
          new qe(
            `Fragment cannot be spread here as objects of type "${i}" can never be of type "${s}".`,
            {
              nodes: e
            }
          )
        );
      }
    },
    FragmentSpread(e) {
      const n = e.name.value, r = za(t, n), i = t.getParentType();
      if (r && i && !Kr(t.getSchema(), r, i)) {
        const s = It(i), o = It(r);
        t.reportError(
          new qe(
            `Fragment "${n}" cannot be spread here as objects of type "${s}" can never be of type "${o}".`,
            {
              nodes: e
            }
          )
        );
      }
    }
  };
}
function za(t, e) {
  const n = t.getFragment(e);
  if (n) {
    const r = fe(t.getSchema(), n.typeCondition);
    if (sc(r))
      return r;
  }
}
function Ja(t) {
  return {
    // eslint-disable-next-line new-cap
    ...Ya(t),
    Field: {
      // Validate on leave to allow for deeper errors to appear first.
      leave(e) {
        var n;
        const r = t.getFieldDef();
        if (!r)
          return false;
        const i = new Set(
          // FIXME: https://github.com/graphql/graphql-js/issues/2203
          /* c8 ignore next */
          (n = e.arguments) === null || n === void 0 ? void 0 : n.map((s) => s.name.value)
        );
        for (const s of r.args)
          if (!i.has(s.name) && pc(s)) {
            const o = It(s.type);
            t.reportError(
              new qe(
                `Field "${r.name}" argument "${s.name}" of type "${o}" is required, but it was not provided.`,
                {
                  nodes: e
                }
              )
            );
          }
      }
    }
  };
}
function Ya(t) {
  var e;
  const n = /* @__PURE__ */ Object.create(null), r = t.getSchema(), i = (e = r?.getDirectives()) !== null && e !== void 0 ? e : tn;
  for (const a of i)
    n[a.name] = zo(
      a.args.filter(pc),
      (l) => l.name
    );
  const s = t.getDocument().definitions;
  for (const a of s)
    if (a.kind === ye.DIRECTIVE_DEFINITION) {
      var o;
      const l = (o = a.arguments) !== null && o !== void 0 ? o : [];
      n[a.name.value] = zo(
        l.filter(Xa),
        (u2) => u2.name.value
      );
    }
  return {
    Directive: {
      // Validate on leave to allow for deeper errors to appear first.
      leave(a) {
        const l = a.name.value, u2 = n[l];
        if (u2) {
          var c;
          const f2 = (c = a.arguments) !== null && c !== void 0 ? c : [], d = new Set(f2.map((h) => h.name.value));
          for (const [h, g2] of Object.entries(u2))
            if (!d.has(h)) {
              const w = Gs(g2.type) ? It(g2.type) : gr(g2.type);
              t.reportError(
                new qe(
                  `Directive "@${l}" argument "${h}" of type "${w}" is required, but it was not provided.`,
                  {
                    nodes: a
                  }
                )
              );
            }
        }
      }
    }
  };
}
function Xa(t) {
  return t.type.kind === ye.NON_NULL_TYPE && t.defaultValue == null;
}
function Za(t) {
  return {
    Field(e) {
      const n = t.getType(), r = e.selectionSet;
      if (n)
        if (ac(lc(n))) {
          if (r) {
            const i = e.name.value, s = It(n);
            t.reportError(
              new qe(
                `Field "${i}" must not have a selection since type "${s}" has no subfields.`,
                {
                  nodes: r
                }
              )
            );
          }
        } else if (r) {
          if (r.selections.length === 0) {
            const i = e.name.value, s = It(n);
            t.reportError(
              new qe(
                `Field "${i}" of type "${s}" must have at least one field selected.`,
                {
                  nodes: e
                }
              )
            );
          }
        } else {
          const i = e.name.value, s = It(n);
          t.reportError(
            new qe(
              `Field "${i}" of type "${s}" must have a selection of subfields. Did you mean "${i} { ... }"?`,
              {
                nodes: e
              }
            )
          );
        }
    }
  };
}
function el(t) {
  return t.map(
    (e) => typeof e == "number" ? "[" + e.toString() + "]" : "." + e
  ).join("");
}
function ei(t, e, n) {
  return {
    prev: t,
    key: e,
    typename: n
  };
}
function ke(t) {
  const e = [];
  let n = t;
  for (; n; )
    e.push(n.key), n = n.prev;
  return e.reverse();
}
function tl(t, e, n = nl) {
  return Ut(t, e, n, void 0);
}
function nl(t, e, n) {
  let r = "Invalid value " + It(e);
  throw t.length > 0 && (r += ` at "value${el(t)}"`), n.message = r + ": " + n.message, n;
}
function Ut(t, e, n, r) {
  if (Nn(e)) {
    if (t != null)
      return Ut(t, e.ofType, n, r);
    n(
      ke(r),
      t,
      new qe(
        `Expected non-nullable type "${It(e)}" not to be null.`
      )
    );
    return;
  }
  if (t == null)
    return null;
  if (Ms(e)) {
    const i = e.ofType;
    return os(t) ? Array.from(t, (s, o) => {
      const a = ei(r, o, void 0);
      return Ut(s, i, n, a);
    }) : [Ut(t, i, n, r)];
  }
  if (Yr(e)) {
    if (!Rs(t) || Array.isArray(t)) {
      n(
        ke(r),
        t,
        new qe(`Expected type "${e.name}" to be an object.`)
      );
      return;
    }
    const i = {}, s = e.getFields();
    for (const o of Object.values(s)) {
      const a = t[o.name];
      if (a === void 0) {
        if (o.defaultValue !== void 0)
          i[o.name] = o.defaultValue;
        else if (Nn(o.type)) {
          const l = It(o.type);
          n(
            ke(r),
            t,
            new qe(
              `Field "${o.name}" of required type "${l}" was not provided.`
            )
          );
        }
        continue;
      }
      i[o.name] = Ut(
        a,
        o.type,
        n,
        ei(r, o.name, e.name)
      );
    }
    for (const o of Object.keys(t))
      if (!s[o]) {
        const a = Wo(
          o,
          Object.keys(e.getFields())
        );
        n(
          ke(r),
          t,
          new qe(
            `Field "${o}" is not defined by type "${e.name}".` + Jo(a)
          )
        );
      }
    if (e.isOneOf) {
      const o = Object.keys(i);
      o.length !== 1 && n(
        ke(r),
        t,
        new qe(
          `Exactly one key must be specified for OneOf type "${e.name}".`
        )
      );
      const a = o[0], l = i[a];
      l === null && n(
        ke(r).concat(a),
        l,
        new qe(`Field "${a}" must be non-null.`)
      );
    }
    return i;
  }
  if (ac(e)) {
    let i;
    try {
      i = e.parseValue(t);
    } catch (s) {
      s instanceof qe ? n(ke(r), t, s) : n(
        ke(r),
        t,
        new qe(`Expected type "${e.name}". ` + s.message, {
          originalError: s
        })
      );
      return;
    }
    return i === void 0 && n(
      ke(r),
      t,
      new qe(`Expected type "${e.name}".`)
    ), i;
  }
  Ps(false, "Unexpected input type: " + It(e));
}
function tt(t, e, n) {
  if (t) {
    if (t.kind === ye.VARIABLE) {
      const r = t.name.value;
      if (n == null || n[r] === void 0)
        return;
      const i = n[r];
      return i === null && Nn(e) ? void 0 : i;
    }
    if (Nn(e))
      return t.kind === ye.NULL ? void 0 : tt(t, e.ofType, n);
    if (t.kind === ye.NULL)
      return null;
    if (Ms(e)) {
      const r = e.ofType;
      if (t.kind === ye.LIST) {
        const s = [];
        for (const o of t.values)
          if (ti(o, n)) {
            if (Nn(r))
              return;
            s.push(null);
          } else {
            const a = tt(o, r, n);
            if (a === void 0)
              return;
            s.push(a);
          }
        return s;
      }
      const i = tt(t, r, n);
      return i === void 0 ? void 0 : [i];
    }
    if (Yr(e)) {
      if (t.kind !== ye.OBJECT)
        return;
      const r = /* @__PURE__ */ Object.create(null), i = zo(t.fields, (s) => s.name.value);
      for (const s of Object.values(e.getFields())) {
        const o = i[s.name];
        if (!o || ti(o.value, n)) {
          if (s.defaultValue !== void 0)
            r[s.name] = s.defaultValue;
          else if (Nn(s.type))
            return;
          continue;
        }
        const a = tt(o.value, s.type, n);
        if (a === void 0)
          return;
        r[s.name] = a;
      }
      if (e.isOneOf) {
        const s = Object.keys(r);
        if (s.length !== 1 || r[s[0]] === null)
          return;
      }
      return r;
    }
    if (ac(e)) {
      let r;
      try {
        r = e.parseLiteral(t, n);
      } catch {
        return;
      }
      return r === void 0 ? void 0 : r;
    }
    Ps(false, "Unexpected input type: " + It(e));
  }
}
function ti(t, e) {
  return t.kind === ye.VARIABLE && (e == null || e[t.name.value] === void 0);
}
function rl(t, e, n) {
  var r;
  const i = {}, s = (r = e.arguments) !== null && r !== void 0 ? r : [], o = zo(s, (a) => a.name.value);
  for (const a of t.args) {
    const l = a.name, u2 = a.type, c = o[l];
    if (!c) {
      if (a.defaultValue !== void 0)
        i[l] = a.defaultValue;
      else if (Nn(u2))
        throw new qe(
          `Argument "${l}" of required type "${It(u2)}" was not provided.`,
          {
            nodes: e
          }
        );
      continue;
    }
    const f2 = c.value;
    let d = f2.kind === ye.NULL;
    if (f2.kind === ye.VARIABLE) {
      const g2 = f2.name.value;
      if (n == null || !il(n, g2)) {
        if (a.defaultValue !== void 0)
          i[l] = a.defaultValue;
        else if (Nn(u2))
          throw new qe(
            `Argument "${l}" of required type "${It(u2)}" was provided the variable "$${g2}" which was not provided a runtime value.`,
            {
              nodes: f2
            }
          );
        continue;
      }
      d = n[g2] == null;
    }
    if (d && Nn(u2))
      throw new qe(
        `Argument "${l}" of non-null type "${It(u2)}" must not be null.`,
        {
          nodes: f2
        }
      );
    const h = tt(f2, u2, n);
    if (h === void 0)
      throw new qe(
        `Argument "${l}" has invalid value ${gr(f2)}.`,
        {
          nodes: f2
        }
      );
    i[l] = h;
  }
  return i;
}
function Ot(t, e, n) {
  var r;
  const i = (r = e.directives) === null || r === void 0 ? void 0 : r.find(
    (s) => s.name.value === t.name
  );
  if (i)
    return rl(t, i, n);
}
function il(t, e) {
  return Object.prototype.hasOwnProperty.call(t, e);
}
function sl(t, e, n, r, i) {
  const s = /* @__PURE__ */ new Map();
  return yr(
    t,
    e,
    n,
    r,
    i,
    s,
    /* @__PURE__ */ new Set()
  ), s;
}
function yr(t, e, n, r, i, s, o) {
  for (const a of i.selections)
    switch (a.kind) {
      case ye.FIELD: {
        if (!tr(n, a))
          continue;
        const l = ol(a), u2 = s.get(l);
        u2 !== void 0 ? u2.push(a) : s.set(l, [a]);
        break;
      }
      case ye.INLINE_FRAGMENT: {
        if (!tr(n, a) || !ni(t, a, r))
          continue;
        yr(
          t,
          e,
          n,
          r,
          a.selectionSet,
          s,
          o
        );
        break;
      }
      case ye.FRAGMENT_SPREAD: {
        const l = a.name.value;
        if (o.has(l) || !tr(n, a))
          continue;
        o.add(l);
        const u2 = e[l];
        if (!u2 || !ni(t, u2, r))
          continue;
        yr(
          t,
          e,
          n,
          r,
          u2.selectionSet,
          s,
          o
        );
        break;
      }
    }
}
function tr(t, e) {
  const n = Ot(_r, e, t);
  if (n?.if === true)
    return false;
  const r = Ot(
    Or,
    e,
    t
  );
  return r?.if !== false;
}
function ni(t, e, n) {
  const r = e.typeCondition;
  if (!r)
    return true;
  const i = fe(t, r);
  return i === n ? true : oc(i) ? t.isSubType(i, n) : false;
}
function ol(t) {
  return t.alias ? t.alias.value : t.name.value;
}
function al(t) {
  return {
    OperationDefinition(e) {
      if (e.operation === "subscription") {
        const n = t.getSchema(), r = n.getSubscriptionType();
        if (r) {
          const i = e.name ? e.name.value : null, s = /* @__PURE__ */ Object.create(null), o = t.getDocument(), a = /* @__PURE__ */ Object.create(null);
          for (const u2 of o.definitions)
            u2.kind === ye.FRAGMENT_DEFINITION && (a[u2.name.value] = u2);
          const l = sl(
            n,
            a,
            s,
            r,
            e.selectionSet
          );
          if (l.size > 1) {
            const f2 = [...l.values()].slice(1).flat();
            t.reportError(
              new qe(
                i != null ? `Subscription "${i}" must select only one top level field.` : "Anonymous Subscription must select only one top level field.",
                {
                  nodes: f2
                }
              )
            );
          }
          for (const u2 of l.values())
            u2[0].name.value.startsWith("__") && t.reportError(
              new qe(
                i != null ? `Subscription "${i}" must not select an introspection top level field.` : "Anonymous Subscription must not select an introspection top level field.",
                {
                  nodes: u2
                }
              )
            );
        }
      }
    }
  };
}
function Ss(t, e) {
  const n = /* @__PURE__ */ new Map();
  for (const r of t) {
    const i = e(r), s = n.get(i);
    s === void 0 ? n.set(i, [r]) : s.push(r);
  }
  return n;
}
function ll(t) {
  return {
    Field: e,
    Directive: e
  };
  function e(n) {
    var r;
    const i = (r = n.arguments) !== null && r !== void 0 ? r : [], s = Ss(i, (o) => o.name.value);
    for (const [o, a] of s)
      a.length > 1 && t.reportError(
        new qe(
          `There can be only one argument named "${o}".`,
          {
            nodes: a.map((l) => l.name)
          }
        )
      );
  }
}
function ul(t) {
  const e = /* @__PURE__ */ Object.create(null), n = t.getSchema(), r = n ? n.getDirectives() : tn;
  for (const a of r)
    e[a.name] = !a.isRepeatable;
  const i = t.getDocument().definitions;
  for (const a of i)
    a.kind === ye.DIRECTIVE_DEFINITION && (e[a.name.value] = !a.repeatable);
  const s = /* @__PURE__ */ Object.create(null), o = /* @__PURE__ */ Object.create(null);
  return {
    // Many different AST nodes may contain directives. Rather than listing
    // them all, just listen for entering any node, and check to see if it
    // defines any directives.
    enter(a) {
      if (!("directives" in a) || !a.directives)
        return;
      let l;
      if (a.kind === ye.SCHEMA_DEFINITION || a.kind === ye.SCHEMA_EXTENSION)
        l = s;
      else if (Ar(a) || bs(a)) {
        const u2 = a.name.value;
        l = o[u2], l === void 0 && (o[u2] = l = /* @__PURE__ */ Object.create(null));
      } else
        l = /* @__PURE__ */ Object.create(null);
      for (const u2 of a.directives) {
        const c = u2.name.value;
        e[c] && (l[c] ? t.reportError(
          new qe(
            `The directive "@${c}" can only be used once at this location.`,
            {
              nodes: [l[c], u2]
            }
          )
        ) : l[c] = u2);
      }
    }
  };
}
function cl(t) {
  const e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: () => false,
    FragmentDefinition(n) {
      const r = n.name.value;
      return e[r] ? t.reportError(
        new qe(
          `There can be only one fragment named "${r}".`,
          {
            nodes: [e[r], n.name]
          }
        )
      ) : e[r] = n.name, false;
    }
  };
}
function fl(t) {
  const e = [];
  let n = /* @__PURE__ */ Object.create(null);
  return {
    ObjectValue: {
      enter() {
        e.push(n), n = /* @__PURE__ */ Object.create(null);
      },
      leave() {
        const r = e.pop();
        r || Ps(false), n = r;
      }
    },
    ObjectField(r) {
      const i = r.name.value;
      n[i] ? t.reportError(
        new qe(
          `There can be only one input field named "${i}".`,
          {
            nodes: [n[i], r.name]
          }
        )
      ) : n[i] = r.name;
    }
  };
}
function dl(t) {
  const e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition(n) {
      const r = n.name;
      return r && (e[r.value] ? t.reportError(
        new qe(
          `There can be only one operation named "${r.value}".`,
          {
            nodes: [
              e[r.value],
              r
            ]
          }
        )
      ) : e[r.value] = r), false;
    },
    FragmentDefinition: () => false
  };
}
function pl(t) {
  return {
    OperationDefinition(e) {
      var n;
      const r = (n = e.variableDefinitions) !== null && n !== void 0 ? n : [], i = Ss(
        r,
        (s) => s.variable.name.value
      );
      for (const [s, o] of i)
        o.length > 1 && t.reportError(
          new qe(
            `There can be only one variable named "$${s}".`,
            {
              nodes: o.map((a) => a.variable.name)
            }
          )
        );
    }
  };
}
function hl(t) {
  let e = {};
  return {
    OperationDefinition: {
      enter() {
        e = {};
      }
    },
    VariableDefinition(n) {
      e[n.variable.name.value] = n;
    },
    ListValue(n) {
      const r = uc(t.getParentInputType());
      if (!Ms(r))
        return He(t, n), false;
    },
    ObjectValue(n) {
      const r = lc(t.getInputType());
      if (!Yr(r))
        return He(t, n), false;
      const i = zo(n.fields, (s) => s.name.value);
      for (const s of Object.values(r.getFields()))
        if (!i[s.name] && dc(s)) {
          const a = It(s.type);
          t.reportError(
            new qe(
              `Field "${r.name}.${s.name}" of required type "${a}" was not provided.`,
              {
                nodes: n
              }
            )
          );
        }
      r.isOneOf && gl(
        t,
        n,
        r,
        i,
        e
      );
    },
    ObjectField(n) {
      const r = lc(t.getParentInputType());
      if (!t.getInputType() && Yr(r)) {
        const s = Wo(
          n.name.value,
          Object.keys(r.getFields())
        );
        t.reportError(
          new qe(
            `Field "${n.name.value}" is not defined by type "${r.name}".` + Jo(s),
            {
              nodes: n
            }
          )
        );
      }
    },
    NullValue(n) {
      const r = t.getInputType();
      Nn(r) && t.reportError(
        new qe(
          `Expected value of type "${It(r)}", found ${gr(n)}.`,
          {
            nodes: n
          }
        )
      );
    },
    EnumValue: (n) => He(t, n),
    IntValue: (n) => He(t, n),
    FloatValue: (n) => He(t, n),
    StringValue: (n) => He(t, n),
    BooleanValue: (n) => He(t, n)
  };
}
function He(t, e) {
  const n = t.getInputType();
  if (!n)
    return;
  const r = lc(n);
  if (!ac(r)) {
    const i = It(n);
    t.reportError(
      new qe(
        `Expected value of type "${i}", found ${gr(e)}.`,
        {
          nodes: e
        }
      )
    );
    return;
  }
  try {
    if (r.parseLiteral(
      e,
      void 0
      /* variables */
    ) === void 0) {
      const s = It(n);
      t.reportError(
        new qe(
          `Expected value of type "${s}", found ${gr(e)}.`,
          {
            nodes: e
          }
        )
      );
    }
  } catch (i) {
    const s = It(n);
    i instanceof qe ? t.reportError(i) : t.reportError(
      new qe(
        `Expected value of type "${s}", found ${gr(e)}; ` + i.message,
        {
          nodes: e,
          originalError: i
        }
      )
    );
  }
}
function gl(t, e, n, r, i) {
  var s;
  const o = Object.keys(r);
  if (o.length !== 1) {
    t.reportError(
      new qe(
        `OneOf Input Object "${n.name}" must specify exactly one key.`,
        {
          nodes: [e]
        }
      )
    );
    return;
  }
  const l = (s = r[o[0]]) === null || s === void 0 ? void 0 : s.value, u2 = !l || l.kind === ye.NULL, c = l?.kind === ye.VARIABLE;
  if (u2) {
    t.reportError(
      new qe(`Field "${n.name}.${o[0]}" must be non-null.`, {
        nodes: [e]
      })
    );
    return;
  }
  if (c) {
    const f2 = l.name.value;
    i[f2].type.kind !== ye.NON_NULL_TYPE && t.reportError(
      new qe(
        `Variable "${f2}" must be non-nullable to be used for OneOf Input Object "${n.name}".`,
        {
          nodes: [e]
        }
      )
    );
  }
}
function ml(t) {
  return {
    VariableDefinition(e) {
      const n = fe(t.getSchema(), e.type);
      if (n !== void 0 && !uu(n)) {
        const r = e.variable.name.value, i = gr(e.type);
        t.reportError(
          new qe(
            `Variable "$${r}" cannot be non-input type "${i}".`,
            {
              nodes: e.type
            }
          )
        );
      }
    }
  };
}
function yl(t) {
  let e = /* @__PURE__ */ Object.create(null);
  return {
    OperationDefinition: {
      enter() {
        e = /* @__PURE__ */ Object.create(null);
      },
      leave(n) {
        const r = t.getRecursiveVariableUsages(n);
        for (const { node: i, type: s, defaultValue: o, parentType: a } of r) {
          const l = i.name.value, u2 = e[l];
          if (u2 && s) {
            const c = t.getSchema(), f2 = fe(c, u2.type);
            if (f2 && !bl(
              c,
              f2,
              u2.defaultValue,
              s,
              o
            )) {
              const d = It(f2), h = It(s);
              t.reportError(
                new qe(
                  `Variable "$${l}" of type "${d}" used in position expecting type "${h}".`,
                  {
                    nodes: [u2, i]
                  }
                )
              );
            }
            Yr(a) && a.isOneOf && du(f2) && t.reportError(
              new qe(
                `Variable "$${l}" is of type "${f2}" but must be non-nullable to be used for OneOf Input Object "${a}".`,
                {
                  nodes: [u2, i]
                }
              )
            );
          }
        }
      }
    },
    VariableDefinition(n) {
      e[n.variable.name.value] = n;
    }
  };
}
function bl(t, e, n, r, i) {
  if (Nn(r) && !Nn(e)) {
    if (!(n != null && n.kind !== ye.NULL) && !(i !== void 0))
      return false;
    const a = r.ofType;
    return Tt(t, e, a);
  }
  return Tt(t, e, r);
}
var vl = Object.freeze([La]);
var Is = Object.freeze([
  Ta,
  dl,
  xa,
  al,
  Fa,
  Ia,
  ml,
  Za,
  Ea,
  cl,
  Aa,
  ja,
  Wa,
  $a,
  pl,
  Pa,
  Ma2,
  Na,
  ul,
  Oa,
  ll,
  hl,
  Ja,
  yl,
  Va,
  fl,
  ...vl
]);
var Tl = class {
  constructor(e, n) {
    this._ast = e, this._fragments = void 0, this._fragmentSpreads = /* @__PURE__ */ new Map(), this._recursivelyReferencedFragments = /* @__PURE__ */ new Map(), this._onError = n;
  }
  get [Symbol.toStringTag]() {
    return "ASTValidationContext";
  }
  reportError(e) {
    this._onError(e);
  }
  getDocument() {
    return this._ast;
  }
  getFragment(e) {
    let n;
    if (this._fragments)
      n = this._fragments;
    else {
      n = /* @__PURE__ */ Object.create(null);
      for (const r of this.getDocument().definitions)
        r.kind === ye.FRAGMENT_DEFINITION && (n[r.name.value] = r);
      this._fragments = n;
    }
    return n[e];
  }
  getFragmentSpreads(e) {
    let n = this._fragmentSpreads.get(e);
    if (!n) {
      n = [];
      const r = [e];
      let i;
      for (; i = r.pop(); )
        for (const s of i.selections)
          s.kind === ye.FRAGMENT_SPREAD ? n.push(s) : s.selectionSet && r.push(s.selectionSet);
      this._fragmentSpreads.set(e, n);
    }
    return n;
  }
  getRecursivelyReferencedFragments(e) {
    let n = this._recursivelyReferencedFragments.get(e);
    if (!n) {
      n = [];
      const r = /* @__PURE__ */ Object.create(null), i = [e.selectionSet];
      let s;
      for (; s = i.pop(); )
        for (const o of this.getFragmentSpreads(s)) {
          const a = o.name.value;
          if (r[a] !== true) {
            r[a] = true;
            const l = this.getFragment(a);
            l && (n.push(l), i.push(l.selectionSet));
          }
        }
      this._recursivelyReferencedFragments.set(e, n);
    }
    return n;
  }
};
var El = class extends Tl {
  constructor(e, n, r, i) {
    super(n, i), this._schema = e, this._typeInfo = r, this._variableUsages = /* @__PURE__ */ new Map(), this._recursiveVariableUsages = /* @__PURE__ */ new Map();
  }
  get [Symbol.toStringTag]() {
    return "ValidationContext";
  }
  getSchema() {
    return this._schema;
  }
  getVariableUsages(e) {
    let n = this._variableUsages.get(e);
    if (!n) {
      const r = [], i = new ms(this._schema);
      iu(
        e,
        ys(i, {
          VariableDefinition: () => false,
          Variable(s) {
            r.push({
              node: s,
              type: i.getInputType(),
              defaultValue: i.getDefaultValue(),
              parentType: i.getParentInputType()
            });
          }
        })
      ), n = r, this._variableUsages.set(e, n);
    }
    return n;
  }
  getRecursiveVariableUsages(e) {
    let n = this._recursiveVariableUsages.get(e);
    if (!n) {
      n = this.getVariableUsages(e);
      for (const r of this.getRecursivelyReferencedFragments(e))
        n = n.concat(this.getVariableUsages(r));
      this._recursiveVariableUsages.set(e, n);
    }
    return n;
  }
  getType() {
    return this._typeInfo.getType();
  }
  getParentType() {
    return this._typeInfo.getParentType();
  }
  getInputType() {
    return this._typeInfo.getInputType();
  }
  getParentInputType() {
    return this._typeInfo.getParentInputType();
  }
  getFieldDef() {
    return this._typeInfo.getFieldDef();
  }
  getDirective() {
    return this._typeInfo.getDirective();
  }
  getArgument() {
    return this._typeInfo.getArgument();
  }
  getEnumValue() {
    return this._typeInfo.getEnumValue();
  }
};
function wl(t, e, n = Is, r, i = new ms(t)) {
  var s;
  const o = (s = r?.maxErrors) !== null && s !== void 0 ? s : 100;
  e || we(false, "Must provide document."), gs(t);
  const a = Object.freeze({}), l = [], u2 = new El(
    t,
    e,
    i,
    (f2) => {
      if (l.length >= o)
        throw l.push(
          new qe(
            "Too many validation errors, error limit reached. Validation aborted."
          )
        ), a;
      l.push(f2);
    }
  ), c = ic(n.map((f2) => f2(u2)));
  try {
    iu(e, ys(i, c));
  } catch (f2) {
    if (f2 !== a)
      throw f2;
  }
  return l;
}
function Sl(t) {
  return t instanceof Error ? t : new Il(t);
}
var Il = class extends Error {
  constructor(e) {
    super("Unexpected error value: " + It(e)), this.name = "NonErrorThrown", this.thrownValue = e;
  }
};
function ae(t, e, n) {
  var r;
  const i = Sl(t);
  return Ol(i) ? i : new qe(i.message, {
    nodes: (r = i.nodes) !== null && r !== void 0 ? r : e,
    source: i.source,
    positions: i.positions,
    path: n,
    originalError: i
  });
}
function Ol(t) {
  return Array.isArray(t.path);
}
var ri = function(t, e, n, r) {
  if (Rs(t) && typeof t.__typename == "string")
    return t.__typename;
  const i = n.schema.getPossibleTypes(r), s = [];
  for (let o = 0; o < i.length; o++) {
    const a = i[o];
    if (a.isTypeOf) {
      const l = a.isTypeOf(t, e, n);
      if (Jo2(l))
        s[o] = l;
      else if (l)
        return a.name;
    }
  }
  if (s.length)
    return Promise.all(s).then((o) => {
      for (let a = 0; a < o.length; a++)
        if (o[a])
          return i[a].name;
    });
};
var yn = function(t, e, n, r) {
  if (Rs(t) || typeof t == "function") {
    const i = t[r.fieldName];
    return typeof i == "function" ? t[r.fieldName](e, n, r) : i;
  }
};
function xr2(t, e) {
  let n = null;
  for (const i of t.definitions)
    if (i.kind === ye.OPERATION_DEFINITION) {
      var r;
      if (e == null) {
        if (n)
          return null;
        n = i;
      } else if (((r = i.name) === null || r === void 0 ? void 0 : r.value) === e)
        return i;
    }
  return n;
}
function _l(t) {
  const e = t.toConfig(), n = xr(
    nr(e.types),
    (d) => d.name,
    f2
  );
  return new Rr({
    ...e,
    types: Object.values(n),
    directives: nr(e.directives).map(o),
    query: s(e.query),
    mutation: s(e.mutation),
    subscription: s(e.subscription)
  });
  function r(d) {
    return Ms(d) ? new lu(r(d.ofType)) : Nn(d) ? new pu(r(d.ofType)) : i(d);
  }
  function i(d) {
    return n[d.name];
  }
  function s(d) {
    return d && i(d);
  }
  function o(d) {
    const h = d.toConfig();
    return new Le({
      ...h,
      locations: Os(h.locations, (g2) => g2),
      args: a(h.args)
    });
  }
  function a(d) {
    return an(d, (h) => ({ ...h, type: r(h.type) }));
  }
  function l(d) {
    return an(d, (h) => ({
      ...h,
      type: r(h.type),
      args: h.args && a(h.args)
    }));
  }
  function u2(d) {
    return an(d, (h) => ({
      ...h,
      type: r(h.type)
    }));
  }
  function c(d) {
    return nr(d).map(i);
  }
  function f2(d) {
    if (Un(d) || ps(d))
      return d;
    if (Nr(d)) {
      const h = d.toConfig();
      return new fu({
        ...h,
        interfaces: () => c(h.interfaces),
        fields: () => l(h.fields)
      });
    }
    if (Qn(d)) {
      const h = d.toConfig();
      return new hu({
        ...h,
        interfaces: () => c(h.interfaces),
        fields: () => l(h.fields)
      });
    }
    if (qn(d)) {
      const h = d.toConfig();
      return new vu({
        ...h,
        types: () => c(h.types)
      });
    }
    if (Kn(d)) {
      const h = d.toConfig();
      return new bu({
        ...h,
        values: an(h.values, (g2) => g2)
      });
    }
    if (Yr(d)) {
      const h = d.toConfig();
      return new gu({
        ...h,
        fields: () => u2(h.fields)
      });
    }
    Ps(false, "Unexpected type: " + It(d));
  }
}
function an(t, e) {
  const n = /* @__PURE__ */ Object.create(null);
  for (const r of Object.keys(t).sort(Xo))
    n[r] = e(t[r]);
  return n;
}
function nr(t) {
  return Os(t, (e) => e.name);
}
function Os(t, e) {
  return t.slice().sort((n, r) => {
    const i = e(n), s = e(r);
    return Xo(i, s);
  });
}
var Vn = "@whatwg-node/promise-helpers/FakePromise";
function Nn2(t) {
  return t?.then != null;
}
function Nl(t) {
  const e = t;
  return e && e.then && e.catch && e.finally;
}
function oe(t, e, n, r) {
  let i = we2().then(t).then(e, n);
  return Cl(i);
}
function we2(t) {
  return t && Nl(t) ? t : Nn2(t) ? {
    then: (e, n) => we2(t.then(e, n)),
    catch: (e) => we2(t.then((n) => n, e)),
    finally: (e) => we2(e ? Fl(t, e) : t),
    [Symbol.toStringTag]: "Promise"
  } : {
    then(e) {
      if (e)
        try {
          return we2(e(t));
        } catch (n) {
          return Et(n);
        }
      return this;
    },
    catch() {
      return this;
    },
    finally(e) {
      if (e)
        try {
          return we2(e()).then(() => t, () => t);
        } catch (n) {
          return Et(n);
        }
      return this;
    },
    [Symbol.toStringTag]: "Promise",
    __fakePromiseValue: t,
    [Symbol.for(Vn)]: "resolved"
  };
}
function Ke(t, e, n) {
  if (t?.length === 0)
    return;
  const r = t[Symbol.iterator]();
  let i = 0;
  function s() {
    const { done: o, value: a } = r.next();
    if (o)
      return;
    let l = false;
    function u2() {
      l = true;
    }
    return oe(function() {
      return e(a, u2, i++);
    }, function(f2) {
      if (f2 && n?.push(f2), !l)
        return s();
    });
  }
  return s();
}
function Et(t) {
  return {
    then(e, n) {
      if (n)
        try {
          return we2(n(t));
        } catch (r) {
          return Et(r);
        }
      return this;
    },
    catch(e) {
      if (e)
        try {
          return we2(e(t));
        } catch (n) {
          return Et(n);
        }
      return this;
    },
    finally(e) {
      if (e)
        try {
          e();
        } catch (n) {
          return Et(n);
        }
      return this;
    },
    __fakeRejectError: t,
    [Symbol.toStringTag]: "Promise",
    [Symbol.for(Vn)]: "rejected"
  };
}
function ii(t, e, n, r) {
  Symbol.asyncIterator in t && (t = t[Symbol.asyncIterator]());
  let i, s;
  typeof t.return == "function" && (i = t.return, s = (l) => {
    const u2 = () => {
      throw l;
    };
    return i.call(t).then(u2, u2);
  });
  function o(l) {
    return l.done ? l : oe(() => l.value, (u2) => oe(() => e(u2), Rl, s));
  }
  let a;
  return {
    next() {
      return t.next().then(o, a);
    },
    return() {
      return i ? i.call(t).then(o, a) : we2({ value: void 0, done: true });
    },
    throw(l) {
      return typeof t.throw == "function" ? t.throw(l).then(o, a) : s ? s(l) : Et(l);
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
function Rl(t) {
  return { value: t, done: false };
}
function kl(t) {
  return t?.[Symbol.for(Vn)] === "resolved";
}
function Al(t) {
  return t?.[Symbol.for(Vn)] === "rejected";
}
function Fl(t, e) {
  return "finally" in t ? t.finally(e) : t.then((n) => {
    const r = e();
    return Nn2(r) ? r.then(() => n) : n;
  }, (n) => {
    const r = e();
    if (Nn2(r))
      return r.then(() => {
        throw n;
      });
    throw n;
  });
}
function Cl(t) {
  if (kl(t))
    return t.__fakePromiseValue;
  if (Al(t))
    throw t.__fakeRejectError;
  return t;
}
function Dr(t, e) {
  const n = { ...e, ...t };
  for (const r of Object.keys(n))
    r in t && r in e && (n[r] = (i, s) => t[r](i, () => e[r](i, s)));
  return n;
}
var Rn = (t) => ({
  /**
   * Wraps the `wrapped` function with the given `instrument` wrapper.
   * @returns The wrapped function, or `undefined` if the instrument is `undefined`.
   */
  fn(e, n) {
    return e ? (...r) => {
      let i;
      return e(t, () => {
        i = n(...r);
      }), i;
    } : n;
  },
  /**
   * Wraps the `wrapped` function with the given `instrument` wrapper.
   * @returns The wrapped function, or `undefined` if the instrument is `undefined`.
   */
  asyncFn(e, n) {
    return e ? (...r) => {
      let i;
      return oe(() => e(t, () => (i = n(...r), Nn2(i) ? i.then(() => {
      }) : void 0)), () => i);
    } : n;
  }
});
var xl = /* @__PURE__ */ new WeakMap();
var rr2 = Symbol("ENVELOP_IS_INTROSPECTION");
function Dl(t) {
  return (typeof t == "string" ? t : t.body).indexOf("__schema") !== -1;
}
function Ll(t) {
  return t.length === 1 ? t[0] : {
    schema: t[0],
    document: t[1],
    rootValue: t[2],
    contextValue: t[3],
    variableValues: t[4],
    operationName: t[5],
    fieldResolver: t[6],
    subscribeFieldResolver: t[7]
  };
}
var si = (t) => (...e) => t(Ll(e));
function $l(t) {
  return t.length === 1 ? t[0] : {
    schema: t[0],
    document: t[1],
    rootValue: t[2],
    contextValue: t[3],
    variableValues: t[4],
    operationName: t[5],
    fieldResolver: t[6],
    typeResolver: t[7]
  };
}
var oi = (t) => (...e) => t($l(e));
function Ne(t) {
  return typeof t == "object" && t != null && typeof t[Symbol.asyncIterator] == "function";
}
function ai(t, e) {
  if (Ne(t.result))
    return { onNext: e };
  e({
    args: t.args,
    result: t.result,
    setResult: t.setResult
  });
}
function li(t, e) {
  const n = t[Symbol.asyncIterator]();
  let r = false;
  const i = {
    [Symbol.asyncIterator]() {
      return i;
    },
    next() {
      return n.next().then((s) => (s.done && r === false && (r = true, e()), s));
    },
    return() {
      const s = n.return?.();
      return r === false && (r = true, e()), s || we2({ done: true, value: void 0 });
    },
    throw(s) {
      const o = n.throw?.();
      if (o)
        return o;
      throw s;
    }
  };
  return i;
}
function Pl(t, e) {
  const n = t[Symbol.asyncIterator](), r = {
    [Symbol.asyncIterator]() {
      return r;
    },
    next() {
      return n.next().catch((i) => (e(i), { done: true, value: void 0 }));
    },
    return() {
      return n.return?.() || we2({ done: true, value: void 0 });
    },
    throw(i) {
      const s = n.throw?.();
      if (s)
        return s;
      throw i;
    }
  };
  return r;
}
function ln(t) {
  throw Error(`No \`${t}\` function found! Register it using "useEngine" plugin.`);
}
function jl({ plugins: t }) {
  let e = null, n = false;
  const r = () => ln("parse"), i = () => ln("validate"), s = () => ln("execute"), o = () => ln("subscribe");
  let a;
  const l = (E, _ = -1) => {
    if (e !== E && (e = E, n))
      for (const [I, S2] of t.entries())
        I !== _ && S2.onSchemaChange && S2.onSchemaChange({
          schema: e,
          replaceSchema: ($2) => {
            l($2, I);
          }
        });
  }, u2 = [];
  for (let E = 0; E < t.length; E++) {
    const _ = t[E], I = [];
    _.onPluginInit?.({
      plugins: t,
      addPlugin: (S2) => {
        I.push(S2);
      },
      setSchema: (S2) => l(S2, E),
      registerContextErrorHandler: (S2) => u2.push(S2)
    }), I.length && t.splice(E + 1, 0, ...I);
  }
  const c = {
    init: [],
    parse: [],
    validate: [],
    subscribe: [],
    execute: [],
    context: []
  };
  for (const { onContextBuilding: E, onExecute: _, onParse: I, onSubscribe: S2, onValidate: $2, onEnveloped: m, instrumentation: y } of t)
    m && c.init.push(m), E && c.context.push(E), _ && c.execute.push(_), I && c.parse.push(I), S2 && c.subscribe.push(S2), $2 && c.validate.push($2), y && (a = a ? Dr(a, y) : y);
  const f2 = (E) => {
    for (const [_, I] of c.init.entries())
      I({
        context: E,
        extendContext: (S2) => {
          E && Object.assign(E, S2);
        },
        setSchema: (S2) => l(S2, _)
      });
  }, d = c.parse.length ? (E) => (_, I) => {
    let S2 = null, $2 = r;
    const m = E, y = [];
    for (const b of c.parse) {
      const N = b({
        context: m,
        extendContext: (C) => {
          Object.assign(m, C);
        },
        params: { source: _, options: I },
        parseFn: $2,
        setParseFn: (C) => {
          $2 = C;
        },
        setParsedDocument: (C) => {
          S2 = C;
        }
      });
      N && y.push(N);
    }
    if (S2 === null)
      try {
        S2 = $2(_, I);
      } catch (b) {
        S2 = b;
      }
    for (const b of y)
      b({
        context: m,
        extendContext: (N) => {
          Object.assign(m, N);
        },
        replaceParseResult: (N) => {
          S2 = N;
        },
        result: S2
      });
    if (S2 === null)
      throw new Error("Failed to parse document.");
    if (S2 instanceof Error)
      throw S2;
    return xl.set(S2, _.toString()), S2;
  } : () => r, h = c.validate.length ? (E) => (_, I, S2, $2, m) => {
    let y = S2 ? [...S2] : void 0, b = i, N = null;
    const C = E, P = [];
    for (const Y of c.validate) {
      const G = Y({
        context: C,
        extendContext: (se) => {
          Object.assign(C, se);
        },
        params: {
          schema: _,
          documentAST: I,
          rules: y,
          typeInfo: $2,
          options: m
        },
        validateFn: b,
        addValidationRule: (se) => {
          y || (y = []), y.push(se);
        },
        setValidationFn: (se) => {
          b = se;
        },
        setResult: (se) => {
          N = se;
        }
      });
      G && P.push(G);
    }
    if (N || (N = b(_, I, y, $2, m)), !N)
      return;
    const U = N.length === 0;
    for (const Y of P)
      Y({
        valid: U,
        result: N,
        context: C,
        extendContext: (G) => {
          Object.assign(C, G);
        },
        setResult: (G) => {
          N = G;
        }
      });
    return N;
  } : () => i, g2 = c.context.length ? (E) => (_) => {
    const I = [], S2 = E;
    _ && Object.assign(S2, _);
    let $2 = false;
    return oe(() => Ke(c.context, (m, y) => m({
      context: S2,
      extendContext: (b) => {
        Object.assign(S2, b);
      },
      breakContextBuilding: () => {
        $2 = true, y();
      }
    }), I), () => $2 ? S2 : oe(() => Ke(I, (m) => m({
      context: S2,
      extendContext(y) {
        Object.assign(S2, y);
      }
    })), () => S2), (m) => {
      let y = m;
      for (const b of u2)
        b({
          context: S2,
          error: y,
          setError: (N) => {
            y = N;
          }
        });
      throw y;
    });
  } : (E) => (_) => (_ && Object.assign(E, _), E), w = c.subscribe.length, k = si(w ? (E) => {
    let _ = o;
    const I = [], S2 = E.contextValue || {};
    let $2;
    return oe(() => Ke(c.subscribe, (m, y) => m({
      subscribeFn: _,
      setSubscribeFn: (b) => {
        _ = b;
      },
      context: S2,
      extendContext: (b) => {
        Object.assign(S2, b);
      },
      args: E,
      setResultAndStopExecution: (b) => {
        $2 = b, y();
      }
    }), I), () => {
      const m = [], y = [];
      for (const { onSubscribeResult: b, onSubscribeError: N } of I)
        b && m.push(b), N && y.push(N);
      return oe(() => $2 || _(E), (b) => {
        const N = [], C = [];
        for (const P of m) {
          const U = P({
            args: E,
            result: b,
            setResult: (Y) => {
              b = Y;
            }
          });
          U && (U.onNext && N.push(U.onNext), U.onEnd && C.push(U.onEnd));
        }
        return N.length && Ne(b) && (b = ii(b, (P) => oe(() => Ke(N, (U) => U({
          args: E,
          result: P,
          setResult: (Y) => P = Y
        })), () => P))), C.length && Ne(b) && (b = li(b, () => {
          for (const P of C)
            P();
        })), y.length && Ne(b) && (b = Pl(b, (P) => {
          let U = P;
          for (const Y of y)
            Y({
              error: U,
              setError: (G) => {
                U = G;
              }
            });
          throw U;
        })), b;
      });
    });
  } : o), T = c.execute.length, x = oi(T ? (E) => {
    let _ = s, I;
    const S2 = [], $2 = [], m = E.contextValue || {};
    return oe(() => Ke(c.execute, (y, b) => y({
      executeFn: _,
      setExecuteFn: (N) => {
        _ = N;
      },
      setResultAndStopExecution: (N) => {
        I = N, b();
      },
      context: m,
      extendContext: (N) => {
        if (typeof N == "object")
          Object.assign(m, N);
        else
          throw new Error(`Invalid context extension provided! Expected "object", got: "${JSON.stringify(N)}" (${typeof N})`);
      },
      args: E
    }), S2), () => oe(() => I || _({
      ...E,
      contextValue: m
    }), (y) => oe(() => Ke(S2, (b) => b.onExecuteDone?.({
      args: E,
      result: y,
      setResult: (N) => {
        y = N;
      }
    }), $2), () => {
      const b = [], N = [];
      for (const { onNext: C, onEnd: P } of $2)
        C && b.push(C), P && N.push(P);
      return b.length && Ne(y) && (y = ii(y, (C) => oe(() => Ke(b, (P) => P({
        args: E,
        result: C,
        setResult: (U) => {
          C = U;
        }
      })), () => C))), N.length && Ne(y) && (y = li(y, () => {
        for (const C of N)
          C();
      })), y;
    })));
  } : s);
  if (n = true, e)
    for (const [E, _] of t.entries())
      _.onSchemaChange?.({
        schema: e,
        replaceSchema: (I) => l(I, E)
      });
  return {
    getCurrentSchema() {
      return e;
    },
    init: f2,
    parse: d,
    validate: h,
    execute: x,
    subscribe: k,
    contextFactory: g2,
    instrumentation: a
  };
}
function Ml(t) {
  return t != null;
}
function Ul(t) {
  const e = t.plugins.filter(Ml), n = jl({
    plugins: e
  }), r = n.instrumentation, i = (s = {}) => {
    const o = Rn({ context: s }), a = n;
    return o.fn(r?.init, n.init)(s), {
      parse: o.fn(r?.parse, a.parse(s)),
      validate: o.fn(r?.validate, a.validate(s)),
      contextFactory: o.fn(r?.context, a.contextFactory(s)),
      execute: o.asyncFn(r?.execute, a.execute),
      subscribe: o.asyncFn(r?.subscribe, a.subscribe),
      schema: a.getCurrentSchema()
    };
  };
  return i._plugins = e, i;
}
var ui = {
  logFn: console.log
};
var Vl = (t = ui) => {
  const e = {
    DEFAULT_OPTIONS: ui,
    ...t
  };
  return {
    onParse({ extendContext: n, params: r }) {
      e.skipIntrospection && Dl(r.source) && n({
        [rr2]: true
      });
    },
    onExecute({ args: n }) {
      if (!n.contextValue[rr2])
        return e.logFn("execute-start", { args: n }), {
          onExecuteDone: ({ result: r }) => {
            e.logFn("execute-end", { args: n, result: r });
          }
        };
    },
    onSubscribe({ args: n }) {
      if (!n.contextValue[rr2])
        return e.logFn("subscribe-start", { args: n }), {
          onSubscribeResult: ({ result: r }) => {
            e.logFn("subscribe-end", { args: n, result: r });
          }
        };
    }
  };
};
var ql = "Unexpected error.";
function Bl(t) {
  return t instanceof Error && t.name === "GraphQLError";
}
function _s(t) {
  return Bl(t) ? t.originalError != null ? _s(t.originalError) : true : false;
}
function Gl(t, e, n) {
  const r = new Error(t);
  if (r.name = "GraphQLError", n) {
    const i = e instanceof Error ? { message: e.message, stack: e.stack } : { message: String(e) };
    Object.defineProperty(r, "extensions", {
      get() {
        return i;
      }
    });
  }
  return Object.defineProperty(r, "toJSON", {
    value() {
      return {
        message: r.message,
        extensions: r.extensions
      };
    }
  }), r;
}
var Ql = (t) => (e, n) => _s(e) ? e : Gl(n, e, t);
var Hl = globalThis.process?.env?.NODE_ENV === "development";
var Kl = Ql(Hl);
var Wl = (t, e) => ({ result: n, setResult: r }) => {
  n.errors != null && r({ ...n, errors: n.errors.map((i) => t(i, e)) });
};
function zl(t) {
  const e = t?.maskError ?? Kl, n = t?.errorMessage || ql, r = Wl(e, n);
  return {
    onPluginInit(i) {
      i.registerContextErrorHandler(({ error: s, setError: o }) => {
        o(e(s, n));
      });
    },
    onExecute() {
      return {
        onExecuteDone(i) {
          return ai(i, r);
        }
      };
    },
    onSubscribe() {
      return {
        onSubscribeResult(i) {
          return ai(i, r);
        },
        onSubscribeError({ error: i, setError: s }) {
          s(e(i, n));
        }
      };
    }
  };
}
var Jl = (t) => ({
  onContextBuilding({ context: e, extendContext: n }) {
    return oe(() => t(e), (r) => n(r));
  }
});
var Yl = (t) => ({
  onExecute: ({ setExecuteFn: e }) => {
    t.execute && e(t.execute);
  },
  onParse: ({ setParseFn: e }) => {
    t.parse && e(t.parse);
  },
  onValidate: ({ setValidationFn: e, addValidationRule: n }) => {
    t.validate && e(t.validate), t.specifiedRules?.map(n);
  },
  onSubscribe: ({ setSubscribeFn: e }) => {
    t.subscribe && e(t.subscribe);
  }
});
var qn2 = class extends qe {
  constructor(e, n) {
    super(e, n), this.name = "PothosError";
  }
};
var M = class extends qn2 {
  constructor(e, n) {
    super(e, n), this.name = "PothosSchemaError";
  }
};
var ci = Symbol.for("Pothos.contextCache");
function Xl(t) {
  const e = /* @__PURE__ */ new WeakMap(), n = (r, ...i) => {
    const s = r[ci] || r;
    if (e.has(s))
      return e.get(s);
    const o = t(r, ...i);
    return e.set(s, o), o;
  };
  return n.delete = (r) => {
    const i = r[ci] || r;
    e.delete(i);
  }, n;
}
function kt2(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var At = /* @__PURE__ */ new WeakMap();
var Zl = class {
  /**
  * Called for each type defined with the SchemaBuilder
  * @param  {PothosTypeConfig} typeConfig - Config object describing the added type
  * @return {PothosTypeConfig} Original or updated `typeConfig`
  */
  onTypeConfig(e) {
    return e;
  }
  /**
  * Called for each field on an Object or Interface type
  * @param  {PothosOutputFieldConfig} fieldConfig - Config object describing the added field
  * @return {PothosOutputFieldConfig} Original or updated `fieldConfig`
  */
  onOutputFieldConfig(e) {
    return e;
  }
  /**
  * Called for each argument or field on an Input object defined in your schema
  * @param  {PothosInputFieldConfig} fieldConfig - Config object describing the added field
  * @return {PothosInputFieldConfig} Original or updated `fieldConfig`
  */
  onInputFieldConfig(e) {
    return e;
  }
  /**
  * Called for each Enum value defined in your schema
  * @param  {PothosEnumValueConfig} valueConfig - Config object describing the enum value
  * @return {PothosEnumValueConfig} Original or updated `valueConfig`
  */
  onEnumValueConfig(e) {
    return e;
  }
  /**
  * Called before builder.toSchema() schema is called
  */
  beforeBuild() {
  }
  /**
  * Called after all fields and types have been built during `builder.toSchema()`
  * @param  {GraphQLSchema} schema - the generated schema
  * @return {PothosEnumValueConfig} Original or updated `schema`
  */
  afterBuild(e) {
    return e;
  }
  /**
  * Called with the resolver for each field in the schema
  * @param  {GraphQLFieldResolver} resolve - the resolve function
  * @param  {PothosOutputFieldConfig} fieldConfig - the config object for the field associated with this resolve function
  * @return {GraphQLFieldResolver} - Either the original, or a new resolver function to use for this field
  */
  wrapResolve(e, n) {
    return e;
  }
  /**
  * Called with the subscribe for each field on the Subscription type
  * @param  {GraphQLFieldResolver} subscribe - the subscribe function
  * @param  {PothosOutputFieldConfig} fieldConfig - the config object for the field associated with this subscribe function
  * @return {GraphQLFieldResolver} - Either the original, or a new subscribe function to use for this field
  */
  wrapSubscribe(e, n) {
    return e;
  }
  /**
  * Called with the resolveType for each Interface or Union type
  * @param  {GraphQLTypeResolver} resolveType - the resolveType function
  * @param  {PothosInterfaceTypeConfig | PothosUnionTypeConfig} typeConfig - the config object for the Interface or Union type
  * @return {GraphQLTypeResolver} - Either the original, or a new resolveType function to use for this field
  */
  wrapResolveType(e, n) {
    return e;
  }
  /**
  * Called with the isTypeOf for each Object type
  * @param  {GraphQLTypeResolver} resolveType - the resolveType function
  * @param  {PothosObjectTypeConfig} typeConfig - the config object for the Interface or Union type
  * @return {GraphQLTypeResolver} - Either the original, or a new resolveType function to use for this field
  */
  wrapIsTypeOf(e, n) {
    return e;
  }
  runUnique(e, n) {
    if (At.has(this.builder) || At.set(this.builder, /* @__PURE__ */ new Map()), !At.get(this.builder).has(e)) {
      const r = n();
      return At.get(this.builder).set(e, r), r;
    }
    return At.get(this.builder).get(e);
  }
  /**
  * Creates a data object unique to the current request for use by this plugin
  * @param  {Types['Context']} context - the context object for the current request
  * @return {object} - The data object for the current request
  */
  createRequestData(e) {
    throw new qn2("createRequestData not implemented");
  }
  /**
  * Returns a data object for the current request.  requires `createRequestData` to be implemented
  * @param  {Types['Context']} context - the context object for the current request
  * @return {object} - The data object for the current request
  */
  requestData(e) {
    return this.requestDataMap(e);
  }
  constructor(e, n) {
    kt2(this, "name", void 0), kt2(this, "builder", void 0), kt2(this, "buildCache", void 0), kt2(this, "options", void 0), kt2(this, "requestDataMap", Xl((r) => this.createRequestData(r))), this.name = n, this.builder = e.builder, this.buildCache = e, this.options = e.options;
  }
};
function eu(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var tu = class extends Zl {
  onTypeConfig(e) {
    return this.plugins.reduceRight((n, r) => n === null ? n : r.onTypeConfig(n), e);
  }
  onInputFieldConfig(e) {
    return this.plugins.reduceRight((n, r) => n === null ? n : r.onInputFieldConfig(n), e);
  }
  onOutputFieldConfig(e) {
    return this.plugins.reduceRight((n, r) => n === null ? n : r.onOutputFieldConfig(n), e);
  }
  onEnumValueConfig(e) {
    return this.plugins.reduceRight((n, r) => n === null ? n : r.onEnumValueConfig(n), e);
  }
  beforeBuild() {
    for (const e of this.plugins)
      e.beforeBuild();
  }
  afterBuild(e) {
    return this.plugins.reduceRight((n, r) => r.afterBuild(n), e);
  }
  wrapResolve(e, n) {
    const r = this.plugins.reduceRight((i, s) => s.wrapResolve(i, n), e);
    if (n.argMappers.length) {
      const i = n.argMappers;
      return (s, o, a, l) => {
        const u2 = i.reduce((c, f2) => f2(c, a, l), o);
        return r(s, u2, a, l);
      };
    }
    return r;
  }
  wrapSubscribe(e, n) {
    const r = this.plugins.reduceRight((s, o) => o.wrapSubscribe(s, n), e);
    if (!r || !n.argMappers.length)
      return r;
    const i = n.argMappers;
    return (s, o, a, l) => {
      const u2 = i.reduce((c, f2) => f2(c, a, l), o);
      return r(s, u2, a, l);
    };
  }
  wrapResolveType(e, n) {
    return this.plugins.reduceRight((r, i) => i.wrapResolveType(r, n), e);
  }
  wrapIsTypeOf(e, n) {
    return this.plugins.reduceRight((r, i) => i.wrapIsTypeOf(r, n), e);
  }
  constructor(e, n) {
    super(e, "PothosMergedPlugin"), eu(this, "plugins", void 0), this.plugins = n;
  }
};
var Nt = Symbol.for("Pothos.outputShapeKey");
var nn = Symbol.for("Pothos.parentShapeKey");
var Bn = Symbol.for("Pothos.inputShapeKey");
var Ns = Symbol.for("Pothos.inputFieldShapeKey");
var nu = Symbol.for("Pothos.outputFieldShapeKey");
var kn = Symbol.for("Pothos.typeBrandKey");
function ut(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Z = class {
  toString() {
    return `${this.kind}Ref<${this.name}>`;
  }
  associate(e) {
    if (this.association && typeof this.associate != "string")
      throw new M(`${this} is already associated with ${this.association}`);
    this.association = e;
  }
  onConfig(e) {
    this.configCallbacks.add(e), this.currentConfig && e(this.currentConfig);
  }
  updateConfig(e) {
    if (typeof e == "function") {
      this.onceOnConfig((n) => {
        this.updateConfig(e(n));
      });
      return;
    }
    this.currentConfig = e;
    for (const n of this.configCallbacks) {
      if (this.currentConfig !== e)
        break;
      n(e);
    }
  }
  prepareForBuild() {
    this.preparedForBuild = true;
  }
  onceOnConfig(e) {
    const n = (r) => {
      this.configCallbacks.delete(n), e(r);
    };
    this.onConfig(n);
  }
  constructor(e, n, r) {
    ut(this, "kind", void 0), ut(this, "name", void 0), ut(this, "association", null), ut(this, "configCallbacks", /* @__PURE__ */ new Set()), ut(this, "preparedForBuild", false), ut(this, "currentConfig", void 0), this.kind = e, this.name = n, this.currentConfig = r ?? null;
  }
};
function ct(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var ru = Nt;
var iu2 = nn;
var su = Bn;
var Rs2 = class extends Z {
  constructor(e, n) {
    super("Scalar", e, n), ct(this, "kind", "Scalar"), ct(this, "$inferType", void 0), ct(this, "$inferInput", void 0), ct(this, ru, void 0), ct(this, iu2, void 0), ct(this, su, void 0);
  }
};
function ou(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var au = class extends Rs2 {
  constructor(e) {
    super(e.name), ou(this, "type", void 0), this.type = e;
  }
};
function Ft(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var lu2 = Bn;
var ks = class extends Z {
  constructor(e, n) {
    super("InputList", `InputList<${String(e)}>`), Ft(this, "kind", "InputList"), Ft(this, lu2, void 0), Ft(this, "$inferInput", void 0), Ft(this, "listType", void 0), Ft(this, "required", void 0), this.listType = e, this.required = n;
  }
};
function ft(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var uu2 = Nt;
var cu2 = nn;
var As = class extends Z {
  constructor(e, n) {
    super("List", `List<${String(e)}>`), ft(this, "kind", "List"), ft(this, "$inferType", void 0), ft(this, uu2, void 0), ft(this, cu2, void 0), ft(this, "listType", void 0), ft(this, "nullable", void 0), this.listType = e, this.nullable = n;
  }
};
function fu2(t) {
  const e = {};
  if (Array.isArray(t))
    for (const n of t)
      e[String(n)] = {
        pothosOptions: {}
      };
  else
    for (const [n, r] of Object.entries(t))
      r && typeof r == "object" ? e[n] = {
        ...r,
        pothosOptions: r
      } : typeof r == "string" && (e[r] = {
        pothosOptions: {}
      });
  return e;
}
function du2(t, e) {
  const n = {};
  for (const r of Object.keys(t).filter((i) => typeof t[t[i]] != "number"))
    n[r] = {
      value: t[r],
      pothosOptions: {},
      ...e?.[r]
    };
  return n;
}
function br(t, e, n) {
  const r = typeof n == "object" ? n.items : false, i = typeof n == "object" ? n.list : !!n;
  if (Array.isArray(t))
    return {
      kind: "List",
      type: br(t[0], e, r),
      nullable: i
    };
  if (t instanceof As)
    return {
      kind: "List",
      type: br(t.listType, e, t.nullable),
      nullable: i
    };
  const s = e.getOutputTypeRef(t), o = s instanceof Z ? s.kind : e.getTypeConfig(s).graphqlKind, a = s instanceof Z ? s.name : e.getTypeConfig(s).name;
  if (o !== "InputObject" && o !== "List" && o !== "InputList")
    return {
      kind: o,
      ref: s,
      nullable: i
    };
  throw new M(`Expected input param ${a} to be an output type but got ${o}`);
}
function An(t, e, n) {
  const r = typeof n == "object" ? n.items : true, i = typeof n == "object" ? n.list : !!n;
  if (Array.isArray(t))
    return {
      kind: "List",
      type: An(t[0], e, r),
      required: i
    };
  if (t instanceof ks)
    return {
      kind: "List",
      type: An(t.listType, e, t.required),
      required: i
    };
  const s = e.getInputTypeRef(t), o = s instanceof Z ? s.kind : e.getTypeConfig(s).graphqlKind, a = s instanceof Z ? s.name : e.getTypeConfig(s).name;
  if (o === "InputObject" || o === "Enum" || o === "Scalar")
    return {
      kind: o,
      ref: s,
      required: i
    };
  throw new M(`Expected input param ${a} to be an InputObject, Enum, or Scalar but got ${o}`);
}
function pu2(t) {
  throw new TypeError(`Unexpected value: ${t}`);
}
function hu2(t) {
  return !!(t && (typeof t == "object" || typeof t == "function") && typeof t.then == "function");
}
function Ae(t) {
  if (t === void 0)
    throw new M(`Received undefined as a type ref.

This is often caused by a circular import
If this ref is imported from a file that re-exports it (like index.ts)
you may be able to resolve this by importing it directly from the file that defines it.
`);
}
function fi(t) {
  if (!(!t || typeof t == "function")) {
    if (!Array.isArray(t))
      throw new M("interfaces must be an array or function");
    for (const e of t)
      if (e === void 0)
        throw new M(`Received undefined in list of interfaces.

This is often caused by a circular import
If this ref is imported from a file that re-exports it (like index.ts)
you may be able to resolve this by importing it directly from the file that defines it.

Alternatively you can define interfaces with a function that will be lazily evaluated,
which may resolver issues with circular dependencies:

Example:
builder.objectType('MyObject', {
  interface: () => [Interface1, Interface2],
  ...
});
`);
  }
}
function gu2(t) {
  return typeof t == "object" && t !== null && kn in t ? t[kn] : null;
}
function ve(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var mu2 = class {
  getTypeConfig(e, n) {
    const r = this.configStore.getTypeConfig(e, n);
    return this.typeConfigs.has(r.name) || this.typeConfigs.set(r.name, this.plugin.onTypeConfig(r)), this.typeConfigs.get(r.name);
  }
  getInputTypeFieldConfigs(e) {
    const n = this.getTypeConfig(e, "InputObject"), r = this.types.get(n.name);
    if (!r)
      throw new M(`Input type ${n.name} has not been built yet`);
    const i = r.getFields(), s = {};
    for (const a of Object.keys(i)) {
      var o;
      s[a] = (o = i[a].extensions) === null || o === void 0 ? void 0 : o.pothosConfig;
    }
    return s;
  }
  getImplementers(e) {
    if (this.implementers.has(e.name))
      return this.implementers.get(e.name);
    const n = [
      ...this.configStore.typeConfigs.values()
    ].filter((r) => r.kind === "Object" && r.interfaces.find((i) => this.configStore.getTypeConfig(i).name === e.name));
    return this.implementers.set(e.name, n), n;
  }
  buildAll() {
    this.configStore.prepareForBuild();
    for (const e of this.configStore.typeConfigs.values())
      (e.kind === "Enum" || e.kind === "Scalar") && this.buildTypeFromConfig(e);
    for (const e of this.configStore.typeConfigs.values())
      e.kind === "InputObject" && this.buildTypeFromConfig(e);
    for (const e of this.types.values())
      e instanceof gu && e.getFields();
    for (const e of this.configStore.typeConfigs.values())
      e.kind === "Interface" && this.buildTypeFromConfig(e);
    for (const e of this.configStore.typeConfigs.values())
      e.kind === "Object" && this.buildTypeFromConfig(e);
    for (const e of this.configStore.typeConfigs.values())
      e.kind === "Union" && this.buildTypeFromConfig(e);
    for (const e of this.configStore.typeConfigs.values())
      (e.kind === "Query" || e.kind === "Mutation" || e.kind === "Subscription") && this.buildTypeFromConfig(e);
    for (const e of this.types.values())
      e instanceof fu || e instanceof hu ? e.getFields() : e instanceof vu && e.getTypes();
  }
  buildTypeFromConfig(e) {
    const n = this.getTypeConfig(e.name), { name: r } = n;
    switch (this.typeConfigs.set(r, n), n.kind) {
      case "Enum":
        this.addType(r, this.buildEnum(n));
        break;
      case "InputObject":
        this.addType(r, this.buildInputObject(n));
        break;
      case "Interface":
        this.addType(r, this.buildInterface(n));
        break;
      case "Scalar":
        this.addType(r, this.buildScalar(n));
        break;
      case "Union":
        this.addType(r, this.buildUnion(n));
        break;
      case "Object":
      case "Query":
      case "Mutation":
      case "Subscription":
        this.addType(r, this.buildObject(n));
        break;
      default:
        pu2(n);
    }
  }
  addType(e, n) {
    if (this.types.has(e))
      throw new M(`reference or name has already been used to create another type (${n.name})`);
    this.types.set(e, n);
  }
  buildOutputTypeParam(e) {
    return e.kind === "List" ? e.nullable ? new lu(this.buildOutputTypeParam(e.type)) : new pu(new lu(this.buildOutputTypeParam(e.type))) : e.nullable ? this.getOutputType(e.ref) : new pu(this.getOutputType(e.ref));
  }
  buildInputTypeParam(e) {
    return e.kind === "List" ? e.required ? new pu(new lu(this.buildInputTypeParam(e.type))) : new lu(this.buildInputTypeParam(e.type)) : e.required ? new pu(this.getInputType(e.ref)) : this.getInputType(e.ref);
  }
  buildFields(e) {
    const n = {};
    for (const [s, o] of e) {
      this.outputFieldConfigs.has(o) || this.outputFieldConfigs.set(o, this.plugin.onOutputFieldConfig(o));
      const a = this.outputFieldConfigs.get(o);
      if (!a)
        continue;
      const l = {
        ...a
      }, u2 = /* @__PURE__ */ new Map();
      for (const g2 of Object.keys(l.args))
        u2.set(g2, l.args[g2]);
      const c = this.buildInputFields(u2), f2 = {};
      for (const g2 of Object.keys(l.args))
        f2[g2] = this.inputFieldConfigs.get(l.args[g2]);
      l.args = f2;
      var r;
      const d = this.plugin.wrapResolve((r = l.resolve) !== null && r !== void 0 ? r : yn, l), h = this.plugin.wrapSubscribe(l.subscribe, l);
      var i;
      n[s] = {
        ...l,
        type: this.buildOutputTypeParam(l.type),
        args: c,
        extensions: {
          ...l.extensions,
          pothosResolveWrapped: d !== ((i = l.resolve) !== null && i !== void 0 ? i : yn),
          pothosSubscribeWrapped: h !== l.subscribe,
          pothosOptions: l.pothosOptions,
          pothosConfig: l,
          pothosArgMappers: l.argMappers
        },
        resolve: d === yn ? void 0 : d,
        subscribe: h
      };
    }
    return n;
  }
  buildInputFields(e) {
    const n = {};
    for (const [r, i] of e) {
      this.inputFieldConfigs.has(i) || this.inputFieldConfigs.set(i, this.plugin.onInputFieldConfig(i));
      const s = this.inputFieldConfigs.get(i);
      s && (n[r] = {
        ...s,
        type: this.buildInputTypeParam(s.type),
        extensions: {
          ...s.extensions,
          pothosOptions: s.pothosOptions,
          pothosConfig: s
        }
      });
    }
    return n;
  }
  getInterfaceFields(e) {
    const n = e.getInterfaces().reduce((s, o) => Object.assign(s, this.getFields(o)), {}), r = this.configStore.getFields(e.name, "Interface"), i = this.buildFields(r);
    return {
      ...n,
      ...i
    };
  }
  getObjectFields(e) {
    const n = e.getInterfaces().reduce((i, s) => Object.assign(i, this.getFields(s)), {}), r = this.buildFields(this.configStore.getFields(e.name, "Object"));
    return {
      ...n,
      ...r
    };
  }
  getRootFields(e) {
    return this.buildFields(this.configStore.getFields(e.name, "Object"));
  }
  getFields(e) {
    if (e instanceof fu) {
      const n = this.configStore.getTypeConfig(e.name);
      return n.kind === "Query" || n.kind === "Mutation" || n.kind === "Subscription" ? this.getRootFields(e) : this.getObjectFields(e);
    }
    if (e instanceof hu)
      return this.getInterfaceFields(e);
    throw new M(`Type ${e.name} does not have fields to resolve`);
  }
  getInputFields(e) {
    return this.buildInputFields(this.configStore.getFields(e.name, "InputObject"));
  }
  getType(e) {
    if (e instanceof au)
      return e.type;
    const n = this.configStore.getTypeConfig(e), r = this.types.get(n.name);
    return r || (this.buildTypeFromConfig(n), this.types.get(n.name));
  }
  getOutputType(e) {
    const n = this.getType(e);
    if (n instanceof gu)
      throw new M(`Expected ${String(e)} to be an output type but it was defined as an InputObject`);
    return n;
  }
  getInputType(e) {
    const n = this.getType(e);
    if (!n)
      throw new M(`Missing implementation of for type ${String(e)}`);
    if (n instanceof fu)
      throw new M(`Expected ${n.name} to be an input type but it was defined as a GraphQLObjectType`);
    if (n instanceof hu)
      throw new M(`Expected ${n.name} to be an input type but it was defined as a GraphQLInterfaceType`);
    if (n instanceof vu)
      throw new M(`Expected ${String(e)} to be an input type but it was defined as an GraphQLUnionType`);
    return n;
  }
  getTypeOfKind(e, n) {
    const r = this.getType(e);
    switch (n) {
      case "Object":
      case "Query":
      case "Mutation":
      case "Subscription":
        if (r instanceof fu)
          return r;
        break;
      case "Interface":
        if (r instanceof hu)
          return r;
        break;
      case "Union":
        if (r instanceof vu)
          return r;
        break;
      case "Enum":
        if (r instanceof bu)
          return r;
        break;
      case "Scalar":
        if (r instanceof zr)
          return r;
        break;
      case "InputObject":
        if (r instanceof zr)
          return r;
        break;
    }
    throw new M(`Expected ${String(e)} to be of type ${n}`);
  }
  buildObject(e) {
    var n;
    const r = new fu({
      ...e,
      extensions: {
        ...e.extensions,
        pothosOptions: e.pothosOptions,
        pothosConfig: e
      },
      fields: () => this.getFields(r),
      isTypeOf: e.kind === "Object" ? this.plugin.wrapIsTypeOf((n = e.isTypeOf) !== null && n !== void 0 ? n : void 0, e) : void 0,
      interfaces: e.kind === "Object" ? () => e.interfaces.map((i) => this.getTypeOfKind(i, "Interface")) : void 0
    });
    return r;
  }
  buildInterface(e) {
    const n = (i, s, o) => {
      const a = gu2(i);
      if (a)
        return typeof a == "string" ? a : this.getTypeConfig(a).name;
      var l;
      return ((l = e.resolveType) !== null && l !== void 0 ? l : ri)(i, s, o, r);
    }, r = new hu({
      ...e,
      extensions: {
        ...e.extensions,
        pothosOptions: e.pothosOptions,
        pothosConfig: e
      },
      interfaces: () => e.interfaces.map((i) => this.getTypeOfKind(i, "Interface")),
      fields: () => this.getFields(r),
      resolveType: this.plugin.wrapResolveType(n, e)
    });
    return r;
  }
  buildUnion(e) {
    const n = (r, i, s, o) => {
      if (typeof r == "object" && r !== null && kn in r) {
        const u2 = r[kn];
        return typeof u2 == "string" ? u2 : this.getTypeConfig(u2).name;
      }
      if (!e.resolveType)
        return ri(r, i, s, o);
      const a = e.resolveType(r, i, s, o), l = (u2) => {
        if (typeof u2 == "string" || !u2)
          return u2;
        if (u2 instanceof fu)
          return u2.name;
        try {
          return this.configStore.getTypeConfig(u2).name;
        } catch {
        }
        return u2;
      };
      return hu2(a) ? a.then(l) : l(a);
    };
    return new vu({
      ...e,
      extensions: {
        ...e.extensions,
        pothosOptions: e.pothosOptions,
        pothosConfig: e
      },
      types: () => e.types.map((r) => this.getTypeOfKind(r, "Object")),
      resolveType: this.plugin.wrapResolveType(n, e)
    });
  }
  buildInputObject(e) {
    const n = new gu({
      ...e,
      extensions: {
        ...e.extensions,
        pothosOptions: e.pothosOptions,
        pothosConfig: e
      },
      fields: () => this.getInputFields(n)
    });
    return n;
  }
  buildScalar(e) {
    return e.name === "ID" ? Mn : e.name === "Int" ? jn : e.name === "Float" ? Ir : e.name === "Boolean" ? X : e.name === "String" ? V : new zr({
      ...e,
      extensions: {
        ...e.extensions,
        pothosOptions: e.pothosOptions,
        pothosConfig: e
      }
    });
  }
  buildEnum(e) {
    const n = {}, r = typeof e.values == "function" ? e.values() : e.values;
    for (const i of Object.keys(e.values)) {
      const s = r[i];
      this.enumValueConfigs.has(s) || this.enumValueConfigs.set(s, this.plugin.onEnumValueConfig(s)), this.enumValueConfigs.get(s) && (n[i] = this.enumValueConfigs.get(s));
    }
    return new bu({
      ...e,
      values: n,
      extensions: {
        ...e.extensions,
        pothosOptions: e.pothosOptions,
        pothosConfig: e
      }
    });
  }
  constructor(e, n) {
    ve(this, "types", /* @__PURE__ */ new Map()), ve(this, "builder", void 0), ve(this, "plugin", void 0), ve(this, "options", void 0), ve(this, "configStore", void 0), ve(this, "pluginList", void 0), ve(this, "implementers", /* @__PURE__ */ new Map()), ve(this, "typeConfigs", /* @__PURE__ */ new Map()), ve(this, "enumValueConfigs", /* @__PURE__ */ new Map()), ve(this, "outputFieldConfigs", /* @__PURE__ */ new Map()), ve(this, "inputFieldConfigs", /* @__PURE__ */ new Map()), this.builder = e, this.configStore = e.configStore, this.options = n;
    const r = {};
    var i;
    this.pluginList = ((i = e.options.plugins) !== null && i !== void 0 ? i : []).map((s) => {
      const o = this.builder.constructor.plugins[s];
      if (!o)
        throw new qn2(`No plugin named ${s} was registered`);
      return r[s] = new o(this, s), r[s];
    }), this.plugin = new tu(this, this.pluginList);
  }
};
function ht(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var yu2 = Bn;
var Fn = class extends Z {
  addFields(e) {
    this.fields.add(e);
    for (const n of this.fieldCbs)
      for (const [r, i] of Object.entries(e()))
        i && n(r, i);
  }
  onField(e) {
    this.fieldCbs.add(e);
    for (const n of this.fields)
      for (const [r, i] of Object.entries(n()))
        i && e(r, i);
  }
  constructor(e) {
    super("InputObject", e), ht(this, "kind", "InputObject"), ht(this, "$inferInput", void 0), ht(this, yu2, void 0), ht(this, "fields", /* @__PURE__ */ new Set()), ht(this, "fieldCbs", /* @__PURE__ */ new Set());
  }
};
var bu2 = class extends Fn {
  implement(e) {
    return this.builder.inputType(this, e), this;
  }
  constructor(e, n) {
    super(n), ht(this, "builder", void 0), this.builder = e;
  }
};
function ir(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Fs = class extends Z {
  addFields(e) {
    for (const n of this.fieldCbs)
      for (const [r, i] of Object.entries(e()))
        i && n(r, i);
    this.fields.add(e);
  }
  addInterfaces(e) {
    Array.isArray(e) && e.length === 0 || (this.preparedForBuild ? this.updateConfig((n) => ({
      ...n,
      interfaces: [
        ...n.interfaces,
        ...typeof e == "function" ? e() : e
      ]
    })) : this.interfaces.push(() => Array.isArray(e) ? e : e()));
  }
  onField(e) {
    this.fieldCbs.add(e);
    for (const n of this.fields)
      for (const [r, i] of Object.entries(n()))
        i && e(r, i);
  }
  prepareForBuild() {
    this.preparedForBuild || (super.prepareForBuild(), this.interfaces.length > 0 && this.updateConfig((e) => {
      var n;
      return {
        ...e,
        interfaces: [
          ...(n = e.interfaces) !== null && n !== void 0 ? n : [],
          ...this.interfaces.flatMap((r) => r())
        ]
      };
    }));
  }
  constructor(...e) {
    super(...e), ir(this, "fields", /* @__PURE__ */ new Set()), ir(this, "fieldCbs", /* @__PURE__ */ new Set()), ir(this, "interfaces", []);
  }
};
function Vt(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var vu2 = Nt;
var Tu = nn;
var Cn = class extends Fs {
  constructor(e, n) {
    super("Interface", e, n), Vt(this, "kind", "Interface"), Vt(this, "$inferType", void 0), Vt(this, vu2, void 0), Vt(this, Tu, void 0);
  }
};
var Eu = class extends Cn {
  implement(e) {
    return this.builder.interfaceType(this, e);
  }
  constructor(e, n) {
    super(n), Vt(this, "builder", void 0), this.builder = e;
  }
};
function qt(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var wu = Nt;
var Su = nn;
var ot = class extends Fs {
  constructor(e, n) {
    super("Object", e, n), qt(this, "kind", "Object"), qt(this, "$inferType", void 0), qt(this, wu, void 0), qt(this, Su, void 0);
  }
};
var Iu = class extends ot {
  implement(e) {
    return this.builder.objectType(this, e);
  }
  constructor(e, n) {
    super(n), qt(this, "builder", void 0), this.builder = e;
  }
};
function Ou2(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Cs = class extends ot {
  constructor(...e) {
    super(...e), Ou2(this, "kind", "Object");
  }
};
var xs = class extends ot {
};
var Ds = class extends ot {
};
function Fe(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var _u = class {
  addFields(e, n) {
    this.onTypeConfig(e, (r, i) => {
      if (!(i instanceof Cn || i instanceof ot || i instanceof xs || i instanceof Cs || i instanceof Ds))
        throw new M(`Can not add fields to ${i} because it is not an object`);
      i.addFields(n);
    });
  }
  addInputFields(e, n) {
    this.onTypeConfig(e, (r, i) => {
      if (!(i instanceof Fn))
        throw new M(`Can not add fields to ${i} because it is not an input object`);
      i.addFields(n);
    });
  }
  associateParamWithRef(e, n) {
    const r = this.resolveParamAssociations(n);
    this.paramAssociations.set(e, r);
    var i;
    const s = (i = this.pendingTypeConfigResolutions.get(e)) !== null && i !== void 0 ? i : [];
    if (s.length > 0)
      if (typeof r == "string" && this.typeConfigs.has(r))
        for (const o of s) {
          const a = this.typeConfigs.get(r);
          o(a, this.implementors.get(a.name));
        }
      else
        for (const o of s)
          this.onTypeConfig(r, o);
    this.pendingTypeConfigResolutions.delete(e);
  }
  onTypeConfig(e, n) {
    const r = this.resolveParamAssociations(e);
    if (typeof r == "string" && this.typeConfigs.has(r)) {
      const i = this.typeConfigs.get(r);
      n(i, this.implementors.get(i.name));
    } else
      this.pendingTypeConfigResolutions.has(r) || this.pendingTypeConfigResolutions.set(r, []), this.pendingTypeConfigResolutions.get(r).push(n);
  }
  onTypeConfigOfKind(e, n, r) {
    this.onTypeConfig(e, (i) => {
      if (i.kind !== n)
        throw new M(`Expected ${this.describeRef(e)} to be of kind ${n} but it is of kind ${i.kind}`);
      r(i);
    });
  }
  addTypeRef(e) {
    this.refs.has(e) || (this.pending || e.prepareForBuild(), this.refs.add(e), e.onConfig((n) => {
      const r = this.implementors.get(n.name);
      if (r && r !== e)
        throw new M(`Duplicate typename: Another type with name ${n.name} already exists.`);
      if (r || (this.implementors.set(n.name, e), this.associateParamWithRef(e, n.name), (e instanceof ot || e instanceof Cn || e instanceof Fn) && (this.fields.has(n.name) || this.fields.set(n.name, /* @__PURE__ */ new Map()), this.onPrepare(() => {
        e.onField((i, s) => {
          const o = this.fields.get(n.name);
          if (o.has(i))
            throw new M(`Duplicate field ${i} on ${n.name}`);
          var a;
          o.set(i, s.getConfig(i, (a = this.typeConfigs.get(n.name)) !== null && a !== void 0 ? a : n));
        });
      }))), this.typeConfigs.set(n.name, n), this.pendingTypeConfigResolutions.has(n.name)) {
        const i = this.pendingTypeConfigResolutions.get(n.name);
        for (const s of i)
          s(n, e);
      }
      this.pendingTypeConfigResolutions.delete(n.name);
    }));
  }
  subscribeToFields(e) {
  }
  hasImplementation(e) {
    return this.typeConfigs.has(e);
  }
  hasConfig(e) {
    const n = this.resolveParamAssociations(e);
    return !(typeof n != "string" || !this.typeConfigs.has(n));
  }
  getTypeConfig(e, n) {
    const r = this.resolveParamAssociations(e);
    if (typeof r != "string" || !this.typeConfigs.has(r))
      throw new M(`${this.describeRef(e)} has not been implemented`);
    const i = this.typeConfigs.get(r);
    if (n && i.graphqlKind !== n)
      throw new M(`Expected ref to resolve to a ${n} type, but got ${i.kind}`);
    return i;
  }
  getInputTypeRef(e) {
    const n = this.resolveParamAssociations(e);
    if (e instanceof Z) {
      if (e.kind !== "InputObject" && e.kind !== "Enum" && e.kind !== "Scalar")
        throw new M(`Expected ${this.describeRef(e)} to be an input type but got ${e.kind}`);
      return e;
    }
    if (typeof n == "string" && this.typeConfigs.has(n)) {
      const r = this.implementors.get(n);
      if (r instanceof Z) {
        if (r.kind !== "InputObject" && r.kind !== "Enum" && r.kind !== "Scalar")
          throw new M(`Expected ${this.describeRef(r)} to be an input type but got ${r.kind}`);
        return r;
      }
    }
    throw new M(`${this.describeRef(e)} has not been implemented`);
  }
  getOutputTypeRef(e) {
    const n = this.resolveParamAssociations(e);
    if (e instanceof Z) {
      if (e.kind === "InputObject" || e.kind === "InputList")
        throw new M(`Expected ${e.name} to be an output type but got ${e.kind}`);
      return e;
    }
    if (typeof n == "string" && this.typeConfigs.has(n)) {
      const r = this.implementors.get(n);
      if (r instanceof Z) {
        if (r.kind === "InputObject" || r.kind === "InputList")
          throw new M(`Expected ${r.name} to be an output type but got ${r.kind}`);
        return r;
      }
    }
    throw new M(`${this.describeRef(e)} has not been implemented`);
  }
  getFields(e, n) {
    const r = this.getTypeConfig(e);
    this.fields.has(e) || this.fields.set(e, /* @__PURE__ */ new Map());
    const i = this.fields.get(e);
    if (n && r.graphqlKind !== n)
      throw new M(`Expected ${e} to be a ${n} type, but found ${r.graphqlKind}`);
    return i;
  }
  prepareForBuild() {
    this.pending = false;
    for (const n of this.refs)
      n.prepareForBuild();
    const { pendingActions: e } = this;
    this.pendingActions = [];
    for (const n of e)
      n();
    if (this.pendingTypeConfigResolutions.size > 0)
      throw new M(`Missing implementations for some references (${[
        ...this.pendingTypeConfigResolutions.keys()
      ].map((n) => this.describeRef(n)).join(", ")}).`);
  }
  onPrepare(e) {
    this.pending ? this.pendingActions.push(e) : e();
  }
  resolveParamAssociations(e) {
    let n = this.paramAssociations.get(e);
    for (; n && this.paramAssociations.has(n); )
      n = this.paramAssociations.get(n);
    return n ?? e;
  }
  describeRef(e) {
    return typeof e == "string" ? e : e && e.toString !== {}.toString ? String(e) : typeof e == "function" && e.name !== (() => {
    }).name ? `function ${e.name}` : "<unnamed ref or enum>";
  }
  constructor(e) {
    Fe(this, "typeConfigs", /* @__PURE__ */ new Map()), Fe(this, "fields", /* @__PURE__ */ new Map()), Fe(this, "refs", /* @__PURE__ */ new Set()), Fe(this, "implementors", /* @__PURE__ */ new Map()), Fe(this, "pendingActions", []), Fe(this, "paramAssociations", /* @__PURE__ */ new Map()), Fe(this, "pendingTypeConfigResolutions", /* @__PURE__ */ new Map()), Fe(this, "pending", true), Fe(this, "builder", void 0), this.builder = e;
  }
};
function We(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Nu = Ns;
var Ru = class {
  onConfig(e) {
    this.pendingActions.push(e);
  }
  updateConfig(e) {
    this.pendingActions.push(e);
  }
  getConfig(e, n, r) {
    const i = this.pendingActions.reduce((s, o) => {
      var a;
      return (a = o(s)) !== null && a !== void 0 ? a : s;
    }, this.initConfig(e, n, r));
    for (const s of this.onUseCallbacks)
      this.onUseCallbacks.delete(s), s(i);
    return i;
  }
  onFirstUse(e) {
    this.onUseCallbacks.add(e);
  }
  constructor(e) {
    We(this, "kind", "Arg"), We(this, "fieldName", void 0), We(this, "$inferInput", void 0), We(this, Nu, void 0), We(this, "pendingActions", []), We(this, "initConfig", void 0), We(this, "onUseCallbacks", /* @__PURE__ */ new Set()), this.initConfig = e;
  }
};
function ze(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var ku = Ns;
var Au = class {
  updateConfig(e) {
    this.pendingActions.push(e);
  }
  getConfig(e, n) {
    const r = this.pendingActions.reduce((i, s) => {
      var o;
      return (o = s(i)) !== null && o !== void 0 ? o : i;
    }, this.initConfig(e, n));
    for (const i of this.onUseCallbacks)
      this.onUseCallbacks.delete(i), i(r);
    return r;
  }
  onFirstUse(e) {
    this.onUseCallbacks.add(e);
  }
  constructor(e) {
    ze(this, "kind", "InputObject"), ze(this, "fieldName", void 0), ze(this, "$inferInput", void 0), ze(this, ku, void 0), ze(this, "pendingActions", []), ze(this, "initConfig", void 0), ze(this, "onUseCallbacks", /* @__PURE__ */ new Set()), this.initConfig = e;
  }
};
function de(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var vr = class {
  argBuilder() {
    const e = this.field.bind(this), n = Object.keys(Object.getPrototypeOf(this)).filter((r) => typeof this[r] == "function" && Function.prototype[r] === void 0);
    for (const r of [
      ...Object.keys(this),
      ...n
    ])
      e[r] = typeof this[r] == "function" ? this[r].bind(this) : this[r];
    return e;
  }
  /**
  * Create in input field or argument for the current type
  * @param {PothosSchemaTypes.InputFieldOptions} [options={}] - Options for this field
  */
  field(e) {
    return this.kind === "Arg" ? new Ru((r, i, s) => {
      const o = e;
      var a, l;
      return {
        name: r,
        parentField: i,
        kind: this.kind,
        graphqlKind: this.kind,
        parentType: s.name,
        type: An(o.type, this.builder.configStore, (a = o.required) !== null && a !== void 0 ? a : this.builder.defaultInputFieldRequiredness),
        pothosOptions: o,
        description: o.description,
        deprecationReason: o.deprecationReason,
        defaultValue: o.defaultValue,
        extensions: (l = o.extensions) !== null && l !== void 0 ? l : {}
      };
    }) : new Au((r, i) => {
      const s = e;
      var o, a;
      return {
        name: r,
        parentField: void 0,
        kind: this.kind,
        graphqlKind: this.kind,
        parentType: i.name,
        type: An(s.type, this.builder.configStore, (o = s.required) !== null && o !== void 0 ? o : this.builder.defaultInputFieldRequiredness),
        pothosOptions: s,
        description: s.description,
        deprecationReason: s.deprecationReason,
        defaultValue: s.defaultValue,
        extensions: (a = s.extensions) !== null && a !== void 0 ? a : {}
      };
    });
  }
  helper(e) {
    return (...n) => {
      const [r = {}] = n;
      return this.field({
        ...r,
        type: e
      });
    };
  }
  constructor(e, n) {
    de(this, "kind", void 0), de(this, "builder", void 0), de(this, "boolean", this.helper("Boolean")), de(this, "float", this.helper("Float")), de(this, "id", this.helper("ID")), de(this, "int", this.helper("Int")), de(this, "string", this.helper("String")), de(this, "booleanList", this.helper([
      "Boolean"
    ])), de(this, "floatList", this.helper([
      "Float"
    ])), de(this, "idList", this.helper([
      "ID"
    ])), de(this, "intList", this.helper([
      "Int"
    ])), de(this, "stringList", this.helper([
      "String"
    ])), de(this, "listRef", (r, i) => {
      var s;
      return new ks(r, (s = i?.required) !== null && s !== void 0 ? s : true);
    }), this.builder = e, this.kind = n;
  }
};
function Je(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Fu = nu;
var Cu = class {
  updateConfig(e) {
    this.pendingActions.push(e);
  }
  getConfig(e, n) {
    const r = this.pendingActions.reduce((i, s) => {
      var o;
      return (o = s(i)) !== null && o !== void 0 ? o : i;
    }, this.initConfig(e, n));
    for (const i of this.onUseCallbacks)
      this.onUseCallbacks.delete(i), i(r);
    return r;
  }
  onFirstUse(e) {
    this.onUseCallbacks.add(e);
  }
  constructor(e, n) {
    Je(this, "kind", void 0), Je(this, "fieldName", void 0), Je(this, "$inferType", void 0), Je(this, Fu, void 0), Je(this, "pendingActions", []), Je(this, "initConfig", void 0), Je(this, "onUseCallbacks", /* @__PURE__ */ new Set()), this.kind = e, this.initConfig = n;
  }
};
function sr(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var xu = class {
  createField(e) {
    return new Cu(this.kind, (r, i) => {
      var s;
      const o = {};
      if (e.args)
        for (const [c, f2] of Object.entries(e.args))
          o[c] = f2.getConfig(c, r, i);
      let { resolve: a } = e;
      ((s = e.extensions) === null || s === void 0 ? void 0 : s.pothosExposedField) === r && (a = yn);
      const { subscribe: l } = e;
      var u2;
      return {
        kind: this.kind,
        graphqlKind: i.graphqlKind,
        parentType: i.name,
        name: r,
        args: o,
        argMappers: [],
        type: br(e.type, this.builder.configStore, (u2 = e.nullable) !== null && u2 !== void 0 ? u2 : this.builder.defaultFieldNullability),
        pothosOptions: e,
        extensions: {
          pothosOriginalResolve: a,
          pothosOriginalSubscribe: l,
          ...e.extensions
        },
        description: e.description,
        deprecationReason: e.deprecationReason,
        resolve: a,
        subscribe: l
      };
    });
  }
  exposeField(e, { extensions: n, ...r }) {
    return this.createField({
      ...r,
      extensions: {
        pothosExposedField: e,
        ...n
      },
      resolve: (i) => i[e]
    });
  }
  constructor(e, n, r) {
    sr(this, "kind", void 0), sr(this, "graphqlKind", void 0), sr(this, "builder", void 0), this.builder = e, this.kind = n, this.graphqlKind = r;
  }
};
function Du(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Gn = class extends xu {
  /**
  * Create a Boolean field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  boolean(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: "Boolean"
    });
  }
  /**
  * Create a Float field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  float(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: "Float"
    });
  }
  /**
  * Create a ID field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  id(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: "ID"
    });
  }
  /**
  * Create a Int field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  int(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: "Int"
    });
  }
  /**
  * Create a String field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  string(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: "String"
    });
  }
  /**
  * Create a Boolean list field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  booleanList(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: [
        "Boolean"
      ]
    });
  }
  /**
  * Create a Float list field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  floatList(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: [
        "Float"
      ]
    });
  }
  /**
  * Create a ID list field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  idList(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: [
        "ID"
      ]
    });
  }
  /**
  * Create a Int list field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  intList(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: [
        "Int"
      ]
    });
  }
  /**
  * Create a String list field
  * @param {PothosSchemaTypes.FieldOptions} options - Options for this field
  */
  stringList(...e) {
    const [n = {}] = e;
    return this.createField({
      resolve: void 0,
      ...n,
      type: [
        "String"
      ]
    });
  }
  /**
  * create a new field for the current type
  * @param {PothosSchemaTypes.FieldOptions} options - options for this field
  */
  field(e) {
    return this.createField(e);
  }
  listRef(e, n) {
    var r;
    return new As(e, (r = n?.nullable) !== null && r !== void 0 ? r : false);
  }
  constructor(...e) {
    super(...e), Du(this, "arg", new vr(this.builder, "Arg").argBuilder());
  }
};
var Ls = class extends Gn {
  /**
  * Create a Boolean field from a boolean property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeBoolean(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: "Boolean"
    });
  }
  /**
  * Create a Float field from a numeric property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeFloat(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: "Float"
    });
  }
  /**
  * Create an ID field from a property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeID(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: "ID"
    });
  }
  /**
  * Create an Int field from a numeric property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeInt(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: "Int"
    });
  }
  /**
  * Create a String field from a string property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeString(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: "String"
    });
  }
  /**
  * Create a Boolean list field from a boolean[] property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeBooleanList(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: [
        "Boolean"
      ]
    });
  }
  /**
  * Create a Float list field from a number[] property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeFloatList(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: [
        "Float"
      ]
    });
  }
  /**
  * Create an ID list field from an id[] property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeIDList(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: [
        "ID"
      ]
    });
  }
  /**
  * Create a Int list field from a number[] property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeIntList(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: [
        "Int"
      ]
    });
  }
  /**
  * Create a String list field from a string[] property on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  exposeStringList(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, {
      ...r,
      type: [
        "String"
      ]
    });
  }
  /**
  * Create a field that resolves to a property of the corresponding type on the parent object
  * @param {string} name - the name of the property on the source object (does not need to match the field name).
  * @param {object} [options={}] - Options for this field
  */
  expose(e, ...n) {
    const [r = {}] = n;
    return this.exposeField(e, r);
  }
};
var un = class extends Ls {
  constructor(e) {
    super(e, "Interface", "Interface");
  }
};
var cn = class extends Gn {
  constructor(e) {
    super(e, "Mutation", "Object");
  }
};
var fn = class extends Ls {
  constructor(e) {
    super(e, "Object", "Object");
  }
};
var dn = class extends Gn {
  constructor(e) {
    super(e, "Query", "Object");
  }
};
var pn = class extends Gn {
  constructor(e) {
    super(e, "Subscription", "Object");
  }
};
function Ct(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Lu = Nt;
var $u = Bn;
var Pu = class extends Z {
  constructor(e, n) {
    super("Enum", e, n), Ct(this, "kind", "Enum"), Ct(this, "$inferType", void 0), Ct(this, "$inferInput", void 0), Ct(this, Lu, void 0), Ct(this, $u, void 0);
  }
};
function xt(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var ju = Nt;
var Mu = nn;
var Uu = class extends Z {
  addTypes(e) {
    Array.isArray(e) && e.length === 0 || (this.preparedForBuild ? this.updateConfig((n) => ({
      ...n,
      types: [
        ...n.types,
        ...typeof e == "function" ? e() : e
      ]
    })) : this.types.push(() => Array.isArray(e) ? e : e()));
  }
  prepareForBuild() {
    this.preparedForBuild || (super.prepareForBuild(), this.types.length > 0 && this.updateConfig((e) => ({
      ...e,
      types: [
        ...e.types,
        ...this.types.flatMap((n) => n())
      ]
    })));
  }
  constructor(e, n) {
    super("Union", e, n), xt(this, "kind", "Union"), xt(this, "$inferType", void 0), xt(this, ju, void 0), xt(this, Mu, void 0), xt(this, "types", []);
  }
};
function Te(t, e, n) {
  return e in t ? Object.defineProperty(t, e, {
    value: n,
    enumerable: true,
    configurable: true,
    writable: true
  }) : t[e] = n, t;
}
var Qn2 = class gt {
  static registerPlugin(e, n, r) {
    if (!gt.allowPluginReRegistration && gt.plugins[e])
      throw new qn2(`Received multiple implementations for plugin ${e}`);
    gt.plugins[e] = n, r && gt.optionNormalizers.set(e, r);
  }
  objectType(e, n, r) {
    Ae(e), fi(n.interfaces);
    var i;
    const s = typeof e == "string" ? e : (i = n.name) !== null && i !== void 0 ? i : e.name, o = e instanceof Z ? e : new ot(s);
    return o.updateConfig({
      kind: "Object",
      graphqlKind: "Object",
      name: s,
      interfaces: [],
      description: n.description,
      extensions: n.extensions,
      isTypeOf: n.isTypeOf,
      pothosOptions: n
    }), n.interfaces && o.addInterfaces(n.interfaces), o !== e && typeof e != "string" && this.configStore.associateParamWithRef(e, o), r && o.addFields(() => r(new fn(this))), n.fields && o.addFields(() => {
      const a = new fn(this);
      return n.fields(a);
    }), this.configStore.addTypeRef(o), o;
  }
  objectFields(e, n) {
    Ae(e), this.configStore.addFields(e, () => n(new fn(this)));
  }
  objectField(e, n, r) {
    Ae(e), this.configStore.addFields(e, () => ({
      [n]: r(new fn(this))
    }));
  }
  queryType(...e) {
    const [n = {}, r] = e;
    var i;
    return this.queryRef.updateConfig({
      kind: "Query",
      graphqlKind: "Object",
      name: (i = n.name) !== null && i !== void 0 ? i : "Query",
      description: n.description,
      pothosOptions: n,
      extensions: n.extensions
    }), n.name && (this.queryRef.name = n.name), this.configStore.addTypeRef(this.queryRef), r && this.queryRef.addFields(() => r(new dn(this))), n.fields && this.queryRef.addFields(() => n.fields(new dn(this))), this.queryRef;
  }
  queryFields(e) {
    this.configStore.addFields(this.queryRef, () => e(new dn(this)));
  }
  queryField(e, n) {
    this.configStore.addFields(this.queryRef, () => ({
      [e]: n(new dn(this))
    }));
  }
  mutationType(...e) {
    const [n = {}, r] = e;
    var i;
    return this.mutationRef.updateConfig({
      kind: "Mutation",
      graphqlKind: "Object",
      name: (i = n.name) !== null && i !== void 0 ? i : "Mutation",
      description: n.description,
      pothosOptions: n,
      extensions: n.extensions
    }), this.configStore.addTypeRef(this.mutationRef), n.name && (this.mutationRef.name = n.name), r && this.configStore.addFields(this.mutationRef, () => r(new cn(this))), n.fields && this.configStore.addFields(this.mutationRef, () => n.fields(new cn(this))), this.mutationRef;
  }
  mutationFields(e) {
    this.configStore.addFields(this.mutationRef, () => e(new cn(this)));
  }
  mutationField(e, n) {
    this.configStore.addFields(this.mutationRef, () => ({
      [e]: n(new cn(this))
    }));
  }
  subscriptionType(...e) {
    const [n = {}, r] = e;
    var i;
    return this.subscriptionRef.updateConfig({
      kind: "Subscription",
      graphqlKind: "Object",
      name: (i = n.name) !== null && i !== void 0 ? i : "Subscription",
      description: n.description,
      pothosOptions: n,
      extensions: n.extensions
    }), this.configStore.addTypeRef(this.subscriptionRef), n.name && (this.subscriptionRef.name = n.name), r && this.configStore.addFields(this.subscriptionRef, () => r(new pn(this))), n.fields && this.configStore.addFields(this.subscriptionRef, () => n.fields(new pn(this))), this.subscriptionRef;
  }
  subscriptionFields(e) {
    this.configStore.addFields(this.subscriptionRef, () => e(new pn(this)));
  }
  subscriptionField(e, n) {
    this.configStore.addFields(this.subscriptionRef, () => ({
      [e]: n(new pn(this))
    }));
  }
  args(e) {
    return e(new vr(this, "Arg"));
  }
  interfaceType(e, n, r) {
    Ae(e), fi(n.interfaces);
    var i;
    const s = typeof e == "string" ? e : (i = n.name) !== null && i !== void 0 ? i : e.name, o = e instanceof Z ? e : new Cn(s), a = o.name;
    return o.updateConfig({
      kind: "Interface",
      graphqlKind: "Interface",
      name: a,
      interfaces: [],
      description: n.description,
      pothosOptions: n,
      extensions: n.extensions,
      resolveType: n.resolveType
    }), this.configStore.addTypeRef(o), n.interfaces && o.addInterfaces(n.interfaces), o !== e && typeof e != "string" && this.configStore.associateParamWithRef(e, o), r && this.configStore.addFields(o, () => r(new un(this))), n.fields && this.configStore.addFields(o, () => n.fields(new un(this))), o;
  }
  interfaceFields(e, n) {
    Ae(e), this.configStore.addFields(e, () => n(new un(this)));
  }
  interfaceField(e, n, r) {
    Ae(e), this.configStore.addFields(e, () => ({
      [n]: r(new un(this))
    }));
  }
  unionType(e, n) {
    const r = new Uu(e, {
      kind: "Union",
      graphqlKind: "Union",
      name: e,
      types: [],
      description: n.description,
      resolveType: n.resolveType,
      pothosOptions: n,
      extensions: n.extensions
    });
    if (Array.isArray(n.types))
      for (const i of n.types)
        Ae(i);
    return this.configStore.addTypeRef(r), r.addTypes(n.types), r;
  }
  enumType(e, n) {
    Ae(e);
    const r = typeof e == "string" ? e : n.name, i = typeof e == "object" ? du2(e, n?.values) : fu2(n.values), s = new Pu(r, {
      kind: "Enum",
      graphqlKind: "Enum",
      name: r,
      values: i,
      description: n.description,
      pothosOptions: n,
      extensions: n.extensions
    });
    return this.configStore.addTypeRef(s), typeof e != "string" && this.configStore.associateParamWithRef(e, s), s;
  }
  scalarType(e, n) {
    const r = new Rs2(e, {
      kind: "Scalar",
      graphqlKind: "Scalar",
      name: e,
      description: n.description,
      parseLiteral: n.parseLiteral,
      parseValue: n.parseValue,
      serialize: n.serialize,
      pothosOptions: n,
      extensions: n.extensions
    });
    return this.configStore.addTypeRef(r), r;
  }
  addScalarType(e, n, ...r) {
    const [i = {}] = r, s = n.toConfig();
    return this.scalarType(e, {
      ...s,
      ...i,
      extensions: {
        ...s.extensions,
        ...i.extensions
      }
    });
  }
  inputType(e, n) {
    Ae(e);
    const r = typeof e == "string" ? e : e.name, i = typeof e == "string" ? new Fn(r) : e;
    return i.updateConfig({
      kind: "InputObject",
      graphqlKind: "InputObject",
      name: r,
      isOneOf: n.isOneOf,
      description: n.description,
      pothosOptions: n,
      extensions: n.extensions
    }), this.configStore.addTypeRef(i), e !== i && typeof e != "string" && this.configStore.associateParamWithRef(e, i), this.configStore.addInputFields(i, () => n.fields(new vr(this, "InputObject"))), i;
  }
  inputRef(e) {
    return new bu2(this, e);
  }
  objectRef(e) {
    return new Iu(this, e);
  }
  interfaceRef(e) {
    return new Eu(this, e);
  }
  toSchema(...e) {
    const [n = {}] = e, { directives: r, extensions: i } = n, s = [
      Mn,
      jn,
      Ir,
      V,
      X
    ];
    for (const h of s)
      this.configStore.hasImplementation(h.name) || this.addScalarType(h.name, h);
    const o = new mu2(this, n);
    o.plugin.beforeBuild(), o.buildAll();
    const a = [
      ...o.types.values()
    ], l = this.configStore.hasConfig(this.queryRef) ? this.configStore.getTypeConfig(this.queryRef).name : "Query", u2 = this.configStore.hasConfig(this.mutationRef) ? this.configStore.getTypeConfig(this.mutationRef).name : "Mutation", c = this.configStore.hasConfig(this.subscriptionRef) ? this.configStore.getTypeConfig(this.subscriptionRef).name : "Subscription", f2 = new Rr({
      query: o.types.get(l),
      mutation: o.types.get(u2),
      subscription: o.types.get(c),
      extensions: i ?? {},
      directives: r,
      types: a
    }), d = o.plugin.afterBuild(f2);
    return n.sortSchema === false ? d : _l(d);
  }
  constructor(e) {
    Te(this, "$inferSchemaTypes", void 0), Te(this, "queryRef", new xs("Query")), Te(this, "mutationRef", new Cs("Mutation")), Te(this, "subscriptionRef", new Ds("Subscription")), Te(this, "configStore", void 0), Te(this, "options", void 0), Te(this, "defaultFieldNullability", void 0), Te(this, "defaultInputFieldRequiredness", void 0), this.options = [
      ...gt.optionNormalizers.values()
    ].reduce((i, s) => e.defaults && typeof s[e.defaults] == "function" ? Object.assign(i, s[e.defaults](i)) : i, e), this.configStore = new _u(this);
    var n;
    this.defaultFieldNullability = (n = e.defaultFieldNullability) !== null && n !== void 0 ? n : e.defaults !== "v3";
    var r;
    this.defaultInputFieldRequiredness = (r = e.defaultInputFieldRequiredness) !== null && r !== void 0 ? r : false;
  }
};
Te(Qn2, "plugins", {});
Te(Qn2, "optionNormalizers", /* @__PURE__ */ new Map());
Te(Qn2, "allowPluginReRegistration", false);
var Vu = Qn2;
var qe2 = {
  red: "\x1B[31m",
  yellow: "\x1B[33m",
  magenta: "\x1B[35m",
  cyan: "\x1B[36m",
  reset: "\x1B[0m"
};
var di = qe2.yellow + "WARN" + qe2.reset;
var pi = qe2.cyan + "INFO" + qe2.reset;
var hi = qe2.red + "ERR" + qe2.reset;
var gi = qe2.magenta + "DEBUG" + qe2.reset;
var Dt = {
  debug: 0,
  info: 1,
  warn: 2,
  error: 3,
  silent: 4
};
var hn = () => {
};
var Hn = (t) => (...e) => console.log(t, ...e);
var qu = console.debug ? (...t) => console.debug(gi, ...t) : Hn(gi);
var Bu = console.info ? (...t) => console.info(pi, ...t) : Hn(pi);
var Gu = console.warn ? (...t) => console.warn(di, ...t) : Hn(di);
var Qu = console.error ? (...t) => console.error(hi, ...t) : Hn(hi);
var or = (t = globalThis.process?.env.DEBUG === "1" ? "debug" : "info") => {
  const e = Dt[t];
  return {
    debug: e > Dt.debug ? hn : qu,
    info: e > Dt.info ? hn : Bu,
    warn: e > Dt.warn ? hn : Gu,
    error: e > Dt.error ? hn : Qu
  };
};
var Kn2 = Symbol.for("@whatwg-node/promise-helpers/FakePromise");
function B(t) {
  return t?.then != null;
}
function Hu(t) {
  const e = t;
  return e && e.then && e.catch && e.finally;
}
function D(t, e, n, r) {
  let i = Q().then(t).then(e, n);
  return Lr(i);
}
function Q(t) {
  return t && Hu(t) ? t : B(t) ? {
    then: (e, n) => Q(t.then(e, n)),
    catch: (e) => Q(t.then((n) => n, e)),
    finally: (e) => Q(e ? zu(t, e) : t),
    [Symbol.toStringTag]: "Promise"
  } : {
    then(e) {
      if (e)
        try {
          return Q(e(t));
        } catch (n) {
          return wt(n);
        }
      return this;
    },
    catch() {
      return this;
    },
    finally(e) {
      if (e)
        try {
          return Q(e()).then(() => t, () => t);
        } catch (n) {
          return wt(n);
        }
      return this;
    },
    [Symbol.toStringTag]: "Promise",
    __fakePromiseValue: t,
    [Kn2]: "resolved"
  };
}
function $s() {
  if (Promise.withResolvers)
    return Promise.withResolvers();
  let t, e;
  return {
    promise: new Promise(function(i, s) {
      t = i, e = s;
    }),
    get resolve() {
      return t;
    },
    get reject() {
      return e;
    }
  };
}
function nt(t, e, n) {
  if (t?.length === 0)
    return;
  const r = t[Symbol.iterator]();
  let i = 0;
  function s() {
    const { done: o, value: a } = r.next();
    if (o)
      return;
    let l = false;
    function u2() {
      l = true;
    }
    return D(function() {
      return e(a, u2, i++);
    }, function(f2) {
      if (f2 && n?.push(f2), !l)
        return s();
    });
  }
  return s();
}
function wt(t) {
  return {
    then(e, n) {
      if (n)
        try {
          return Q(n(t));
        } catch (r) {
          return wt(r);
        }
      return this;
    },
    catch(e) {
      if (e)
        try {
          return Q(e(t));
        } catch (n) {
          return wt(n);
        }
      return this;
    },
    finally(e) {
      if (e)
        try {
          e();
        } catch (n) {
          return wt(n);
        }
      return this;
    },
    __fakeRejectError: t,
    [Symbol.toStringTag]: "Promise",
    [Kn2]: "rejected"
  };
}
function Ps2(t, e, n, r) {
  Symbol.asyncIterator in t && (t = t[Symbol.asyncIterator]());
  let i, s, o;
  typeof t.return == "function" && (i = t.return, s = (u2) => {
    const c = () => {
      throw u2;
    };
    return i.call(t).then(c, c);
  });
  function a(u2) {
    return u2.done ? u2 : D(() => u2.value, (c) => D(() => e(c), mi, s));
  }
  let l;
  if (n) {
    let u2;
    const c = n;
    l = (f2) => (u2 ||= D(() => f2, (d) => D(() => c(d), mi, s)), u2);
  }
  return {
    next() {
      return t.next().then(a, l);
    },
    return() {
      const u2 = i ? i.call(t).then(a, l) : Q({ value: void 0, done: true });
      return o ? u2.then(o) : u2;
    },
    throw(u2) {
      return typeof t.throw == "function" ? t.throw(u2).then(a, l) : s ? s(u2) : wt(u2);
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
function mi(t) {
  return { value: t, done: false };
}
function Ku(t) {
  return t?.[Kn2] === "resolved";
}
function Wu(t) {
  return t?.[Kn2] === "rejected";
}
function zu(t, e) {
  return "finally" in t ? t.finally(e) : t.then((n) => {
    const r = e();
    return B(r) ? r.then(() => n) : n;
  }, (n) => {
    const r = e();
    if (B(r))
      return r.then(() => {
        throw n;
      });
    throw n;
  });
}
function Lr(t) {
  if (Ku(t))
    return t.__fakePromiseValue;
  if (Wu(t))
    throw t.__fakeRejectError;
  return t;
}
var Ju = '<!doctype html><html lang=en><head><meta charset=utf-8><title>__TITLE__</title><link rel=icon href=https://raw.githubusercontent.com/graphql-hive/graphql-yoga/main/website/public/favicon.ico><link crossorigin rel=stylesheet href=https://unpkg.com/@graphql-yoga/graphiql@4.3.5/dist/graphiql.css></head><body id=body class=no-focus-outline><noscript>You need to enable JavaScript to run this app.</noscript><div id=root></div><script type=module>import{renderYogaGraphiQL}from"https://unpkg.com/@graphql-yoga/graphiql@4.3.5/dist/yoga-graphiql.es.js";renderYogaGraphiQL(root,__OPTS__)<\/script></body></html>';
function Yu({ headers: t, method: e }) {
  return e === "GET" && !!t?.get("accept")?.includes("text/html");
}
var Xu = (t) => Ju.replace("__TITLE__", t?.title || "Yoga GraphiQL").replace("__OPTS__", JSON.stringify(t ?? {}));
function Zu(t) {
  const e = t.logger ?? console;
  let n;
  typeof t?.options == "function" ? n = t?.options : typeof t?.options == "object" ? n = () => t?.options : t?.options === false ? n = () => false : n = () => ({});
  const r = t?.render ?? Xu;
  let i;
  const s = ({ URLPattern: o }) => (i ||= new o({
    pathname: t.graphqlEndpoint
  }), i);
  return {
    onRequest({ request: o, serverContext: a, fetchAPI: l, endResponse: u2, url: c }) {
      if (Yu(o) && (o.url.endsWith(t.graphqlEndpoint) || o.url.endsWith(`${t.graphqlEndpoint}/`) || c.pathname === t.graphqlEndpoint || c.pathname === `${t.graphqlEndpoint}/` || s(l).test(c)))
        return e.debug("Rendering GraphiQL"), D(() => n(o, a), (f2) => {
          if (f2)
            return D(() => r({
              ...f2 === true ? {} : f2
            }), (d) => {
              const h = new l.Response(d, {
                headers: {
                  "Content-Type": "text/html"
                },
                status: 200
              });
              u2(h);
            });
        });
    }
  };
}
var ec = (t) => {
  if (t == null)
    return {};
  if (hs(t))
    return {
      onPluginInit({ setSchema: n }) {
        n(t);
      }
    };
  if ("then" in t) {
    let n;
    return {
      onRequestParse() {
        return {
          onRequestParseDone() {
            if (!n)
              return D(() => t, (r) => {
                n = r;
              });
          }
        };
      },
      onEnveloped({ setSchema: r }) {
        if (!n)
          throw new Error("You provide a promise of a schema but it hasn't been resolved yet. Make sure you use this plugin with GraphQL Yoga.");
        r(n);
      }
    };
  }
  const e = /* @__PURE__ */ new WeakMap();
  return {
    onRequestParse({ request: n, serverContext: r }) {
      return {
        onRequestParseDone() {
          return D(() => t({
            ...r,
            request: n
          }), (i) => {
            e.set(n, i);
          });
        }
      };
    },
    onEnveloped({ setSchema: n, context: r }) {
      if (r?.request == null)
        throw new Error("Request object is not available in the context. Make sure you use this plugin with GraphQL Yoga.");
      const i = e.get(r.request);
      if (i == null)
        throw new Error("No schema found for this request. Make sure you use this plugin with GraphQL Yoga.");
      n(i);
    }
  };
};
var js = 3;
function ce(t) {
  return Wn(t, []);
}
function Wn(t, e) {
  switch (typeof t) {
    case "string":
      return JSON.stringify(t);
    case "function":
      return t.name ? `[function ${t.name}]` : "[function]";
    case "object":
      return tc(t, e);
    default:
      return String(t);
  }
}
function yi(t) {
  return (t.name = "GraphQLError") ? t.toString() : `${t.name}: ${t.message};
 ${t.stack}`;
}
function tc(t, e) {
  if (t === null)
    return "null";
  if (t instanceof Error)
    return t.name === "AggregateError" ? yi(t) + `
` + bi2(t.errors, e) : yi(t);
  if (e.includes(t))
    return "[Circular]";
  const n = [...e, t];
  if (nc2(t)) {
    const r = t.toJSON();
    if (r !== t)
      return typeof r == "string" ? r : Wn(r, n);
  } else if (Array.isArray(t))
    return bi2(t, n);
  return rc(t, n);
}
function nc2(t) {
  return typeof t.toJSON == "function";
}
function rc(t, e) {
  const n = Object.entries(t);
  return n.length === 0 ? "{}" : e.length > js ? "[" + ic2(t) + "]" : "{ " + n.map(([i, s]) => i + ": " + Wn(s, e)).join(", ") + " }";
}
function bi2(t, e) {
  if (t.length === 0)
    return "[]";
  if (e.length > js)
    return "[Array]";
  const n = t.length, r = [];
  for (let i = 0; i < n; ++i)
    r.push(Wn(t[i], e));
  return "[" + r.join(", ") + "]";
}
function ic2(t) {
  const e = Object.prototype.toString.call(t).replace(/^\[object /, "").replace(/]$/, "");
  if (e === "Object" && typeof t.constructor == "function") {
    const n = t.constructor.name;
    if (typeof n == "string" && n !== "")
      return n;
  }
  return e;
}
var sc2 = [
  "message",
  "locations",
  "path",
  "nodes",
  "source",
  "positions",
  "originalError",
  "name",
  "stack",
  "extensions"
];
function oc2(t) {
  return t != null && typeof t == "object" && Object.keys(t).every((e) => sc2.includes(e));
}
function F(t, e) {
  return e?.originalError && !(e.originalError instanceof Error) && oc2(e.originalError) && (e.originalError = F(e.originalError.message, e.originalError)), ns.major >= 17 ? new qe(t, e) : new qe(t, e?.nodes, e?.source, e?.positions, e?.path, e?.originalError, e?.extensions);
}
function ac2(t) {
  return t != null && typeof t == "object" && Symbol.iterator in t;
}
function Ms2(t) {
  return typeof t == "object" && t !== null;
}
function lc2(t, e, n) {
  let r = n;
  for (const i of t)
    r = D(() => r, (s) => e(s, i));
  return r;
}
function Us(t, e) {
  return Object.prototype.hasOwnProperty.call(t, e);
}
function Vs3(t, e, n = {}) {
  const r = {}, s = (e.arguments ?? []).reduce((o, a) => ({
    ...o,
    [a.name.value]: a
  }), {});
  for (const { name: o, type: a, defaultValue: l } of t.args) {
    const u2 = s[o];
    if (!u2) {
      if (l !== void 0)
        r[o] = l;
      else if (Nn(a))
        throw F(`Argument "${o}" of required type "${ce(a)}" was not provided.`, {
          nodes: [e]
        });
      continue;
    }
    const c = u2.value;
    let f2 = c.kind === ye.NULL;
    if (c.kind === ye.VARIABLE) {
      const h = c.name.value;
      if (n == null || !Us(n, h)) {
        if (l !== void 0)
          r[o] = l;
        else if (Nn(a))
          throw F(`Argument "${o}" of required type "${ce(a)}" was provided the variable "$${h}" which was not provided a runtime value.`, {
            nodes: [c]
          });
        continue;
      }
      f2 = n[h] == null;
    }
    if (f2 && Nn(a))
      throw F(`Argument "${o}" of non-null type "${ce(a)}" must not be null.`, {
        nodes: [c]
      });
    const d = tt(c, a, n);
    if (d === void 0)
      throw F(`Argument "${o}" has invalid value ${gr(c)}.`, {
        nodes: [c]
      });
    r[o] = d;
  }
  return r;
}
function qs(t) {
  const e = /* @__PURE__ */ new WeakMap();
  return function(r) {
    const i = e.get(r);
    if (i === void 0) {
      const s = t(r);
      return e.set(r, s), s;
    }
    return i;
  };
}
function uc2(t) {
  const e = /* @__PURE__ */ new WeakMap();
  return function(r, i, s) {
    let o = e.get(r);
    if (!o) {
      o = /* @__PURE__ */ new WeakMap(), e.set(r, o);
      const u2 = /* @__PURE__ */ new WeakMap();
      o.set(i, u2);
      const c = t(r, i, s);
      return u2.set(s, c), c;
    }
    let a = o.get(i);
    if (!a) {
      a = /* @__PURE__ */ new WeakMap(), o.set(i, a);
      const u2 = t(r, i, s);
      return a.set(s, u2), u2;
    }
    const l = a.get(s);
    if (l === void 0) {
      const u2 = t(r, i, s);
      return a.set(s, u2), u2;
    }
    return l;
  };
}
function cc2(t) {
  const e = /* @__PURE__ */ new WeakMap();
  return function(r, i, s, o, a) {
    let l = e.get(r);
    if (!l) {
      l = /* @__PURE__ */ new WeakMap(), e.set(r, l);
      const h = /* @__PURE__ */ new WeakMap();
      l.set(i, h);
      const g2 = /* @__PURE__ */ new WeakMap();
      h.set(s, g2);
      const w = /* @__PURE__ */ new WeakMap();
      g2.set(o, w);
      const k = t(r, i, s, o, a);
      return w.set(a, k), k;
    }
    let u2 = l.get(i);
    if (!u2) {
      u2 = /* @__PURE__ */ new WeakMap(), l.set(i, u2);
      const h = /* @__PURE__ */ new WeakMap();
      u2.set(s, h);
      const g2 = /* @__PURE__ */ new WeakMap();
      h.set(o, g2);
      const w = t(r, i, s, o, a);
      return g2.set(a, w), w;
    }
    let c = u2.get(s);
    if (!c) {
      c = /* @__PURE__ */ new WeakMap(), u2.set(s, c);
      const h = /* @__PURE__ */ new WeakMap();
      c.set(o, h);
      const g2 = t(r, i, s, o, a);
      return h.set(a, g2), g2;
    }
    let f2 = c.get(o);
    if (!f2) {
      f2 = /* @__PURE__ */ new WeakMap(), c.set(o, f2);
      const h = t(r, i, s, o, a);
      return f2.set(a, h), h;
    }
    const d = f2.get(a);
    if (d === void 0) {
      const h = t(r, i, s, o, a);
      return f2.set(a, h), h;
    }
    return d;
  };
}
function fc2(t, e, n) {
  const i = dc2(t).get(e);
  if (i == null)
    throw F(`Schema is not configured to execute ${e} operation.`, {
      nodes: n
    });
  return i;
}
var dc2 = qs(function(e) {
  const n = /* @__PURE__ */ new Map(), r = e.getQueryType();
  r && n.set("query", r);
  const i = e.getMutationType();
  i && n.set("mutation", i);
  const s = e.getSubscriptionType();
  return s && n.set("subscription", s), n;
});
var xn = class extends Map {
  get [Symbol.toStringTag]() {
    return "AccumulatorMap";
  }
  add(e, n) {
    const r = this.get(e);
    r === void 0 ? this.set(e, [n]) : r.push(n);
  }
};
var pc2 = new Le({
  name: "defer",
  description: "Directs the executor to defer this fragment when the `if` argument is true or undefined.",
  locations: [A.FRAGMENT_SPREAD, A.INLINE_FRAGMENT],
  args: {
    if: {
      type: new pu(X),
      description: "Deferred when true or undefined.",
      defaultValue: true
    },
    label: {
      type: V,
      description: "Unique name"
    }
  }
});
var hc2 = new Le({
  name: "stream",
  description: "Directs the executor to stream plural fields when the `if` argument is true or undefined.",
  locations: [A.FIELD],
  args: {
    if: {
      type: new pu(X),
      description: "Stream when true or undefined.",
      defaultValue: true
    },
    label: {
      type: V,
      description: "Unique name"
    },
    initialCount: {
      defaultValue: 0,
      type: jn,
      description: "Number of items to return immediately"
    }
  }
});
function bt(t, e, n, r, i, s, o, a) {
  for (const l of i.selections)
    switch (l.kind) {
      case ye.FIELD: {
        if (!ar2(n, l))
          continue;
        s.add(gc(l), l);
        break;
      }
      case ye.INLINE_FRAGMENT: {
        if (!ar2(n, l) || !vi(t, l, r))
          continue;
        const u2 = Ti(n, l);
        if (u2) {
          const c = new xn();
          bt(t, e, n, r, l.selectionSet, c, o, a), o.push({
            label: u2.label,
            fields: c
          });
        } else
          bt(t, e, n, r, l.selectionSet, s, o, a);
        break;
      }
      case ye.FRAGMENT_SPREAD: {
        const u2 = l.name.value;
        if (!ar2(n, l))
          continue;
        const c = Ti(n, l);
        if (a.has(u2) && !c)
          continue;
        const f2 = e[u2];
        if (!f2 || !vi(t, f2, r))
          continue;
        if (c || a.add(u2), c) {
          const d = new xn();
          bt(t, e, n, r, f2.selectionSet, d, o, a), o.push({
            label: c.label,
            fields: d
          });
        } else
          bt(t, e, n, r, f2.selectionSet, s, o, a);
        break;
      }
    }
}
function Bs2(t, e, n, r, i) {
  const s = new xn(), o = [];
  return bt(t, e, n, r, i, s, o, /* @__PURE__ */ new Set()), { fields: s, patches: o };
}
function ar2(t, e) {
  return !(Ot(_r, e, t)?.if === true || Ot(Or, e, t)?.if === false);
}
function vi(t, e, n) {
  const r = e.typeCondition;
  if (!r)
    return true;
  const i = fe(t, r);
  return i === n ? true : oc(i) ? t.getPossibleTypes(i).includes(n) : false;
}
function gc(t) {
  return t.alias ? t.alias.value : t.name.value;
}
function Ti(t, e) {
  const n = Ot(pc2, e, t);
  if (n && n.if !== false)
    return {
      label: typeof n.label == "string" ? n.label : void 0
    };
}
var mc = cc2(function(e, n, r, i, s) {
  const o = new xn(), a = /* @__PURE__ */ new Set(), l = [], u2 = {
    fields: o,
    patches: l
  };
  for (const c of s)
    c.selectionSet && bt(e, n, r, i, c.selectionSet, o, l, a);
  return u2;
});
function zn(t) {
  return t?.[Symbol.asyncIterator] != null;
}
function Rt(t, e, n) {
  return { prev: t, key: e, typename: n };
}
function ee(t) {
  const e = [];
  let n = t;
  for (; n; )
    e.push(n.key), n = n.prev;
  return e.reverse();
}
function yc(t) {
  return t.map((e) => typeof e == "number" ? "[" + e.toString() + "]" : "." + e).join("");
}
var bc = class extends Error {
  error;
  suppressed;
  // eslint-disable-next-line n/handle-callback-err
  constructor(e, n, r) {
    super(r), this.error = e, this.suppressed = n, this.name = "SuppressedError", Error.captureStackTrace(this, this.constructor);
  }
};
var ie = {
  get dispose() {
    return Symbol.dispose || Symbol.for("dispose");
  },
  get asyncDispose() {
    return Symbol.asyncDispose || Symbol.for("asyncDispose");
  }
};
function vc(t) {
  return t?.[ie.dispose] != null;
}
function Tc(t) {
  return t?.[ie.asyncDispose] != null;
}
var Ec = globalThis.SuppressedError || bc;
var $r = class _$r {
  callbacks = [];
  get disposed() {
    return this.callbacks.length === 0;
  }
  use(e) {
    return Tc(e) ? this.callbacks.push(() => e[ie.asyncDispose]()) : vc(e) && this.callbacks.push(() => e[ie.dispose]()), e;
  }
  adopt(e, n) {
    return n && this.callbacks.push(() => n(e)), e;
  }
  defer(e) {
    e && this.callbacks.push(e);
  }
  move() {
    const e = new _$r();
    return e.callbacks = this.callbacks, this.callbacks = [], e;
  }
  disposeAsync() {
    return this[ie.asyncDispose]();
  }
  _error;
  _iterateCallbacks() {
    const e = this.callbacks.pop();
    if (e)
      return D(e, () => this._iterateCallbacks(), (n) => (this._error = this._error ? new Ec(n, this._error) : n, this._iterateCallbacks()));
  }
  [ie.asyncDispose]() {
    const e = this._iterateCallbacks();
    if (e?.then)
      return e.then(() => {
        if (this._error) {
          const n = this._error;
          throw this._error = void 0, n;
        }
      });
    if (this._error) {
      const n = this._error;
      throw this._error = void 0, n;
    }
  }
  [Symbol.toStringTag] = "AsyncDisposableStack";
};
var wc = globalThis.AsyncDisposableStack || $r;
function Re(t) {
  if (t instanceof Error)
    return t;
  if (typeof t == "object" && t != null && "message" in t && typeof t.message == "string") {
    let e;
    "cause" in t && (e = { cause: t.cause });
    const n = new Error(t.message, e);
    return "stack" in t && typeof t.stack == "string" && (n.stack = t.stack), "name" in t && typeof t.name == "string" && (n.name = t.name), n;
  }
  return new Error(String(t));
}
function Sc(t) {
  const e = t[Symbol.asyncIterator]();
  let n, r, i = false;
  async function s() {
    if (i)
      return { value: void 0, done: true };
    try {
      if (!n) {
        if (r)
          return await r, await s();
        let l;
        r = new Promise((c) => {
          l = c;
        });
        const u2 = await e.next();
        return u2.done ? (i = true, await s()) : (n = u2.value[Symbol.asyncIterator](), r = void 0, l(), await s());
      }
      const o = n, a = await n.next();
      return a.done ? (n === o && (n = void 0), await s()) : a;
    } catch (o) {
      throw i = true, o;
    }
  }
  return {
    next: s,
    async return() {
      return i = true, await Promise.all([n?.return?.(), e.return?.()]), { value: void 0, done: true };
    },
    async throw(o) {
      throw i = true, await Promise.all([n?.throw?.(o), e.throw?.(o)]), o;
    },
    [Symbol.asyncIterator]() {
      return this;
    },
    async [ie.asyncDispose]() {
      i = true, await Promise.all([
        n?.[ie.asyncDispose]?.(),
        e?.[ie.asyncDispose]?.()
      ]);
    }
  };
}
function Ei(t, e) {
  if (!t)
    throw new Error(e ?? "Unexpected invariant triggered.");
}
function wi(t, e, n) {
  e?.throwIfAborted();
  const r = /* @__PURE__ */ Object.create(null), i = [];
  for (const o in t) {
    const a = D(() => t[o], (l) => {
      r[o] = l;
    });
    B(a) && i.push(a);
  }
  if (!i.length)
    return r;
  const s = i.length === 1 ? i[0] : Promise.all(i);
  return n ? Promise.race([n, s]).then(() => r) : s.then(() => r);
}
function Ic(t, e, n, r) {
  const i = [], s = r?.maxErrors;
  try {
    const o = Oc(t, e, n, (a) => {
      if (s != null && i.length >= s)
        throw F("Too many errors processing variables, error limit reached. Execution aborted.");
      i.push(a);
    });
    if (i.length === 0)
      return { coerced: o };
  } catch (o) {
    i.push(o);
  }
  return { errors: i };
}
function Oc(t, e, n, r) {
  const i = {};
  for (const s of e) {
    const o = s.variable.name.value, a = fe(t, s.type);
    if (!uu(a)) {
      const u2 = gr(s.type);
      r(F(`Variable "$${o}" expected value of type "${u2}" which cannot be used as an input type.`, { nodes: s.type }));
      continue;
    }
    if (!Us(n, o)) {
      if (s.defaultValue)
        i[o] = tt(s.defaultValue, a);
      else if (Nn(a)) {
        const u2 = ce(a);
        r(F(`Variable "$${o}" of required type "${u2}" was not provided.`, {
          nodes: s
        }));
      }
      continue;
    }
    const l = n[o];
    if (l === null && Nn(a)) {
      const u2 = ce(a);
      r(F(`Variable "$${o}" of non-null type "${u2}" must not be null.`, {
        nodes: s
      }));
      continue;
    }
    i[o] = tl(l, a, (u2, c, f2) => {
      let d = `Variable "$${o}" got invalid value ` + ce(c);
      u2.length > 0 && (d += ` at "${o}${yc(u2)}"`), r(F(d + "; " + f2.message, {
        nodes: s,
        originalError: f2
      }));
    });
  }
  return i;
}
var _c = uc2((t, e, n) => mc(t.schema, t.fragments, t.variableValues, e, n));
function Nc(t) {
  const e = Qs(t);
  return "schema" in e ? Gs2(e) : {
    errors: e.map((n) => (Object.defineProperty(n, "extensions", {
      value: {
        ...n.extensions,
        http: {
          ...n.extensions?.http || {},
          status: 400
        }
      }
    }), n))
  };
}
function Gs2(t) {
  return t.signal?.throwIfAborted(), D(() => Ac(t), (e) => {
    const n = Si(e, t.errors);
    return t.subsequentPayloads.size > 0 ? {
      initialResult: {
        ...n,
        hasNext: true
      },
      subsequentResults: Kc(t)
    } : n;
  }, (e) => (t.signal?.throwIfAborted(), e.errors ? t.errors.push(...e.errors) : t.errors.push(e), Si(null, t.errors)));
}
function Si(t, e) {
  return e.length === 0 ? { data: t } : { errors: e, data: t };
}
var Rc = qs(function(e) {
  const n = /* @__PURE__ */ Object.create(null);
  for (const r of e.definitions)
    r.kind === ye.FRAGMENT_DEFINITION && (n[r.name.value] = r);
  return n;
});
function Qs(t) {
  const { schema: e, document: n, rootValue: r, contextValue: i, variableValues: s, operationName: o, fieldResolver: a, typeResolver: l, subscribeFieldResolver: u2, signal: c } = t;
  c?.throwIfAborted(), gs(e);
  const f2 = Rc(n);
  let d;
  for (const T of n.definitions)
    switch (T.kind) {
      case ye.OPERATION_DEFINITION:
        if (o == null) {
          if (d !== void 0)
            return [
              F("Must provide operation name if query contains multiple operations.", {
                extensions: {
                  code: "OPERATION_RESOLUTION_FAILURE"
                }
              })
            ];
          d = T;
        } else T.name?.value === o && (d = T);
        break;
    }
  if (d == null)
    return o != null ? [
      F(`Unknown operation named "${o}".`, {
        extensions: {
          code: "OPERATION_RESOLUTION_FAILURE"
        }
      })
    ] : [
      F("Must provide an operation.", {
        extensions: {
          code: "OPERATION_RESOLUTION_FAILURE"
        }
      })
    ];
  const h = d.variableDefinitions ?? [], g2 = Ic(e, h, s ?? {}, {
    maxErrors: 50
  });
  if (g2.errors)
    return g2.errors;
  c?.throwIfAborted();
  let w, k;
  if (c) {
    const T = /* @__PURE__ */ new Set(), x = $s();
    k = x.promise;
    const E = () => {
      x.reject(c.reason), c.removeEventListener("abort", E);
    };
    c.addEventListener("abort", E, { once: true }), k.catch(() => {
      for (const _ of T)
        _();
      T.clear();
    }), w = (_) => {
      T.add(_);
    };
  }
  return {
    schema: e,
    fragments: f2,
    rootValue: r,
    contextValue: i,
    operation: d,
    variableValues: g2.coerced,
    fieldResolver: a ?? Ni,
    typeResolver: l ?? Pc,
    subscribeFieldResolver: u2 ?? Ni,
    subsequentPayloads: /* @__PURE__ */ new Set(),
    errors: [],
    signal: c,
    onSignalAbort: w,
    signalPromise: k
  };
}
function kc(t, e) {
  return {
    ...t,
    rootValue: e,
    subsequentPayloads: /* @__PURE__ */ new Set(),
    errors: []
  };
}
function Ac(t) {
  const { operation: e, schema: n, fragments: r, variableValues: i, rootValue: s } = t, o = fc2(n, e.operation, [e]);
  o == null && F(`Schema is not configured to execute ${e.operation} operation.`, {
    nodes: e
  });
  const { fields: a, patches: l } = Bs2(n, r, i, o, e.selectionSet), u2 = void 0;
  let c;
  e.operation === "mutation" ? c = Fc(t, o, s, u2, a) : c = Pr(t, o, s, u2, a);
  for (const f2 of l) {
    const { label: d, fields: h } = f2;
    Ys(t, o, s, h, d, u2);
  }
  return c;
}
function Fc(t, e, n, r, i) {
  return lc2(i, (s, [o, a]) => {
    const l = Rt(r, o, e.name);
    return t.signal?.throwIfAborted(), D(() => Hs(t, e, n, a, l), (u2) => (u2 === void 0 || (s[o] = u2), s));
  }, /* @__PURE__ */ Object.create(null));
}
function Pr(t, e, n, r, i, s) {
  const o = /* @__PURE__ */ Object.create(null);
  let a = false;
  try {
    for (const [l, u2] of i) {
      t.signal?.throwIfAborted();
      const c = Rt(r, l, e.name), f2 = Hs(t, e, n, u2, c, s);
      f2 !== void 0 && (o[l] = f2, B(f2) && (a = true));
    }
  } catch (l) {
    if (l !== t.signal?.reason && a)
      return D(() => wi(o, t.signal), () => {
        throw l;
      }, () => {
        throw l;
      });
    throw l;
  }
  return a ? wi(o, t.signal, t.signalPromise) : o;
}
function Hs(t, e, n, r, i, s) {
  const o = s?.errors ?? t.errors, a = Zs(t.schema, e, r[0]);
  if (!a)
    return;
  const l = a.type, u2 = a.resolve ?? t.fieldResolver, c = Ks(t, a, r, e, i);
  try {
    t.signal?.throwIfAborted();
    const f2 = Vs3(a, r[0], t.variableValues), d = t.contextValue, h = u2(n, f2, d, c);
    let g2;
    return B(h) ? g2 = h.then((w) => Be(t, l, r, c, i, w, s)) : g2 = Be(t, l, r, c, i, h, s), B(g2) ? g2.then(void 0, (w) => {
      if (w instanceof AggregateError) {
        let x;
        for (let E of w.errors) {
          E = Re(E);
          const _ = ae(E, r, ee(i));
          x = me(_, l, o), le(t, i, s);
        }
        return x;
      }
      w = Re(w);
      const k = ae(w, r, ee(i)), T = me(k, l, o);
      return le(t, i, s), T;
    }) : g2;
  } catch (f2) {
    if (f2 instanceof AggregateError) {
      let w;
      for (let k of f2.errors) {
        k = Re(k);
        const T = ae(k, r, ee(i));
        w = me(T, l, o), le(t, i, s);
      }
      return w;
    }
    const d = Re(f2), h = ae(d, r, ee(i)), g2 = me(h, l, o);
    return le(t, i, s), g2;
  }
}
function Ks(t, e, n, r, i) {
  return {
    fieldName: e.name,
    fieldNodes: n,
    returnType: e.type,
    parentType: r,
    path: i,
    schema: t.schema,
    fragments: t.fragments,
    rootValue: t.rootValue,
    operation: t.operation,
    variableValues: t.variableValues,
    signal: t.signal
  };
}
var Cc = "CRITICAL_ERROR";
function me(t, e, n) {
  if (Nn(e) || t.extensions?.[Cc])
    throw t;
  return n.push(t), null;
}
function Be(t, e, n, r, i, s, o) {
  if (s instanceof Error)
    throw s;
  if (Nn(e)) {
    const a = Be(t, e.ofType, n, r, i, s, o);
    if (a === null)
      throw new Error(`Cannot return null for non-nullable field ${r.parentType.name}.${r.fieldName}.`);
    return a;
  }
  if (s == null)
    return null;
  if (Ms(e))
    return Dc(t, e, n, r, i, s, o);
  if (ac(e))
    return Lc(e, s);
  if (oc(e))
    return $c(t, e, n, r, i, s, o);
  if (Nr(e))
    return Tr(t, e, n, r, i, s, o);
  console.assert(false, "Cannot complete value of unexpected output type: " + ce(e));
}
function Ws(t, e, n) {
  if (typeof n.key == "number")
    return;
  const r = Ot(hc2, e[0], t.variableValues);
  if (r && r.if !== false)
    return Ei(typeof r.initialCount == "number", "initialCount must be a number"), Ei(r.initialCount >= 0, "initialCount must be a positive integer"), {
      initialCount: r.initialCount,
      label: typeof r.label == "string" ? r.label : void 0
    };
}
async function xc(t, e, n, r, i, s, o) {
  t.signal?.throwIfAborted(), s.return && t.onSignalAbort?.(() => {
    s.return?.();
  });
  const a = o?.errors ?? t.errors, l = Ws(t, n, i);
  let u2 = false;
  const c = [];
  let f2 = 0;
  for (; ; ) {
    if (l && typeof l.initialCount == "number" && f2 >= l.initialCount) {
      Qc(f2, s, t, n, r, e, i, l.label, o);
      break;
    }
    const d = Rt(i, f2, void 0);
    let h;
    try {
      if (h = await s.next(), h.done)
        break;
    } catch (g2) {
      const w = Re(g2), k = ae(w, n, ee(d));
      c.push(me(k, e, a));
      break;
    }
    zs(h.value, c, a, t, e, n, r, d, o) && (u2 = true), f2 += 1;
  }
  return u2 ? Promise.all(c) : c;
}
function Dc(t, e, n, r, i, s, o) {
  const a = e.ofType, l = o?.errors ?? t.errors;
  if (zn(s)) {
    const g2 = s[Symbol.asyncIterator]();
    return xc(t, a, n, r, i, g2, o);
  }
  if (!ac2(s))
    throw F(`Expected Iterable, but did not find one for field "${r.parentType.name}.${r.fieldName}".`);
  const u2 = Ws(t, n, i);
  let c = false, f2 = o;
  const d = [];
  let h = 0;
  for (const g2 of s) {
    const w = Rt(i, h, void 0);
    if (u2 && typeof u2.initialCount == "number" && h >= u2.initialCount) {
      f2 = Bc(i, w, g2, t, n, r, a, u2.label, f2), h++;
      continue;
    }
    zs(g2, d, l, t, a, n, r, w, o) && (c = true), h++;
  }
  return c ? Promise.all(d) : d;
}
function zs(t, e, n, r, i, s, o, a, l) {
  try {
    let u2;
    if (B(t) ? u2 = t.then((c) => Be(r, i, s, o, a, c, l)) : u2 = Be(r, i, s, o, a, t, l), B(u2))
      return e.push(u2.then(void 0, (c) => {
        c = Re(c);
        const f2 = ae(c, s, ee(a)), d = me(f2, i, n);
        return le(r, a, l), d;
      })), true;
    e.push(u2);
  } catch (u2) {
    const c = Re(u2), f2 = ae(c, s, ee(a)), d = me(f2, i, n);
    le(r, a, l), e.push(d);
  }
  return false;
}
function Lc(t, e) {
  let n;
  try {
    n = t.serialize(e);
  } catch (r) {
    throw r instanceof qe ? new Error(r.message) : r;
  }
  if (n == null)
    throw new Error(`Expected \`${ce(t)}.serialize(${ce(e)})\` to return non-nullable value, returned: ${ce(n)}`);
  return n;
}
function $c(t, e, n, r, i, s, o) {
  const a = e.resolveType ?? t.typeResolver, l = t.contextValue, u2 = a(s, l, r, e);
  return B(u2) ? u2.then((c) => Tr(t, Ii(c, t, e, n, r, s), n, r, i, s, o)) : Tr(t, Ii(u2, t, e, n, r, s), n, r, i, s, o);
}
function Ii(t, e, n, r, i, s) {
  if (t == null)
    throw F(`Abstract type "${n.name}" must resolve to an Object type at runtime for field "${i.parentType.name}.${i.fieldName}". Either the "${n.name}" type should provide a "resolveType" function or each possible type should provide an "isTypeOf" function.`, { nodes: r });
  if (Nr(t)) {
    if (ns.major >= 16)
      throw F("Support for returning GraphQLObjectType from resolveType was removed in graphql-js@16.0.0 please return type name instead.");
    t = t.name;
  }
  if (typeof t != "string")
    throw F(`Abstract type "${n.name}" must resolve to an Object type at runtime for field "${i.parentType.name}.${i.fieldName}" with value ${ce(s)}, received "${ce(t)}".`);
  const o = e.schema.getType(t);
  if (o == null)
    throw F(`Abstract type "${n.name}" was resolved to a type "${t}" that does not exist inside the schema.`, { nodes: r });
  if (!Nr(o))
    throw F(`Abstract type "${n.name}" was resolved to a non-object type "${t}".`, { nodes: r });
  if (!e.schema.isSubType(n, o))
    throw F(`Runtime Object type "${o.name}" is not a possible type for "${n.name}".`, { nodes: r });
  return o;
}
function Tr(t, e, n, r, i, s, o) {
  if (e.isTypeOf) {
    const a = e.isTypeOf(s, t.contextValue, r);
    if (B(a))
      return a.then((l) => {
        if (!l)
          throw Oi(e, s, n);
        return _i(t, e, n, i, s, o);
      });
    if (!a)
      throw Oi(e, s, n);
  }
  return _i(t, e, n, i, s, o);
}
function Oi(t, e, n) {
  return F(`Expected value of type "${t.name}" but got: ${ce(e)}.`, {
    nodes: n
  });
}
function _i(t, e, n, r, i, s) {
  const { fields: o, patches: a } = _c(t, e, n), l = Pr(t, e, i, r, o, s);
  for (const u2 of a) {
    const { label: c, fields: f2 } = u2;
    Ys(t, e, i, f2, c, r, s);
  }
  return l;
}
var Pc = function(t, e, n, r) {
  if (Ms2(t) && typeof t.__typename == "string")
    return t.__typename;
  const i = n.schema.getPossibleTypes(r), s = [];
  for (let o = 0; o < i.length; o++) {
    const a = i[o];
    if (a.isTypeOf) {
      const l = a.isTypeOf(t, e, n);
      if (B(l))
        s[o] = l;
      else if (l)
        return a.name;
    }
  }
  if (s.length)
    return Promise.all(s).then((o) => {
      for (let a = 0; a < o.length; a++)
        if (o[a])
          return i[a].name;
    });
};
var Ni = function(t, e, n, r) {
  if (Ms2(t) || typeof t == "function") {
    const i = t[r.fieldName];
    return typeof i == "function" ? t[r.fieldName](e, n, r) : i;
  }
};
function jc(t) {
  const e = Qs(t);
  if (!("schema" in e)) {
    for (const r of e) {
      const i = r.extensions ||= {}, s = i.http ||= {};
      s.status = 400, r.extensions.code = "BAD_USER_INPUT";
    }
    return {
      errors: e
    };
  }
  const n = Vc(e);
  return B(n) ? n.then((r) => Ri(e, r)) : Ri(e, n);
}
function Mc(t) {
  return t?.initialResult;
}
function Js(t) {
  const e = t.subsequentResults;
  let n = false, r = false;
  return {
    [Symbol.asyncIterator]() {
      return this;
    },
    next() {
      return r ? Q({ value: void 0, done: r }) : n ? e.next() : (n = true, Q({
        value: t.initialResult,
        done: r
      }));
    },
    return() {
      return r = true, e.return();
    },
    throw(i) {
      return r = true, e.throw(i);
    },
    [ie.asyncDispose]() {
      return r = true, e?.[ie.asyncDispose]?.();
    }
  };
}
async function* Uc(t) {
  "initialResult" in t ? yield* Js(t) : yield t;
}
function Ri(t, e) {
  return zn(e) ? Sc(Ps2(e, (n) => D(() => Gs2(kc(t, n)), Uc), (n) => {
    throw n instanceof AggregateError ? new AggregateError(n.errors.map((r) => ki(r, t.operation)), n.message) : ki(n, t.operation);
  })) : e;
}
function ki(t, e) {
  return F(t.message, {
    originalError: t,
    nodes: [e]
  });
}
function Vc(t) {
  try {
    const e = qc(t);
    return B(e) ? e.then(void 0, (n) => ({ errors: [n] })) : e;
  } catch (e) {
    return { errors: [e] };
  }
}
function qc(t) {
  const { schema: e, fragments: n, operation: r, variableValues: i, rootValue: s } = t, o = e.getSubscriptionType();
  if (o == null)
    throw F("Schema is not configured to execute subscription operation.", {
      nodes: r
    });
  const { fields: a } = Bs2(e, n, i, o, r.selectionSet), [l, u2] = [...a.entries()][0], c = u2[0].name.value, f2 = Zs(e, o, u2[0]);
  if (!f2)
    throw F(`The subscription field "${c}" is not defined.`, {
      nodes: u2
    });
  const d = Rt(void 0, l, o.name), h = Ks(t, f2, u2, o, d);
  try {
    const g2 = Vs3(f2, u2[0], i), w = t.contextValue, T = (f2.subscribe ?? t.subscribeFieldResolver)(s, g2, w, h);
    return B(T) ? T.then((x) => Ai(x, t.signal, t.onSignalAbort)).then(void 0, (x) => {
      throw ae(x, u2, ee(d));
    }) : Ai(T, t.signal, t.onSignalAbort);
  } catch (g2) {
    throw ae(g2, u2, ee(d));
  }
}
function Ai(t, e, n) {
  if (e?.throwIfAborted(), t instanceof Error)
    throw t;
  if (!zn(t))
    throw F(`Subscription field must return Async Iterable. Received: ${ce(t)}.`);
  return n ? {
    [Symbol.asyncIterator]() {
      const r = t[Symbol.asyncIterator]();
      return r.return && n?.(() => {
        r.return?.();
      }), r;
    }
  } : t;
}
function Ys(t, e, n, r, i, s, o) {
  const a = new Wc({
    label: i,
    path: s,
    parentContext: o,
    exeContext: t
  });
  let l;
  try {
    l = Pr(t, e, n, s, r, a), B(l) && (l = l.then(null, (u2) => (a.errors.push(u2), null)));
  } catch (u2) {
    a.errors.push(u2), l = null;
  }
  a.addData(l);
}
function Bc(t, e, n, r, i, s, o, a, l) {
  const u2 = new Xs({
    label: a,
    path: e,
    parentContext: l,
    exeContext: r
  });
  let c;
  try {
    try {
      B(n) ? c = n.then((d) => Be(r, o, i, s, e, d, u2)) : c = Be(r, o, i, s, e, n, u2), B(c) && (c = c.then(void 0, (d) => {
        d = Re(d);
        const h = ae(d, i, ee(e)), g2 = me(h, o, u2.errors);
        return le(r, e, u2), g2;
      }));
    } catch (d) {
      const h = Re(d), g2 = ae(h, i, ee(e));
      c = me(g2, o, u2.errors), le(r, e, u2);
    }
  } catch (d) {
    return u2.errors.push(d), le(r, t, u2), u2.addItems(null), u2;
  }
  let f2;
  return B(c) ? f2 = c.then((d) => [d], (d) => (u2.errors.push(d), le(r, t, u2), null)) : f2 = [c], u2.addItems(f2), u2;
}
async function Gc(t, e, n, r, i, s, o) {
  let a;
  try {
    const { value: u2, done: c } = await t.next();
    if (c)
      return s.setIsCompletedIterator(), { done: c, value: void 0 };
    a = u2;
  } catch (u2) {
    const c = Re(u2), f2 = ae(c, n, ee(o));
    return { done: true, value: me(f2, i, s.errors) };
  }
  let l;
  try {
    return l = Be(e, i, n, r, o, a, s), B(l) && (l = l.then(void 0, (u2) => {
      const c = ae(u2, n, ee(o)), f2 = me(c, i, s.errors);
      return le(e, o, s), f2;
    })), { done: false, value: l };
  } catch (u2) {
    const c = ae(u2, n, ee(o)), f2 = me(c, i, s.errors);
    return le(e, o, s), { done: false, value: f2 };
  }
}
async function Qc(t, e, n, r, i, s, o, a, l) {
  let u2 = t, c = l ?? void 0;
  for (; ; ) {
    const f2 = Rt(o, u2, void 0), d = new Xs({
      label: a,
      path: f2,
      parentContext: c,
      iterator: e,
      exeContext: n
    });
    let h;
    try {
      h = await Gc(e, n, r, i, s, d, f2);
    } catch (T) {
      d.errors.push(T), le(n, o, d), d.addItems(null), e?.return && e.return().catch(() => {
      });
      return;
    }
    const { done: g2, value: w } = h;
    let k;
    if (B(w) ? k = w.then((T) => [T], (T) => (d.errors.push(T), le(n, o, d), null)) : k = [w], d.addItems(k), g2)
      break;
    c = d, u2++;
  }
}
function le(t, e, n) {
  const r = ee(e);
  t.subsequentPayloads.forEach((i) => {
    if (i !== n) {
      for (let s = 0; s < r.length; s++)
        if (i.path[s] !== r[s])
          return;
      jr(i) && i.iterator?.return && i.iterator.return().catch(() => {
      }), t.subsequentPayloads.delete(i);
    }
  });
}
function Hc(t) {
  const e = [];
  for (const n of t.subsequentPayloads) {
    const r = {};
    if (n.isCompleted) {
      if (t.subsequentPayloads.delete(n), jr(n)) {
        const i = n.items;
        if (n.isCompletedIterator)
          continue;
        r.items = i;
      } else {
        const i = n.data;
        r.data = i ?? null;
      }
      r.path = n.path, n.label && (r.label = n.label), n.errors.length > 0 && (r.errors = n.errors), e.push(r);
    }
  }
  return e;
}
function Kc(t) {
  let e = false;
  async function n() {
    if (e)
      return { value: void 0, done: true };
    const i = Array.from(t.subsequentPayloads).map((a) => a.promise);
    if (t.signalPromise ? await Promise.race([t.signalPromise, ...i]) : await Promise.race(i), e)
      return { value: void 0, done: true };
    const s = Hc(t), o = t.subsequentPayloads.size > 0;
    return !s.length && o ? n() : (o || (e = true), {
      value: s.length ? { incremental: s, hasNext: o } : { hasNext: o },
      done: false
    });
  }
  function r() {
    const i = [];
    return t.subsequentPayloads.forEach((s) => {
      jr(s) && s.iterator?.return && i.push(s.iterator.return());
    }), Promise.all(i);
  }
  return {
    [Symbol.asyncIterator]() {
      return this;
    },
    next: n,
    async return() {
      return await r(), e = true, { value: void 0, done: true };
    },
    async throw(i) {
      throw await r(), e = true, i;
    },
    async [ie.asyncDispose]() {
      await r(), e = true;
    }
  };
}
var Wc = class {
  type;
  errors;
  label;
  path;
  promise;
  data;
  parentContext;
  isCompleted;
  _exeContext;
  _resolve;
  constructor(e) {
    this.type = "defer", this.label = e.label, this.path = ee(e.path), this.parentContext = e.parentContext, this.errors = [], this._exeContext = e.exeContext, this._exeContext.subsequentPayloads.add(this), this.isCompleted = false, this.data = null, this.promise = new Promise((n) => {
      this._resolve = (r) => {
        n(r);
      };
    }).then((n) => {
      this.data = n, this.isCompleted = true;
    });
  }
  addData(e) {
    const n = this.parentContext?.promise;
    if (n) {
      this._resolve?.(n.then(() => e));
      return;
    }
    this._resolve?.(e);
  }
};
var Xs = class {
  type;
  errors;
  label;
  path;
  items;
  promise;
  parentContext;
  iterator;
  isCompletedIterator;
  isCompleted;
  _exeContext;
  _resolve;
  constructor(e) {
    this.type = "stream", this.items = null, this.label = e.label, this.path = ee(e.path), this.parentContext = e.parentContext, this.iterator = e.iterator, this.errors = [], this._exeContext = e.exeContext, this._exeContext.subsequentPayloads.add(this), this.isCompleted = false, this.items = null, this.promise = new Promise((n) => {
      this._resolve = (r) => {
        n(r);
      };
    }).then((n) => {
      this.items = n, this.isCompleted = true;
    });
  }
  addItems(e) {
    const n = this.parentContext?.promise;
    if (n) {
      this._resolve?.(n.then(() => e));
      return;
    }
    this._resolve?.(e);
  }
  setIsCompletedIterator() {
    this.isCompletedIterator = true;
  }
};
function jr(t) {
  return t.type === "stream";
}
function Zs(t, e, n) {
  const r = n.name.value;
  return r === En.name && t.getQueryType() === e ? En : r === wn.name && t.getQueryType() === e ? wn : r === Sn.name ? Sn : e.getFields()[r];
}
function Fi(t) {
  const e = xr2(t.document, t.operationName);
  if (e == null)
    throw new Error("Must provide an operation.");
  return e.operation === "subscription" ? jc(t) : D(() => Nc(t), (n) => Mc(n) ? Js(n) : n);
}
var q = {};
var Ci;
function zc() {
  return Ci || (Ci = 1, q.fetch = globalThis.fetch, q.Headers = globalThis.Headers, q.Request = globalThis.Request, q.Response = globalThis.Response, q.FormData = globalThis.FormData, q.ReadableStream = globalThis.ReadableStream, q.WritableStream = globalThis.WritableStream, q.TransformStream = globalThis.TransformStream, q.CompressionStream = globalThis.CompressionStream, q.DecompressionStream = globalThis.DecompressionStream, q.TextDecoderStream = globalThis.TextDecoderStream, q.TextEncoderStream = globalThis.TextEncoderStream, q.Blob = globalThis.Blob, q.File = globalThis.File, q.crypto = globalThis.crypto, q.btoa = globalThis.btoa, q.TextEncoder = globalThis.TextEncoder, q.TextDecoder = globalThis.TextDecoder, q.URLPattern = globalThis.URLPattern, q.URL = globalThis.URL, q.URLSearchParams = globalThis.URLSearchParams, q.createFetch = () => globalThis), q;
}
var Jn = zc();
var Jc = f(Jn);
var eo = zo2({
  __proto__: null,
  default: Jc
}, [Jn]);
function to(t) {
  return t != null && typeof t == "object" && typeof t[Symbol.asyncIterator] == "function";
}
function Yc(t) {
  if (t.socket?.localPort)
    return t.socket?.localPort;
  const n = (t.headers?.[":authority"] || t.headers?.host)?.split(":")?.[1];
  return n || 80;
}
function Xc(t) {
  if (t.headers?.[":authority"])
    return t.headers?.[":authority"];
  if (t.headers?.host)
    return t.headers?.host;
  const e = Yc(t);
  if (t.hostname)
    return t.hostname + ":" + e;
  const n = t.socket?.localAddress;
  return n && !n?.includes("::") && !n?.includes("ffff") ? `${n}:${e}` : "localhost";
}
function Zc(t) {
  const e = Xc(t), n = t.protocol || (t.socket?.encrypted ? "https" : "http"), r = t.originalUrl || t.url || "/graphql";
  return `${n}://${e}${r}`;
}
function ef(t) {
  const e = t[Symbol.toStringTag];
  return !!(typeof t == "string" || e === "Uint8Array" || e === "Blob" || e === "FormData" || e === "URLSearchParams" || to(t));
}
function xi(t, e, n, r) {
  const i = t.raw || t.req || t;
  let s = Zc(i);
  if (t.query) {
    const u2 = new e.URL(s);
    for (const c in t.query)
      u2.searchParams.set(c, t.query[c]);
    s = u2.toString();
  }
  let o = t.headers;
  if (t.headers?.[":method"]) {
    o = {};
    for (const u2 in t.headers)
      u2.startsWith(":") || (o[u2] = t.headers[u2]);
  }
  const a = r ? ro() : new AbortController();
  if (n?.once) {
    const u2 = () => {
      a.signal.aborted || (Object.defineProperty(i, "aborted", { value: true }), a.abort(n.errored ?? void 0));
    };
    n.once("error", u2), n.once("close", u2), n.once("finish", () => {
      n.removeListener("close", u2);
    });
  }
  if (t.method === "GET" || t.method === "HEAD")
    return new e.Request(s, {
      method: t.method,
      headers: o,
      signal: a.signal
    });
  const l = t.body;
  if (l != null && Object.keys(l).length > 0) {
    if (ef(l))
      return new e.Request(s, {
        method: t.method || "GET",
        headers: o,
        body: l,
        signal: a.signal
      });
    const u2 = new e.Request(s, {
      method: t.method || "GET",
      headers: o,
      signal: a.signal
    });
    return u2.headers.get("content-type")?.includes("json") || u2.headers.set("content-type", "application/json; charset=utf-8"), new Proxy(u2, {
      get: (c, f2, d) => {
        switch (f2) {
          case "json":
            return () => Q(l);
          case "text":
            return () => Q(JSON.stringify(l));
          default:
            return globalThis.Bun ? Reflect.get(c, f2) : Reflect.get(c, f2, d);
        }
      }
    });
  }
  return new e.Request(s, {
    method: t.method,
    headers: o,
    signal: a.signal,
    // @ts-expect-error - AsyncIterable is supported as body
    body: i,
    duplex: "half"
  });
}
function no(t) {
  return t.read != null;
}
function tf(t) {
  return no(t);
}
function Di(t) {
  return t != null && t.setHeader != null && t.end != null && t.once != null && t.write != null;
}
function nf(t) {
  return t != null && t.getReader != null;
}
function rf(t) {
  return t != null && t.request != null && t.respondWith != null;
}
function sf(t) {
  t?.socket?.setTimeout?.(0), t?.socket?.setNoDelay?.(true), t?.socket?.setKeepAlive?.(true);
}
function Xe(t) {
  t.end(null, null, null);
}
function of(t, e) {
  let n = false;
  const r = () => {
    n = true;
  };
  t.once("error", r), t.once("close", r), t.once("finish", () => {
    t.removeListener("close", r), t.removeListener("error", r);
  });
  const i = e[Symbol.asyncIterator](), s = () => i.next().then(({ done: o, value: a }) => {
    if (!(n || o))
      return D(() => Qt(a, t), () => n ? Xe(t) : s());
  });
  return s();
}
function Qt(t, e) {
  if (!e.write(t))
    return new Promise((r) => e.once("drain", r));
}
function af(t, e, n, r) {
  if (e.closed || e.destroyed || e.writableEnded)
    return;
  if (!t) {
    e.statusCode = 404, Xe(e);
    return;
  }
  if (r && // @ts-expect-error - headersInit is a private property
  t.headers?.headersInit && // @ts-expect-error - headersInit is a private property
  !Array.isArray(t.headers.headersInit) && // @ts-expect-error - headersInit is a private property
  !t.headers.headersInit.get && // @ts-expect-error - map is a private property
  !t.headers._map && // @ts-expect-error - _setCookies is a private property
  !t.headers._setCookies?.length)
    e.writeHead(
      t.status,
      t.statusText,
      // @ts-expect-error - headersInit is a private property
      t.headers.headersInit
    );
  else {
    if (e.setHeaders)
      e.setHeaders(t.headers);
    else {
      let o = false;
      t.headers.forEach((a, l) => {
        if (l === "set-cookie") {
          if (o)
            return;
          o = true;
          const u2 = t.headers.getSetCookie?.();
          if (u2) {
            e.setHeader("set-cookie", u2);
            return;
          }
        }
        e.setHeader(l, a);
      });
    }
    e.writeHead(t.status, t.statusText);
  }
  if (t.bodyType === "String")
    return D(
      // @ts-expect-error - bodyInit is a private property
      () => Qt(t.bodyInit, e),
      () => Xe(e)
    );
  const i = (
    // @ts-expect-error - _buffer is a private property
    t._buffer
  );
  if (i)
    return D(() => Qt(i, e), () => Xe(e));
  const s = t.body;
  if (s == null) {
    Xe(e);
    return;
  }
  if (
    // @ts-expect-error - Uint8Array is a valid body type
    s[Symbol.toStringTag] === "Uint8Array"
  )
    return D(() => Qt(s, e), () => Xe(e));
  if (sf(n), no(s)) {
    e.once("close", () => {
      s.destroy();
    }), s.pipe(e, {
      end: true
    });
    return;
  }
  if (nf(s))
    return lf(n, e, s);
  if (to(s))
    return of(e, s);
}
function lf(t, e, n) {
  const r = n.getReader();
  t?.once?.("error", (s) => {
    r.cancel(s);
  });
  function i() {
    return r.read().then(({ done: s, value: o }) => s ? Xe(e) : D(() => Qt(o, e), i));
  }
  return i();
}
function uf(t) {
  return t != null && typeof t == "object" && ("body" in t || "cache" in t || "credentials" in t || "headers" in t || "integrity" in t || "keepalive" in t || "method" in t || "mode" in t || "redirect" in t || "referrer" in t || "referrerPolicy" in t || "signal" in t || "window" in t);
}
function mt(...t) {
  const [e, ...n] = t.filter((r) => r != null && typeof r == "object");
  return n.forEach((r) => {
    const i = Object.getOwnPropertyNames(r).reduce((s, o) => (Object.getOwnPropertyDescriptor(r, o) && (s[o] = Object.getOwnPropertyDescriptor(r, o)), s), {});
    Object.getOwnPropertySymbols(r).forEach((s) => {
      const o = Object.getOwnPropertyDescriptor(r, s);
      o?.enumerable && (i[s] = o);
    }), Object.defineProperties(e, i);
  }), e;
}
function Li(t, e) {
  return new e(t.stack || t.message || t.toString(), {
    status: t.status || 500
  });
}
function $i(t, e) {
  return t == null ? e == null ? {} : {
    waitUntil: e
  } : mt(Object.create(t), {
    waitUntil: e
  }, t);
}
function Pi(t, e) {
  if (e?.aborted)
    throw e.reason;
  if (B(t) && e) {
    let r = function() {
      n.reject(e.reason);
    };
    const n = $s();
    return e.addEventListener("abort", r, { once: true }), t.then(function(s) {
      n.resolve(s);
    }).catch(function(s) {
      n.reject(s);
    }).finally(() => {
      e.removeEventListener("abort", r);
    }), n.promise;
  }
  return t;
}
var cf = ["SIGINT", "exit", "SIGTERM"];
var bn = /* @__PURE__ */ new Set();
var ji = false;
function ff() {
  if (!ji) {
    ji = true;
    for (const t of cf)
      globalThis.process.once(t, function() {
        return Promise.allSettled([...bn].map((n) => !n.disposed && n.disposeAsync()));
      });
  }
}
function df(t) {
  globalThis.process && (ff(), bn.has(t) || (bn.add(t), t.defer(() => {
    bn.delete(t);
  })));
}
var pf = class extends EventTarget {
  aborted = false;
  _onabort = null;
  _reason;
  constructor() {
    super();
    const e = globalThis.process?.getBuiltinModule?.("node:events");
    e?.kMaxEventTargetListeners && (this[e.kMaxEventTargetListeners] = 0);
  }
  throwIfAborted() {
    if (this._nativeCtrl?.signal?.throwIfAborted)
      return this._nativeCtrl.signal.throwIfAborted();
    if (this.aborted)
      throw this._reason;
  }
  _nativeCtrl;
  ensureNativeCtrl() {
    if (!this._nativeCtrl) {
      const e = this.aborted;
      this._nativeCtrl = new AbortController(), e && this._nativeCtrl.abort(this._reason);
    }
    return this._nativeCtrl;
  }
  abort(e) {
    if (this._nativeCtrl?.abort)
      return this._nativeCtrl?.abort(e);
    this._reason = e || new DOMException("This operation was aborted", "AbortError"), this.aborted = true, this.dispatchEvent(new Event("abort"));
  }
  get signal() {
    return this._nativeCtrl?.signal ? this._nativeCtrl.signal : this;
  }
  get reason() {
    return this._nativeCtrl?.signal ? this._nativeCtrl.signal.reason : this._reason;
  }
  get onabort() {
    return this._onabort ? this._onabort : this._onabort;
  }
  set onabort(e) {
    if (this._nativeCtrl?.signal) {
      this._nativeCtrl.signal.onabort = e;
      return;
    }
    this._onabort && this.removeEventListener("abort", this._onabort), this._onabort = e, e && this.addEventListener("abort", e);
  }
};
function ro() {
  return globalThis.Bun || globalThis.Deno ? new AbortController() : new Proxy(new pf(), {
    get(t, e, n) {
      if (e.toString().includes("kDependantSignals")) {
        const r = t.ensureNativeCtrl();
        return Reflect.get(r.signal, e, r.signal);
      }
      return Reflect.get(t, e, n);
    },
    set(t, e, n, r) {
      if (e.toString().includes("kDependantSignals")) {
        const i = t.ensureNativeCtrl();
        return Reflect.set(i.signal, e, n, i.signal);
      }
      return Reflect.set(t, e, n, r);
    },
    getPrototypeOf() {
      return AbortSignal.prototype;
    }
  });
}
function hf(t) {
  return !!t.onData;
}
function gf({ req: t, res: e, fetchAPI: n, controller: r }) {
  const i = t.getMethod();
  let s;
  const o = [], a = [
    (I) => {
      o.push(I);
    }
  ], l = (I) => {
    for (const S2 of a)
      S2(I);
  };
  let u2 = false;
  const c = [
    () => {
      u2 = true;
    }
  ], f2 = () => {
    for (const I of c)
      I();
  };
  e.onData(function(I, S2) {
    l(Buffer.from(Buffer.from(I, 0, I.byteLength))), S2 && f2();
  });
  let d;
  if (i !== "get" && i !== "head") {
    s = "half", r.signal.addEventListener("abort", () => {
      f2();
    }, { once: true });
    let I;
    d = () => (I || (I = new n.ReadableStream({
      start(S2) {
        for (const $2 of o)
          S2.enqueue($2);
        if (u2) {
          S2.close();
          return;
        }
        a.push(($2) => {
          S2.enqueue($2);
        }), c.push(() => {
          if (r.signal.reason) {
            S2.error(r.signal.reason);
            return;
          }
          S2.desiredSize && S2.close();
        });
      }
    })), I);
  }
  const h = new n.Headers();
  t.forEach((I, S2) => {
    h.append(I, S2);
  });
  let g2 = `http://localhost${t.getUrl()}`;
  const w = t.getQuery();
  w && (g2 += `?${w}`);
  let k;
  function T() {
    return d ? u2 ? E() : d() : null;
  }
  const x = new n.Request(g2, {
    method: i,
    headers: h,
    get body() {
      return T();
    },
    signal: r.signal,
    // eslint-disable-next-line @typescript-eslint/ban-ts-comment
    // @ts-ignore - not in the TS types yet
    duplex: s
  });
  function E() {
    return k || (k = o.length === 1 ? o[0] : Buffer.concat(o)), k;
  }
  function _() {
    return u2 ? Q(E()) : new Promise((I, S2) => {
      try {
        c.push(() => {
          I(E());
        });
      } catch ($2) {
        S2($2);
      }
    });
  }
  return Object.defineProperties(x, {
    body: {
      get() {
        return T();
      },
      configurable: true,
      enumerable: true
    },
    json: {
      value() {
        return _().then((I) => I.toString("utf8")).then((I) => JSON.parse(I));
      },
      configurable: true,
      enumerable: true
    },
    text: {
      value() {
        return _().then((I) => I.toString("utf8"));
      },
      configurable: true,
      enumerable: true
    },
    arrayBuffer: {
      value() {
        return _();
      },
      configurable: true,
      enumerable: true
    }
  }), x;
}
function mf(t, e) {
  return new e.WritableStream({
    write(n) {
      t.cork(() => {
        t.write(n);
      });
    },
    close() {
      t.cork(() => {
        t.end();
      });
    }
  });
}
function yf(t, e, n, r) {
  if (!e) {
    t.writeStatus("404 Not Found"), t.end();
    return;
  }
  const i = e._buffer;
  if (!n.signal.aborted && (t.cork(() => {
    t.writeStatus(`${e.status} ${e.statusText}`);
    for (const [s, o] of e.headers)
      if (s !== "content-length") {
        if (s === "set-cookie") {
          const a = e.headers.getSetCookie?.();
          if (a) {
            for (const l of a)
              t.writeHeader(s, l);
            continue;
          }
        }
        t.writeHeader(s, o);
      }
    i ? t.end(i) : e.body || t.end();
  }), !(i || !e.body)))
    return n.signal.addEventListener("abort", () => {
      e.body?.locked || e.body?.cancel(n.signal.reason);
    }, { once: true }), e.body.pipeTo(mf(t, r), {
      signal: n.signal
    }).catch((s) => {
      if (!n.signal.aborted)
        throw s;
    });
}
function bf(t) {
  try {
    return !!t?.request;
  } catch {
    return false;
  }
}
var vf = {};
function Tf(t, e) {
  const n = e?.__useSingleWriteHead == null ? true : e.__useSingleWriteHead, r = {
    ...eo,
    ...e?.fetchAPI
  }, i = e?.__useCustomAbortCtrl == null ? r.Request !== globalThis.Request : e.__useCustomAbortCtrl, s = typeof t == "function" ? t : t.handle, o = [], a = [];
  let l;
  const u2 = /* @__PURE__ */ new Set();
  let c;
  function f2() {
    return c || (c = new wc(), e?.disposeOnProcessTerminate && df(c), c.defer(() => {
      if (u2.size > 0)
        return Promise.allSettled(u2).then(() => {
          u2.clear();
        }, () => {
          u2.clear();
        });
    })), c;
  }
  function d(m) {
    B(m) && (f2(), u2.add(m), m.then(() => {
      u2.delete(m);
    }, (y) => {
      console.error(`Unexpected error while waiting: ${y.message || y}`), u2.delete(m);
    }));
  }
  if (e?.plugins != null)
    for (const m of e.plugins) {
      m.instrumentation && (l = l ? Dr(l, m.instrumentation) : m.instrumentation), m.onRequest && o.push(m.onRequest), m.onResponse && a.push(m.onResponse);
      const y = m[ie.dispose];
      y && f2().defer(y);
      const b = m[ie.asyncDispose];
      b && f2().defer(b), m.onDispose && f2().defer(m.onDispose);
    }
  let h = o.length > 0 || a.length > 0 ? function(y, b) {
    let N = s, C;
    if (o.length === 0)
      return Y();
    let P = y.parsedUrl || new Proxy(vf, {
      get(G, se, ye2) {
        return P = new r.URL(y.url, "http://localhost"), Reflect.get(P, se, P);
      }
    });
    function U(G) {
      return a.length === 0 ? G : D(() => nt(a, (se) => se({
        request: y,
        response: G,
        serverContext: b,
        setResponse(ye2) {
          G = ye2;
        },
        fetchAPI: r
      })), () => G);
    }
    function Y() {
      return C ? U(C) : D(() => N(y, b), U);
    }
    return D(() => nt(o, (G, se) => G({
      request: y,
      setRequest(ye2) {
        y = ye2;
      },
      serverContext: b,
      fetchAPI: r,
      url: P,
      requestHandler: N,
      setRequestHandler(ye2) {
        N = ye2;
      },
      endResponse(ye2) {
        C = ye2, ye2 && se();
      }
    })), Y);
  } : s;
  if (l?.request) {
    const m = h;
    h = (y, b) => Rn({ request: y }).asyncFn(l.request, m)(y, b);
  }
  function g2(m, ...y) {
    const b = y.length > 1 ? mt(...y) : y[0] || {};
    b.waitUntil || (b.waitUntil = d);
    const N = xi(m, r, void 0, i);
    return h(N, b);
  }
  function w(m, y, ...b) {
    const N = y.raw || y, C = b.length > 1 ? mt(...b) : b[0] || {};
    C.waitUntil || (C.waitUntil = d);
    const P = xi(m, r, N, i);
    return h(P, C);
  }
  function k(m, y, ...b) {
    const N = {
      req: m,
      res: y,
      waitUntil: d
    };
    return Lr(Q().then(() => w(m, y, N, ...b)).catch((C) => Li(C, r.Response)).then((C) => af(C, y, m, n)).catch((C) => console.error(`Unexpected error while handling request: ${C.message || C}`)));
  }
  function T(m, y, ...b) {
    const N = {
      res: m,
      req: y,
      waitUntil: d
    }, P = b.filter((be) => be != null).length > 0 ? mt(N, ...b) : N, U = i ? ro() : new AbortController(), Y = m.end.bind(m);
    let G = false;
    m.end = function(be) {
      return G = true, Y(be);
    }, m.onAborted.bind(m)(function() {
      U.abort();
    }), m.onAborted = function(be) {
      U.signal.addEventListener("abort", be, { once: true });
    };
    const ye2 = gf({
      req: y,
      res: m,
      fetchAPI: r,
      controller: U
    });
    return D(() => D(() => h(ye2, P), (be) => be, (be) => Li(be, r.Response)), (be) => {
      if (!U.signal.aborted && !G)
        return D(() => yf(m, be, U, r), (sn) => sn, (sn) => {
          console.error(`Unexpected error while handling request: ${sn.message || sn}`);
        });
    });
  }
  function x(m, ...y) {
    if (!m.respondWith || !m.request)
      throw new TypeError(`Expected FetchEvent, got ${m}`);
    const b = y.filter((P) => P != null), N = b.length > 0 ? mt({}, m, ...b) : $i(m), C = h(m.request, N);
    m.respondWith(C);
  }
  function E(m, ...y) {
    const b = y.filter((C) => C != null), N = b.length > 1 ? mt({}, ...b) : $i(b[0], b[0] == null || b[0].waitUntil == null ? d : void 0);
    return h(m, N);
  }
  const _ = (m, ...y) => {
    if (typeof m == "string" || "href" in m) {
      const [N, ...C] = y;
      if (uf(N)) {
        const U = new r.Request(m, N), Y = E(U, ...C), G = N.signal;
        return G ? Pi(Y, G) : Y;
      }
      const P = new r.Request(m);
      return E(P, ...y);
    }
    const b = E(m, ...y);
    return Pi(b, m.signal);
  }, I = (m, ...y) => {
    const [b, ...N] = y;
    if (tf(m)) {
      if (!Di(b))
        throw new TypeError(`Expected ServerResponse, got ${b}`);
      return k(m, b, ...N);
    }
    if (hf(m))
      return T(m, b, ...N);
    if (Di(b))
      throw new TypeError("Got Node response without Node request");
    return bf(m) ? rf(m) ? x(m, ...y) : E(m.request, m, ...y) : _(m, ...y);
  }, S2 = {
    handleRequest: E,
    fetch: _,
    handleNodeRequest: g2,
    handleNodeRequestAndResponse: w,
    requestListener: k,
    handleEvent: x,
    handleUWS: T,
    handle: I,
    get disposableStack() {
      return f2();
    },
    [ie.asyncDispose]() {
      return c && !c.disposed ? c.disposeAsync() : Q();
    },
    dispose() {
      return c && !c.disposed ? c.disposeAsync() : Q();
    },
    waitUntil: d
  }, $2 = new Proxy(I, {
    // It should have all the attributes of the handler function and the server instance
    has: (m, y) => y in S2 || y in I || t && y in t,
    get: (m, y) => {
      if (globalThis.Deno || y === Symbol.asyncDispose || y === Symbol.dispose) {
        const C = Reflect.get(S2, y, S2);
        if (C)
          return C;
      }
      const b = S2[y];
      if (b)
        return b.bind ? b.bind(S2) : b;
      const N = I[y];
      if (N)
        return N.bind ? N.bind(I) : N;
      if (t) {
        const C = t[y];
        if (C)
          return C.bind ? function(...P) {
            const U = t[y](...P);
            return U === t ? $2 : U;
          } : C;
      }
    },
    apply(m, y, b) {
      return I(...b);
    }
  });
  return $2;
}
function Ef(t, e) {
  const n = t.headers.get("origin");
  if (e === false || n == null)
    return null;
  const r = {};
  if (e.origin == null || e.origin.length === 0 || e.origin.includes("*") ? (r["Access-Control-Allow-Origin"] = n, r.Vary = "Origin") : typeof e.origin == "string" ? r["Access-Control-Allow-Origin"] = e.origin : Array.isArray(e.origin) && (e.origin.length === 1 ? r["Access-Control-Allow-Origin"] = e.origin[0] : e.origin.includes(n) ? (r["Access-Control-Allow-Origin"] = n, r.Vary = "Origin") : r["Access-Control-Allow-Origin"] = "null"), e.methods?.length)
    r["Access-Control-Allow-Methods"] = e.methods.join(", ");
  else {
    const i = t.headers.get("access-control-request-method");
    i && (r["Access-Control-Allow-Methods"] = i);
  }
  if (e.allowedHeaders?.length)
    r["Access-Control-Allow-Headers"] = e.allowedHeaders.join(", ");
  else {
    const i = t.headers.get("access-control-request-headers");
    i && (r["Access-Control-Allow-Headers"] = i, r.Vary ? r.Vary += ", Access-Control-Request-Headers" : r.Vary = "Access-Control-Request-Headers");
  }
  return e.credentials != null ? e.credentials === true && (r["Access-Control-Allow-Credentials"] = "true") : r["Access-Control-Allow-Origin"] !== "*" && (r["Access-Control-Allow-Credentials"] = "true"), e.exposedHeaders && (r["Access-Control-Expose-Headers"] = e.exposedHeaders.join(", ")), e.maxAge && (r["Access-Control-Max-Age"] = e.maxAge.toString()), r;
}
function wf(t, e, n) {
  return D(() => e(t, n), (r) => Ef(t, r));
}
function Sf(t) {
  let e = () => ({});
  if (t != null)
    if (typeof t == "function")
      e = t;
    else if (typeof t == "object") {
      const n = {
        ...t
      };
      e = () => n;
    } else t === false && (e = () => false);
  return {
    onRequest({ request: n, fetchAPI: r, endResponse: i }) {
      if (n.method.toUpperCase() === "OPTIONS") {
        const s = new r.Response(null, {
          status: 204,
          // Safari (and potentially other browsers) need content-length 0,
          // for 204 or they just hang waiting for a body
          // see: https://github.com/expressjs/cors/blob/master/lib/index.js#L176
          headers: {
            "Content-Length": "0"
          }
        });
        i(s);
      }
    },
    onResponse({ request: n, serverContext: r, response: i }) {
      return D(() => wf(n, e, r), (s) => {
        if (s != null)
          for (const o in s)
            i.headers.set(o, s[o]);
      });
    }
  };
}
function If(t) {
  return t != null && typeof t == "object" && "errors" in t;
}
function Of(t) {
  return t != null && typeof t.toString == "function";
}
function at(t) {
  return t instanceof qe;
}
function Mr(t) {
  return t instanceof qe ? t.originalError != null ? Mr(t.originalError) : true : false;
}
function io(t) {
  return typeof t == "object" && t?.constructor?.name === "DOMException" && (t.name === "AbortError" || t.name === "TimeoutError");
}
function vn(t, e, n) {
  const r = /* @__PURE__ */ new Set();
  if (If(t))
    for (const i of t.errors) {
      const s = vn(i, e, n);
      for (const o of s)
        r.add(o);
    }
  else if (io(t))
    n.debug("Request aborted");
  else if (e) {
    const i = e.maskError(t, e.errorMessage, e.isDev);
    i !== t && n.error(t), r.add(at(i) ? i : F(i.message, {
      originalError: i
    }));
  } else at(t) ? r.add(t) : t instanceof Error ? r.add(F(t.message, {
    originalError: t
  })) : typeof t == "string" ? r.add(F(t, {
    extensions: {
      code: "INTERNAL_SERVER_ERROR",
      unexpected: true
    }
  })) : Of(t) ? r.add(F(t.toString(), {
    extensions: {
      code: "INTERNAL_SERVER_ERROR",
      unexpected: true
    }
  })) : (n.error(t), r.add(F("Unexpected error.", {
    extensions: {
      http: {
        unexpected: true
      }
    }
  })));
  return Array.from(r);
}
function Ur(t, e = {}, n = false) {
  let r, i = false;
  if ("extensions" in t && t.extensions?.http && (t.extensions.http.headers && Object.assign(e, t.extensions.http.headers), t.extensions.http.status && (r = t.extensions.http.status)), "errors" in t && t.errors?.length)
    for (const s of t.errors)
      if (s.extensions?.http) {
        if (s.extensions.http.headers && Object.assign(e, s.extensions.http.headers), n && s.extensions.http.spec)
          continue;
        s.extensions.http.status && (!r || s.extensions.http.status > r) && (r = s.extensions.http.status);
      } else (!Mr(s) || s.extensions?.unexpected) && (i = true);
  else
    r ||= 200;
  return r || (i && !("data" in t) ? r = 500 : r = 200), {
    status: r,
    headers: e
  };
}
function _f(t) {
  return Array.isArray(t) && t.length > 0 && // if one item in the array is a GraphQLError, we're good
  t.some(at);
}
function so(t) {
  const e = t.get("operationName") || void 0, n = t.get("query") || void 0, r = t.get("variables") || void 0, i = t.get("extensions") || void 0;
  return {
    operationName: e,
    query: n,
    variables: r ? JSON.parse(r) : void 0,
    extensions: i ? JSON.parse(i) : void 0
  };
}
function Nf(t) {
  const e = new Jn.URLSearchParams(t);
  return so(e);
}
function Yt(t, e) {
  let n = t.headers.get("content-type");
  return n = n?.split(",")[0] || null, n === e || !!n?.startsWith(`${e};`);
}
function Rf(t) {
  return t.method === "GET";
}
function kf(t) {
  const e = t.url.substring(t.url.indexOf("?") + 1), n = new Jn.URLSearchParams(e);
  return so(n);
}
function Af(t) {
  return t.method === "POST" && Yt(t, "application/x-www-form-urlencoded");
}
function Ff(t) {
  return t.text().then(Nf);
}
function Cf(t) {
  return t.method === "POST" && Yt(t, "application/graphql");
}
function xf(t) {
  return t.text().then((e) => ({ query: e }));
}
function Df(t) {
  return t.method === "POST" && (Yt(t, "application/json") || Yt(t, "application/graphql+json"));
}
function Lf(t) {
  return D(() => t.json(), (e) => {
    if (e == null)
      throw F(`POST body is expected to be object but received ${e}`, {
        extensions: {
          http: {
            status: 400
          },
          code: "BAD_REQUEST"
        }
      });
    const n = typeof e;
    if (n !== "object")
      throw F(`POST body is expected to be object but received ${n}`, {
        extensions: {
          http: {
            status: 400
          },
          code: "BAD_REQUEST"
        }
      });
    return e;
  }, (e) => {
    if (e instanceof qe)
      throw e;
    const n = {
      http: {
        spec: true,
        status: 400
      },
      code: "BAD_REQUEST"
    };
    throw e instanceof Error && (n.originalError = {
      name: e.name,
      message: e.message
    }), F("POST body sent invalid JSON.", {
      extensions: n
    });
  });
}
function $f(t, e, n) {
  e.split && (e = e.split("."));
  for (var r = 0, i = e.length, s = t, o, a; r < i && (a = "" + e[r++], !(a === "__proto__" || a === "constructor" || a === "prototype")); )
    s = s[a] = r === i ? n : typeof (o = s[a]) == typeof e ? o : e[r] * 0 !== 0 || ~("" + e[r]).indexOf(".") ? {} : [];
}
function Pf(t) {
  return t.method === "POST" && Yt(t, "multipart/form-data");
}
function jf(t) {
  return D(() => t.formData(), (e) => {
    const n = e.get("operations");
    if (!n)
      throw F('Missing multipart form field "operations"');
    if (typeof n != "string")
      throw F('Multipart form field "operations" must be a string');
    let r;
    try {
      r = JSON.parse(n);
    } catch {
      throw F('Multipart form field "operations" must be a valid JSON string');
    }
    const i = e.get("map");
    if (i != null) {
      if (typeof i != "string")
        throw F('Multipart form field "map" must be a string');
      let s;
      try {
        s = JSON.parse(i);
      } catch {
        throw F('Multipart form field "map" must be a valid JSON string');
      }
      for (const o in s) {
        const a = e.get(o), l = s[o];
        for (const u2 of l)
          $f(r, u2, a);
      }
    }
    return r;
  }, (e) => {
    throw e instanceof Error && e.message.startsWith("File size limit exceeded: ") ? F(e.message, {
      extensions: {
        http: {
          status: 413
        }
      }
    }) : e;
  });
}
var Mf = /* @__PURE__ */ new Set(["query", "variables", "operationName", "extensions"]);
function Uf(t, e) {
  if (t == null || typeof t != "object")
    throw F('Invalid "params" in the request body', {
      extensions: {
        http: {
          spec: true,
          status: 400
        },
        code: "BAD_REQUEST"
      }
    });
  for (const n in t)
    if (t[n] != null && !Mf.has(n)) {
      if (e?.includes(n))
        continue;
      throw F(`Unexpected parameter "${n}" in the request body.`, {
        extensions: {
          http: {
            status: 400
          },
          code: "BAD_REQUEST"
        }
      });
    }
}
function Vf(t, e) {
  if (!Bf(t))
    throw F(`Expected params to be an object but given ${Bt(t)}.`, {
      extensions: {
        http: {
          status: 400,
          headers: {
            Allow: "GET, POST"
          }
        },
        code: "BAD_REQUEST"
      }
    });
  if (Uf(t, e), t.query == null)
    throw F("Must provide query string.", {
      extensions: {
        http: {
          spec: true,
          status: 400,
          headers: {
            Allow: "GET, POST"
          }
        },
        code: "BAD_REQUEST"
      }
    });
  const n = Bt(t.query);
  if (n !== "string")
    throw F(`Expected "query" param to be a string, but given ${n}.`, {
      extensions: {
        http: {
          status: 400,
          headers: {
            Allow: "GET, POST"
          }
        },
        code: "BAD_REQUEST"
      }
    });
  const r = Bt(t.variables);
  if (!["object", "null", "undefined"].includes(r))
    throw F(`Expected "variables" param to be empty or an object, but given ${r}.`, {
      extensions: {
        http: {
          status: 400,
          headers: {
            Allow: "GET, POST"
          }
        },
        code: "BAD_REQUEST"
      }
    });
  const i = Bt(t.extensions);
  if (!["object", "null", "undefined"].includes(i))
    throw F(`Expected "extensions" param to be empty or an object, but given ${i}.`, {
      extensions: {
        http: {
          status: 400,
          headers: {
            Allow: "GET, POST"
          }
        },
        code: "BAD_REQUEST"
      }
    });
  return t;
}
function qf(t) {
  return {
    onParams({ params: e }) {
      Vf(e, t);
    }
  };
}
function Bt(t) {
  return t === null ? "null" : Array.isArray(t) ? "array" : typeof t;
}
function Bf(t) {
  return Bt(t) === "object";
}
function Gf(t) {
  return t === "GET" || t === "POST";
}
function Qf() {
  return {
    onRequestParse({ request: t }) {
      if (!Gf(t.method))
        throw F("GraphQL only supports GET and POST requests.", {
          extensions: {
            http: {
              status: 405,
              headers: {
                Allow: "GET, POST"
              }
            },
            code: "BAD_REQUEST"
          }
        });
    }
  };
}
function Hf() {
  return {
    onValidate() {
      return ({ valid: t, result: e }) => {
        if (!t)
          for (const n of e)
            n.extensions ||= {}, n.extensions.code ||= "GRAPHQL_VALIDATION_FAILED", n.extensions.http ||= {}, n.extensions.http.spec = n.extensions.http.spec == null ? true : n.extensions.http.spec, n.extensions.http.status ||= 400;
      };
    }
  };
}
function Kf(t) {
  return {
    onRequestParse() {
      return {
        onRequestParseDone({ requestParserResult: e }) {
          if (Array.isArray(e)) {
            if (!t)
              throw F("Batching is not supported.", {
                extensions: {
                  http: {
                    status: 400
                  },
                  code: "BAD_REQUEST"
                }
              });
            if (e.length > t)
              throw F(`Batching is limited to ${t} operations per request.`, {
                extensions: {
                  http: {
                    status: 413
                  },
                  code: "BAD_REQUEST"
                }
              });
          }
        }
      };
    }
  };
}
function Wf(t, e, n) {
  const r = e ? xr2(e, n) ?? void 0 : void 0;
  if (!r)
    throw F("Could not determine what operation to execute.", {
      extensions: {
        code: "OPERATION_RESOLUTION_FAILURE",
        http: {
          status: 400
        }
      }
    });
  if (r.operation === "mutation" && t === "GET")
    throw F("Can only perform a mutation operation from a POST request.", {
      extensions: {
        http: {
          status: 405,
          headers: {
            Allow: "POST"
          }
        },
        code: "BAD_REQUEST"
      }
    });
}
function zf() {
  return {
    onParse() {
      return ({ result: t, context: {
        request: e,
        // the `params` might be missing in cases where the user provided
        // malformed context to getEnveloped (like `yoga.getEnveloped({})`)
        params: { operationName: n } = {}
      } }) => {
        if (e)
          if (t instanceof Error) {
            if (t instanceof qe) {
              const r = t.extensions ||= {};
              r.code ||= "GRAPHQL_PARSE_FAILED";
              const i = r.http ||= {};
              i.spec ||= true, i.status ||= 400;
            }
          } else
            Wf(e.method, t, n);
      };
    }
  };
}
function Jf({ id: t = Date.now().toString(), logger: e = console, endpoint: n = "/health" } = {}) {
  return {
    onRequest({ endResponse: r, fetchAPI: i, request: s }) {
      if (s.url.endsWith(n)) {
        e.debug("Responding Health Check");
        const o = new i.Response(null, {
          status: 200,
          headers: {
            "x-yoga-id": t
          }
        });
        r(o);
      }
    }
  };
}
var dt2 = typeof performance == "object" && performance && typeof performance.now == "function" ? performance : Date;
var oo = /* @__PURE__ */ new Set();
var Er = typeof process == "object" && process ? process : {};
var ao = (t, e, n, r) => {
  typeof Er.emitWarning == "function" ? Er.emitWarning(t, e, n, r) : console.error(`[${n}] ${e}: ${t}`);
};
var Dn = globalThis.AbortController;
var Mi = globalThis.AbortSignal;
if (typeof Dn > "u") {
  Mi = class {
    onabort;
    _onabort = [];
    reason;
    aborted = false;
    addEventListener(r, i) {
      this._onabort.push(i);
    }
  }, Dn = class {
    constructor() {
      e();
    }
    signal = new Mi();
    abort(r) {
      if (!this.signal.aborted) {
        this.signal.reason = r, this.signal.aborted = true;
        for (const i of this.signal._onabort)
          i(r);
        this.signal.onabort?.(r);
      }
    }
  };
  let t = Er.env?.LRU_CACHE_IGNORE_AC_WARNING !== "1";
  const e = () => {
    t && (t = false, ao("AbortController is not defined. If using lru-cache in node 14, load an AbortController polyfill from the `node-abort-controller` package. A minimal polyfill is provided for use by LRUCache.fetch(), but it should not be relied upon in other contexts (eg, passing it to other APIs that use AbortController/AbortSignal might have undesirable effects). You may disable this with LRU_CACHE_IGNORE_AC_WARNING=1 in the env.", "NO_ABORT_CONTROLLER", "ENOTSUP", e));
  };
}
var Yf = (t) => !oo.has(t);
var Pe = (t) => t && t === Math.floor(t) && t > 0 && isFinite(t);
var lo = (t) => Pe(t) ? t <= Math.pow(2, 8) ? Uint8Array : t <= Math.pow(2, 16) ? Uint16Array : t <= Math.pow(2, 32) ? Uint32Array : t <= Number.MAX_SAFE_INTEGER ? Tn : null : null;
var Tn = class extends Array {
  constructor(e) {
    super(e), this.fill(0);
  }
};
var vt = class _vt {
  heap;
  length;
  // private constructor
  static #l = false;
  static create(e) {
    const n = lo(e);
    if (!n)
      return [];
    _vt.#l = true;
    const r = new _vt(e, n);
    return _vt.#l = false, r;
  }
  constructor(e, n) {
    if (!_vt.#l)
      throw new TypeError("instantiate Stack using Stack.create(n)");
    this.heap = new n(e), this.length = 0;
  }
  push(e) {
    this.heap[this.length++] = e;
  }
  pop() {
    return this.heap[--this.length];
  }
};
var Vr = class _Vr {
  // options that cannot be changed without disaster
  #l;
  #f;
  #g;
  #m;
  #A;
  #F;
  /**
   * {@link LRUCache.OptionsBase.ttl}
   */
  ttl;
  /**
   * {@link LRUCache.OptionsBase.ttlResolution}
   */
  ttlResolution;
  /**
   * {@link LRUCache.OptionsBase.ttlAutopurge}
   */
  ttlAutopurge;
  /**
   * {@link LRUCache.OptionsBase.updateAgeOnGet}
   */
  updateAgeOnGet;
  /**
   * {@link LRUCache.OptionsBase.updateAgeOnHas}
   */
  updateAgeOnHas;
  /**
   * {@link LRUCache.OptionsBase.allowStale}
   */
  allowStale;
  /**
   * {@link LRUCache.OptionsBase.noDisposeOnSet}
   */
  noDisposeOnSet;
  /**
   * {@link LRUCache.OptionsBase.noUpdateTTL}
   */
  noUpdateTTL;
  /**
   * {@link LRUCache.OptionsBase.maxEntrySize}
   */
  maxEntrySize;
  /**
   * {@link LRUCache.OptionsBase.sizeCalculation}
   */
  sizeCalculation;
  /**
   * {@link LRUCache.OptionsBase.noDeleteOnFetchRejection}
   */
  noDeleteOnFetchRejection;
  /**
   * {@link LRUCache.OptionsBase.noDeleteOnStaleGet}
   */
  noDeleteOnStaleGet;
  /**
   * {@link LRUCache.OptionsBase.allowStaleOnFetchAbort}
   */
  allowStaleOnFetchAbort;
  /**
   * {@link LRUCache.OptionsBase.allowStaleOnFetchRejection}
   */
  allowStaleOnFetchRejection;
  /**
   * {@link LRUCache.OptionsBase.ignoreFetchAbort}
   */
  ignoreFetchAbort;
  // computed properties
  #i;
  #y;
  #r;
  #n;
  #e;
  #u;
  #d;
  #a;
  #s;
  #b;
  #o;
  #v;
  #T;
  #p;
  #E;
  #_;
  #c;
  /**
   * Do not call this method unless you need to inspect the
   * inner workings of the cache.  If anything returned by this
   * object is modified in any way, strange breakage may occur.
   *
   * These fields are private for a reason!
   *
   * @internal
   */
  static unsafeExposeInternals(e) {
    return {
      // properties
      starts: e.#T,
      ttls: e.#p,
      sizes: e.#v,
      keyMap: e.#r,
      keyList: e.#n,
      valList: e.#e,
      next: e.#u,
      prev: e.#d,
      get head() {
        return e.#a;
      },
      get tail() {
        return e.#s;
      },
      free: e.#b,
      // methods
      isBackgroundFetch: (n) => e.#t(n),
      backgroundFetch: (n, r, i, s) => e.#D(n, r, i, s),
      moveToTail: (n) => e.#k(n),
      indexes: (n) => e.#w(n),
      rindexes: (n) => e.#S(n),
      isStale: (n) => e.#h(n)
    };
  }
  // Protected read-only members
  /**
   * {@link LRUCache.OptionsBase.max} (read-only)
   */
  get max() {
    return this.#l;
  }
  /**
   * {@link LRUCache.OptionsBase.maxSize} (read-only)
   */
  get maxSize() {
    return this.#f;
  }
  /**
   * The total computed size of items in the cache (read-only)
   */
  get calculatedSize() {
    return this.#y;
  }
  /**
   * The number of items stored in the cache (read-only)
   */
  get size() {
    return this.#i;
  }
  /**
   * {@link LRUCache.OptionsBase.fetchMethod} (read-only)
   */
  get fetchMethod() {
    return this.#A;
  }
  get memoMethod() {
    return this.#F;
  }
  /**
   * {@link LRUCache.OptionsBase.dispose} (read-only)
   */
  get dispose() {
    return this.#g;
  }
  /**
   * {@link LRUCache.OptionsBase.disposeAfter} (read-only)
   */
  get disposeAfter() {
    return this.#m;
  }
  constructor(e) {
    const { max: n = 0, ttl: r, ttlResolution: i = 1, ttlAutopurge: s, updateAgeOnGet: o, updateAgeOnHas: a, allowStale: l, dispose: u2, disposeAfter: c, noDisposeOnSet: f2, noUpdateTTL: d, maxSize: h = 0, maxEntrySize: g2 = 0, sizeCalculation: w, fetchMethod: k, memoMethod: T, noDeleteOnFetchRejection: x, noDeleteOnStaleGet: E, allowStaleOnFetchRejection: _, allowStaleOnFetchAbort: I, ignoreFetchAbort: S2 } = e;
    if (n !== 0 && !Pe(n))
      throw new TypeError("max option must be a nonnegative integer");
    const $2 = n ? lo(n) : Array;
    if (!$2)
      throw new Error("invalid max value: " + n);
    if (this.#l = n, this.#f = h, this.maxEntrySize = g2 || this.#f, this.sizeCalculation = w, this.sizeCalculation) {
      if (!this.#f && !this.maxEntrySize)
        throw new TypeError("cannot set sizeCalculation without setting maxSize or maxEntrySize");
      if (typeof this.sizeCalculation != "function")
        throw new TypeError("sizeCalculation set to non-function");
    }
    if (T !== void 0 && typeof T != "function")
      throw new TypeError("memoMethod must be a function if defined");
    if (this.#F = T, k !== void 0 && typeof k != "function")
      throw new TypeError("fetchMethod must be a function if specified");
    if (this.#A = k, this.#_ = !!k, this.#r = /* @__PURE__ */ new Map(), this.#n = new Array(n).fill(void 0), this.#e = new Array(n).fill(void 0), this.#u = new $2(n), this.#d = new $2(n), this.#a = 0, this.#s = 0, this.#b = vt.create(n), this.#i = 0, this.#y = 0, typeof u2 == "function" && (this.#g = u2), typeof c == "function" ? (this.#m = c, this.#o = []) : (this.#m = void 0, this.#o = void 0), this.#E = !!this.#g, this.#c = !!this.#m, this.noDisposeOnSet = !!f2, this.noUpdateTTL = !!d, this.noDeleteOnFetchRejection = !!x, this.allowStaleOnFetchRejection = !!_, this.allowStaleOnFetchAbort = !!I, this.ignoreFetchAbort = !!S2, this.maxEntrySize !== 0) {
      if (this.#f !== 0 && !Pe(this.#f))
        throw new TypeError("maxSize must be a positive integer if specified");
      if (!Pe(this.maxEntrySize))
        throw new TypeError("maxEntrySize must be a positive integer if specified");
      this.#V();
    }
    if (this.allowStale = !!l, this.noDeleteOnStaleGet = !!E, this.updateAgeOnGet = !!o, this.updateAgeOnHas = !!a, this.ttlResolution = Pe(i) || i === 0 ? i : 1, this.ttlAutopurge = !!s, this.ttl = r || 0, this.ttl) {
      if (!Pe(this.ttl))
        throw new TypeError("ttl must be a positive integer if specified");
      this.#L();
    }
    if (this.#l === 0 && this.ttl === 0 && this.#f === 0)
      throw new TypeError("At least one of max, maxSize, or ttl is required");
    if (!this.ttlAutopurge && !this.#l && !this.#f) {
      const m = "LRU_CACHE_UNBOUNDED";
      Yf(m) && (oo.add(m), ao("TTL caching without ttlAutopurge, max, or maxSize can result in unbounded memory consumption.", "UnboundedCacheWarning", m, _Vr));
    }
  }
  /**
   * Return the number of ms left in the item's TTL. If item is not in cache,
   * returns `0`. Returns `Infinity` if item is in cache without a defined TTL.
   */
  getRemainingTTL(e) {
    return this.#r.has(e) ? 1 / 0 : 0;
  }
  #L() {
    const e = new Tn(this.#l), n = new Tn(this.#l);
    this.#p = e, this.#T = n, this.#$ = (s, o, a = dt2.now()) => {
      if (n[s] = o !== 0 ? a : 0, e[s] = o, o !== 0 && this.ttlAutopurge) {
        const l = setTimeout(() => {
          this.#h(s) && this.#I(this.#n[s], "expire");
        }, o + 1);
        l.unref && l.unref();
      }
    }, this.#N = (s) => {
      n[s] = e[s] !== 0 ? dt2.now() : 0;
    }, this.#O = (s, o) => {
      if (e[o]) {
        const a = e[o], l = n[o];
        if (!a || !l)
          return;
        s.ttl = a, s.start = l, s.now = r || i();
        const u2 = s.now - l;
        s.remainingTTL = a - u2;
      }
    };
    let r = 0;
    const i = () => {
      const s = dt2.now();
      if (this.ttlResolution > 0) {
        r = s;
        const o = setTimeout(() => r = 0, this.ttlResolution);
        o.unref && o.unref();
      }
      return s;
    };
    this.getRemainingTTL = (s) => {
      const o = this.#r.get(s);
      if (o === void 0)
        return 0;
      const a = e[o], l = n[o];
      if (!a || !l)
        return 1 / 0;
      const u2 = (r || i()) - l;
      return a - u2;
    }, this.#h = (s) => {
      const o = n[s], a = e[s];
      return !!a && !!o && (r || i()) - o > a;
    };
  }
  // conditionally set private methods related to TTL
  #N = () => {
  };
  #O = () => {
  };
  #$ = () => {
  };
  /* c8 ignore stop */
  #h = () => false;
  #V() {
    const e = new Tn(this.#l);
    this.#y = 0, this.#v = e, this.#R = (n) => {
      this.#y -= e[n], e[n] = 0;
    }, this.#P = (n, r, i, s) => {
      if (this.#t(r))
        return 0;
      if (!Pe(i))
        if (s) {
          if (typeof s != "function")
            throw new TypeError("sizeCalculation must be a function");
          if (i = s(r, n), !Pe(i))
            throw new TypeError("sizeCalculation return invalid (expect positive integer)");
        } else
          throw new TypeError("invalid size value (must be positive integer). When maxSize or maxEntrySize is used, sizeCalculation or size must be set.");
      return i;
    }, this.#C = (n, r, i) => {
      if (e[n] = r, this.#f) {
        const s = this.#f - e[n];
        for (; this.#y > s; )
          this.#x(true);
      }
      this.#y += e[n], i && (i.entrySize = r, i.totalCalculatedSize = this.#y);
    };
  }
  #R = (e) => {
  };
  #C = (e, n, r) => {
  };
  #P = (e, n, r, i) => {
    if (r || i)
      throw new TypeError("cannot set size without setting maxSize or maxEntrySize on cache");
    return 0;
  };
  *#w({ allowStale: e = this.allowStale } = {}) {
    if (this.#i)
      for (let n = this.#s; !(!this.#j(n) || ((e || !this.#h(n)) && (yield n), n === this.#a)); )
        n = this.#d[n];
  }
  *#S({ allowStale: e = this.allowStale } = {}) {
    if (this.#i)
      for (let n = this.#a; !(!this.#j(n) || ((e || !this.#h(n)) && (yield n), n === this.#s)); )
        n = this.#u[n];
  }
  #j(e) {
    return e !== void 0 && this.#r.get(this.#n[e]) === e;
  }
  /**
   * Return a generator yielding `[key, value]` pairs,
   * in order from most recently used to least recently used.
   */
  *entries() {
    for (const e of this.#w())
      this.#e[e] !== void 0 && this.#n[e] !== void 0 && !this.#t(this.#e[e]) && (yield [this.#n[e], this.#e[e]]);
  }
  /**
   * Inverse order version of {@link LRUCache.entries}
   *
   * Return a generator yielding `[key, value]` pairs,
   * in order from least recently used to most recently used.
   */
  *rentries() {
    for (const e of this.#S())
      this.#e[e] !== void 0 && this.#n[e] !== void 0 && !this.#t(this.#e[e]) && (yield [this.#n[e], this.#e[e]]);
  }
  /**
   * Return a generator yielding the keys in the cache,
   * in order from most recently used to least recently used.
   */
  *keys() {
    for (const e of this.#w()) {
      const n = this.#n[e];
      n !== void 0 && !this.#t(this.#e[e]) && (yield n);
    }
  }
  /**
   * Inverse order version of {@link LRUCache.keys}
   *
   * Return a generator yielding the keys in the cache,
   * in order from least recently used to most recently used.
   */
  *rkeys() {
    for (const e of this.#S()) {
      const n = this.#n[e];
      n !== void 0 && !this.#t(this.#e[e]) && (yield n);
    }
  }
  /**
   * Return a generator yielding the values in the cache,
   * in order from most recently used to least recently used.
   */
  *values() {
    for (const e of this.#w())
      this.#e[e] !== void 0 && !this.#t(this.#e[e]) && (yield this.#e[e]);
  }
  /**
   * Inverse order version of {@link LRUCache.values}
   *
   * Return a generator yielding the values in the cache,
   * in order from least recently used to most recently used.
   */
  *rvalues() {
    for (const e of this.#S())
      this.#e[e] !== void 0 && !this.#t(this.#e[e]) && (yield this.#e[e]);
  }
  /**
   * Iterating over the cache itself yields the same results as
   * {@link LRUCache.entries}
   */
  [Symbol.iterator]() {
    return this.entries();
  }
  /**
   * A String value that is used in the creation of the default string
   * description of an object. Called by the built-in method
   * `Object.prototype.toString`.
   */
  [Symbol.toStringTag] = "LRUCache";
  /**
   * Find a value for which the supplied fn method returns a truthy value,
   * similar to `Array.find()`. fn is called as `fn(value, key, cache)`.
   */
  find(e, n = {}) {
    for (const r of this.#w()) {
      const i = this.#e[r], s = this.#t(i) ? i.__staleWhileFetching : i;
      if (s !== void 0 && e(s, this.#n[r], this))
        return this.get(this.#n[r], n);
    }
  }
  /**
   * Call the supplied function on each item in the cache, in order from most
   * recently used to least recently used.
   *
   * `fn` is called as `fn(value, key, cache)`.
   *
   * If `thisp` is provided, function will be called in the `this`-context of
   * the provided object, or the cache if no `thisp` object is provided.
   *
   * Does not update age or recenty of use, or iterate over stale values.
   */
  forEach(e, n = this) {
    for (const r of this.#w()) {
      const i = this.#e[r], s = this.#t(i) ? i.__staleWhileFetching : i;
      s !== void 0 && e.call(n, s, this.#n[r], this);
    }
  }
  /**
   * The same as {@link LRUCache.forEach} but items are iterated over in
   * reverse order.  (ie, less recently used items are iterated over first.)
   */
  rforEach(e, n = this) {
    for (const r of this.#S()) {
      const i = this.#e[r], s = this.#t(i) ? i.__staleWhileFetching : i;
      s !== void 0 && e.call(n, s, this.#n[r], this);
    }
  }
  /**
   * Delete any stale entries. Returns true if anything was removed,
   * false otherwise.
   */
  purgeStale() {
    let e = false;
    for (const n of this.#S({ allowStale: true }))
      this.#h(n) && (this.#I(this.#n[n], "expire"), e = true);
    return e;
  }
  /**
   * Get the extended info about a given entry, to get its value, size, and
   * TTL info simultaneously. Returns `undefined` if the key is not present.
   *
   * Unlike {@link LRUCache#dump}, which is designed to be portable and survive
   * serialization, the `start` value is always the current timestamp, and the
   * `ttl` is a calculated remaining time to live (negative if expired).
   *
   * Always returns stale values, if their info is found in the cache, so be
   * sure to check for expirations (ie, a negative {@link LRUCache.Entry#ttl})
   * if relevant.
   */
  info(e) {
    const n = this.#r.get(e);
    if (n === void 0)
      return;
    const r = this.#e[n], i = this.#t(r) ? r.__staleWhileFetching : r;
    if (i === void 0)
      return;
    const s = { value: i };
    if (this.#p && this.#T) {
      const o = this.#p[n], a = this.#T[n];
      if (o && a) {
        const l = o - (dt2.now() - a);
        s.ttl = l, s.start = Date.now();
      }
    }
    return this.#v && (s.size = this.#v[n]), s;
  }
  /**
   * Return an array of [key, {@link LRUCache.Entry}] tuples which can be
   * passed to {@link LRLUCache#load}.
   *
   * The `start` fields are calculated relative to a portable `Date.now()`
   * timestamp, even if `performance.now()` is available.
   *
   * Stale entries are always included in the `dump`, even if
   * {@link LRUCache.OptionsBase.allowStale} is false.
   *
   * Note: this returns an actual array, not a generator, so it can be more
   * easily passed around.
   */
  dump() {
    const e = [];
    for (const n of this.#w({ allowStale: true })) {
      const r = this.#n[n], i = this.#e[n], s = this.#t(i) ? i.__staleWhileFetching : i;
      if (s === void 0 || r === void 0)
        continue;
      const o = { value: s };
      if (this.#p && this.#T) {
        o.ttl = this.#p[n];
        const a = dt2.now() - this.#T[n];
        o.start = Math.floor(Date.now() - a);
      }
      this.#v && (o.size = this.#v[n]), e.unshift([r, o]);
    }
    return e;
  }
  /**
   * Reset the cache and load in the items in entries in the order listed.
   *
   * The shape of the resulting cache may be different if the same options are
   * not used in both caches.
   *
   * The `start` fields are assumed to be calculated relative to a portable
   * `Date.now()` timestamp, even if `performance.now()` is available.
   */
  load(e) {
    this.clear();
    for (const [n, r] of e) {
      if (r.start) {
        const i = Date.now() - r.start;
        r.start = dt2.now() - i;
      }
      this.set(n, r.value, r);
    }
  }
  /**
   * Add a value to the cache.
   *
   * Note: if `undefined` is specified as a value, this is an alias for
   * {@link LRUCache#delete}
   *
   * Fields on the {@link LRUCache.SetOptions} options param will override
   * their corresponding values in the constructor options for the scope
   * of this single `set()` operation.
   *
   * If `start` is provided, then that will set the effective start
   * time for the TTL calculation. Note that this must be a previous
   * value of `performance.now()` if supported, or a previous value of
   * `Date.now()` if not.
   *
   * Options object may also include `size`, which will prevent
   * calling the `sizeCalculation` function and just use the specified
   * number if it is a positive integer, and `noDisposeOnSet` which
   * will prevent calling a `dispose` function in the case of
   * overwrites.
   *
   * If the `size` (or return value of `sizeCalculation`) for a given
   * entry is greater than `maxEntrySize`, then the item will not be
   * added to the cache.
   *
   * Will update the recency of the entry.
   *
   * If the value is `undefined`, then this is an alias for
   * `cache.delete(key)`. `undefined` is never stored in the cache.
   */
  set(e, n, r = {}) {
    if (n === void 0)
      return this.delete(e), this;
    const { ttl: i = this.ttl, start: s, noDisposeOnSet: o = this.noDisposeOnSet, sizeCalculation: a = this.sizeCalculation, status: l } = r;
    let { noUpdateTTL: u2 = this.noUpdateTTL } = r;
    const c = this.#P(e, n, r.size || 0, a);
    if (this.maxEntrySize && c > this.maxEntrySize)
      return l && (l.set = "miss", l.maxEntrySizeExceeded = true), this.#I(e, "set"), this;
    let f2 = this.#i === 0 ? void 0 : this.#r.get(e);
    if (f2 === void 0)
      f2 = this.#i === 0 ? this.#s : this.#b.length !== 0 ? this.#b.pop() : this.#i === this.#l ? this.#x(false) : this.#i, this.#n[f2] = e, this.#e[f2] = n, this.#r.set(e, f2), this.#u[this.#s] = f2, this.#d[f2] = this.#s, this.#s = f2, this.#i++, this.#C(f2, c, l), l && (l.set = "add"), u2 = false;
    else {
      this.#k(f2);
      const d = this.#e[f2];
      if (n !== d) {
        if (this.#_ && this.#t(d)) {
          d.__abortController.abort(new Error("replaced"));
          const { __staleWhileFetching: h } = d;
          h !== void 0 && !o && (this.#E && this.#g?.(h, e, "set"), this.#c && this.#o?.push([h, e, "set"]));
        } else o || (this.#E && this.#g?.(d, e, "set"), this.#c && this.#o?.push([d, e, "set"]));
        if (this.#R(f2), this.#C(f2, c, l), this.#e[f2] = n, l) {
          l.set = "replace";
          const h = d && this.#t(d) ? d.__staleWhileFetching : d;
          h !== void 0 && (l.oldValue = h);
        }
      } else l && (l.set = "update");
    }
    if (i !== 0 && !this.#p && this.#L(), this.#p && (u2 || this.#$(f2, i, s), l && this.#O(l, f2)), !o && this.#c && this.#o) {
      const d = this.#o;
      let h;
      for (; h = d?.shift(); )
        this.#m?.(...h);
    }
    return this;
  }
  /**
   * Evict the least recently used item, returning its value or
   * `undefined` if cache is empty.
   */
  pop() {
    try {
      for (; this.#i; ) {
        const e = this.#e[this.#a];
        if (this.#x(true), this.#t(e)) {
          if (e.__staleWhileFetching)
            return e.__staleWhileFetching;
        } else if (e !== void 0)
          return e;
      }
    } finally {
      if (this.#c && this.#o) {
        const e = this.#o;
        let n;
        for (; n = e?.shift(); )
          this.#m?.(...n);
      }
    }
  }
  #x(e) {
    const n = this.#a, r = this.#n[n], i = this.#e[n];
    return this.#_ && this.#t(i) ? i.__abortController.abort(new Error("evicted")) : (this.#E || this.#c) && (this.#E && this.#g?.(i, r, "evict"), this.#c && this.#o?.push([i, r, "evict"])), this.#R(n), e && (this.#n[n] = void 0, this.#e[n] = void 0, this.#b.push(n)), this.#i === 1 ? (this.#a = this.#s = 0, this.#b.length = 0) : this.#a = this.#u[n], this.#r.delete(r), this.#i--, n;
  }
  /**
   * Check if a key is in the cache, without updating the recency of use.
   * Will return false if the item is stale, even though it is technically
   * in the cache.
   *
   * Check if a key is in the cache, without updating the recency of
   * use. Age is updated if {@link LRUCache.OptionsBase.updateAgeOnHas} is set
   * to `true` in either the options or the constructor.
   *
   * Will return `false` if the item is stale, even though it is technically in
   * the cache. The difference can be determined (if it matters) by using a
   * `status` argument, and inspecting the `has` field.
   *
   * Will not update item age unless
   * {@link LRUCache.OptionsBase.updateAgeOnHas} is set.
   */
  has(e, n = {}) {
    const { updateAgeOnHas: r = this.updateAgeOnHas, status: i } = n, s = this.#r.get(e);
    if (s !== void 0) {
      const o = this.#e[s];
      if (this.#t(o) && o.__staleWhileFetching === void 0)
        return false;
      if (this.#h(s))
        i && (i.has = "stale", this.#O(i, s));
      else return r && this.#N(s), i && (i.has = "hit", this.#O(i, s)), true;
    } else i && (i.has = "miss");
    return false;
  }
  /**
   * Like {@link LRUCache#get} but doesn't update recency or delete stale
   * items.
   *
   * Returns `undefined` if the item is stale, unless
   * {@link LRUCache.OptionsBase.allowStale} is set.
   */
  peek(e, n = {}) {
    const { allowStale: r = this.allowStale } = n, i = this.#r.get(e);
    if (i === void 0 || !r && this.#h(i))
      return;
    const s = this.#e[i];
    return this.#t(s) ? s.__staleWhileFetching : s;
  }
  #D(e, n, r, i) {
    const s = n === void 0 ? void 0 : this.#e[n];
    if (this.#t(s))
      return s;
    const o = new Dn(), { signal: a } = r;
    a?.addEventListener("abort", () => o.abort(a.reason), {
      signal: o.signal
    });
    const l = {
      signal: o.signal,
      options: r,
      context: i
    }, u2 = (w, k = false) => {
      const { aborted: T } = o.signal, x = r.ignoreFetchAbort && w !== void 0;
      if (r.status && (T && !k ? (r.status.fetchAborted = true, r.status.fetchError = o.signal.reason, x && (r.status.fetchAbortIgnored = true)) : r.status.fetchResolved = true), T && !x && !k)
        return f2(o.signal.reason);
      const E = h;
      return this.#e[n] === h && (w === void 0 ? E.__staleWhileFetching ? this.#e[n] = E.__staleWhileFetching : this.#I(e, "fetch") : (r.status && (r.status.fetchUpdated = true), this.set(e, w, l.options))), w;
    }, c = (w) => (r.status && (r.status.fetchRejected = true, r.status.fetchError = w), f2(w)), f2 = (w) => {
      const { aborted: k } = o.signal, T = k && r.allowStaleOnFetchAbort, x = T || r.allowStaleOnFetchRejection, E = x || r.noDeleteOnFetchRejection, _ = h;
      if (this.#e[n] === h && (!E || _.__staleWhileFetching === void 0 ? this.#I(e, "fetch") : T || (this.#e[n] = _.__staleWhileFetching)), x)
        return r.status && _.__staleWhileFetching !== void 0 && (r.status.returnedStale = true), _.__staleWhileFetching;
      if (_.__returned === _)
        throw w;
    }, d = (w, k) => {
      const T = this.#A?.(e, s, l);
      T && T instanceof Promise && T.then((x) => w(x === void 0 ? void 0 : x), k), o.signal.addEventListener("abort", () => {
        (!r.ignoreFetchAbort || r.allowStaleOnFetchAbort) && (w(void 0), r.allowStaleOnFetchAbort && (w = (x) => u2(x, true)));
      });
    };
    r.status && (r.status.fetchDispatched = true);
    const h = new Promise(d).then(u2, c), g2 = Object.assign(h, {
      __abortController: o,
      __staleWhileFetching: s,
      __returned: void 0
    });
    return n === void 0 ? (this.set(e, g2, { ...l.options, status: void 0 }), n = this.#r.get(e)) : this.#e[n] = g2, g2;
  }
  #t(e) {
    if (!this.#_)
      return false;
    const n = e;
    return !!n && n instanceof Promise && n.hasOwnProperty("__staleWhileFetching") && n.__abortController instanceof Dn;
  }
  async fetch(e, n = {}) {
    const {
      // get options
      allowStale: r = this.allowStale,
      updateAgeOnGet: i = this.updateAgeOnGet,
      noDeleteOnStaleGet: s = this.noDeleteOnStaleGet,
      // set options
      ttl: o = this.ttl,
      noDisposeOnSet: a = this.noDisposeOnSet,
      size: l = 0,
      sizeCalculation: u2 = this.sizeCalculation,
      noUpdateTTL: c = this.noUpdateTTL,
      // fetch exclusive options
      noDeleteOnFetchRejection: f2 = this.noDeleteOnFetchRejection,
      allowStaleOnFetchRejection: d = this.allowStaleOnFetchRejection,
      ignoreFetchAbort: h = this.ignoreFetchAbort,
      allowStaleOnFetchAbort: g2 = this.allowStaleOnFetchAbort,
      context: w,
      forceRefresh: k = false,
      status: T,
      signal: x
    } = n;
    if (!this.#_)
      return T && (T.fetch = "get"), this.get(e, {
        allowStale: r,
        updateAgeOnGet: i,
        noDeleteOnStaleGet: s,
        status: T
      });
    const E = {
      allowStale: r,
      updateAgeOnGet: i,
      noDeleteOnStaleGet: s,
      ttl: o,
      noDisposeOnSet: a,
      size: l,
      sizeCalculation: u2,
      noUpdateTTL: c,
      noDeleteOnFetchRejection: f2,
      allowStaleOnFetchRejection: d,
      allowStaleOnFetchAbort: g2,
      ignoreFetchAbort: h,
      status: T,
      signal: x
    };
    let _ = this.#r.get(e);
    if (_ === void 0) {
      T && (T.fetch = "miss");
      const I = this.#D(e, _, E, w);
      return I.__returned = I;
    } else {
      const I = this.#e[_];
      if (this.#t(I)) {
        const b = r && I.__staleWhileFetching !== void 0;
        return T && (T.fetch = "inflight", b && (T.returnedStale = true)), b ? I.__staleWhileFetching : I.__returned = I;
      }
      const S2 = this.#h(_);
      if (!k && !S2)
        return T && (T.fetch = "hit"), this.#k(_), i && this.#N(_), T && this.#O(T, _), I;
      const $2 = this.#D(e, _, E, w), y = $2.__staleWhileFetching !== void 0 && r;
      return T && (T.fetch = S2 ? "stale" : "refresh", y && S2 && (T.returnedStale = true)), y ? $2.__staleWhileFetching : $2.__returned = $2;
    }
  }
  async forceFetch(e, n = {}) {
    const r = await this.fetch(e, n);
    if (r === void 0)
      throw new Error("fetch() returned undefined");
    return r;
  }
  memo(e, n = {}) {
    const r = this.#F;
    if (!r)
      throw new Error("no memoMethod provided to constructor");
    const { context: i, forceRefresh: s, ...o } = n, a = this.get(e, o);
    if (!s && a !== void 0)
      return a;
    const l = r(e, a, {
      options: o,
      context: i
    });
    return this.set(e, l, o), l;
  }
  /**
   * Return a value from the cache. Will update the recency of the cache
   * entry found.
   *
   * If the key is not found, get() will return `undefined`.
   */
  get(e, n = {}) {
    const { allowStale: r = this.allowStale, updateAgeOnGet: i = this.updateAgeOnGet, noDeleteOnStaleGet: s = this.noDeleteOnStaleGet, status: o } = n, a = this.#r.get(e);
    if (a !== void 0) {
      const l = this.#e[a], u2 = this.#t(l);
      return o && this.#O(o, a), this.#h(a) ? (o && (o.get = "stale"), u2 ? (o && r && l.__staleWhileFetching !== void 0 && (o.returnedStale = true), r ? l.__staleWhileFetching : void 0) : (s || this.#I(e, "expire"), o && r && (o.returnedStale = true), r ? l : void 0)) : (o && (o.get = "hit"), u2 ? l.__staleWhileFetching : (this.#k(a), i && this.#N(a), l));
    } else o && (o.get = "miss");
  }
  #M(e, n) {
    this.#d[n] = e, this.#u[e] = n;
  }
  #k(e) {
    e !== this.#s && (e === this.#a ? this.#a = this.#u[e] : this.#M(this.#d[e], this.#u[e]), this.#M(this.#s, e), this.#s = e);
  }
  /**
   * Deletes a key out of the cache.
   *
   * Returns true if the key was deleted, false otherwise.
   */
  delete(e) {
    return this.#I(e, "delete");
  }
  #I(e, n) {
    let r = false;
    if (this.#i !== 0) {
      const i = this.#r.get(e);
      if (i !== void 0)
        if (r = true, this.#i === 1)
          this.#U(n);
        else {
          this.#R(i);
          const s = this.#e[i];
          if (this.#t(s) ? s.__abortController.abort(new Error("deleted")) : (this.#E || this.#c) && (this.#E && this.#g?.(s, e, n), this.#c && this.#o?.push([s, e, n])), this.#r.delete(e), this.#n[i] = void 0, this.#e[i] = void 0, i === this.#s)
            this.#s = this.#d[i];
          else if (i === this.#a)
            this.#a = this.#u[i];
          else {
            const o = this.#d[i];
            this.#u[o] = this.#u[i];
            const a = this.#u[i];
            this.#d[a] = this.#d[i];
          }
          this.#i--, this.#b.push(i);
        }
    }
    if (this.#c && this.#o?.length) {
      const i = this.#o;
      let s;
      for (; s = i?.shift(); )
        this.#m?.(...s);
    }
    return r;
  }
  /**
   * Clear the cache entirely, throwing away all values.
   */
  clear() {
    return this.#U("delete");
  }
  #U(e) {
    for (const n of this.#S({ allowStale: true })) {
      const r = this.#e[n];
      if (this.#t(r))
        r.__abortController.abort(new Error("deleted"));
      else {
        const i = this.#n[n];
        this.#E && this.#g?.(r, i, e), this.#c && this.#o?.push([r, i, e]);
      }
    }
    if (this.#r.clear(), this.#e.fill(void 0), this.#n.fill(void 0), this.#p && this.#T && (this.#p.fill(0), this.#T.fill(0)), this.#v && this.#v.fill(0), this.#a = 0, this.#s = 0, this.#b.length = 0, this.#y = 0, this.#i = 0, this.#c && this.#o) {
      const n = this.#o;
      let r;
      for (; r = n?.shift(); )
        this.#m?.(...r);
    }
  }
};
var Xf = 1024;
var Zf = 36e5;
function lr({ max: t = Xf, ttl: e = Zf } = {}) {
  return new Vr({ max: t, ttl: e });
}
function ed({ documentCache: t = lr(), errorCache: e = lr(), validationCache: n = true }) {
  const r = lr();
  return {
    onParse({ params: i, setParsedDocument: s }) {
      const o = i.source.toString(), a = t.get(o);
      if (a) {
        s(a);
        return;
      }
      const l = e.get(o);
      if (l)
        throw l;
      return ({ result: u2 }) => {
        u2 != null && (u2 instanceof Error ? e.set(o, u2) : t.set(o, u2));
      };
    },
    onValidate({
      params: { schema: i, documentAST: s, rules: o },
      setResult: a
      // eslint-disable-next-line @typescript-eslint/no-empty-object-type
    }) {
      if (i != null && n !== false) {
        const l = o?.map((d) => d.name).join(",") || "";
        let u2 = r.get(l);
        u2 || (u2 = /* @__PURE__ */ new WeakMap(), r.set(l, u2));
        let c = u2.get(i);
        c || (c = /* @__PURE__ */ new WeakMap(), u2.set(i, c));
        const f2 = c.get(s);
        if (f2) {
          a(f2);
          return;
        }
        return ({ result: d }) => {
          d != null && c?.set(s, d);
        };
      }
    }
  };
}
var td = () => true;
function Lt(t) {
  const e = t.match || td;
  return {
    onRequestParse({ request: n, setRequestParser: r }) {
      e(n) && r(t.parse);
    }
  };
}
function nd(t) {
  const e = (t.headers.get("accept") || "*/*").replace(/\s/g, "").toLowerCase().split(","), n = [];
  for (const r of e) {
    const [i, ...s] = r.split(";");
    i === void 0 || (s?.find((a) => a.includes("charset=")) || "charset=utf-8") !== "charset=utf-8" || n.push(i);
  }
  return n.reverse();
}
function rd(t, e) {
  const [n, r] = t.split("/"), [i, s] = e.split("/");
  return (i === "*" || i === n) && (s === "*" || s === r);
}
function qr(t) {
  if (Array.isArray(t))
    return `[${t.map((r) => {
      const i = Ui(r);
      return (r.stringify || JSON.stringify)(i);
    }).join(",")}]`;
  const e = Ui(t);
  return (t.stringify || JSON.stringify)(e);
}
function Ui(t) {
  if (t.errors?.length || t.extensions?.http) {
    const e = { ...t };
    if (e.errors &&= e.errors.map(uo), e.extensions) {
      const { http: n, ...r } = t.extensions;
      e.extensions = Object.keys(r).length ? r : void 0;
    }
    return e;
  }
  return t;
}
function uo(t) {
  if (at(t)) {
    const e = "toJSON" in t && typeof t.toJSON == "function" ? t.toJSON() : Object(t), { http: n, unexpected: r, ...i } = e.extensions || {};
    return F(t.message, {
      nodes: t.nodes,
      source: t.source,
      positions: t.positions,
      path: t.path,
      originalError: uo(t.originalError || void 0),
      extensions: Object.keys(i).length ? i : void 0
    });
  }
  return t;
}
function id(t, e) {
  const r = Ur(t, {
    Connection: "keep-alive",
    "Content-Type": 'multipart/mixed; boundary="-"',
    "Transfer-Encoding": "chunked"
  });
  let i;
  const s = new e.TextEncoder(), o = new e.ReadableStream({
    start(a) {
      if (Ne(t))
        i = t[Symbol.asyncIterator]();
      else {
        let l = false;
        i = {
          next: () => l ? Q({ done: true, value: null }) : (l = true, Q({ done: false, value: t }))
        };
      }
      a.enqueue(s.encode(`\r
`)), a.enqueue(s.encode("---"));
    },
    pull(a) {
      return D(() => i.next(), ({ done: l, value: u2 }) => {
        if (u2 != null) {
          a.enqueue(s.encode(`\r
`)), a.enqueue(s.encode("Content-Type: application/json; charset=utf-8")), a.enqueue(s.encode(`\r
`));
          const c = qr(u2), f2 = s.encode(c);
          a.enqueue(s.encode("Content-Length: " + f2.byteLength)), a.enqueue(s.encode(`\r
`)), a.enqueue(s.encode(`\r
`)), a.enqueue(f2), a.enqueue(s.encode(`\r
`)), a.enqueue(s.encode("---"));
        }
        l && (a.enqueue(s.encode(`--\r
`)), a.close());
      }, (l) => {
        a.error(l);
      });
    },
    cancel(a) {
      if (i.return)
        return D(() => i.return?.(a), () => {
        });
    }
  });
  return new e.Response(o, r);
}
function sd(t, e, n) {
  if (zn(t))
    return new e.Response(null, {
      status: 406,
      statusText: "Not Acceptable",
      headers: {
        accept: "application/json; charset=utf-8, application/graphql-response+json; charset=utf-8"
      }
    });
  const r = {
    "Content-Type": n + "; charset=utf-8"
  }, i = Ur(
    t,
    r,
    // prefer 200 only if accepting application/json and all errors are exclusively GraphQL errors
    n === "application/json" && !Array.isArray(t) && _f(t.errors) && t.errors.some((o) => !o.extensions?.originalError || at(o.extensions.originalError))
  ), s = qr(t);
  return new e.Response(s, i);
}
function od() {
  return function(e, n) {
    let r = 12e3;
    globalThis.process?.env?.NODE_ENV === "test" && (r = 300);
    const s = Ur(e, {
      "Content-Type": "text/event-stream",
      Connection: "keep-alive",
      "Cache-Control": "no-cache",
      "Content-Encoding": "none"
    }, true);
    let o, a;
    const l = new n.TextEncoder(), u2 = new n.ReadableStream({
      start(c) {
        if (c.enqueue(l.encode(`:

`)), a = setInterval(() => {
          if (!c.desiredSize) {
            clearInterval(a);
            return;
          }
          c.enqueue(l.encode(`:

`));
        }, r), Ne(e))
          o = e[Symbol.asyncIterator]();
        else {
          let f2 = false;
          o = {
            next: () => f2 ? Q({ done: true, value: null }) : (f2 = true, Q({ done: false, value: e }))
          };
        }
      },
      pull(c) {
        return D(() => o.next(), (f2) => {
          if (f2.value != null) {
            c.enqueue(l.encode(`event: next
`));
            const d = qr(f2.value);
            c.enqueue(l.encode(`data: ${d}

`));
          }
          f2.done && (c.enqueue(l.encode(`event: complete
`)), c.enqueue(l.encode(`data:

`)), clearInterval(a), c.close());
        }, (f2) => {
          c.error(f2);
        });
      },
      cancel(c) {
        if (clearInterval(a), o.return)
          return D(() => o.return?.(c), () => {
          });
      }
    });
    return new n.Response(u2, s);
  };
}
var ad = {
  mediaTypes: ["multipart/mixed"],
  asyncIterables: true,
  processResult: id
};
function ld() {
  return {
    mediaTypes: ["text/event-stream"],
    asyncIterables: true,
    processResult: od()
  };
}
var Vi = {
  mediaTypes: ["application/graphql-response+json", "application/json"],
  asyncIterables: false,
  processResult: sd
};
function ud() {
  const t = /* @__PURE__ */ new WeakMap(), e = ld(), n = [e, ad, Vi], r = [e, Vi];
  return {
    onSubscribe({ args: { contextValue: i } }) {
      i.request && t.set(i.request, true);
    },
    onResultProcess({ request: i, result: s, acceptableMediaTypes: o, setResultProcessor: a }) {
      const u2 = t.get(i) ? r : n, c = nd(i), f2 = Ne(s);
      for (const d of u2)
        for (const h of c)
          if (!(f2 && !d.asyncIterables))
            for (const g2 of d.mediaTypes)
              o.push(g2), rd(g2, h) && a(d.processResult, g2);
    }
  };
}
var cd = `<!doctype html><html lang=en><head><meta charset=utf-8><title>Welcome to GraphQL Yoga</title><link rel=icon href=https://raw.githubusercontent.com/graphql-hive/graphql-yoga/main/website/public/favicon.ico><style>body,html{padding:0;margin:0;height:100%;font-family:Inter,-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,Oxygen,Ubuntu,Cantarell,'Fira Sans','Droid Sans','Helvetica Neue',sans-serif;color:#fff;background-color:#000}main>section.hero{display:flex;height:90vh;justify-content:center;align-items:center;flex-direction:column}.logo{display:flex;align-items:center}.buttons{margin-top:24px}h1{font-size:80px}h2{color:#888;max-width:50%;margin-top:0;text-align:center}a{color:#fff;text-decoration:none;margin-left:10px;margin-right:10px;font-weight:700;transition:color .3s ease;padding:4px;overflow:visible}a.graphiql:hover{color:rgba(255,0,255,.7)}a.docs:hover{color:rgba(28,200,238,.7)}a.tutorial:hover{color:rgba(125,85,245,.7)}svg{margin-right:24px}.not-what-your-looking-for{margin-top:5vh}.not-what-your-looking-for>*{margin-left:auto;margin-right:auto}.not-what-your-looking-for>p{text-align:center}.not-what-your-looking-for>h2{color:#464646}.not-what-your-looking-for>p{max-width:600px;line-height:1.3em}.not-what-your-looking-for>pre{max-width:300px}</style></head><body id=body><main><section class=hero><div class=logo><div><svg xmlns=http://www.w3.org/2000/svg viewBox="-0.41 0.445 472.812 499.811" height=150><defs><linearGradient id=paint0_linear_1677_11483 x1=16 y1=14 x2=87.2132 y2=44.5982 gradientUnits=userSpaceOnUse gradientTransform="matrix(8.139854, 0, 0, 8.139854, -130.346407, -113.25101)"><stop stop-color=#7433FF /><stop offset=1 stop-color=#FFA3FD /></linearGradient><linearGradient id=paint1_linear_1677_11483 x1=16 y1=14 x2=87.2132 y2=44.5982 gradientUnits=userSpaceOnUse gradientTransform="matrix(8.139854, 0, 0, 8.139854, -130.346407, -113.25101)"><stop stop-color=#7433FF /><stop offset=1 stop-color=#FFA3FD /></linearGradient><linearGradient id=paint2_linear_1677_11483 x1=16 y1=14 x2=87.2132 y2=44.5982 gradientUnits=userSpaceOnUse gradientTransform="matrix(8.139854, 0, 0, 8.139854, -130.346407, -113.25101)"><stop stop-color=#7433FF /><stop offset=1 stop-color=#FFA3FD /></linearGradient><linearGradient id=paint3_linear_1677_11483 x1=16 y1=14 x2=87.2132 y2=44.5982 gradientUnits=userSpaceOnUse><stop stop-color=#7433FF /><stop offset=1 stop-color=#FFA3FD /></linearGradient><linearGradient id=paint4_linear_1677_11483 x1=16 y1=14 x2=87.2132 y2=44.5982 gradientUnits=userSpaceOnUse><stop stop-color=#7433FF /><stop offset=1 stop-color=#FFA3FD /></linearGradient><linearGradient id=paint5_linear_1677_11483 x1=16 y1=14 x2=87.2132 y2=44.5982 gradientUnits=userSpaceOnUse><stop stop-color=#7433FF /><stop offset=1 stop-color=#FFA3FD /></linearGradient><filter id=filter0_f_1677_11483 x=23 y=-25 width=100 height=100 filterUnits=userSpaceOnUse color-interpolation-filters=sRGB><feFlood flood-opacity=0 result=BackgroundImageFix /><feBlend mode=normal in=SourceGraphic in2=BackgroundImageFix result=shape /><feGaussianBlur stdDeviation=12 result=effect1_foregroundBlur_1677_11483 /></filter><filter id=filter1_f_1677_11483 x=-24 y=19 width=100 height=100 filterUnits=userSpaceOnUse color-interpolation-filters=sRGB><feFlood flood-opacity=0 result=BackgroundImageFix /><feBlend mode=normal in=SourceGraphic in2=BackgroundImageFix result=shape /><feGaussianBlur stdDeviation=12 result=effect1_foregroundBlur_1677_11483 /></filter><linearGradient id=paint6_linear_1677_11483 x1=30 y1=28 x2=66.1645 y2=44.4363 gradientUnits=userSpaceOnUse gradientTransform="matrix(8.139854, 0, 0, 8.139854, -130.346407, -113.25101)"><stop stop-color=#7433FF /><stop offset=1 stop-color=#FFA3FD /></linearGradient><filter id=filter2_f_1677_11483 x=-12 y=-44 width=100 height=100 filterUnits=userSpaceOnUse color-interpolation-filters=sRGB><feFlood flood-opacity=0 result=BackgroundImageFix /><feBlend mode=normal in=SourceGraphic in2=BackgroundImageFix result=shape /><feGaussianBlur stdDeviation=12 result=effect1_foregroundBlur_1677_11483 /></filter><filter id=filter3_f_1677_11483 x=13 y=19 width=100 height=100 filterUnits=userSpaceOnUse color-interpolation-filters=sRGB><feFlood flood-opacity=0 result=BackgroundImageFix /><feBlend mode=normal in=SourceGraphic in2=BackgroundImageFix result=shape /><feGaussianBlur stdDeviation=12 result=effect1_foregroundBlur_1677_11483 /></filter></defs><mask id=mask0_1677_11483 style=mask-type:alpha maskUnits=userSpaceOnUse x=16 y=14 width=58 height=62><path d="M21 25.3501C21.7279 25.3501 22.4195 25.5056 23.0433 25.7853L42.1439 14.8C43.0439 14.3 44.1439 14 45.1439 14C46.2439 14 47.2439 14.3 48.1439 14.8L64.5439 24.3C63.3439 25.1 62.4439 26.3 61.8439 27.7L45.9438 18.5C45.6439 18.3 45.344 18.3 45.0441 18.3C44.7441 18.3 44.4439 18.4 44.1439 18.5L25.8225 29.0251C25.9382 29.4471 26 29.8914 26 30.3501C26 33.1115 23.7614 35.3501 21 35.3501C18.2386 35.3501 16 33.1115 16 30.3501C16 27.5887 18.2386 25.3501 21 25.3501Z" fill=url(#paint3_linear_1677_11483) /><path d="M67.2438 35.0329C65.3487 34.3219 64 32.4934 64 30.35C64 27.5886 66.2386 25.35 69 25.35C71.7614 25.35 74 27.5886 74 30.35C74 32.1825 73.0142 33.7848 71.5439 34.6554V55.2C71.5439 57.4 70.3439 59.4 68.5439 60.5L52.1439 69.9C52.1439 68.4 51.6438 66.9 50.7438 65.8L66.3439 56.8C66.9439 56.5 67.2438 55.9 67.2438 55.2V35.0329Z" fill=url(#paint4_linear_1677_11483) /><path d="M49.8439 69.1055C49.9458 69.5034 50 69.9204 50 70.3501C50 73.1115 47.7614 75.3501 45 75.3501C42.5102 75.3501 40.4454 73.5302 40.0633 71.1481L21.8439 60.6C19.9439 59.5 18.8439 57.5 18.8439 55.3V36.8C19.5439 37 20.3439 37.2 21.0439 37.2C21.7439 37.2 22.4439 37.1 23.0439 36.9V55.3C23.0439 56 23.4438 56.6 23.9438 56.9L41.3263 66.9583C42.2398 65.9694 43.5476 65.3501 45 65.3501C47.3291 65.3501 49.2862 66.9426 49.8419 69.0981L49.8436 69.0997L49.8439 69.1055Z" fill=url(#paint5_linear_1677_11483) /></mask><mask id=mask1_1677_11483 style=mask-type:alpha maskUnits=userSpaceOnUse x=30 y=28 width=30 height=30><path fill-rule=evenodd clip-rule=evenodd d="M49.3945 32.3945C49.3945 34.7088 47.5796 38.5469 45 38.5469C42.4271 38.5469 40.6055 34.7112 40.6055 32.3945C40.6055 29.9714 42.5769 28 45 28C47.4231 28 49.3945 29.9714 49.3945 32.3945ZM35.332 49.0433V48.2148C35.332 42.8117 37.8535 41.0004 39.8796 39.545L39.8801 39.5447C40.3928 39.1767 40.8604 38.8404 41.2488 38.4742C42.3293 39.6642 43.626 40.3047 45 40.3047C46.3752 40.3047 47.6725 39.6642 48.7529 38.4754C49.1408 38.841 49.6078 39.1773 50.1199 39.5447L50.1204 39.545C52.1465 41.0004 54.668 42.8117 54.668 48.2148V49.0433L53.8406 49.092C49.9848 49.3185 46.8646 46.9002 45 43.5777C43.1159 46.935 39.9847 49.318 36.1594 49.092L35.332 49.0433ZM58.1463 51.0747L58.1463 51.0746C57.0179 50.891 50.0128 49.7507 45.0007 55.693C40.0116 49.7553 33.1965 50.8592 31.9095 51.0677L31.9095 51.0677C31.7906 51.087 31.7189 51.0986 31.7002 51.0963C31.7005 51.0969 31.7011 51.1045 31.7023 51.1187C31.726 51.4003 31.9682 54.2745 34.0566 56.2422L30 58H60L55.8956 56.2422C57.8537 54.4764 58.1396 52.2685 58.2508 51.4092V51.4091C58.2697 51.2628 58.2836 51.1556 58.2998 51.0963C58.2881 51.0977 58.2356 51.0892 58.1463 51.0747ZM40.4836 50.104C42.3956 49.3212 43.6746 48.1737 45 46.61C46.332 48.1841 47.6159 49.3259 49.5164 50.104C49.5356 50.1425 49.5557 50.1805 49.5756 50.2182C49.5793 50.2253 49.583 50.2323 49.5867 50.2393C48.0911 50.8127 46.4264 51.825 45.0047 53.1444C43.5906 51.8221 41.9673 50.8196 40.4256 50.2153C40.4455 50.1784 40.4648 50.1415 40.4836 50.104Z" fill=black /></mask><path d="M 40.59 93.095 C 46.517 93.095 52.14 94.365 57.22 96.635 L 212.7 7.22 C 220.025 3.149 228.978 0.706 237.12 0.706 C 246.073 0.706 254.213 3.149 261.54 7.22 L 395.032 84.547 C 385.264 91.059 377.939 100.827 373.055 112.224 L 243.631 37.338 C 241.19 35.71 238.747 35.71 236.305 35.71 C 233.863 35.71 231.42 36.523 228.978 37.338 L 79.84 123.009 C 80.786 126.443 81.29 130.058 81.29 133.793 C 81.29 156.269 63.065 174.493 40.59 174.493 C 18.116 174.493 -0.109 156.269 -0.109 133.793 C -0.109 111.32 18.116 93.095 40.59 93.095 Z" fill=url(#paint0_linear_1677_11483) /><path d="M 417.01 171.913 C 401.585 166.126 390.603 151.238 390.603 133.793 C 390.603 111.32 408.83 93.095 431.303 93.095 C 453.777 93.095 472.001 111.32 472.001 133.793 C 472.001 148.706 463.976 161.755 452.011 168.835 L 452.011 336.07 C 452.011 353.977 442.243 370.258 427.591 379.21 L 294.098 455.726 C 294.098 443.516 290.029 431.306 282.703 422.353 L 409.683 349.093 C 414.568 346.651 417.01 341.767 417.01 336.07 L 417.01 171.913 Z" fill=url(#paint1_linear_1677_11483) /><path d="M 275.376 449.253 C 276.206 452.495 276.646 455.889 276.646 459.389 C 276.646 481.863 258.422 500.087 235.947 500.087 C 215.679 500.087 198.87 485.272 195.761 465.883 L 47.46 380.025 C 31.995 371.071 23.041 354.792 23.041 336.884 L 23.041 186.296 C 28.738 187.923 35.25 189.553 40.948 189.553 C 46.646 189.553 52.345 188.738 57.228 187.111 L 57.228 336.884 C 57.228 342.582 60.485 347.465 64.554 349.908 L 206.042 431.777 C 213.481 423.728 224.127 418.689 235.947 418.689 C 254.905 418.689 270.833 431.656 275.36 449.196 L 275.376 449.214 L 275.376 449.253 Z" fill=url(#paint2_linear_1677_11483) /><g mask=url(#mask0_1677_11483) transform="matrix(8.139854, 0, 0, 8.139854, -130.346375, -113.251038)"><g filter=url(#filter0_f_1677_11483)><circle cx=73 cy=25 r=26 fill=#ED2E7E /></g><g filter=url(#filter1_f_1677_11483)><circle cx=26 cy=69 r=26 fill=#1CC8EE /></g></g><path fill-rule=evenodd clip-rule=evenodd d="M 271.713 150.431 C 271.713 169.275 256.948 200.517 235.947 200.517 C 215.003 200.517 200.172 169.292 200.172 150.431 C 200.172 130.708 216.225 114.666 235.947 114.666 C 255.67 114.666 271.713 130.708 271.713 150.431 Z M 157.251 285.952 L 157.251 279.212 C 157.251 235.233 177.771 220.485 194.27 208.641 C 198.447 205.644 202.247 202.901 205.414 199.923 C 214.204 209.608 224.763 214.826 235.947 214.826 C 247.138 214.826 257.697 209.608 266.496 199.931 C 269.653 202.911 273.456 205.644 277.622 208.641 C 294.114 220.485 314.642 235.233 314.642 279.212 L 314.642 285.952 L 307.912 286.351 C 276.525 288.191 251.128 268.509 235.947 241.468 C 220.611 268.795 195.126 288.191 163.981 286.351 L 157.251 285.952 Z M 342.953 302.492 C 333.771 300.994 276.751 291.715 235.955 340.082 C 195.345 291.749 139.865 300.734 129.389 302.436 C 128.428 302.59 127.841 302.688 127.687 302.665 C 127.687 302.673 127.695 302.729 127.702 302.85 C 127.897 305.138 129.867 328.532 146.872 344.55 L 113.849 358.862 L 358.044 358.862 L 324.639 344.55 C 340.576 330.177 342.905 312.202 343.807 305.212 C 343.962 304.022 344.077 303.153 344.206 302.665 C 344.108 302.68 343.686 302.606 342.953 302.492 Z M 199.188 294.59 C 214.751 288.215 225.161 278.879 235.947 266.15 C 246.788 278.96 257.241 288.255 272.707 294.59 C 272.869 294.898 273.031 295.207 273.196 295.518 C 273.219 295.574 273.252 295.631 273.285 295.688 C 261.107 300.361 247.555 308.598 235.989 319.334 C 224.477 308.573 211.258 300.417 198.715 295.493 C 198.87 295.191 199.033 294.891 199.188 294.59 Z" fill=url(#paint6_linear_1677_11483) /><g mask=url(#mask1_1677_11483) transform="matrix(8.139854, 0, 0, 8.139854, -130.346375, -113.251038)"><g filter=url(#filter2_f_1677_11483)><circle cx=38 cy=6 r=26 fill=#ED2E7E /></g><g filter=url(#filter3_f_1677_11483)><circle cx=63 cy=69 r=26 fill=#1CC8EE /></g></g></svg></div><h1>GraphQL Yoga</h1><p>Version: 5.13.5</p></div><h2>The batteries-included cross-platform GraphQL Server.</h2><div class=buttons><a href=https://www.the-guild.dev/graphql/yoga-server/docs class=docs>Read the Docs</a> <a href=https://www.the-guild.dev/graphql/yoga-server/tutorial/basic class=tutorial>Start the Tutorial </a><a href=__GRAPHIQL_LINK__ class=graphiql>Visit GraphiQL</a></div></section><section class=not-what-your-looking-for><h2>Not the page you are looking for? 👀</h2><p>This page is shown be default whenever a 404 is hit.<br>You can disable this by behavior via the <code>landingPage</code> option.</p><pre>
          <code>
import { createYoga } from 'graphql-yoga';

const yoga = createYoga({
  landingPage: false
})
          </code>
        </pre><p>If you expected this page to be the GraphQL route, you need to configure Yoga. Currently, the GraphQL route is configured to be on <code>__GRAPHIQL_LINK__</code>.</p><pre>
          <code>
import { createYoga } from 'graphql-yoga';

const yoga = createYoga({
  graphqlEndpoint: '__REQUEST_PATH__',
})
          </code>
        </pre></section></main></body></html>`;
var fd = function(e) {
  return new e.fetchAPI.Response(cd.replace(/__GRAPHIQL_LINK__/g, e.graphqlEndpoint).replace(/__REQUEST_PATH__/g, e.url.pathname), {
    status: 200,
    statusText: "OK",
    headers: {
      "Content-Type": "text/html"
    }
  });
};
function dd(t) {
  let e;
  function n({ URLPattern: i }) {
    return e ||= new i({
      pathname: t.graphqlEndpoint
    }), e;
  }
  const r = t.landingPageRenderer || fd;
  return {
    onRequest({ request: i, fetchAPI: s, endResponse: o, url: a }) {
      if (!i.url.endsWith(t.graphqlEndpoint) && !i.url.endsWith(`${t.graphqlEndpoint}/`) && a.pathname !== t.graphqlEndpoint && a.pathname !== `${t.graphqlEndpoint}/` && !n(s).test(a)) {
        if (t.showLandingPage === true && i.method === "GET" && i.headers?.get("accept")?.includes("text/html")) {
          const l = r({
            request: i,
            fetchAPI: s,
            url: a,
            graphqlEndpoint: t.graphqlEndpoint,
            get urlPattern() {
              return n(s);
            }
          });
          if (B(l))
            return l.then(o);
          o(l);
          return;
        }
        o(new s.Response("", {
          status: 404,
          statusText: "Not Found"
        }));
      }
    }
  };
}
function ur({ request: t, result: e, fetchAPI: n, onResultProcessHooks: r, serverContext: i }) {
  let s;
  const o = [];
  let a = "*/*";
  return D(() => nt(r, (l) => l({
    request: t,
    acceptableMediaTypes: o,
    result: e,
    setResult(u2) {
      e = u2;
    },
    resultProcessor: s,
    setResultProcessor(u2, c) {
      s = u2, a = c;
    },
    serverContext: i
  })), () => s ? s(e, n, a) : new n.Response(null, {
    status: 406,
    statusText: "Not Acceptable",
    headers: {
      accept: o.join("; charset=utf-8, ")
    }
  }));
}
function pd({ params: t, enveloped: e }) {
  const n = e.parse(t.query), r = e.validate(e.schema, n);
  return r.length > 0 ? { errors: r } : D(() => e.contextFactory(), (i) => {
    const s = {
      schema: e.schema,
      document: n,
      contextValue: i,
      variableValues: t.variables,
      operationName: t.operationName
    };
    return (xr2(n, t.operationName)?.operation === "subscription" ? e.subscribe : e.execute)(s);
  });
}
function qi(t) {
  return at(t) ? t.toJSON() : t instanceof Error ? {
    message: t.message,
    stack: t.stack,
    cause: t.cause
  } : t;
}
var hd = (t, e, n = globalThis.process?.env?.NODE_ENV === "development") => {
  if (Mr(t))
    return t;
  const r = {
    code: "INTERNAL_SERVER_ERROR",
    unexpected: true
  }, i = {
    extensions: r
  };
  return at(t) ? (i.nodes = t.nodes, i.source = t.source, i.positions = t.positions, i.path = t.path, n && t.originalError && (r.originalError = qi(t.originalError)), t.extensions?.http && (r.http = t.extensions.http)) : n && (r.originalError = qi(t)), F(e, i);
};
var gd = class {
  /**
   * Instance of envelop
   */
  getEnveloped;
  logger;
  graphqlEndpoint;
  fetchAPI;
  plugins;
  instrumentation;
  onRequestParseHooks;
  onParamsHooks;
  onExecutionResultHooks;
  onResultProcessHooks;
  maskedErrorsOpts;
  id;
  version = "5.13.5";
  constructor(e) {
    if (this.id = e?.id ?? "yoga", this.fetchAPI = {
      ...eo
    }, e?.fetchAPI)
      for (const l in e.fetchAPI)
        e.fetchAPI[l] && (this.fetchAPI[l] = e.fetchAPI[l]);
    const n = e?.logging == null ? true : e.logging;
    this.logger = typeof n == "boolean" ? n === true ? or() : or("silent") : typeof n == "string" ? or(n) : n;
    const r = typeof e?.maskedErrors == "object" && e.maskedErrors.maskError || hd, i = /* @__PURE__ */ new WeakSet();
    this.maskedErrorsOpts = e?.maskedErrors === false ? null : {
      errorMessage: "Unexpected error.",
      ...typeof e?.maskedErrors == "object" ? e.maskedErrors : {},
      maskError: (l, u2) => {
        if (i.has(l))
          return l;
        const c = r(l, u2, this.maskedErrorsOpts?.isDev);
        return c !== l && this.logger.error(l), i.add(c), c;
      }
    };
    const s = this.maskedErrorsOpts == null ? null : this.maskedErrorsOpts;
    let o = 0;
    e?.batching && (typeof e.batching == "boolean" ? o = 10 : o = e.batching.limit ?? 10), this.graphqlEndpoint = e?.graphqlEndpoint || "/graphql";
    const a = this.graphqlEndpoint;
    this.plugins = [
      Yl({
        parse: Yo,
        validate: wl,
        execute: Fi,
        subscribe: Fi,
        specifiedRules: Is
      }),
      // Use the schema provided by the user
      !!e?.schema && ec(e.schema),
      e?.context != null && Jl((l) => e?.context ? typeof e.context == "function" ? e.context(l) : e.context : {}),
      // Middlewares before processing the incoming HTTP request
      Jf({
        id: this.id,
        logger: this.logger,
        endpoint: e?.healthCheckEndpoint
      }),
      e?.cors !== false && Sf(e?.cors),
      e?.graphiql !== false && Zu({
        graphqlEndpoint: a,
        options: e?.graphiql,
        render: e?.renderGraphiQL,
        logger: this.logger
      }),
      // Middlewares before the GraphQL execution
      Lt({
        match: Rf,
        parse: kf
      }),
      Lt({
        match: Df,
        parse: Lf
      }),
      e?.multipart !== false && Lt({
        match: Pf,
        parse: jf
      }),
      Lt({
        match: Cf,
        parse: xf
      }),
      Lt({
        match: Af,
        parse: Ff
      }),
      // Middlewares after the GraphQL execution
      ud(),
      ...e?.plugins ?? [],
      // To make sure those are called at the end
      {
        onPluginInit({ addPlugin: l }) {
          e?.parserAndValidationCache !== false && l(
            // @ts-expect-error Add plugins has context but this hook doesn't care
            ed(!e?.parserAndValidationCache || e?.parserAndValidationCache === true ? {} : e?.parserAndValidationCache)
          ), l(Kf(o)), l(qf(e?.extraParamNames));
          const u2 = !!(e?.landingPage ?? true);
          l(
            // @ts-expect-error Add plugins has context but this hook doesn't care
            dd({
              graphqlEndpoint: a,
              showLandingPage: u2,
              landingPageRenderer: typeof e?.landingPage == "function" ? e.landingPage : void 0
            })
          ), l(Qf()), l(zf()), s && (l({
            onSubscribe() {
              return {
                onSubscribeError({ error: c }) {
                  if (io(c))
                    throw c;
                }
              };
            }
          }), l(zl(s))), l(
            // We handle validation errors at the end
            Hf()
          );
        }
      }
    ], this.getEnveloped = Ul({
      plugins: this.plugins
    }), this.plugins = this.getEnveloped._plugins, this.onRequestParseHooks = [], this.onParamsHooks = [], this.onExecutionResultHooks = [], this.onResultProcessHooks = [];
    for (const l of this.plugins)
      l && (l.onYogaInit && l.onYogaInit({
        yoga: this
      }), l.onRequestParse && this.onRequestParseHooks.push(l.onRequestParse), l.onParams && this.onParamsHooks.push(l.onParams), l.onExecutionResult && this.onExecutionResultHooks.push(l.onExecutionResult), l.onResultProcess && this.onResultProcessHooks.push(l.onResultProcess), l.instrumentation && (this.instrumentation = this.instrumentation ? Dr(this.instrumentation, l.instrumentation) : l.instrumentation));
  }
  handleParams = ({ request: e, context: n, params: r }) => {
    const i = n.request === e ? {
      params: r
    } : {
      request: e,
      params: r
    };
    Object.assign(n, i);
    const s = this.getEnveloped(n);
    return this.logger.debug("Processing GraphQL Parameters"), D(() => D(() => pd({ params: r, enveloped: s }), (o) => (this.logger.debug("Processing GraphQL Parameters done."), o), (o) => ({
      errors: vn(o, this.maskedErrorsOpts, this.logger)
    })), (o) => (Ne(o) && (o = Ps2(o, (a) => a, (a) => {
      if (a.name === "AbortError")
        throw this.logger.debug("Request aborted"), a;
      return {
        errors: vn(a, this.maskedErrorsOpts, this.logger)
      };
    })), o));
  };
  getResultForParams = ({ params: e, request: n }, r) => {
    let i, s = this.handleParams;
    return D(() => nt(this.onParamsHooks, (o) => o({
      params: e,
      request: n,
      setParams(a) {
        e = a;
      },
      paramsHandler: s,
      setParamsHandler(a) {
        s = a;
      },
      setResult(a) {
        i = a;
      },
      fetchAPI: this.fetchAPI,
      context: r
    })), () => D(() => i || s({
      request: n,
      params: e,
      context: r
    }), (o) => D(() => nt(this.onExecutionResultHooks, (a) => a({
      result: o,
      setResult(l) {
        o = l;
      },
      request: n,
      context: r
    })), () => o)));
  };
  parseRequest = (e, n) => {
    let r = new Proxy({}, {
      get: (o, a, l) => (r = new this.fetchAPI.URL(e.url, "http://localhost"), Reflect.get(r, a, r))
    }), i;
    const s = [];
    return D(() => nt(this.onRequestParseHooks, (o) => D(() => o({
      request: e,
      url: r,
      requestParser: i,
      serverContext: n,
      setRequestParser(a) {
        i = a;
      }
    }), (a) => a?.onRequestParseDone), s), () => (this.logger.debug("Parsing request to extract GraphQL parameters"), i ? D(() => i(e), (o) => D(() => nt(s, (a) => a({
      requestParserResult: o,
      setRequestParserResult(l) {
        o = l;
      }
    })), () => ({
      requestParserResult: o
    }))) : {
      response: new this.fetchAPI.Response(null, {
        status: 415,
        statusText: "Unsupported Media Type"
      })
    }));
  };
  handle = (e, n) => {
    const r = this.instrumentation && Rn({ request: e }), i = this.instrumentation?.requestParse ? r.asyncFn(this.instrumentation?.requestParse, this.parseRequest) : this.parseRequest;
    return Lr(Q().then(() => i(e, n)).then(({ response: s, requestParserResult: o }) => {
      if (s)
        return s;
      const a = this.instrumentation?.operation ? (l, u2) => Rn({ context: u2, request: l.request }).asyncFn(this.instrumentation?.operation, this.getResultForParams)(l, u2) : this.getResultForParams;
      return D(() => Array.isArray(o) ? Promise.all(o.map((l) => a({
        params: l,
        request: e
      }, Object.create(n)))) : a({
        params: o,
        request: e
      }, n), (l) => (this.instrumentation?.resultProcess ? r.asyncFn(this.instrumentation.resultProcess, ur) : ur)({
        request: e,
        result: l,
        fetchAPI: this.fetchAPI,
        onResultProcessHooks: this.onResultProcessHooks,
        serverContext: n
      }));
    }).catch((s) => {
      const a = {
        errors: vn(s, this.maskedErrorsOpts, this.logger)
      };
      return ur({
        request: e,
        result: a,
        fetchAPI: this.fetchAPI,
        onResultProcessHooks: this.onResultProcessHooks,
        serverContext: n
      });
    }));
  };
};
function md(t) {
  const e = new gd(t);
  return Tf(e, {
    fetchAPI: e.fetchAPI,
    plugins: e.plugins,
    disposeOnProcessTerminate: t.disposeOnProcessTerminate
  });
}
function Xt(t, e) {
  var n = {};
  for (var r in t) Object.prototype.hasOwnProperty.call(t, r) && e.indexOf(r) < 0 && (n[r] = t[r]);
  if (t != null && typeof Object.getOwnPropertySymbols == "function") {
    var i = 0;
    for (r = Object.getOwnPropertySymbols(t); i < r.length; i++) e.indexOf(r[i]) < 0 && Object.prototype.propertyIsEnumerable.call(t, r[i]) && (n[r[i]] = t[r[i]]);
  }
  return n;
}
function yd(t, e) {
  var n = {};
  for (var r in t) Object.prototype.hasOwnProperty.call(t, r) && e.indexOf(r) < 0 && (n[r] = t[r]);
  if (t != null && typeof Object.getOwnPropertySymbols == "function") {
    var i = 0;
    for (r = Object.getOwnPropertySymbols(t); i < r.length; i++) e.indexOf(r[i]) < 0 && Object.prototype.propertyIsEnumerable.call(t, r[i]) && (n[r[i]] = t[r[i]]);
  }
  return n;
}
var bd = (t = {}, e, n, r) => {
  const i = `/${e}`, s = typeof t["/**"] == "function" ? t["/**"]({ key: e, path: n, value: r }) : t["/**"], o = typeof t["/*"] == "function" ? t["/*"]({ key: e, path: n, value: r }) : t["/*"];
  let a = {};
  if (i in t) a = t[i];
  else if (!s && !o) return;
  return a = typeof a == "function" ? a({ key: e, path: n, value: r }) : a, o && (a = Object.assign(Object.assign({}, o), a)), s ? Object.assign(Object.assign({ "/**": t["/**"] }, s), a) : a;
};
var co = (t) => {
  const e = {}, n = t.reduce((r, i) => (Object.keys(i).forEach((s) => r.add(s)), r), /* @__PURE__ */ new Set());
  for (const r of n.keys()) {
    const i = t.filter((s) => r in s);
    if (i.length !== 1) {
      if (r.charAt(0) !== "/") throw new Error(`Cannot merge rules. Duplicate key: ${r}. Rules should not have same Rule key`);
      e[r] = (s, o) => {
        const a = i.map((l) => typeof l[r] == "function" ? l[r](s, o) : l[r]);
        return co(a);
      };
    } else e[r] = i[0][r];
  }
  return e;
};
var De = (t) => typeof t == "object" && t !== null;
var cr = (t) => Array.isArray(t);
var vd = (t, e, n = {}) => {
  var r, i;
  e = cr(e) ? e : [e];
  const s = cr(n.rules) ? co(n.rules) : n.rules, o = [{ data: t, state: n.state, path: [], keys: [], keyIndex: -1, rules: s }];
  for (; o.length > 0; ) {
    const a = o[o.length - 1];
    if (a.keyIndex >= a.keys.length) {
      for (; !((r = a.hooks) === null || r === void 0) && r.length; ) a.hooks.pop()();
      o.pop();
      continue;
    }
    const l = a.keys[a.keyIndex++], [u2, c, f2] = o.length > 1 ? [a.data[l], [...a.path, l], bd(a.rules, l, [...a.path, l], a.data[l])] : [a.data, a.path, s];
    let d = { value: u2, path: c, key: l, state: a.state, rules: f2 };
    const h = [];
    for (const g2 of e) {
      if (!g2 || typeof g2 != "function") continue;
      const w = (i = g2(d)) !== null && i !== void 0 ? i : {}, { terminate: k, done: T, exitHook: x } = w, E = yd(w, ["terminate", "done", "exitHook"]);
      if (k) return;
      if (x && h.push(x), d = Object.assign(Object.assign({}, d), E), T) {
        d = null;
        break;
      }
    }
    if (d && De(d.value)) {
      const g2 = cr(d.value) ? [...d.value.keys()] : Object.keys(d.value);
      o.push({ hooks: h, state: d.state, data: d.value, path: c, keys: g2, keyIndex: 0, rules: d.rules });
    } else for (; h.length; ) h.pop()();
  }
};
var Td = () => {
  const t = /* @__PURE__ */ new WeakMap();
  let e;
  return [({ value: n }) => {
    e = n;
  }, ({ value: n, path: r, key: i, state: s }) => {
    if (i = r.length ? i : "#", De(e) && De(n)) {
      if (t.has(e)) return s.node[i] = t.get(e), { done: true };
      const o = Array.isArray(n) ? [] : {};
      s.node[i] = o, t.set(e, o);
    } else s.node[i] = n;
    return { value: n, state: Object.assign(Object.assign({}, s), { node: s.node[i] }) };
  }];
};
var fo = class extends Map {
  add(e, n) {
    const r = this.get(e);
    return r ? r.push(n) : this.set(e, [n]), this;
  }
};
var Ed = (t, e) => {
  const n = {};
  for (const r of e) r in t && (n[r] = t[r], delete t[r]);
  return n;
};
var po = (t, e) => {
  if (Array.isArray(t) && Array.isArray(e)) return [...t, ...e];
  if (De(t) && De(e)) {
    const n = Object.assign({}, t);
    for (const r of Object.keys(e)) n[r] = po(n[r], e[r]);
    return n;
  }
  return e;
};
var wr = (t) => t && t.$ref && typeof t.$ref == "string";
var wd = (t) => t && t.anyOf && Array.isArray(t.anyOf);
var Sd = (t) => t && t.oneOf && Array.isArray(t.oneOf);
var ho = (t, e = "") => {
  const [n = e, r] = t.split("#"), i = r && r !== "/" ? r : "";
  return { filePath: n, pointer: i, normalized: Id(n, i), jsonPath: mo(i) };
};
var Id = (t, e) => t ? `${t}${e ? "#" + e : ""}` : e ? `#${e}` : "#";
var go = (t, e, n = [e]) => {
  if (!De(t)) return;
  let r = t;
  const i = mo(e);
  for (const s of i) if (Array.isArray(r) && r.length > +s) r = r[+s];
  else if (De(r) && s in r) r = r[s];
  else {
    if (!wr(r)) return;
    {
      const o = ho(r.$ref);
      r = o.filePath || n.includes(o.pointer) ? void 0 : go(t, o.pointer, [...n, o.pointer]);
    }
  }
  return r;
};
var Ln = { slash: /\//g, tilde: /~/g, escapedSlash: /~1/g, escapedTilde: /~0/g };
var mo = (t) => t.split("/").map((e) => decodeURIComponent(e.replace(Ln.escapedSlash, "/").replace(Ln.escapedTilde, "~"))).slice(1);
var Br = (t) => t.length ? "/" + t.map((e) => encodeURIComponent(String(e).replace(Ln.tilde, "~0").replace(Ln.slash, "~1"))).join("/") : "";
var Od = (t, e) => JSON.stringify(t) == JSON.stringify(e);
var _d = (t) => {
  let e = 1;
  for (; t * e % 1 != 0; ) e *= 10;
  return e;
};
function Nd(t) {
  const e = t.reduce((n, r) => Math.max(n, _d(r)), 0);
  return t.reduce((n, r) => Math.round(n * e * r * e / yo(n * e, r * e)) / e);
}
function yo(t, e) {
  return e === 0 ? t : yo(e, t % e);
}
var bo = (t) => {
  if (t.length === 0) return [[]];
  const e = t[0], n = t.slice(1), r = bo(n), i = [];
  for (const s of e) for (const o of r) i.push([s, ...o]);
  return i;
};
var Rd = (t) => {
  const e = [];
  for (const n of t) {
    if (!("properties" in n)) continue;
    const r = new Set(Object.keys(n.properties)), i = new fo();
    for (const o of r.values()) for (const a of t) if (!(a == n || a.properties && o in a.properties)) if ("patternProperties" in a && a.patternProperties) for (const l of Object.keys(a.patternProperties)) new RegExp(l).test(o) || r.delete(o);
    else "additionalProperties" in a && (a.additionalProperties === false ? r.delete(o) : typeof a.additionalProperties == "object" && i.add(o, a.additionalProperties));
    if (!r.size) continue;
    const s = {};
    for (const o of r.values()) i.has(o) ? s[o] = { allOf: [n.properties[o], ...i.get(o)] } : s[o] = n.properties[o];
    e.push(s);
  }
  return e;
};
var kd = (t) => {
  const e = [];
  for (const n of t) {
    if (!("patternProperties" in n)) continue;
    const r = new Set(Object.keys(n.patternProperties));
    for (const s of t) if (s != n && "additionalProperties" in s && !s.additionalProperties) for (const o of r.values()) s.patternProperties && o in s.patternProperties || r.delete(o);
    if (!r.size) continue;
    const i = {};
    for (const s of r.values()) i[s] = n.patternProperties[s];
    e.push(i);
  }
  return e;
};
var Bi = (t) => {
  const e = {}, n = Gr(t);
  for (const [r, i] of Object.entries(n)) i.includes(false) ? e[r] = false : e[r] = i.length > 1 ? { allOf: i } : i[0];
  return e;
};
var Ad = (t, e) => !!t.reduce((n, r) => n && r === true, true) || rn(t, e);
var Fd = () => "Could not merge values, they are probably incompatible";
var Cd = (t) => `Merge rule not found for key: ${t}`;
var Gr = (t) => {
  const e = {};
  for (const n of t) for (const r of Object.keys(n)) Array.isArray(e[r]) ? e[r].push(n[r]) : e[r] = [n[r]];
  return e;
};
var rn = (t, e) => {
  if (t.includes(false)) return false;
  const n = {}, r = Gr(t);
  "properties" in r && (r.properties = Rd(t)), "patternProperties" in r && (r.patternProperties = kd(t));
  for (let [i, s] of Object.entries(r)) {
    if (!s.length) continue;
    const o = e.mergeRules;
    let a = `/${i}` in o ? o[`/${i}`] : o["/?"];
    a = a && !("$" in a) && "/" in a ? a["/"] : a, a = typeof a == "function" ? a() : a;
    const l = a && "$" in a ? a.$ : void 0;
    if (!l) throw new Error(Cd(i));
    const u2 = s.length > 1 ? l(s, Object.assign(Object.assign({}, e), { allOfItems: t })) : s[0];
    u2 === void 0 ? e.mergeError(s) : n[i] = u2;
  }
  return Object.keys(n).length ? n : void 0;
};
var Oe = (t) => t[t.length - 1];
var Ye = (t) => t.reduce((e, n) => e || n, false);
var $t = (t) => Math.min(...t);
var Pt = (t) => Math.max(...t);
var xd = (t) => t.length > 1 ? t.reduce((e, n) => `${e}(?=${n})`, "") : t[0];
var vo = ([t, ...e]) => e.reduce((n, r) => n.filter((i) => r.includes(i)), t);
var Dd = ([t, ...e]) => e.find((n) => !Od(n, t)) ? void 0 : t;
var ge = ([t, ...e]) => e.reduce((n, r) => po(n, r), t);
var Ht = (t) => bo(t).map((e) => ({ allOf: e }));
var Ld = (t) => ({ anyOf: t });
var $d = (t) => Nd(t);
var Pd = (t, e) => {
  const n = t.map((i) => i.map((s) => JSON.stringify(s))), r = vo(n).map((i) => JSON.parse(i)).sort();
  return r.length || e.mergeError(t), r;
};
var jd = (t, e) => {
  const n = t.map((i) => Array.isArray(i) ? i : [i]), r = vo(n);
  return r.length === 1 ? r[0] : r.length ? r : void 0;
};
var To = (t) => {
  const e = /* @__PURE__ */ new Set();
  for (const n of t) for (const r of n) e.add(r);
  return Array.from(e).sort();
};
var Md = (t, e) => {
  const n = {}, r = Gr(t);
  for (const [i, s] of Object.entries(r))
    if (s.reduce((o, a) => o && Array.isArray(a), true)) n[i] = To(s);
    else {
      const o = s.map((a) => Array.isArray(a) ? { required: a } : a);
      n[i] = o.length > 1 ? { allOf: o } : o[0];
    }
  return n;
};
var Eo = (t, e) => {
  if (!t.reduce((l, u2) => Array.isArray(u2) || l, false)) return rn(t, e);
  const n = [];
  let r = 1 / 0, i = 0;
  const s = new fo();
  for (const l of e.allOfItems) "additionalItems" in l && l.additionalItems && "items" in l && Array.isArray(l.items) && s.add(l.items.length, l.additionalItems), "items" in l && (n.push(l.items), Array.isArray(l.items) && (i = Math.max(i, l.items.length), "additionalItems" in l && l.additionalItems === false && (r = Math.min(r, l.items.length))));
  const o = Math.min(i, r), a = [...Array(o)].map(() => ({ allOf: [] }));
  for (const l of n) if (Array.isArray(l)) for (let u2 = 0; u2 < o; u2++) {
    const c = [];
    for (let f2 = 0; f2 <= u2; f2++) s.has(f2) && c.push(...s.get(f2));
    u2 < l.length ? a[u2].allOf.push(c.length ? Object.assign(Object.assign({}, l[u2]), { allOf: c }) : l[u2]) : a[u2].allOf.push(!c.length || { allOf: c });
  }
  else a.forEach(({ allOf: u2 }) => u2.push(l));
  return a;
};
var Ud = (t, e) => {
  const n = [], r = [];
  for (const i of e.allOfItems) "items" in i && !Array.isArray(i.items) && r.push(i.items), "additionalItems" in i && "items" in i && Array.isArray(i.items) && n.push(i.additionalItems);
  return r.length && n.forEach((i) => i.allOf = r), rn(n, e);
};
var W = (t = "draft-06", e = {}) => Object.assign(Object.assign(Object.assign(Object.assign({ "/maximum": { $: $t }, "/exclusiveMaximum": { $: Ye }, "/minimum": { $: Pt }, "/exclusiveMinimum": { $: Ye }, "/maxLength": { $: $t }, "/minLength": { $: Pt }, "/maxItems": { $: $t }, "/minItems": { $: Pt }, "/uniqueItems": { $: Ye }, "/maxProperties": { $: $t }, "/minProperties": { $: Pt }, "/required": { $: To }, "/multipleOf": { $: $d }, "/enum": { $: Pd }, "/type": { $: jd }, "/allOf": { "/*": () => W(t, e), $: Ht }, "/not": { $: Ld }, "/oneOf": { "/*": () => W(t, e), $: Ht, sibling: ["definitions", "$defs", "$id", "$schema"] }, "/anyOf": { "/*": () => W(t, e), $: Ht, sibling: ["definitions", "$defs", "$id", "$schema"] }, "/properties": { "/*": () => W(t, e), $: Bi }, "/items": () => Object.assign(Object.assign({}, W(t, e)), { $: Eo, "/*": ({ key: n }) => typeof n == "number" ? W(t, e) : {} }), "/additionalProperties": () => Object.assign(Object.assign({}, W(t, e)), { $: Ad }), "/additionalItems": () => Object.assign(Object.assign({}, W(t, e)), { $: Ud }), "/patternProperties": { "/*": () => W(t, e), $: Bi }, "/pattern": { $: xd }, "/readOnly": { $: Ye }, "/writeOnly": { $: Ye }, "/example": { $: ge }, "/examples": { $: ge }, "/deprecated": { $: Ye } }, t !== "draft-04" ? { "/propertyNames": () => W(t, e), "/contains": () => W(t, e), "/dependencies": { "/*": () => W(t, e), $: Md }, "/const": { $: Dd }, "/exclusiveMaximum": { $: $t }, "/exclusiveMinimum": { $: Pt }, "/$defs": { "/*": () => W(t, e), $: ge } } : {}), { "/definitions": { "/*": () => W(t, e), $: ge }, "/xml": { $: ge }, "/externalDocs": { $: Oe }, "/description": { $: Oe }, "/title": { $: Oe }, "/format": { $: Oe }, "/default": { $: Oe }, "/?": { $: Oe } }), e), { $: rn });
var Kt = (t) => W(t, { "/discriminator": { $: ge }, "/oneOf": { "/*": () => Kt(t), $: Ht, sibling: ["discriminator"] }, "/anyOf": { "/*": () => Kt(t), $: Ht, sibling: ["discriminator"] } });
var Yn = (t) => t === "3.0.x" ? Object.assign(Object.assign({}, Kt("draft-04")), { "/items": ({ key: e }) => Object.assign(Object.assign({}, Kt("draft-04")), { $: Eo }) }) : Kt("draft-06");
var rt = (t) => ({ "/*": { "/schema": Yn(t) } });
var Gi = (t) => ({ "/content": { "/*": { "/schema": Yn(t), "/encoding": { "/headers": rt(t) } } } });
var Qi = (t) => ({ "/*": { "/headers": rt(t), "/content": { "/*": { "/schema": Yn(t), "/encoding": { "/headers": rt(t) } } } } });
var Vd = (t = "3.0.x") => ({ "/paths": { "/*": { "/*": { "/parameters": rt(t), "/requestBody": Gi(t), "/responses": Qi(t) }, "/parameters": rt(t) } }, "/components": { "/schemas": { "/*": Yn(t) }, "/responses": Qi(t), "/parameters": rt(t), "/requestBodies": { "/*": Gi(t) }, "/headers": rt(t) } });
var Ze = W("draft-06", { "/args": () => Ze, "/nullable": { $: Ye }, "/specifiedByURL": { $: Oe }, "/values": { $: ge, "/*": { $: ge, "/description": { $: Oe }, "/deprecated": { $: Oe, "/reason": { $: Oe } } } }, "/interfaces": { $: ge, "/*": { $: ge } }, "/directives": { $: ge, "/*": () => Object.assign(Object.assign({}, Ze), { "/meta": { $: ge } }) } });
var qd = { "/queries": { "/*": () => Ze }, "/mutations": { "/*": () => Ze }, "/subscriptions": { "/*": () => Ze }, "/components": { "/*": { "/*": Ze }, "/directives": { "/*": { "/args": () => Ze } } } };
var Hi = (t, e, n) => {
  const r = n.sibling || [], i = t, s = e, o = i[s], a = Xt(i, [typeof s == "symbol" ? s : s + ""]), l = Ed(a, r);
  return Object.keys(a).length ? Object.assign({ [e]: o.map((u2) => ({ allOf: [a, u2] })) }, l) : t;
};
var wo = (t, e, n, r) => {
  const i = [], s = Br(e), o = [], a = { pointer: s, data: "", refs: [] };
  for (const u2 of t) if (wr(u2) && !o.includes(u2.$ref)) {
    a.data === "" && (a.data = JSON.stringify(t));
    const { $ref: c } = u2, f2 = Xt(u2, ["$ref"]), { filePath: d, normalized: h, pointer: g2 } = ho(c), w = r.find((T) => T.refs.includes(u2.$ref) && T.data === a.data && g2 !== T.pointer);
    if (w) return { allOfItems: [{ $ref: "#" + w.pointer }], brokenRefs: o };
    a.refs.push(h);
    const k = d ? void 0 : go(n, g2);
    k === void 0 ? (o.push(h), i.push(u2)) : i.push(k), Object.keys(f2).length && i.push(f2);
  } else i.push(u2);
  a.refs.length && r.push(a);
  const l = So(i);
  return l.find((u2) => wr(u2) && !o.includes(u2.$ref)) ? wo(l, e, n, r) : { allOfItems: l, brokenRefs: o };
};
var So = (t) => {
  const e = [];
  for (const n of t) if (De(n)) if (n.allOf && Array.isArray(n.allOf)) {
    const { allOf: r } = n, i = Xt(n, ["allOf"]), s = Object.keys(i).length ? [...r, i] : r;
    e.push(...So(s));
  } else e.push(n);
  return e;
};
var Bd = (t) => {
  if (typeof t != "object" || !t) return W();
  if ("openapi" in t && typeof t.openapi == "string" && /3.+/.test(t.openapi)) {
    const e = t.openapi.startsWith("3.1") ? "3.1.x" : "3.0.x";
    return Vd(e);
  }
  return "graphapi" in t && typeof t.graphapi == "string" ? qd : W();
};
var Gd = (t, e) => {
  var n;
  const r = (n = void 0) !== null && n !== void 0 ? n : Bd(t);
  return ((i, s = [], o = {}) => {
    var a;
    s = Array.isArray(s) ? s : [s];
    const l = { "#": void 0 }, u2 = Object.assign({ state: Object.assign(Object.assign({}, (a = o.state) !== null && a !== void 0 ? a : {}), { root: l, node: l }) }, o.rules ? { rules: o.rules } : {}), [c, f2] = Td();
    return vd(i, [c, ...s, f2], u2), l["#"];
  })(t, Qd(e), { rules: r });
};
var Qd = (t) => {
  const e = /* @__PURE__ */ new Map();
  let n;
  const r = [];
  return ({ value: i, key: s, path: o, rules: a, state: l }) => {
    o.length || (n = i);
    const u2 = (T) => {
      var x;
      const E = Hd(o);
      E ? e.set(...E) : (x = void 0) === null || x === void 0 || x.call(t, Fd(), o, T);
    }, c = () => {
      const { node: T } = l, x = Br(o);
      if (e.has(x)) {
        const E = e.get(x), _ = T[s];
        Array.isArray(_) && (_.length < 2 && u2(i?.allOf || []), _.splice(E, 1));
      }
    };
    if (!De(i) || Array.isArray(i)) return { exitHook: c };
    if (!((T) => !!T && T["/allOf"] && "$" in T["/allOf"])(a)) return { exitHook: c };
    const { allOf: f2 } = i, d = Xt(i, ["allOf"]), h = [];
    if (Array.isArray(f2) && h.push(...f2), h.length) Object.keys(d).length && h.push(d);
    else {
      const { $ref: T } = d;
      Xt(d, ["$ref"]);
    }
    if (!h.length) return { value: d, exitHook: c };
    const { allOfItems: g2, brokenRefs: w } = wo(h, o, n, r);
    if (w.length) return w.forEach((T) => {
      var x;
      return (x = void 0) === null || x === void 0 ? void 0 : x.call(t, "Cannot resolve $ref", o, T);
    }), { value: { allOf: g2 }, exitHook: c };
    if (g2.length < 2) return { value: g2.length ? g2[0] : {}, exitHook: c };
    const k = rn(g2, { allOfItems: g2, mergeRules: a, mergeError: u2 });
    return t?.mergeCombinarySibling && wd(k) ? { value: Hi(k, "anyOf", a["/anyOf"]), exitHook: c } : t?.mergeCombinarySibling && Sd(k) ? { value: Hi(k, "oneOf", a["/oneOf"]), exitHook: c } : { value: k, exitHook: c };
  };
};
var Hd = (t) => {
  for (let e = t.length - 2; e >= 0; e--) if (t[e] === "anyOf" || t[e] === "oneOf") {
    const n = t.slice(0, e + 1);
    return [Br(n), t[e + 1]];
  }
};
var yt = Ou();
var fr = {};
var Ki;
function Kd() {
  return Ki || (Ki = 1, (function(t) {
    t.__esModule = true, t.OpenAPIV2 = t.OpenAPIV3 = void 0, (function(e) {
      (function(n) {
        n.GET = "get", n.PUT = "put", n.POST = "post", n.DELETE = "delete", n.OPTIONS = "options", n.HEAD = "head", n.PATCH = "patch", n.TRACE = "trace";
      })(e.HttpMethods || (e.HttpMethods = {}));
    })(t.OpenAPIV3 || (t.OpenAPIV3 = {})), (function(e) {
      (function(n) {
        n.GET = "get", n.PUT = "put", n.POST = "post", n.DELETE = "delete", n.OPTIONS = "options", n.HEAD = "head", n.PATCH = "patch";
      })(e.HttpMethods || (e.HttpMethods = {}));
    })(t.OpenAPIV2 || (t.OpenAPIV2 = {}));
  })(fr)), fr;
}
var Wd = Kd();
var dr = /* @__PURE__ */ new Map();
var zd = (t, e) => {
  dr.has(t) || dr.set(t, /* @__PURE__ */ new Map());
  const n = dr.get(t);
  if (n.has(e))
    return n.get(e);
  const r = e.split("/").slice(1);
  let i = t;
  for (const s of r)
    (!i || typeof i != "object") && (i = null), i = i[s] ?? null;
  return n.set(e, i), i;
};
var pr = /* @__PURE__ */ new Map();
var Jd = (t) => t !== null && typeof t == "object";
var Yd = async (t, e = []) => {
  if (pr.has(t))
    return pr.get(t);
  const n = structuredClone(t), r = /* @__PURE__ */ new Set(), i = async (o, a) => {
    if (Jd(o)) {
      if (r.has(o))
        return nc;
      if (r.add(o), Array.isArray(o))
        for (let l = 0; l < o.length; l++)
          o[l] = await i(o[l]);
      else {
        if ("$ref" in o && typeof o.$ref == "string") {
          o.__$ref = o.$ref;
          for (const u2 of e) {
            const c = await u2(o.$ref);
            if (c) return await i(c);
          }
          const l = await zd(n, o.$ref);
          return await i(l);
        }
        for (const l in o)
          o[l] = await i(o[l]);
      }
      r.delete(o);
    }
    return o;
  }, s = await i(n);
  return pr.set(t, s), s;
};
var Xd = (t) => (t.openapi?.startsWith("3.0") && (t.openapi = "3.1.1"), t = u(t, (e) => (typeof e.type < "u" && e.nullable === true && (e.type = ["null", e.type], delete e.nullable), e)), t = u(t, (e) => (e.exclusiveMinimum === true ? (e.exclusiveMinimum = e.minimum, delete e.minimum) : e.exclusiveMinimum === false && delete e.exclusiveMinimum, e.exclusiveMaximum === true ? (e.exclusiveMaximum = e.maximum, delete e.maximum) : e.exclusiveMaximum === false && delete e.exclusiveMaximum, e)), t = u(t, (e, n) => (e.example !== void 0 && (Zd(n ?? []) ? e.examples = [e.example] : e.examples = {
  default: {
    value: e.example
  }
}, delete e.example), e)), t = u(t, (e, n) => {
  if (e.type === "object" && e.properties !== void 0 && n?.slice(0, -1)?.some((s, o) => s === "content" && n?.[o + 1] === "multipart/form-data")) {
    const s = Object.entries(e.properties);
    for (const [, o] of s)
      typeof o == "object" && o.type === "string" && o.format === "binary" && (o.contentMediaType = "application/octet-stream", delete o.format);
  }
  return e;
}), t = u(t, (e, n) => n?.includes("content") && n.includes("application/octet-stream") ? {} : e.type === "string" && e.format === "binary" ? {
  type: "string",
  contentMediaType: "application/octet-stream"
} : e), t = u(t, (e) => {
  if (!(e.type === "string" && e.format === "binary"))
    return e;
}), t = u(t, (e) => e.type === "string" && e.format === "base64" ? {
  type: "string",
  contentEncoding: "base64"
} : e), t = u(t, (e, n) => e.type === "string" && e.format === "byte" ? {
  type: "string",
  contentEncoding: "base64",
  contentMediaType: n?.slice(0, -1)?.find(
    (s, o) => n?.[o - 1] === "content"
  )
} : e), t);
function Zd(t) {
  return [
    ["components", "schemas"],
    "properties",
    "items",
    "allOf",
    "anyOf",
    "oneOf",
    "not",
    "additionalProperties"
  ].some((n) => Array.isArray(n) ? n.every((r, i) => t[i] === r) : t.includes(n)) || t.includes("schema") || t.some((n) => n.endsWith("Schema"));
}
var ep = Object.values(Wd.OpenAPIV3.HttpMethods);
var tp = async (t) => {
  if (typeof t == "string") {
    if (t.trim().startsWith("{"))
      try {
        return JSON.parse(t);
      } catch (r) {
        throw new yt.GraphQLError("Invalid JSON schema", {
          originalError: r
        });
      }
    if (t.includes("://")) {
      let r;
      try {
        r = await fetch(t, {
          cache: "force-cache"
        });
      } catch (s) {
        throw new yt.GraphQLError("Failed to fetch schema", {
          originalError: s
        });
      }
      if (!r.ok)
        throw new yt.GraphQLError(
          `Failed to fetch schema: ${r.statusText}`
        );
      const i = await r.text();
      try {
        return i.trim().startsWith("{") ? JSON.parse(i) : (await import("./index-DmNq2fbN-QTHQPZG5.js")).parse(i);
      } catch (s) {
        throw new yt.GraphQLError("Fetched invalid schema", {
          originalError: s
        });
      }
    }
    const n = (await import("./index-DmNq2fbN-QTHQPZG5.js")).parse(t);
    if (typeof n == "object") return n;
  }
  if (typeof t == "object") return t;
  throw new yt.GraphQLError("Unsupported schema input: " + t);
};
var np = async (t) => {
  const e = await tp(t);
  if (!e.openapi)
    throw new yt.GraphQLError("OpenAPI version is not defined");
  const n = await Yd(e), r = Xd(n);
  return Gd(r);
};
var rp = (t, e) => {
  const n = e.summary || e.operationId || `${e.method}-${e.path}`;
  return t(n);
};
var K = new Vu({
  defaultFieldNullability: false
});
var $n = K.addScalarType("JSON", bi);
var Ge = K.addScalarType("JSONObject", fc);
var Qr = K.addScalarType("JSONSchema", hc);
var lt = (t) => Object.fromEntries(
  Object.entries(t).filter(([e]) => e.startsWith("x-"))
);
var Wi = (t, e) => {
  const n = t.tags ?? [], r = new Set(
    Object.values(t.paths ?? {}).flatMap((s) => Object.values(s ?? {})).flatMap((s) => s.tags ?? [])
  ), i = Object.values(t.paths ?? {}).some(
    (s) => Object.values(s ?? {}).some(
      (o) => !o.tags?.length
    )
  );
  return [
    // Keep root tags that are actually used in operations
    ...n.filter((s) => r.has(s.name)).map((s) => ({ ...s, slug: e[s.name] })),
    // Add tags found in operations but not defined in root tags
    ...[...r].filter((s) => !n.some((o) => o.name === s)).map((s) => ({ name: s, slug: e[s] })),
    // Add untagged operations if there are any
    ...i ? [{ name: void 0, slug: void 0 }] : []
  ];
};
var ip = (t, e = []) => {
  const n = S(), r = Array.from(
    /* @__PURE__ */ new Set([
      ...t.flatMap((i) => i.tags ?? []),
      ...e.map((i) => i.name)
    ])
  );
  return {
    operations: Object.fromEntries(
      t.map((i) => [
        Sr(i),
        rp(n, i)
      ])
    ),
    tags: Object.fromEntries(r.map((i) => [i, n(i)]))
  };
};
var Sr = (t) => [t.path, t.method, t.operationId, t.summary].filter(Boolean).join("-");
var zi = (t) => Object.entries(t ?? {}).flatMap(
  ([n, r]) => ep.flatMap((i) => {
    if (!r?.[i]) return [];
    const s = r[i], o = r.parameters ?? [], a = s.parameters ?? [], l = [
      ...o.filter(
        // remove path parameters that are already defined in the operation
        (u2) => !a.some(
          (c) => c.name === u2.name && c.in === u2.in
        )
      ),
      ...a
    ];
    return {
      ...s,
      method: i,
      path: n,
      parameters: l,
      tags: s.tags ?? []
    };
  })
);
var Wt = K.objectRef("SchemaTag");
Wt.implement({
  fields: (t) => ({
    name: t.exposeString("name", { nullable: true }),
    slug: t.exposeString("slug", { nullable: true }),
    isUntagged: t.field({ type: "Boolean", resolve: (e) => !e.name }),
    description: t.exposeString("description", { nullable: true }),
    operations: t.field({
      type: [_o],
      resolve: (e, n, r) => {
        const i = r.tags.map((s) => s.name);
        return r.operations.filter(
          (s) => e.name ? s.tags?.includes(e.name) : s.tags?.length === 0 || // If none of the tags are present in the root tags, then show them here
          s.tags?.every((o) => !i.includes(o))
        ).map((s) => ({ ...s, parentTag: e.name }));
      }
    }),
    prev: t.field({
      type: Wt,
      nullable: true,
      resolve: (e, n, r) => {
        const i = r.tags.findIndex((s) => s.slug === e.slug);
        return r.tags[i - 1];
      }
    }),
    next: t.field({
      type: Wt,
      nullable: true,
      resolve: (e, n, r) => {
        const i = r.tags.findIndex((s) => s.slug === e.slug);
        return r.tags[i + 1];
      }
    }),
    extensions: t.field({
      type: Ge,
      resolve: (e) => lt(e),
      nullable: true
    })
  })
});
var sp = K.objectRef("Server").implement({
  fields: (t) => ({
    url: t.exposeString("url"),
    description: t.exposeString("description", { nullable: true })
  })
});
var op = K.objectRef("PathItem").implement({
  fields: (t) => ({
    path: t.exposeString("path"),
    methods: t.exposeStringList("methods")
  })
});
var ap = K.objectRef("TagItem").implement({
  fields: (t) => ({
    name: t.exposeString("name"),
    description: t.exposeString("description", { nullable: true }),
    extensions: t.field({
      type: Ge,
      resolve: (e) => lt(e),
      nullable: true
    })
  })
});
var lp = K.objectRef("EncodingItem").implement({
  fields: (t) => ({
    name: t.exposeString("name"),
    contentType: t.exposeString("contentType", { nullable: true }),
    headers: t.expose("headers", { type: Ge, nullable: true }),
    style: t.exposeString("style", { nullable: true }),
    explode: t.exposeBoolean("explode", { nullable: true }),
    allowReserved: t.exposeBoolean("allowReserved", { nullable: true })
  })
});
var Io = K.objectRef("ExampleItem").implement({
  fields: (t) => ({
    name: t.exposeString("name"),
    summary: t.exposeString("summary", { nullable: true }),
    description: t.exposeString("description", { nullable: true }),
    value: t.expose("value", { type: $n, nullable: true }),
    externalValue: t.exposeString("externalValue", { nullable: true })
  })
});
var up = K.enumType("ParameterIn", {
  values: ["query", "header", "path", "cookie"]
});
var cp = K.objectRef("ParameterItem").implement({
  fields: (t) => ({
    name: t.exposeString("name"),
    in: t.field({
      type: up,
      resolve: (e) => e.in
    }),
    description: t.exposeString("description", { nullable: true }),
    required: t.exposeBoolean("required", { nullable: true }),
    deprecated: t.exposeBoolean("deprecated", { nullable: true }),
    allowEmptyValue: t.exposeBoolean("allowEmptyValue", { nullable: true }),
    style: t.exposeString("style", { nullable: true }),
    explode: t.exposeBoolean("explode", { nullable: true }),
    allowReserved: t.exposeBoolean("allowReserved", { nullable: true }),
    examples: t.field({
      type: [Io],
      resolve: (e) => Object.entries(e.examples ?? {}).map(([n, r]) => ({
        name: n,
        ...typeof r == "string" ? { value: r } : r
      })),
      nullable: true
    }),
    schema: t.expose("schema", { type: Qr, nullable: true }),
    extensions: t.field({
      type: Ge,
      resolve: (e) => lt(e),
      nullable: true
    })
  })
});
var Oo = K.objectRef("MediaTypeObject").implement({
  fields: (t) => ({
    mediaType: t.exposeString("mediaType"),
    schema: t.expose("schema", { type: Qr, nullable: true }),
    examples: t.expose("examples", { type: [Io], nullable: true }),
    encoding: t.expose("encoding", { type: [lp], nullable: true })
  })
});
var fp = K.objectRef("RequestBodyObject").implement({
  fields: (t) => ({
    description: t.exposeString("description", { nullable: true }),
    content: t.expose("content", { type: [Oo], nullable: true }),
    required: t.exposeBoolean("required", { nullable: true })
  })
});
var dp = K.objectRef("ResponseItem").implement({
  fields: (t) => ({
    statusCode: t.exposeString("statusCode"),
    description: t.exposeString("description", { nullable: true }),
    content: t.expose("content", { type: [Oo], nullable: true }),
    headers: t.expose("headers", { type: $n, nullable: true }),
    links: t.expose("links", { type: $n, nullable: true }),
    extensions: t.field({
      type: Ge,
      resolve: (e) => lt(e),
      nullable: true
    })
  })
});
var _o = K.objectRef("OperationItem").implement({
  fields: (t) => ({
    slug: t.field({
      type: "String",
      resolve: (e, n, r) => {
        const i = r.slugs.operations[Sr(e)];
        if (!i)
          throw new Error(
            `No slug found for operation: ${Sr(e)}`
          );
        return i;
      }
    }),
    path: t.exposeString("path"),
    method: t.exposeString("method"),
    operationId: t.exposeString("operationId", { nullable: true }),
    summary: t.exposeString("summary", { nullable: true }),
    description: t.exposeString("description", { nullable: true }),
    contentTypes: t.stringList({
      resolve: (e) => Object.keys(e.requestBody?.content ?? {})
    }),
    parameters: t.expose("parameters", {
      type: [cp],
      nullable: true
    }),
    requestBody: t.field({
      type: fp,
      resolve: (e) => ({
        description: e.requestBody?.description,
        required: e.requestBody?.required,
        content: Object.entries(e.requestBody?.content ?? {}).map(
          ([n, r]) => ({
            mediaType: n,
            schema: r.schema,
            examples: r.examples ? Object.entries(r.examples).map(([i, s]) => ({
              name: i,
              ...typeof s == "string" ? { value: s } : s
            })) : [],
            encoding: Object.entries(r.encoding ?? {}).map(
              ([i, s]) => ({ name: i, ...s })
            )
          })
        )
      }),
      nullable: true
    }),
    responses: t.field({
      type: [dp],
      resolve: (e) => Object.entries(e.responses ?? {}).map(
        ([n, r]) => ({
          statusCode: n,
          description: r.description,
          content: Object.entries(r.content ?? {}).map(
            ([i, { schema: s, examples: o }]) => ({
              mediaType: i,
              schema: s,
              examples: o ? Object.entries(o).map(([a, l]) => ({
                name: a,
                ...typeof l == "string" ? { value: l } : l
              })) : []
            })
          ),
          headers: r.headers,
          links: r.links
        })
      )
    }),
    tags: t.field({
      type: [ap],
      resolve: (e, n, r) => e.tags?.map((i) => ({
        name: i,
        description: r.schema.tags?.find((s) => s.name === i)?.description
      })),
      nullable: true
    }),
    deprecated: t.exposeBoolean("deprecated", { nullable: true }),
    extensions: t.field({
      type: Ge,
      resolve: (e) => lt(e),
      nullable: true
    })
  })
});
var pp = K.objectRef("SchemaItem").implement({
  fields: (t) => ({
    name: t.exposeString("name"),
    schema: t.expose("schema", { type: Qr }),
    extensions: t.expose("extensions", {
      type: Ge,
      nullable: true
    })
  })
});
var No = K.objectRef("Components");
No.implement({
  fields: (t) => ({
    schemas: t.field({
      type: [pp],
      resolve: (e) => Object.entries(e.schemas ?? {}).map(([n, r]) => ({
        name: n,
        schema: r,
        extensions: lt(r)
      })),
      nullable: true
    })
  })
});
var hp = K.objectRef("Schema").implement({
  fields: (t) => ({
    openapi: t.string({ resolve: (e) => e.openapi }),
    url: t.string({
      resolve: (e) => e.servers?.at(0)?.url,
      nullable: true
    }),
    servers: t.field({
      type: [sp],
      resolve: (e) => e.servers ?? []
    }),
    title: t.string({ resolve: (e) => e.info.title }),
    version: t.string({ resolve: (e) => e.info.version }),
    description: t.string({
      resolve: (e) => e.info.description,
      nullable: true
    }),
    summary: t.string({
      resolve: (e) => e.info.summary,
      nullable: true
    }),
    paths: t.field({
      type: [op],
      resolve: (e) => Object.entries(e.paths ?? {}).map(([n, r]) => ({
        path: n,
        methods: Object.keys(r)
      }))
    }),
    tag: t.field({
      type: Wt,
      args: {
        slug: t.arg.string(),
        name: t.arg.string(),
        untagged: t.arg.boolean()
      },
      nullable: true,
      resolve: (e, n, r) => {
        if (n.untagged)
          return r.tags.find((i) => i.name === void 0);
        if (n.slug)
          return r.tags.find((i) => i.slug === n.slug);
        if (n.name)
          return r.tags.find((i) => i.name === n.name);
      }
    }),
    tags: t.field({
      type: [Wt],
      resolve: (e, n, r) => r.tags
    }),
    operations: t.field({
      type: [_o],
      args: {
        path: t.arg.string(),
        method: t.arg.string(),
        operationId: t.arg.string(),
        tag: t.arg.string(),
        untagged: t.arg.boolean()
      },
      resolve: (e, n, r) => r.operations.filter((i) => (!n.operationId || i.operationId === n.operationId) && (!n.path || i.path === n.path) && (!n.method || i.method === n.method) && (!n.tag || i.tags?.some((s) => n.tag?.includes(s))) && (!n.untagged || (i.tags ?? []).length === 0))
    }),
    components: t.field({
      type: No,
      resolve: (e) => e.components,
      nullable: true
    }),
    extensions: t.field({
      type: Ge,
      resolve: (e) => lt(e),
      nullable: true
    })
  })
});
var gp = K.enumType("SchemaType", {
  values: ["url", "file", "raw"]
});
K.queryType({
  fields: (t) => ({
    schema: t.field({
      type: hp,
      args: {
        type: t.arg({ type: gp, required: true }),
        input: t.arg({ type: $n, required: true })
      },
      resolve: async (e, n, r) => {
        if (n.type === "file" && typeof n.input == "string") {
          const i = r.schemaImports?.[n.input];
          if (!i)
            throw new Error(`No schema loader found for path: ${n.input}`);
          const { schema: s, slugs: o } = await i();
          r.schema = s, r.operations = zi(s.paths), r.slugs = o, r.tags = Wi(s, r.slugs.tags);
        } else
          r.schema = await np(n.input), r.operations = zi(r.schema.paths), r.slugs = ip(r.operations), r.tags = Wi(r.schema, r.slugs.tags);
        return r.schema;
      }
    })
  })
});
var mp = K.toSchema();
var yp = (t) => md({ schema: mp, batching: true, ...t });
var Op = (t) => yp({
  context: {
    schemaImports: t.schemaImports
  },
  plugins: [
    // eslint-disable-next-line react-hooks/rules-of-hooks
    Vl({
      logFn: (e, { args: n }) => {
      }
    })
  ]
});
export {
  Op as createServer
};
/*! Bundled license information:

zudoku/lib/createServer-BPz8ZCrd.js:
  (*!
   * allof-merge v0.6.6
   * Copyright (C) 2012-2024 Damir Yusipov
   * Date: Sat, 17 Feb 2024 19:27:36 GMT
   *)
*/
//# sourceMappingURL=createServer-BPz8ZCrd-OKSGPJVL.js.map
