import {
  A,
  H,
  c,
  f as f2,
  g as g3,
  j as j2,
  t,
  y
} from "./chunk-NS43KWH2.js";
import {
  u
} from "./chunk-WIGGQDRF.js";
import "./chunk-SH2REVY5.js";
import {
  Content as Content2,
  List,
  Root2,
  Trigger as Trigger2
} from "./chunk-WOMZGQBE.js";
import {
  Be,
  Cr,
  Ft,
  Ge,
  Qr,
  V,
  Ve,
  Vt,
  Yt as Yt2,
  ir,
  pa,
  q,
  qt,
  te,
  ua,
  xr
} from "./chunk-JYLCFJRO.js";
import {
  Content,
  Du,
  Root,
  Su,
  Trigger,
  Wc,
  Ye,
  Ye2,
  a,
  aE,
  ao,
  f,
  g,
  g3 as g2,
  i,
  rs,
  sE
} from "./chunk-T457C7XC.js";
import "./chunk-GKNUOLVO.js";
import "./chunk-ONELSXMF.js";
import "./chunk-K6XZ6KXP.js";
import "./chunk-3JE72L2J.js";
import "./chunk-KLBCLHEP.js";
import "./chunk-IWISGUXJ.js";
import {
  Check,
  ChevronsDownUp,
  ChevronsUpDown,
  Copy,
  FoldVertical,
  Minus,
  Plus,
  UnfoldVertical,
  mo
} from "./chunk-R54EFXNU.js";
import "./chunk-JNHOR6SI.js";
import {
  VisuallyHidden
} from "./chunk-72E3F2OW.js";
import "./chunk-JHJKCYTU.js";
import "./chunk-LL7TNUZB.js";
import "./chunk-YPKTU6G3.js";
import "./chunk-YPKBKVIN.js";
import "./chunk-QGJIGGMO.js";
import "./chunk-FXNFNHXZ.js";
import "./chunk-VCPSW7YA.js";
import "./chunk-DW5YFC3V.js";
import "./chunk-IXRQDBTI.js";
import "./chunk-TCGE25WV.js";
import "./chunk-BL6D5YOI.js";
import "./chunk-3R46EL5G.js";
import "./chunk-3JZST3TF.js";
import "./chunk-Z5AKTONR.js";
import {
  Ot,
  X,
  Xt,
  Yt,
  j,
  je
} from "./chunk-KJLHI4KE.js";
import "./chunk-5CMMY7LK.js";
import {
  ae
} from "./chunk-XTV2HAPF.js";
import {
  hr,
  ne,
  sr
} from "./chunk-YY6SLAPL.js";
import {
  require_react
} from "./chunk-S73QDKPN.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/zudoku/lib/OperationList-PnZbf3b2.js
var import_react2 = __toESM(require_react(), 1);

// node_modules/zudoku/lib/zudoku.hooks.js
var import_react = __toESM(require_react(), 1);
var P = X;

// node_modules/zudoku/lib/OperationList-PnZbf3b2.js
function yo(e, n) {
  return n;
}
var Xe = ({
  value: e,
  onChange: n,
  className: t2,
  options: a2,
  showChevrons: c2 = true
}) => ae.jsxs("div", { className: "grid", children: [
  ae.jsx(
    "select",
    {
      className: mo(
        "w-full row-start-1 col-start-1 border border-input text-foreground px-2 py-1 pe-6",
        "rounded-md appearance-none bg-zinc-50 hover:bg-white dark:bg-zinc-800 hover:dark:bg-zinc-800/75",
        t2
      ),
      value: e,
      onChange: n,
      children: a2.map((s) => ae.jsx("option", { value: s.value, children: s.label }, s.value))
    }
  ),
  ae.jsx(
    "div",
    {
      className: mo(
        !c2 && "hidden",
        "row-start-1 col-start-1 self-center justify-self-end relative end-2 pointer-events-none"
      ),
      children: ae.jsx(ChevronsUpDown, { size: 14 })
    }
  )
] });
var mo2 = xr(
  /* GraphQL */
  `
  query ServersQuery($input: JSON!, $type: SchemaType!) {
    schema(input: $input, type: $type) {
      url
      servers {
        url
      }
    }
  }
`
);
var Fr = ({ url: e }) => {
  const [n, t2] = (0, import_react2.useState)(false);
  return ae.jsx(
    g,
    {
      onClick: () => {
        navigator.clipboard.writeText(e).then(() => {
          t2(true), setTimeout(() => t2(false), 2e3);
        });
      },
      variant: "ghost",
      size: "icon",
      children: n ? ae.jsx(Check, { className: "text-green-600", size: 14 }) : ae.jsx(Copy, { size: 14, strokeWidth: 1.3 })
    }
  );
};
var go = () => {
  const { input: e, type: n } = pa(), t2 = ir(mo2, { input: e, type: n }), a2 = Xt(t2), [, c2] = (0, import_react2.useTransition)(), { selectedServer: s, setSelectedServer: r } = Cr(
    a2.data.schema.servers
  ), { servers: o } = a2.data.schema;
  if (o.length === 0) return null;
  const i4 = o.at(0);
  return o.length === 1 ? ae.jsxs("div", { className: "flex items-center gap-2", children: [
    ae.jsx("span", { className: "font-medium text-sm", children: "Endpoint:" }),
    ae.jsx(sE, { className: "text-xs px-2 py-1.5", selectOnClick: true, children: i4.url }),
    ae.jsx(Fr, { url: i4.url })
  ] }) : ae.jsxs("div", { className: "flex flex-wrap items-center gap-2", children: [
    ae.jsx("span", { className: "font-medium text-sm", children: "Endpoint" }),
    ae.jsx(
      Xe,
      {
        className: "font-mono text-xs bg-border/50 dark:bg-border/70 py-1.5 max-w-[450px] truncate",
        onChange: (l) => c2(() => r(l.target.value)),
        value: s,
        showChevrons: o.length > 1,
        options: o.map((l) => ({
          value: l.url,
          label: l.url
        }))
      }
    ),
    ae.jsx(Fr, { url: s })
  ] });
};
var vo = j(
  "inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-medium transition-colors focus:outline-hidden focus:ring-2 focus:ring-ring focus:ring-offset-2",
  {
    variants: {
      variant: {
        default: "border-transparent bg-primary text-primary-foreground hover:bg-primary/80",
        secondary: "border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80",
        muted: "bg-muted text-muted-foreground ",
        destructive: "border-transparent bg-destructive text-destructive-foreground hover:bg-destructive/80",
        outline: "text-foreground"
      }
    },
    defaultVariants: {
      variant: "default"
    }
  }
);
function _r({ className: e, variant: n, ...t2 }) {
  return ae.jsx("div", { className: mo(vo({ variant: n }), e), ...t2 });
}
var Br = (e, n) => e ? n(e) : void 0;
var bo = (e) => e.schema != null && typeof e.schema == "object" ? e.schema : {
  type: "string"
};
var xo = ({
  parameter: e,
  group: n,
  id: t2
}) => {
  const a2 = bo(e), [c2, s] = (0, import_react2.useState)(false);
  return ae.jsxs("li", { className: "p-4 bg-border/20 text-sm flex flex-col gap-1.5", children: [
    ae.jsxs("div", { className: "flex items-center gap-2", children: [
      ae.jsx(aE, { asChild: true, children: ae.jsx("code", { children: n === "path" ? ae.jsx(
        Yt2,
        {
          name: e.name,
          backgroundOpacity: "15%",
          className: "px-2",
          slug: `${t2}-${e.name}`
        }
      ) : e.name }) }),
      ae.jsx(
        j2,
        {
          schema: a2,
          extraItems: [
            e.required && ae.jsx("span", { className: "text-primary", children: "required" }),
            e.style && `style: ${e.style}`,
            e.explode && `explode: ${e.explode}`
          ]
        }
      )
    ] }),
    e.description && ae.jsx(
      rs,
      {
        content: e.description,
        className: "text-sm prose prose-p:my-1 prose-code:whitespace-pre-line"
      }
    ),
    a2.description && ae.jsx(
      rs,
      {
        content: a2.description,
        className: "text-sm prose-p:my-1 prose-code:whitespace-pre-line"
      }
    ),
    a2.type === "array" && a2.items.enum ? ae.jsx(f2, { values: a2.items.enum }) : a2.enum && ae.jsx(f2, { values: a2.enum }),
    ae.jsx(g3, { schema: a2 }),
    (a2.type === "object" || y(a2)) && ae.jsxs(
      Root,
      {
        defaultOpen: false,
        onOpenChange: s,
        open: c2,
        children: [
          ae.jsx(Trigger, { asChild: true, children: ae.jsxs(g, { variant: "expand", size: "sm", children: [
            c2 ? ae.jsx(Minus, { size: 12 }) : ae.jsx(Plus, { size: 12 }),
            c2 ? "Hide properties" : "Show properties"
          ] }) }),
          ae.jsx(Content, { children: ae.jsx("div", { className: "mt-2", children: ae.jsx(
            c,
            {
              schema: "items" in a2 ? a2.items : a2
            }
          ) }) })
        ]
      }
    )
  ] });
};
var $o = ({
  summary: e,
  group: n,
  parameters: t2,
  id: a2
}) => ae.jsxs(ae.Fragment, { children: [
  ae.jsxs(Ye2, { level: 3, id: `${a2}/${n}-parameters`, className: "capitalize", children: [
    e && ae.jsxs(VisuallyHidden, { children: [
      e,
      " › "
    ] }),
    n === "header" ? "Headers" : `${n} Parameters`
  ] }),
  ae.jsx(t, { children: ae.jsx("ul", { className: "list-none m-0 px-0 divide-y ", children: t2.sort((c2, s) => c2.required === s.required ? 0 : c2.required ? -1 : 1).map((c2) => ae.jsx(
    xo,
    {
      parameter: c2,
      id: a2,
      group: n
    },
    `${c2.name}-${c2.in}`
  )) }) })
] });
var wo = ({
  rootMargin: e = "0px",
  threshold: n,
  root: t2
} = {}) => {
  const [a2, c2] = (0, import_react2.useState)(false), s = (0, import_react2.useRef)(null);
  return (0, import_react2.useEffect)(() => {
    const r = new IntersectionObserver(
      ([i4]) => {
        i4 && c2(i4.isIntersecting);
      },
      { rootMargin: e, threshold: n, root: t2 }
    ), o = s.current;
    if (o)
      return r.observe(o), () => r.unobserve(o);
  }, [t2, e, n]), [s, a2];
};
var jo = typeof window < "u" ? import_react2.useLayoutEffect : import_react2.useEffect;
var ha = ({
  children: e,
  maxHeight: n = 250
}) => {
  const t2 = (0, import_react2.useRef)(null), [a2, c2] = (0, import_react2.useState)(false), [s, r] = (0, import_react2.useState)(false);
  return jo(() => {
    const o = t2.current;
    if (!o) return;
    c2(o.scrollHeight > n);
    const i4 = new ResizeObserver(() => {
      c2(o.scrollHeight > n);
    });
    return i4.observe(o), () => i4.disconnect();
  }, [n]), ae.jsxs(
    q,
    {
      className: "group",
      open: s,
      onOpenChange: r,
      style: { "--max-height": `${n}px` },
      children: [
        ae.jsxs(
          V,
          {
            forceMount: true,
            className: mo(
              "relative overflow-hidden group",
              !s && a2 && "max-h-(--max-height)"
            ),
            children: [
              !s && a2 && ae.jsx("div", { className: " absolute inset-0 bg-gradient-to-b from-transparent to-zinc-50/60 dark:to-zinc-950/90 z-10 transition-all group-hover:to-transparent" }),
              ae.jsx("div", { ref: t2, children: e }),
              !s && a2 && ae.jsx(
                Ft,
                {
                  className: "absolute inset-0 grid place-items-center z-10 cursor-pointer peer",
                  asChild: true,
                  children: ae.jsx("div", { children: ae.jsxs(g2, { variant: "outline", className: "hidden group-hover:flex", children: [
                    ae.jsx(UnfoldVertical, { size: 14, className: "me-1.5" }),
                    "Click to expand"
                  ] }) })
                }
              )
            ]
          }
        ),
        a2 && ae.jsx(
          "div",
          {
            className: mo(
              "flex justify-center w-full py-2 bg-muted/50",
              !s && "hidden"
            ),
            children: ae.jsx(Ft, { asChild: true, children: ae.jsxs(g2, { variant: "outline", size: "sm", children: [
              "Collapse",
              ae.jsx(FoldVertical, { size: 14, className: "ms-1.5" })
            ] }) })
          }
        )
      ]
    }
  );
};
var So = ({
  server: e,
  servers: n,
  operation: t2,
  examples: a2
}) => {
  const { isAuthEnabled: c2, login: s, signup: r, isPending: o, isAuthenticated: i4 } = Du(), l = t2.parameters?.filter((f3) => f3.in === "header").sort((f3, h) => f3.required && !h.required ? -1 : 1).map((f3) => ({
    name: f3.name,
    defaultValue: f3.schema?.default ?? f3.examples?.find((h) => h.value)?.value ?? "",
    defaultActive: f3.required ?? false,
    isRequired: f3.required ?? false,
    enum: f3.schema?.type == "array" ? f3.schema?.items?.enum : f3.schema?.enum,
    type: f3.schema?.type ?? "string"
  })), d = t2.parameters?.filter((f3) => f3.in === "query").sort((f3, h) => f3.required && !h.required ? -1 : 1).map((f3) => ({
    name: f3.name,
    defaultActive: f3.required ?? false,
    isRequired: f3.required ?? false,
    enum: f3.schema?.type == "array" ? f3.schema?.items?.enum : f3.schema?.enum,
    type: f3.schema?.type ?? "string",
    defaultValue: f3.schema?.default
  })), y2 = t2.parameters?.filter((f3) => f3.in === "path").map((f3) => ({
    name: f3.name,
    defaultValue: f3.schema?.default
  }));
  return ae.jsx(
    ua,
    {
      server: e,
      servers: n,
      method: t2.method,
      url: t2.path,
      headers: l,
      queryParams: d,
      pathParams: y2,
      examples: a2,
      requiresLogin: c2 && !i4 && !o,
      onLogin: () => s(),
      onSignUp: () => r()
    }
  );
};
var $r = ({ children: e, className: n }) => ae.jsx(
  "div",
  {
    className: mo("rounded-xl overflow-hidden border border-border", n),
    children: e
  }
);
var wr = ({ children: e, className: n }) => ae.jsx(
  "div",
  {
    className: mo(
      "border-b bg-muted dark:bg-transparent text-card-foreground p-3 py-2.5",
      n
    ),
    children: e
  }
);
var ya = ({ children: e, className: n }) => ae.jsx("div", { className: mo("bg-card overflow-auto p-2", n), children: e });
var ma = ({ children: e, className: n }) => ae.jsx("div", { className: mo("border-t bg-muted dark:bg-transparent p-3", n), children: e });
var we = (e, n) => {
  if (!e || A(e))
    return null;
  if (e.example !== void 0)
    return e.example;
  if (e.examples && typeof e.examples == "object" && "default" in e.examples) {
    const t2 = e.examples.default;
    if (t2 !== null)
      return typeof t2 == "object" && "value" in t2 ? t2.value : t2;
  }
  if (e.const !== void 0)
    return e.const;
  if (e.type === "object" && e.properties) {
    const t2 = {};
    for (const [a2, c2] of Object.entries(e.properties))
      typeof c2 == "object" && (t2[a2] = we(c2, a2));
    return t2;
  }
  if (e.type === "array")
    return Array.isArray(e.items) ? e.items.map(
      (t2) => we(t2)
    ) : e.items ? [we(e.items)] : [];
  if (e.format !== void 0)
    switch (e.format) {
      case "date-time":
        return "2024-08-25T15:00:00Z";
      case "date":
        return "2024-08-25";
      case "time":
        return "15:00:00";
      case "email":
        return "test@example.com";
      case "uri":
        return "https://www.example.com/path/to/resource";
      case "uri-reference":
        return "/path/to/resource";
      case "uuid":
        return "00000000-0000-0000-0000-000000000000";
    }
  if (e.enum)
    return e.enum[0];
  if (e.oneOf) {
    const t2 = Math.floor(Math.random() * e.oneOf.length);
    return we(e.oneOf[t2]);
  }
  if (e.anyOf) {
    const t2 = Math.floor(Math.random() * e.anyOf.length);
    return we(e.anyOf[t2]);
  }
  if (e.allOf)
    return e.allOf.reduce((t2, a2) => ({ ...t2, ...we(a2) }), {});
  switch (e.type) {
    case "string":
      return n || "string";
    case "number":
    case "integer":
      return 0;
    case "boolean":
      return true;
    case "null":
      return null;
    case "object":
      return {};
    case void 0:
    default:
      return {};
  }
};
var Oo = (e) => e == null ? "No example" : typeof e == "string" ? e.trim() : JSON.stringify(e, null, 2);
var To = (e) => e ? e.endsWith("+json") ? "json" : e.endsWith("+xml") ? "xml" : e.endsWith("+yaml") ? "yaml" : {
  "text/html": "html",
  "application/x-ndjson": "json",
  "application/json": "json",
  "application/xml": "xml",
  "application/x-yaml": "yaml",
  "text/csv": "csv",
  "application/javascript": "javascript",
  "application/graphql": "graphql"
}[e] ?? "plain" : "plain";
var ga = ({
  content: e,
  description: n,
  onExampleChange: t2
}) => {
  const [a2, c2] = (0, import_react2.useState)(0), [s, r] = (0, import_react2.useState)(0), o = Array.isArray(e) && e.length === 1 ? e[0] : e[a2], i4 = o?.examples ?? [], l = i4[s], d = (0, import_react2.useMemo)(() => {
    if (l)
      return "value" in l ? l.value : l;
    if (o?.schema)
      return we(o.schema);
  }, [l, o?.schema]);
  (0, import_react2.useEffect)(() => {
    d && t2?.(d);
  }, [d, t2]);
  const y2 = Oo(d), f3 = To(o?.mediaType);
  return ae.jsxs(ae.Fragment, { children: [
    ae.jsxs(ya, { className: "p-0", children: [
      l?.externalValue ? ae.jsx("div", { className: "p-2", children: ae.jsx(
        "a",
        {
          href: l.externalValue,
          target: "_blank",
          rel: "noopener noreferrer",
          className: "text-xs text-primary hover:underline",
          children: "View External Example →"
        }
      ) }) : ae.jsx(ha, { children: ae.jsx(
        Wc,
        {
          embedded: true,
          language: f3,
          noBackground: true,
          className: "[--scrollbar-color:gray] rounded-none text-xs max-h-[500px] p-2",
          code: y2
        }
      ) }),
      l?.description && ae.jsx("div", { className: "border-t text-xs px-3 py-1.5 text-muted-foreground", children: l.description })
    ] }),
    ae.jsxs(ma, { className: "text-xs p-0 divide-y divide-border", children: [
      n && ae.jsx("div", { className: "text-muted-foreground text-xs px-3 py-2", children: n }),
      (i4.length !== 0 || e.length !== 0) && ae.jsxs("div", { className: "flex items-center gap-2 justify-between min-w-0 px-3 py-2", children: [
        ae.jsx("div", { className: "flex items-center gap-2 min-w-0", children: e.length > 1 ? ae.jsx(
          Xe,
          {
            className: "max-w-[200px]",
            value: a2.toString(),
            onChange: (h) => c2(Number(h.target.value)),
            options: e.map((h, x) => ({
              value: x.toString(),
              label: h.mediaType
            }))
          }
        ) : ae.jsx("span", { className: "font-mono text-[11px]", children: e[0]?.mediaType }) }),
        i4.length > 1 && ae.jsx("div", { className: "flex items-center gap-1", children: ae.jsx(
          Xe,
          {
            className: "max-w-[180px]",
            value: s.toString(),
            onChange: (h) => r(Number(h.target.value)),
            options: i4.map((h, x) => ({
              value: x.toString(),
              label: h.summary || h.name || h.description || `Example ${x + 1}`
            }))
          }
        ) })
      ] })
    ] })
  ] });
};
var Po = ({
  content: e,
  onExampleChange: n
}) => e.length === 0 ? null : ae.jsxs($r, { children: [
  ae.jsx(wr, { className: "text-xs flex justify-between items-center", children: ae.jsx("span", { className: "font-mono", children: "Request Body Example" }) }),
  ae.jsx(ga, { content: e, onExampleChange: n })
] });
var Co = ({
  responses: e,
  selectedResponse: n,
  onSelectResponse: t2
}) => ae.jsx($r, { children: ae.jsxs(
  Root2,
  {
    defaultValue: e[0]?.statusCode,
    value: n,
    onValueChange: (a2) => t2(a2),
    children: [
      ae.jsxs(wr, { className: "text-xs flex flex-col gap-2 pb-0", children: [
        ae.jsx("span", { className: "font-mono", children: "Example Responses" }),
        ae.jsx(List, { className: "flex gap-2", children: e.map((a2) => ae.jsx(
          Trigger2,
          {
            value: a2.statusCode,
            className: mo(
              "text-xs font-mono px-1.5 py-1 pb-px translate-y-px border-b-2 border-transparent rounded-t cursor-pointer",
              "data-[state=active]:text-primary data-[state=active]:dark:text-inherit data-[state=active]:border-primary",
              "hover:border-accent-foreground/25"
            ),
            children: a2.statusCode
          },
          a2.statusCode
        )) })
      ] }),
      e.map((a2) => ae.jsx(Content2, { value: a2.statusCode, children: ae.jsx(ga, { content: a2.content ?? [] }) }, a2.statusCode))
    ]
  }
) });
var Hr = (e, n) => {
  const t2 = e[n.name];
  return t2 === void 0 ? (e[n.name] = n.value, e) : Array.isArray(t2) ? (t2.push(n.value), e) : (e[n.name] = [t2, n.value], e);
};
var Mr = "";
var zr = `
`;
var G = class {
  /**
   * Helper object to format and aggragate lines of code.
   * Lines are aggregated in a `code` array, and need to be joined to obtain a proper code snippet.
   */
  constructor({ indent: e, join: n } = {}) {
    this.postProcessors = [], this.code = [], this.indentationCharacter = Mr, this.lineJoin = zr, this.indentLine = (t2, a2 = 0) => `${this.indentationCharacter.repeat(a2)}${t2}`, this.unshift = (t2, a2) => {
      const c2 = this.indentLine(t2, a2);
      this.code.unshift(c2);
    }, this.push = (t2, a2) => {
      const c2 = this.indentLine(t2, a2);
      this.code.push(c2);
    }, this.blank = () => {
      this.code.push("");
    }, this.join = () => {
      const t2 = this.code.join(this.lineJoin);
      return this.postProcessors.reduce((c2, s) => s(c2), t2);
    }, this.addPostProcessor = (t2) => {
      this.postProcessors = [...this.postProcessors, t2];
    }, this.indentationCharacter = e || Mr, this.lineJoin = n ?? zr;
  }
};
var ht;
var Wr;
function qo() {
  return Wr || (Wr = 1, ht = function(e) {
    return Object.prototype.toString.call(e) === "[object RegExp]";
  }), ht;
}
var yt;
var Jr;
function Eo() {
  return Jr || (Jr = 1, yt = function(e) {
    var n = typeof e;
    return e !== null && (n === "object" || n === "function");
  }), yt;
}
var De = {};
var Vr;
function Ro() {
  return Vr || (Vr = 1, Object.defineProperty(De, "__esModule", { value: true }), De.default = (e) => Object.getOwnPropertySymbols(e).filter((n) => Object.prototype.propertyIsEnumerable.call(e, n))), De;
}
var mt;
var Gr;
function ko() {
  if (Gr) return mt;
  Gr = 1;
  const e = qo(), n = Eo(), t2 = Ro().default;
  return mt = (a2, c2, s) => {
    const r = [];
    return (function o(i4, l, d) {
      l = l || {}, l.indent = l.indent || "	", d = d || "";
      let y2;
      l.inlineCharacterLimit === void 0 ? y2 = {
        newLine: `
`,
        newLineOrSpace: `
`,
        pad: d,
        indent: d + l.indent
      } : y2 = {
        newLine: "@@__STRINGIFY_OBJECT_NEW_LINE__@@",
        newLineOrSpace: "@@__STRINGIFY_OBJECT_NEW_LINE_OR_SPACE__@@",
        pad: "@@__STRINGIFY_OBJECT_PAD__@@",
        indent: "@@__STRINGIFY_OBJECT_INDENT__@@"
      };
      const f3 = (h) => {
        if (l.inlineCharacterLimit === void 0)
          return h;
        const x = h.replace(new RegExp(y2.newLine, "g"), "").replace(new RegExp(y2.newLineOrSpace, "g"), " ").replace(new RegExp(y2.pad + "|" + y2.indent, "g"), "");
        return x.length <= l.inlineCharacterLimit ? x : h.replace(new RegExp(y2.newLine + "|" + y2.newLineOrSpace, "g"), `
`).replace(new RegExp(y2.pad, "g"), d).replace(new RegExp(y2.indent, "g"), d + l.indent);
      };
      if (r.indexOf(i4) !== -1)
        return '"[Circular]"';
      if (i4 == null || typeof i4 == "number" || typeof i4 == "boolean" || typeof i4 == "function" || typeof i4 == "symbol" || e(i4))
        return String(i4);
      if (i4 instanceof Date)
        return `new Date('${i4.toISOString()}')`;
      if (Array.isArray(i4)) {
        if (i4.length === 0)
          return "[]";
        r.push(i4);
        const h = "[" + y2.newLine + i4.map((x, $) => {
          const u2 = i4.length - 1 === $ ? y2.newLine : "," + y2.newLineOrSpace;
          let m = o(x, l, d + l.indent);
          return l.transform && (m = l.transform(i4, $, m)), y2.indent + m + u2;
        }).join("") + y2.pad + "]";
        return r.pop(), f3(h);
      }
      if (n(i4)) {
        let h = Object.keys(i4).concat(t2(i4));
        if (l.filter && (h = h.filter(($) => l.filter(i4, $))), h.length === 0)
          return "{}";
        r.push(i4);
        const x = "{" + y2.newLine + h.map(($, u2) => {
          const m = h.length - 1 === u2 ? y2.newLine : "," + y2.newLineOrSpace, v = typeof $ == "symbol", S = !v && /^[a-z$_][a-z$_0-9]*$/i.test($), T = v || S ? $ : o($, l);
          let p = o(i4[$], l, d + l.indent);
          return l.transform && (p = l.transform(i4, $, p)), y2.indent + String(T) + ": " + p + m;
        }).join("") + y2.pad + "}";
        return r.pop(), f3(x);
      }
      return i4 = String(i4).replace(/[\r\n]/g, (h) => h === `
` ? "\\n" : "\\r"), l.singleQuotes === false ? (i4 = i4.replace(/"/g, '\\"'), `"${i4}"`) : (i4 = i4.replace(/\\?'/g, "\\'"), `'${i4}'`);
    })(a2, c2, s);
  }, mt;
}
var No = ko();
var ue = f(No);
function Be2(e, n = {}) {
  const { delimiter: t2 = '"', escapeChar: a2 = "\\", escapeNewlines: c2 = true } = n;
  return [...e.toString()].map((r) => r === "\b" ? `${a2}b` : r === "	" ? `${a2}t` : r === `
` ? c2 ? `${a2}n` : r : r === "\f" ? `${a2}f` : r === "\r" ? c2 ? `${a2}r` : r : r === a2 ? a2 + a2 : r === t2 ? a2 + t2 : r < " " || r > "~" ? JSON.stringify(r).slice(1, -1) : r).join("");
}
var He = (e) => Be2(e, { delimiter: "'" });
var le = (e) => Be2(e, { delimiter: '"' });
var Ao = {
  info: {
    key: "libcurl",
    title: "Libcurl",
    link: "http://curl.haxx.se/libcurl",
    description: "Simple REST and HTTP API Client for C",
    extname: ".c"
  },
  convert: ({ method: e, fullUrl: n, headersObj: t2, allHeaders: a2, postData: c2 }) => {
    const { push: s, blank: r, join: o } = new G();
    s("CURL *hnd = curl_easy_init();"), r(), s(`curl_easy_setopt(hnd, CURLOPT_CUSTOMREQUEST, "${e.toUpperCase()}");`), s("curl_easy_setopt(hnd, CURLOPT_WRITEDATA, stdout);"), s(`curl_easy_setopt(hnd, CURLOPT_URL, "${n}");`);
    const i4 = Object.keys(t2);
    return i4.length && (r(), s("struct curl_slist *headers = NULL;"), i4.forEach((l) => {
      s(`headers = curl_slist_append(headers, "${l}: ${le(t2[l])}");`);
    }), s("curl_easy_setopt(hnd, CURLOPT_HTTPHEADER, headers);")), a2.cookie && (r(), s(`curl_easy_setopt(hnd, CURLOPT_COOKIE, "${a2.cookie}");`)), c2.text && (r(), s(`curl_easy_setopt(hnd, CURLOPT_POSTFIELDS, ${JSON.stringify(c2.text)});`)), r(), s("CURLcode ret = curl_easy_perform(hnd);"), o();
  }
};
var Io = {
  info: {
    key: "c",
    title: "C",
    default: "libcurl",
    cli: "c"
  },
  clientsById: {
    libcurl: Ao
  }
};
var fe = (e, n) => Object.keys(e).find((t2) => t2.toLowerCase() === n.toLowerCase());
var ve = (e, n) => {
  const t2 = fe(e, n);
  if (t2)
    return e[t2];
};
var tt = (e, n) => !!fe(e, n);
var Uo = (e) => ["application/json", "application/x-json", "text/json", "text/x-json", "+json"].some(
  (n) => e.indexOf(n) > -1
);
var Dr = class {
  constructor(e) {
    this.name = "", this.toString = () => `:${this.name}`, this.name = e;
  }
};
var Lo = class {
  constructor(e) {
    this.path = "", this.toString = () => `(clojure.java.io/file "${this.path}")`, this.path = e;
  }
};
var va = (e) => e === void 0 ? null : e === null ? "null" : e.constructor.name.toLowerCase();
var ba = (e) => e === void 0 ? true : va(e) === "object" ? Object.keys(e).length === 0 : false;
var Qr2 = (e) => (Object.keys(e).filter((n) => ba(e[n])).forEach((n) => {
  delete e[n];
}), e);
var Qe = (e, n) => {
  const t2 = " ".repeat(e);
  return n.replace(/\n/g, `
${t2}`);
};
var yr = (e) => {
  switch (va(e)) {
    case "string":
      return `"${e.replace(/"/g, '\\"')}"`;
    case "file":
      return e.toString();
    case "keyword":
      return e.toString();
    case "null":
      return "nil";
    case "regexp":
      return `#"${e.source}"`;
    case "object": {
      const n = Object.keys(e).reduce((t2, a2) => {
        const c2 = Qe(a2.length + 2, yr(e[a2]));
        return `${t2}:${a2} ${c2}
 `;
      }, "").trim();
      return `{${Qe(1, n)}}`;
    }
    case "array": {
      const n = e.reduce((t2, a2) => `${t2} ${yr(a2)}`, "").trim();
      return `[${Qe(1, n)}]`;
    }
    default:
      return e.toString();
  }
};
var Fo = {
  info: {
    key: "clj_http",
    title: "clj-http",
    link: "https://github.com/dakrone/clj-http",
    description: "An idiomatic clojure http client wrapping the apache client.",
    extname: ".clj"
  },
  convert: ({ queryObj: e, method: n, postData: t2, url: a2, allHeaders: c2 }, s) => {
    const { push: r, join: o } = new G({ indent: s?.indent }), i4 = ["get", "post", "put", "delete", "patch", "head", "options"];
    if (n = n.toLowerCase(), !i4.includes(n))
      return r("Method not supported"), o();
    const l = {
      headers: c2,
      "query-params": e
    };
    switch (t2.mimeType) {
      case "application/json":
        {
          l["content-type"] = new Dr("json"), l["form-params"] = t2.jsonObj;
          const d = fe(l.headers, "content-type");
          d && delete l.headers[d];
        }
        break;
      case "application/x-www-form-urlencoded":
        {
          l["form-params"] = t2.paramsObj;
          const d = fe(l.headers, "content-type");
          d && delete l.headers[d];
        }
        break;
      case "text/plain":
        {
          l.body = t2.text;
          const d = fe(l.headers, "content-type");
          d && delete l.headers[d];
        }
        break;
      case "multipart/form-data": {
        if (t2.params) {
          l.multipart = t2.params.map((y2) => y2.fileName && !y2.value ? {
            name: y2.name,
            content: new Lo(y2.fileName)
          } : {
            name: y2.name,
            content: y2.value
          });
          const d = fe(l.headers, "content-type");
          d && delete l.headers[d];
        }
        break;
      }
    }
    switch (ve(l.headers, "accept")) {
      case "application/json":
        {
          l.accept = new Dr("json");
          const d = fe(l.headers, "accept");
          d && delete l.headers[d];
        }
        break;
    }
    if (r(`(require '[clj-http.client :as client])
`), ba(Qr2(l)))
      r(`(client/${n} "${a2}")`);
    else {
      const d = 11 + n.length + a2.length, y2 = Qe(d, yr(Qr2(l)));
      r(`(client/${n} "${a2}" ${y2})`);
    }
    return o();
  }
};
var _o = {
  info: {
    key: "clojure",
    title: "Clojure",
    default: "clj_http"
  },
  clientsById: {
    clj_http: Fo
  }
};
var Bo = (e) => {
  let n = ve(e, "accept-encoding");
  if (!n)
    return [];
  const t2 = {
    gzip: "DecompressionMethods.GZip",
    deflate: "DecompressionMethods.Deflate"
  }, a2 = [];
  return typeof n == "string" && (n = [n]), n.forEach((c2) => {
    c2.split(",").forEach((s) => {
      const r = /\s*([^;\s]+)/.exec(s);
      if (r) {
        const o = t2[r[1]];
        o && a2.push(o);
      }
    });
  }), a2;
};
var Ho = {
  info: {
    key: "httpclient",
    title: "HttpClient",
    link: "https://docs.microsoft.com/en-us/dotnet/api/system.net.http.httpclient",
    description: ".NET Standard HTTP Client",
    extname: ".cs"
  },
  convert: ({ allHeaders: e, postData: n, method: t2, fullUrl: a2 }, c2) => {
    const s = {
      indent: "    ",
      ...c2
    }, { push: r, join: o } = new G({ indent: s.indent });
    r("using System.Net.Http.Headers;");
    let i4 = "";
    const l = !!e.cookie, d = Bo(e);
    (l || d.length) && (i4 = "clientHandler", r("var clientHandler = new HttpClientHandler"), r("{"), l && r("UseCookies = false,", 1), d.length && r(`AutomaticDecompression = ${d.join(" | ")},`, 1), r("};")), r(`var client = new HttpClient(${i4});`), r("var request = new HttpRequestMessage"), r("{");
    const y2 = ["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD", "OPTIONS", "TRACE"];
    t2 = t2.toUpperCase(), t2 && y2.includes(t2) ? t2 = `HttpMethod.${t2[0]}${t2.substring(1).toLowerCase()}` : t2 = `new HttpMethod("${t2}")`, r(`Method = ${t2},`, 1), r(`RequestUri = new Uri("${a2}"),`, 1);
    const f3 = Object.keys(e).filter((h) => {
      switch (h.toLowerCase()) {
        case "content-type":
        case "content-length":
        case "accept-encoding":
          return false;
        default:
          return true;
      }
    });
    if (f3.length && (r("Headers =", 1), r("{", 1), f3.forEach((h) => {
      r(`{ "${h}", "${le(e[h])}" },`, 2);
    }), r("},", 1)), n.text) {
      const h = n.mimeType;
      switch (h) {
        case "application/x-www-form-urlencoded":
          r("Content = new FormUrlEncodedContent(new Dictionary<string, string>", 1), r("{", 1), n.params?.forEach((x) => {
            r(`{ "${x.name}", "${x.value}" },`, 2);
          }), r("}),", 1);
          break;
        case "multipart/form-data":
          r("Content = new MultipartFormDataContent", 1), r("{", 1), n.params?.forEach((x) => {
            r(`new StringContent(${JSON.stringify(x.value || "")})`, 2), r("{", 2), r("Headers =", 3), r("{", 3), x.contentType && r(`ContentType = new MediaTypeHeaderValue("${x.contentType}"),`, 4), r('ContentDisposition = new ContentDispositionHeaderValue("form-data")', 4), r("{", 4), r(`Name = "${x.name}",`, 5), x.fileName && r(`FileName = "${x.fileName}",`, 5), r("}", 4), r("}", 3), r("},", 2);
          }), r("},", 1);
          break;
        default:
          r(`Content = new StringContent(${JSON.stringify(n.text || "")})`, 1), r("{", 1), r("Headers =", 2), r("{", 2), r(`ContentType = new MediaTypeHeaderValue("${h}")`, 3), r("}", 2), r("}", 1);
          break;
      }
    }
    return r("};"), r("using (var response = await client.SendAsync(request))"), r("{"), r("response.EnsureSuccessStatusCode();", 1), r("var body = await response.Content.ReadAsStringAsync();", 1), r("Console.WriteLine(body);", 1), r("}"), o();
  }
};
function Mo(e) {
  return e[0].toUpperCase() + e.slice(1).toLowerCase();
}
var zo = {
  info: {
    key: "restsharp",
    title: "RestSharp",
    link: "http://restsharp.org/",
    description: "Simple REST and HTTP API Client for .NET",
    extname: ".cs",
    installation: "dotnet add package RestSharp"
  },
  convert: ({ method: e, fullUrl: n, headersObj: t2, cookies: a2, postData: c2, uriObj: s }) => {
    const { push: r, join: o } = new G();
    if (!["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD", "OPTIONS"].includes(
      e.toUpperCase()
    ))
      return "Method not supported";
    r(`using RestSharp;

`), r(`var options = new RestClientOptions("${n}");`), r("var client = new RestClient(options);"), r('var request = new RestRequest("");');
    const l = c2.mimeType && c2.mimeType === "multipart/form-data";
    switch (l && r("request.AlwaysMultipartFormData = true;"), Object.keys(t2).forEach((d) => {
      if (c2.mimeType && d.toLowerCase() === "content-type" && c2.text) {
        l && c2.boundary && r(`request.FormBoundary = "${c2.boundary}";`);
        return;
      }
      r(`request.AddHeader("${d}", "${le(t2[d])}");`);
    }), a2.forEach(({ name: d, value: y2 }) => {
      r(`request.AddCookie("${d}", "${le(y2)}", "${s.pathname}", "${s.host}");`);
    }), c2.mimeType) {
      case "multipart/form-data":
        if (!c2.params) break;
        c2.params.forEach((d) => {
          d.fileName ? r(`request.AddFile("${d.name}", "${d.fileName}");`) : r(`request.AddParameter("${d.name}", "${d.value}");`);
        });
        break;
      case "application/x-www-form-urlencoded":
        if (!c2.params) break;
        c2.params.forEach((d) => {
          r(`request.AddParameter("${d.name}", "${d.value}");`);
        });
        break;
      case "application/json": {
        if (!c2.text) break;
        const d = JSON.stringify(c2.text);
        r(`request.AddJsonBody(${d}, false);`);
        break;
      }
      default:
        if (!c2.text) break;
        r(`request.AddStringBody("${c2.text}", "${c2.mimeType}");`);
    }
    return r(`var response = await client.${Mo(e)}Async(request);
`), r(`Console.WriteLine("{0}", response.Content);
`), o();
  }
};
var Wo = {
  info: {
    key: "csharp",
    title: "C#",
    default: "restsharp",
    cli: "dotnet"
  },
  clientsById: {
    httpclient: Ho,
    restsharp: zo
  }
};
var Jo = {
  info: {
    key: "native",
    title: "NewRequest",
    link: "http://golang.org/pkg/net/http/#NewRequest",
    description: "Golang HTTP client request",
    extname: ".go"
  },
  convert: ({ postData: e, method: n, allHeaders: t2, fullUrl: a2 }, c2 = {}) => {
    const { blank: s, push: r, join: o } = new G({ indent: "	" }), { showBoilerplate: i4 = true, checkErrors: l = false, printBody: d = true, timeout: y2 = -1 } = c2, f3 = l ? "err" : "_", h = i4 ? 1 : 0, x = () => {
      l && (r("if err != nil {", h), r("panic(err)", h + 1), r("}", h));
    };
    i4 && (r("package main"), s(), r("import ("), r('"fmt"', h), y2 > 0 && r('"time"', h), e.text && r('"strings"', h), r('"net/http"', h), d && r('"io"', h), r(")"), s(), r("func main() {"), s());
    const $ = y2 > 0, u2 = $, m = u2 ? "client" : "http.DefaultClient";
    return u2 && (r("client := http.Client{", h), $ && r(`Timeout: time.Duration(${y2} * time.Second),`, h + 1), r("}", h), s()), r(`url := "${a2}"`, h), s(), e.text ? (r(`payload := strings.NewReader(${JSON.stringify(e.text)})`, h), s(), r(`req, ${f3} := http.NewRequest("${n}", url, payload)`, h), s()) : (r(`req, ${f3} := http.NewRequest("${n}", url, nil)`, h), s()), x(), Object.keys(t2).length && (Object.keys(t2).forEach((v) => {
      r(`req.Header.Add("${v}", "${le(t2[v])}")`, h);
    }), s()), r(`res, ${f3} := ${m}.Do(req)`, h), x(), d && (s(), r("defer res.Body.Close()", h), r(`body, ${f3} := io.ReadAll(res.Body)`, h), x()), s(), d && r("fmt.Println(string(body))", h), i4 && (s(), r("}")), o();
  }
};
var Vo = {
  info: {
    key: "go",
    title: "Go",
    default: "native",
    cli: "go"
  },
  clientsById: {
    native: Jo
  }
};
var Kr = `\r
`;
var Go = {
  info: {
    key: "http1.1",
    title: "HTTP/1.1",
    link: "https://tools.ietf.org/html/rfc7230",
    description: "HTTP/1.1 request string in accordance with RFC 7230",
    extname: null
  },
  convert: ({ method: e, fullUrl: n, uriObj: t2, httpVersion: a2, allHeaders: c2, postData: s }, r) => {
    const o = {
      absoluteURI: false,
      autoContentLength: true,
      autoHost: true,
      ...r
    }, { blank: i4, push: l, join: d } = new G({ indent: "", join: Kr }), y2 = o.absoluteURI ? n : t2.path;
    l(`${e} ${y2} ${a2}`);
    const f3 = Object.keys(c2);
    if (f3.forEach(($) => {
      const u2 = $.toLowerCase().replace(/(^|-)(\w)/g, (m) => m.toUpperCase());
      l(`${u2}: ${c2[$]}`);
    }), o.autoHost && !f3.includes("host") && l(`Host: ${t2.host}`), o.autoContentLength && s.text && !f3.includes("content-length")) {
      const u2 = new TextEncoder().encode(s.text).length.toString();
      l(`Content-Length: ${u2}`);
    }
    i4();
    const h = d(), x = s.text || "";
    return `${h}${Kr}${x}`;
  }
};
var Do = {
  info: {
    key: "http",
    title: "HTTP",
    default: "http1.1"
  },
  clientsById: {
    "http1.1": Go
  }
};
var Qo = {
  info: {
    key: "asynchttp",
    title: "AsyncHttp",
    link: "https://github.com/AsyncHttpClient/async-http-client",
    description: "Asynchronous Http and WebSocket Client library for Java",
    extname: ".java"
  },
  convert: ({ method: e, allHeaders: n, postData: t2, fullUrl: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, { blank: r, push: o, join: i4 } = new G({ indent: s.indent });
    return o("AsyncHttpClient client = new DefaultAsyncHttpClient();"), o(`client.prepare("${e.toUpperCase()}", "${a2}")`), Object.keys(n).forEach((l) => {
      o(`.setHeader("${l}", "${le(n[l])}")`, 1);
    }), t2.text && o(`.setBody(${JSON.stringify(t2.text)})`, 1), o(".execute()", 1), o(".toCompletableFuture()", 1), o(".thenAccept(System.out::println)", 1), o(".join();", 1), r(), o("client.close();"), i4();
  }
};
var Ko = {
  info: {
    key: "nethttp",
    title: "java.net.http",
    link: "https://openjdk.java.net/groups/net/httpclient/intro.html",
    description: "Java Standardized HTTP Client API",
    extname: ".java"
  },
  convert: ({ allHeaders: e, fullUrl: n, method: t2, postData: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, { push: r, join: o } = new G({ indent: s.indent });
    return r("HttpRequest request = HttpRequest.newBuilder()"), r(`.uri(URI.create("${n}"))`, 2), Object.keys(e).forEach((i4) => {
      r(`.header("${i4}", "${le(e[i4])}")`, 2);
    }), a2.text ? r(
      `.method("${t2.toUpperCase()}", HttpRequest.BodyPublishers.ofString(${JSON.stringify(a2.text)}))`,
      2
    ) : r(`.method("${t2.toUpperCase()}", HttpRequest.BodyPublishers.noBody())`, 2), r(".build();", 2), r(
      "HttpResponse<String> response = HttpClient.newHttpClient().send(request, HttpResponse.BodyHandlers.ofString());"
    ), r("System.out.println(response.body());"), o();
  }
};
var Yo = {
  info: {
    key: "okhttp",
    title: "OkHttp",
    link: "http://square.github.io/okhttp/",
    description: "An HTTP Request Client Library",
    extname: ".java"
  },
  convert: ({ postData: e, method: n, fullUrl: t2, allHeaders: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, { push: r, blank: o, join: i4 } = new G({ indent: s.indent }), l = ["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD"], d = ["POST", "PUT", "DELETE", "PATCH"];
    return r("OkHttpClient client = new OkHttpClient();"), o(), e.text && (e.boundary ? r(`MediaType mediaType = MediaType.parse("${e.mimeType}; boundary=${e.boundary}");`) : r(`MediaType mediaType = MediaType.parse("${e.mimeType}");`), r(`RequestBody body = RequestBody.create(mediaType, ${JSON.stringify(e.text)});`)), r("Request request = new Request.Builder()"), r(`.url("${t2}")`, 1), l.includes(n.toUpperCase()) ? d.includes(n.toUpperCase()) ? e.text ? r(`.${n.toLowerCase()}(body)`, 1) : r(`.${n.toLowerCase()}(null)`, 1) : r(`.${n.toLowerCase()}()`, 1) : e.text ? r(`.method("${n.toUpperCase()}", body)`, 1) : r(`.method("${n.toUpperCase()}", null)`, 1), Object.keys(a2).forEach((y2) => {
      r(`.addHeader("${y2}", "${le(a2[y2])}")`, 1);
    }), r(".build();", 1), o(), r("Response response = client.newCall(request).execute();"), i4();
  }
};
var Xo = {
  info: {
    key: "unirest",
    title: "Unirest",
    link: "http://unirest.io/java.html",
    description: "Lightweight HTTP Request Client Library",
    extname: ".java"
  },
  convert: ({ method: e, allHeaders: n, postData: t2, fullUrl: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, { join: r, push: o } = new G({ indent: s.indent });
    return ["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD", "OPTIONS"].includes(e.toUpperCase()) ? o(`HttpResponse<String> response = Unirest.${e.toLowerCase()}("${a2}")`) : o(`HttpResponse<String> response = Unirest.customMethod("${e.toUpperCase()}","${a2}")`), Object.keys(n).forEach((l) => {
      o(`.header("${l}", "${le(n[l])}")`, 1);
    }), t2.text && o(`.body(${JSON.stringify(t2.text)})`, 1), o(".asString();", 1), r();
  }
};
var Zo = {
  info: {
    key: "java",
    title: "Java",
    default: "unirest"
  },
  clientsById: {
    asynchttp: Qo,
    nethttp: Ko,
    okhttp: Yo,
    unirest: Xo
  }
};
var ei = {
  info: {
    key: "axios",
    title: "Axios",
    link: "https://github.com/axios/axios",
    description: "Promise based HTTP client for the browser and node.js",
    extname: ".js",
    installation: "npm install axios --save"
  },
  convert: ({ allHeaders: e, method: n, url: t2, queryObj: a2, postData: c2 }, s) => {
    const r = {
      indent: "  ",
      ...s
    }, { blank: o, push: i4, join: l, addPostProcessor: d } = new G({ indent: r.indent });
    i4("import axios from 'axios';"), o();
    const y2 = {
      method: n,
      url: t2
    };
    switch (Object.keys(a2).length && (y2.params = a2), Object.keys(e).length && (y2.headers = e), c2.mimeType) {
      case "application/x-www-form-urlencoded":
        c2.params && (i4("const encodedParams = new URLSearchParams();"), c2.params.forEach((h) => {
          i4(`encodedParams.set('${h.name}', '${h.value}');`);
        }), o(), y2.data = "encodedParams,", d((h) => h.replace(/'encodedParams,'/, "encodedParams,")));
        break;
      case "application/json":
        c2.jsonObj && (y2.data = c2.jsonObj);
        break;
      case "multipart/form-data":
        if (!c2.params)
          break;
        i4("const form = new FormData();"), c2.params.forEach((h) => {
          i4(`form.append('${h.name}', '${h.value || h.fileName || ""}');`);
        }), o(), y2.data = "[form]";
        break;
      default:
        c2.text && (y2.data = c2.text);
    }
    const f3 = ue(y2, {
      indent: "  ",
      inlineCharacterLimit: 80
    }).replace('"[form]"', "form");
    return i4(`const options = ${f3};`), o(), i4("axios"), i4(".request(options)", 1), i4(".then(function (response) {", 1), i4("console.log(response.data);", 2), i4("})", 1), i4(".catch(function (error) {", 1), i4("console.error(error);", 2), i4("});", 1), l();
  }
};
var ti = {
  info: {
    key: "fetch",
    title: "fetch",
    link: "https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch",
    description: "Perform asynchronous HTTP requests with the Fetch API",
    extname: ".js"
  },
  convert: ({ method: e, allHeaders: n, postData: t2, fullUrl: a2 }, c2) => {
    const s = {
      indent: "  ",
      credentials: null,
      ...c2
    }, { blank: r, join: o, push: i4 } = new G({ indent: s.indent }), l = {
      method: e
    };
    switch (Object.keys(n).length && (l.headers = n), s.credentials !== null && (l.credentials = s.credentials), t2.mimeType) {
      case "application/x-www-form-urlencoded":
        l.body = t2.paramsObj ? t2.paramsObj : t2.text;
        break;
      case "application/json":
        t2.jsonObj && (l.body = t2.jsonObj);
        break;
      case "multipart/form-data":
        if (!t2.params)
          break;
        const d = fe(n, "content-type");
        d && delete n[d], i4("const form = new FormData();"), t2.params.forEach((y2) => {
          i4(`form.append('${y2.name}', '${y2.value || y2.fileName || ""}');`);
        }), r();
        break;
      default:
        t2.text && (l.body = t2.text);
    }
    return l.headers && !Object.keys(l.headers).length && delete l.headers, i4(
      `const options = ${ue(l, {
        indent: s.indent,
        inlineCharacterLimit: 80,
        // The Fetch API body only accepts string parameters, but stringified JSON can be difficult
        // to read, so we keep the object as a literal and use this transform function to wrap the
        // literal in a `JSON.stringify` call.
        transform: (d, y2, f3) => {
          if (y2 === "body") {
            if (t2.mimeType === "application/x-www-form-urlencoded")
              return `new URLSearchParams(${f3})`;
            if (t2.mimeType === "application/json")
              return `JSON.stringify(${f3})`;
          }
          return f3;
        }
      })};`
    ), r(), t2.params && t2.mimeType === "multipart/form-data" && (i4("options.body = form;"), r()), i4(`fetch('${a2}', options)`), i4(".then(response => response.json())", 1), i4(".then(response => console.log(response))", 1), i4(".catch(err => console.error(err));", 1), o();
  }
};
var ri = {
  info: {
    key: "jquery",
    title: "jQuery",
    link: "http://api.jquery.com/jquery.ajax/",
    description: "Perform an asynchronous HTTP (Ajax) requests with jQuery",
    extname: ".js"
  },
  convert: ({ fullUrl: e, method: n, allHeaders: t2, postData: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, { blank: r, push: o, join: i4 } = new G({ indent: s.indent }), l = {
      async: true,
      crossDomain: true,
      url: e,
      method: n,
      headers: t2
    };
    switch (a2.mimeType) {
      case "application/x-www-form-urlencoded":
        l.data = a2.paramsObj ? a2.paramsObj : a2.text;
        break;
      case "application/json":
        l.processData = false, l.data = a2.text;
        break;
      case "multipart/form-data":
        if (!a2.params)
          break;
        if (o("const form = new FormData();"), a2.params.forEach((y2) => {
          o(`form.append('${y2.name}', '${y2.value || y2.fileName || ""}');`);
        }), l.processData = false, l.contentType = false, l.mimeType = "multipart/form-data", l.data = "[form]", tt(t2, "content-type") && ve(t2, "content-type")?.includes("boundary")) {
          const y2 = fe(t2, "content-type");
          y2 && delete l.headers[y2];
        }
        r();
        break;
      default:
        a2.text && (l.data = a2.text);
    }
    const d = ue(l, { indent: s.indent }).replace("'[form]'", "form");
    return o(`const settings = ${d};`), r(), o("$.ajax(settings).done(function (response) {"), o("console.log(response);", 1), o("});"), i4();
  }
};
var ni = {
  info: {
    key: "xhr",
    title: "XMLHttpRequest",
    link: "https://developer.mozilla.org/en-US/docs/Web/API/XMLHttpRequest",
    description: "W3C Standard API that provides scripted client functionality",
    extname: ".js"
  },
  convert: ({ postData: e, allHeaders: n, method: t2, fullUrl: a2 }, c2) => {
    const s = {
      indent: "  ",
      cors: true,
      ...c2
    }, { blank: r, push: o, join: i4 } = new G({ indent: s.indent });
    switch (e.mimeType) {
      case "application/json":
        o(
          `const data = JSON.stringify(${ue(e.jsonObj, {
            indent: s.indent
          })});`
        ), r();
        break;
      case "multipart/form-data":
        if (!e.params)
          break;
        if (o("const data = new FormData();"), e.params.forEach((l) => {
          o(`data.append('${l.name}', '${l.value || l.fileName || ""}');`);
        }), tt(n, "content-type") && ve(n, "content-type")?.includes("boundary")) {
          const l = fe(n, "content-type");
          l && delete n[l];
        }
        r();
        break;
      default:
        o(`const data = ${e.text ? `'${e.text}'` : "null"};`), r();
    }
    return o("const xhr = new XMLHttpRequest();"), s.cors && o("xhr.withCredentials = true;"), r(), o("xhr.addEventListener('readystatechange', function () {"), o("if (this.readyState === this.DONE) {", 1), o("console.log(this.responseText);", 2), o("}", 1), o("});"), r(), o(`xhr.open('${t2}', '${a2}');`), Object.keys(n).forEach((l) => {
      o(`xhr.setRequestHeader('${l}', '${He(n[l])}');`);
    }), r(), o("xhr.send(data);"), i4();
  }
};
var ai = {
  info: {
    key: "javascript",
    title: "JavaScript",
    default: "xhr"
  },
  clientsById: {
    xhr: ni,
    axios: ei,
    fetch: ti,
    jquery: ri
  }
};
var oi = {
  info: {
    key: "native",
    title: "Native JSON",
    link: "https://www.json.org/json-en.html",
    description: "A JSON represetation of any HAR payload.",
    extname: ".json"
  },
  convert: ({ postData: e }, n) => {
    const t2 = {
      indent: "  ",
      ...n
    };
    let a2 = "";
    switch (e.mimeType) {
      case "application/x-www-form-urlencoded":
        a2 = e.paramsObj ? e.paramsObj : e.text;
        break;
      case "application/json":
        e.jsonObj && (a2 = e.jsonObj);
        break;
      case "multipart/form-data":
        if (!e.params)
          break;
        const c2 = {};
        e.params.forEach((s) => {
          c2[s.name] = s.value;
        }), a2 = c2;
        break;
      default:
        e.text && (a2 = e.text);
    }
    return typeof a2 > "u" || a2 === "" ? "No JSON body" : JSON.stringify(a2, null, t2.indent);
  }
};
var ii = {
  info: {
    key: "json",
    title: "JSON",
    default: "native"
  },
  clientsById: {
    native: oi
  }
};
var si = {
  info: {
    key: "okhttp",
    title: "OkHttp",
    link: "http://square.github.io/okhttp/",
    description: "An HTTP Request Client Library",
    extname: ".kt"
  },
  convert: ({ postData: e, fullUrl: n, method: t2, allHeaders: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, { blank: r, join: o, push: i4 } = new G({ indent: s.indent }), l = ["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD"], d = ["POST", "PUT", "DELETE", "PATCH"];
    return i4("val client = OkHttpClient()"), r(), e.text && (e.boundary ? i4(`val mediaType = MediaType.parse("${e.mimeType}; boundary=${e.boundary}")`) : i4(`val mediaType = MediaType.parse("${e.mimeType}")`), i4(`val body = RequestBody.create(mediaType, ${JSON.stringify(e.text)})`)), i4("val request = Request.Builder()"), i4(`.url("${n}")`, 1), l.includes(t2.toUpperCase()) ? d.includes(t2.toUpperCase()) ? e.text ? i4(`.${t2.toLowerCase()}(body)`, 1) : i4(`.${t2.toLowerCase()}(null)`, 1) : i4(`.${t2.toLowerCase()}()`, 1) : e.text ? i4(`.method("${t2.toUpperCase()}", body)`, 1) : i4(`.method("${t2.toUpperCase()}", null)`, 1), Object.keys(a2).forEach((y2) => {
      i4(`.addHeader("${y2}", "${le(a2[y2])}")`, 1);
    }), i4(".build()", 1), r(), i4("val response = client.newCall(request).execute()"), o();
  }
};
var li = {
  info: {
    key: "kotlin",
    title: "Kotlin",
    default: "okhttp"
  },
  clientsById: {
    okhttp: si
  }
};
var ci = {
  info: {
    key: "axios",
    title: "Axios",
    link: "https://github.com/axios/axios",
    description: "Promise based HTTP client for the browser and node.js",
    extname: ".cjs",
    installation: "npm install axios --save"
  },
  convert: ({ method: e, fullUrl: n, allHeaders: t2, postData: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, { blank: r, join: o, push: i4, addPostProcessor: l } = new G({ indent: s.indent });
    i4("const axios = require('axios');");
    const d = {
      method: e,
      url: n
    };
    switch (Object.keys(t2).length && (d.headers = t2), a2.mimeType) {
      case "application/x-www-form-urlencoded":
        a2.params && (i4("const { URLSearchParams } = require('url');"), r(), i4("const encodedParams = new URLSearchParams();"), a2.params.forEach((f3) => {
          i4(`encodedParams.set('${f3.name}', '${f3.value}');`);
        }), r(), d.data = "encodedParams,", l((f3) => f3.replace(/'encodedParams,'/, "encodedParams,")));
        break;
      case "application/json":
        r(), a2.jsonObj && (d.data = a2.jsonObj);
        break;
      default:
        r(), a2.text && (d.data = a2.text);
    }
    const y2 = ue(d, { indent: "  ", inlineCharacterLimit: 80 });
    return i4(`const options = ${y2};`), r(), i4("axios"), i4(".request(options)", 1), i4(".then(function (response) {", 1), i4("console.log(response.data);", 2), i4("})", 1), i4(".catch(function (error) {", 1), i4("console.error(error);", 2), i4("});", 1), o();
  }
};
var ui = {
  info: {
    key: "fetch",
    title: "Fetch",
    link: "https://github.com/bitinn/node-fetch",
    description: "Simplified HTTP node-fetch client",
    extname: ".cjs",
    installation: "npm install node-fetch@2 --save"
  },
  convert: ({ method: e, fullUrl: n, postData: t2, headersObj: a2, cookies: c2 }, s) => {
    const r = {
      indent: "  ",
      ...s
    };
    let o = false;
    const { blank: i4, push: l, join: d, unshift: y2 } = new G({ indent: r.indent });
    l("const fetch = require('node-fetch');");
    const f3 = n, h = {
      method: e
    };
    switch (Object.keys(a2).length && (h.headers = a2), t2.mimeType) {
      case "application/x-www-form-urlencoded":
        y2("const { URLSearchParams } = require('url');"), l("const encodedParams = new URLSearchParams();"), i4(), t2.params?.forEach((u2) => {
          l(`encodedParams.set('${u2.name}', '${u2.value}');`);
        }), h.body = "encodedParams";
        break;
      case "application/json":
        t2.jsonObj && (h.body = t2.jsonObj);
        break;
      case "multipart/form-data":
        if (!t2.params)
          break;
        const $ = fe(a2, "content-type");
        $ && delete a2[$], y2("const FormData = require('form-data');"), l("const formData = new FormData();"), i4(), t2.params.forEach((u2) => {
          if (!u2.fileName && !u2.fileName && !u2.contentType) {
            l(`formData.append('${u2.name}', '${u2.value}');`);
            return;
          }
          u2.fileName && (o = true, l(`formData.append('${u2.name}', fs.createReadStream('${u2.fileName}'));`));
        });
        break;
      default:
        t2.text && (h.body = t2.text);
    }
    if (c2.length) {
      const $ = c2.map(({ name: u2, value: m }) => `${encodeURIComponent(u2)}=${encodeURIComponent(m)}`).join("; ");
      h.headers || (h.headers = {}), h.headers.cookie = $;
    }
    i4(), l(`const url = '${f3}';`), h.headers && !Object.keys(h.headers).length && delete h.headers;
    const x = ue(h, {
      indent: "  ",
      inlineCharacterLimit: 80,
      // The Fetch API body only accepts string parameters, but stringified JSON can be difficult to
      // read, so we keep the object as a literal and use this transform function to wrap the literal
      // in a `JSON.stringify` call.
      transform: ($, u2, m) => u2 === "body" && t2.mimeType === "application/json" ? `JSON.stringify(${m})` : m
    });
    return l(`const options = ${x};`), i4(), o && y2("const fs = require('fs');"), t2.params && t2.mimeType === "multipart/form-data" && (l("options.body = formData;"), i4()), l("fetch(url, options)"), l(".then(res => res.json())", 1), l(".then(json => console.log(json))", 1), l(".catch(err => console.error('error:' + err));", 1), d().replace(/'encodedParams'/, "encodedParams").replace(/"fs\.createReadStream\(\\"(.+)\\"\)"/, 'fs.createReadStream("$1")');
  }
};
var fi = {
  info: {
    key: "native",
    title: "HTTP",
    link: "http://nodejs.org/api/http.html#http_http_request_options_callback",
    description: "Node.js native HTTP interface",
    extname: ".cjs"
  },
  convert: ({ uriObj: e, method: n, allHeaders: t2, postData: a2 }, c2 = {}) => {
    const { indent: s = "  " } = c2, { blank: r, join: o, push: i4, unshift: l } = new G({ indent: s }), d = {
      method: n,
      hostname: e.hostname,
      port: e.port,
      path: e.path,
      headers: t2
    };
    switch (i4(`const http = require('${e.protocol?.replace(":", "")}');`), r(), i4(`const options = ${ue(d, { indent: s })};`), r(), i4("const req = http.request(options, function (res) {"), i4("const chunks = [];", 1), r(), i4("res.on('data', function (chunk) {", 1), i4("chunks.push(chunk);", 2), i4("});", 1), r(), i4("res.on('end', function () {", 1), i4("const body = Buffer.concat(chunks);", 2), i4("console.log(body.toString());", 2), i4("});", 1), i4("});"), r(), a2.mimeType) {
      case "application/x-www-form-urlencoded":
        a2.paramsObj && (l("const qs = require('querystring');"), i4(
          `req.write(qs.stringify(${ue(a2.paramsObj, {
            indent: "  ",
            inlineCharacterLimit: 80
          })}));`
        ));
        break;
      case "application/json":
        a2.jsonObj && i4(
          `req.write(JSON.stringify(${ue(a2.jsonObj, {
            indent: "  ",
            inlineCharacterLimit: 80
          })}));`
        );
        break;
      default:
        a2.text && i4(`req.write(${ue(a2.text, { indent: s })});`);
    }
    return i4("req.end();"), o();
  }
};
var pi = {
  info: {
    key: "request",
    title: "Request",
    link: "https://github.com/request/request",
    description: "Simplified HTTP request client",
    extname: ".cjs",
    installation: "npm install request --save"
  },
  convert: ({ method: e, url: n, fullUrl: t2, postData: a2, headersObj: c2, cookies: s }, r) => {
    const o = {
      indent: "  ",
      ...r
    };
    let i4 = false;
    const { push: l, blank: d, join: y2, unshift: f3, addPostProcessor: h } = new G({ indent: o.indent });
    l("const request = require('request');"), d();
    const x = {
      method: e,
      url: t2
    };
    switch (Object.keys(c2).length && (x.headers = c2), a2.mimeType) {
      case "application/x-www-form-urlencoded":
        x.form = a2.paramsObj;
        break;
      case "application/json":
        a2.jsonObj && (x.body = a2.jsonObj, x.json = true);
        break;
      case "multipart/form-data":
        if (!a2.params)
          break;
        x.formData = {}, a2.params.forEach(($) => {
          if (!$.fileName && !$.fileName && !$.contentType) {
            x.formData[$.name] = $.value;
            return;
          }
          let u2 = {};
          $.fileName ? (i4 = true, u2 = {
            value: `fs.createReadStream(${$.fileName})`,
            options: {
              filename: $.fileName,
              contentType: $.contentType ? $.contentType : null
            }
          }) : $.value && (u2.value = $.value), x.formData[$.name] = u2;
        }), h(($) => $.replace(/'fs\.createReadStream\((.*)\)'/, "fs.createReadStream('$1')"));
        break;
      default:
        a2.text && (x.body = a2.text);
    }
    return s.length && (x.jar = "JAR", l("const jar = request.jar();"), s.forEach(({ name: $, value: u2 }) => {
      l(`jar.setCookie(request.cookie('${encodeURIComponent($)}=${encodeURIComponent(u2)}'), '${n}');`);
    }), d(), h(($) => $.replace(/'JAR'/, "jar"))), i4 && f3("const fs = require('fs');"), l(`const options = ${ue(x, { indent: "  ", inlineCharacterLimit: 80 })};`), d(), l("request(options, function (error, response, body) {"), l("if (error) throw new Error(error);", 1), d(), l("console.log(body);", 1), l("});"), y2();
  }
};
var di = {
  info: {
    key: "unirest",
    title: "Unirest",
    link: "http://unirest.io/nodejs.html",
    description: "Lightweight HTTP Request Client Library",
    extname: ".cjs"
  },
  convert: ({ method: e, url: n, cookies: t2, queryObj: a2, postData: c2, headersObj: s }, r) => {
    const o = {
      indent: "  ",
      ...r
    };
    let i4 = false;
    const { addPostProcessor: l, blank: d, join: y2, push: f3, unshift: h } = new G({
      indent: o.indent
    });
    switch (f3("const unirest = require('unirest');"), d(), f3(`const req = unirest('${e}', '${n}');`), d(), t2.length && (f3("const CookieJar = unirest.jar();"), t2.forEach((x) => {
      f3(`CookieJar.add('${encodeURIComponent(x.name)}=${encodeURIComponent(x.value)}', '${n}');`);
    }), f3("req.jar(CookieJar);"), d()), Object.keys(a2).length && (f3(`req.query(${ue(a2, { indent: o.indent })});`), d()), Object.keys(s).length && (f3(`req.headers(${ue(s, { indent: o.indent })});`), d()), c2.mimeType) {
      case "application/x-www-form-urlencoded":
        c2.paramsObj && (f3(`req.form(${ue(c2.paramsObj, { indent: o.indent })});`), d());
        break;
      case "application/json":
        c2.jsonObj && (f3("req.type('json');"), f3(`req.send(${ue(c2.jsonObj, { indent: o.indent })});`), d());
        break;
      case "multipart/form-data": {
        if (!c2.params)
          break;
        const x = [];
        c2.params.forEach(($) => {
          const u2 = {};
          $.fileName && !$.value ? (i4 = true, u2.body = `fs.createReadStream('${$.fileName}')`, l((m) => m.replace(/'fs\.createReadStream\(\\'(.+)\\'\)'/, "fs.createReadStream('$1')"))) : $.value && (u2.body = $.value), u2.body && ($.contentType && (u2["content-type"] = $.contentType), x.push(u2));
        }), f3(`req.multipart(${ue(x, { indent: o.indent })});`), d();
        break;
      }
      default:
        c2.text && (f3(`req.send(${ue(c2.text, { indent: o.indent })});`), d());
    }
    return i4 && h("const fs = require('fs');"), f3("req.end(function (res) {"), f3("if (res.error) throw new Error(res.error);", 1), d(), f3("console.log(res.body);", 1), f3("});"), y2();
  }
};
var hi = {
  info: {
    key: "node",
    title: "Node.js",
    default: "native",
    cli: "node %s"
  },
  clientsById: {
    native: fi,
    request: pi,
    unirest: di,
    axios: ci,
    fetch: ui
  }
};
var gt = (e, n, t2, a2) => {
  const c2 = `${e} *${n} = `, s = mr(t2, a2 ? c2.length : void 0);
  return `${c2}${s};`;
};
var mr = (e, n) => {
  const t2 = n === void 0 ? ", " : `,
   ${" ".repeat(n)}`;
  switch (Object.prototype.toString.call(e)) {
    case "[object Number]":
      return `@${e}`;
    case "[object Array]":
      return `@[ ${e.map((c2) => mr(c2)).join(t2)} ]`;
    case "[object Object]": {
      const a2 = [];
      return Object.keys(e).forEach((c2) => {
        a2.push(`@"${c2}": ${mr(e[c2])}`);
      }), `@{ ${a2.join(t2)} }`;
    }
    case "[object Boolean]":
      return e ? "@YES" : "@NO";
    default:
      return e == null ? "" : `@"${e.toString().replace(/"/g, '\\"')}"`;
  }
};
var yi = {
  info: {
    key: "nsurlsession",
    title: "NSURLSession",
    link: "https://developer.apple.com/library/mac/documentation/Foundation/Reference/NSURLSession_class/index.html",
    description: "Foundation's NSURLSession request",
    extname: ".m"
  },
  convert: ({ allHeaders: e, postData: n, method: t2, fullUrl: a2 }, c2) => {
    const s = {
      indent: "    ",
      pretty: true,
      timeout: 10,
      ...c2
    }, { push: r, join: o, blank: i4 } = new G({ indent: s.indent }), l = {
      hasHeaders: false,
      hasBody: false
    };
    if (r("#import <Foundation/Foundation.h>"), Object.keys(e).length && (l.hasHeaders = true, i4(), r(gt("NSDictionary", "headers", e, s.pretty))), n.text || n.jsonObj || n.params)
      switch (l.hasBody = true, n.mimeType) {
        case "application/x-www-form-urlencoded":
          if (n.params?.length) {
            i4();
            const [d, ...y2] = n.params;
            r(
              `NSMutableData *postData = [[NSMutableData alloc] initWithData:[@"${d.name}=${d.value}" dataUsingEncoding:NSUTF8StringEncoding]];`
            ), y2.forEach(({ name: f3, value: h }) => {
              r(`[postData appendData:[@"&${f3}=${h}" dataUsingEncoding:NSUTF8StringEncoding]];`);
            });
          } else
            l.hasBody = false;
          break;
        case "application/json":
          n.jsonObj && (r(gt("NSDictionary", "parameters", n.jsonObj, s.pretty)), i4(), r("NSData *postData = [NSJSONSerialization dataWithJSONObject:parameters options:0 error:nil];"));
          break;
        case "multipart/form-data":
          r(gt("NSArray", "parameters", n.params || [], s.pretty)), r(`NSString *boundary = @"${n.boundary}";`), i4(), r("NSError *error;"), r("NSMutableString *body = [NSMutableString string];"), r("for (NSDictionary *param in parameters) {"), r('[body appendFormat:@"--%@\\r\\n", boundary];', 1), r('if (param[@"fileName"]) {', 1), r(
            '[body appendFormat:@"Content-Disposition:form-data; name=\\"%@\\"; filename=\\"%@\\"\\r\\n", param[@"name"], param[@"fileName"]];',
            2
          ), r('[body appendFormat:@"Content-Type: %@\\r\\n\\r\\n", param[@"contentType"]];', 2), r(
            '[body appendFormat:@"%@", [NSString stringWithContentsOfFile:param[@"fileName"] encoding:NSUTF8StringEncoding error:&error]];',
            2
          ), r("if (error) {", 2), r('NSLog(@"%@", error);', 3), r("}", 2), r("} else {", 1), r('[body appendFormat:@"Content-Disposition:form-data; name=\\"%@\\"\\r\\n\\r\\n", param[@"name"]];', 2), r('[body appendFormat:@"%@", param[@"value"]];', 2), r("}", 1), r("}"), r('[body appendFormat:@"\\r\\n--%@--\\r\\n", boundary];'), r("NSData *postData = [body dataUsingEncoding:NSUTF8StringEncoding];");
          break;
        default:
          i4(), r(
            `NSData *postData = [[NSData alloc] initWithData:[@"${n.text}" dataUsingEncoding:NSUTF8StringEncoding]];`
          );
      }
    return i4(), r(`NSMutableURLRequest *request = [NSMutableURLRequest requestWithURL:[NSURL URLWithString:@"${a2}"]`), r("                                                       cachePolicy:NSURLRequestUseProtocolCachePolicy"), r(`                                                   timeoutInterval:${s.timeout.toFixed(1)}];`), r(`[request setHTTPMethod:@"${t2}"];`), l.hasHeaders && r("[request setAllHTTPHeaderFields:headers];"), l.hasBody && r("[request setHTTPBody:postData];"), i4(), r("NSURLSession *session = [NSURLSession sharedSession];"), r("NSURLSessionDataTask *dataTask = [session dataTaskWithRequest:request"), r(
      "                                            completionHandler:^(NSData *data, NSURLResponse *response, NSError *error) {"
    ), r("                                            if (error) {", 1), r('                                            NSLog(@"%@", error);', 2), r("                                            } else {", 1), r(
      "                                            NSHTTPURLResponse *httpResponse = (NSHTTPURLResponse *) response;",
      2
    ), r('                                            NSLog(@"%@", httpResponse);', 2), r("                                            }", 1), r("                                            }];"), r("[dataTask resume];"), o();
  }
};
var mi = {
  info: {
    key: "objc",
    title: "Objective-C",
    default: "nsurlsession"
  },
  clientsById: {
    nsurlsession: yi
  }
};
var gi = {
  info: {
    key: "cohttp",
    title: "CoHTTP",
    link: "https://github.com/mirage/ocaml-cohttp",
    description: "Cohttp is a very lightweight HTTP server using Lwt or Async for OCaml",
    extname: ".ml",
    installation: "opam install cohttp-lwt-unix cohttp-async"
  },
  convert: ({ fullUrl: e, allHeaders: n, postData: t2, method: a2 }, c2) => {
    const s = {
      indent: "  ",
      ...c2
    }, r = ["get", "post", "head", "delete", "patch", "put", "options"], { push: o, blank: i4, join: l } = new G({ indent: s.indent });
    o("open Cohttp_lwt_unix"), o("open Cohttp"), o("open Lwt"), i4(), o(`let uri = Uri.of_string "${e}" in`);
    const d = Object.keys(n);
    d.length === 1 ? o(
      `let headers = Header.add (Header.init ()) "${d[0]}" "${le(
        n[d[0]]
      )}" in`
    ) : d.length > 1 && (o("let headers = Header.add_list (Header.init ()) ["), d.forEach((x) => {
      o(`("${x}", "${le(n[x])}");`, 1);
    }), o("] in")), t2.text && o(`let body = Cohttp_lwt_body.of_string ${JSON.stringify(t2.text)} in`), i4();
    const y2 = d.length ? "~headers " : "", f3 = t2.text ? "~body " : "", h = r.includes(a2.toLowerCase()) ? `\`${a2.toUpperCase()}` : `(Code.method_of_string "${a2}")`;
    return o(`Client.call ${y2}${f3}${h} uri`), o(">>= fun (res, body_stream) ->"), o("(* Do stuff with the result *)", 1), l();
  }
};
var vi = {
  info: {
    key: "ocaml",
    title: "OCaml",
    default: "cohttp"
  },
  clientsById: {
    cohttp: gi
  }
};
var te2 = (e, n, t2) => {
  switch (t2 = t2 || "", n = n || "", Object.prototype.toString.call(e)) {
    case "[object Boolean]":
      return e;
    case "[object Null]":
      return "null";
    case "[object Undefined]":
      return "null";
    case "[object String]":
      return `'${Be2(e, { delimiter: "'", escapeNewlines: false })}'`;
    case "[object Number]":
      return e.toString();
    case "[object Array]": {
      const a2 = e.map((c2) => te2(c2, `${n}${n}`, n)).join(`,
${n}`);
      return `[
${n}${a2}
${t2}]`;
    }
    case "[object Object]": {
      const a2 = [];
      for (const c2 in e)
        Object.prototype.hasOwnProperty.call(e, c2) && a2.push(`${te2(c2, n)} => ${te2(e[c2], `${n}${n}`, n)}`);
      return `[
${n}${a2.join(`,
${n}`)}
${t2}]`;
    }
    default:
      return "null";
  }
};
var Yr = [
  "ACL",
  "BASELINE_CONTROL",
  "CHECKIN",
  "CHECKOUT",
  "CONNECT",
  "COPY",
  "DELETE",
  "GET",
  "HEAD",
  "LABEL",
  "LOCK",
  "MERGE",
  "MKACTIVITY",
  "MKCOL",
  "MKWORKSPACE",
  "MOVE",
  "OPTIONS",
  "POST",
  "PROPFIND",
  "PROPPATCH",
  "PUT",
  "REPORT",
  "TRACE",
  "UNCHECKOUT",
  "UNLOCK",
  "UPDATE",
  "VERSION_CONTROL"
];
var bi = {
  info: {
    key: "curl",
    title: "cURL",
    link: "http://php.net/manual/en/book.curl.php",
    description: "PHP with ext-curl",
    extname: ".php"
  },
  convert: ({ uriObj: e, postData: n, fullUrl: t2, method: a2, httpVersion: c2, cookies: s, headersObj: r }, o = {}) => {
    const {
      closingTag: i4 = false,
      indent: l = "  ",
      maxRedirects: d = 10,
      namedErrors: y2 = false,
      noTags: f3 = false,
      shortTags: h = false,
      timeout: x = 30
    } = o, { push: $, blank: u2, join: m } = new G({ indent: l });
    f3 || ($(h ? "<?" : "<?php"), u2()), $("$curl = curl_init();"), u2();
    const v = [
      {
        escape: true,
        name: "CURLOPT_PORT",
        value: e.port
      },
      {
        escape: true,
        name: "CURLOPT_URL",
        value: t2
      },
      {
        escape: false,
        name: "CURLOPT_RETURNTRANSFER",
        value: "true"
      },
      {
        escape: true,
        name: "CURLOPT_ENCODING",
        value: ""
      },
      {
        escape: false,
        name: "CURLOPT_MAXREDIRS",
        value: d
      },
      {
        escape: false,
        name: "CURLOPT_TIMEOUT",
        value: x
      },
      {
        escape: false,
        name: "CURLOPT_HTTP_VERSION",
        value: c2 === "HTTP/1.0" ? "CURL_HTTP_VERSION_1_0" : "CURL_HTTP_VERSION_1_1"
      },
      {
        escape: true,
        name: "CURLOPT_CUSTOMREQUEST",
        value: a2
      },
      {
        escape: !n.jsonObj,
        name: "CURLOPT_POSTFIELDS",
        value: n ? n.jsonObj ? `json_encode(${te2(n.jsonObj, l.repeat(2), l)})` : n.text : void 0
      }
    ];
    $("curl_setopt_array($curl, [");
    const S = new G({ indent: l, join: `
${l}` });
    v.forEach(({ value: O, name: g4, escape: R2 }) => {
      O != null && S.push(`${g4} => ${R2 ? JSON.stringify(O) : O},`);
    });
    const T = s.map((O) => `${encodeURIComponent(O.name)}=${encodeURIComponent(O.value)}`);
    T.length && S.push(`CURLOPT_COOKIE => "${T.join("; ")}",`);
    const p = Object.keys(r).sort().map((O) => `"${O}: ${le(r[O])}"`);
    return p.length && (S.push("CURLOPT_HTTPHEADER => ["), S.push(p.join(`,
${l}${l}`), 1), S.push("],")), $(S.join(), 1), $("]);"), u2(), $("$response = curl_exec($curl);"), $("$err = curl_error($curl);"), u2(), $("curl_close($curl);"), u2(), $("if ($err) {"), $(y2 ? 'echo array_flip(get_defined_constants(true)["curl"])[$err];' : 'echo "cURL Error #:" . $err;', 1), $("} else {"), $("echo $response;", 1), $("}"), !f3 && i4 && (u2(), $("?>")), m();
  }
};
var xi = {
  info: {
    key: "guzzle",
    title: "Guzzle",
    link: "http://docs.guzzlephp.org/en/stable/",
    description: "PHP with Guzzle",
    extname: ".php",
    installation: "composer require guzzlehttp/guzzle"
  },
  convert: ({ postData: e, fullUrl: n, method: t2, cookies: a2, headersObj: c2 }, s) => {
    const r = {
      closingTag: false,
      indent: "  ",
      noTags: false,
      shortTags: false,
      ...s
    }, { push: o, blank: i4, join: l } = new G({ indent: r.indent }), { code: d, push: y2, join: f3 } = new G({ indent: r.indent });
    switch (r.noTags || o(r.shortTags ? "<?" : "<?php"), o("require_once('vendor/autoload.php');"), i4(), e.mimeType) {
      case "application/x-www-form-urlencoded":
        y2(`'form_params' => ${te2(e.paramsObj, r.indent + r.indent, r.indent)},`, 1);
        break;
      case "multipart/form-data": {
        const $ = [];
        if (e.params && e.params.forEach((u2) => {
          if (u2.fileName) {
            const m = {
              name: u2.name,
              filename: u2.fileName,
              contents: u2.value
            };
            u2.contentType && (m.headers = { "Content-Type": u2.contentType }), $.push(m);
          } else u2.value && $.push({
            name: u2.name,
            contents: u2.value
          });
        }), $.length && (y2(`'multipart' => ${te2($, r.indent + r.indent, r.indent)}`, 1), tt(c2, "content-type") && ve(c2, "content-type")?.indexOf("boundary"))) {
          const u2 = fe(c2, "content-type");
          u2 && delete c2[u2];
        }
        break;
      }
      default:
        e.text && y2(`'body' => ${te2(e.text)},`, 1);
    }
    const h = Object.keys(c2).sort().map(function($) {
      return `${r.indent}${r.indent}'${$}' => '${He(c2[$])}',`;
    }), x = a2.map(($) => `${encodeURIComponent($.name)}=${encodeURIComponent($.value)}`).join("; ");
    return x.length && h.push(`${r.indent}${r.indent}'cookie' => '${He(x)}',`), h.length && (y2("'headers' => [", 1), y2(h.join(`
`)), y2("],", 1)), o("$client = new \\GuzzleHttp\\Client();"), i4(), d.length ? (o(`$response = $client->request('${t2}', '${n}', [`), o(f3()), o("]);")) : o(`$response = $client->request('${t2}', '${n}');`), i4(), o("echo $response->getBody();"), !r.noTags && r.closingTag && (i4(), o("?>")), l();
  }
};
var $i = {
  info: {
    key: "http1",
    title: "HTTP v1",
    link: "http://php.net/manual/en/book.http.php",
    description: "PHP with pecl/http v1",
    extname: ".php"
  },
  convert: ({ method: e, url: n, postData: t2, queryObj: a2, headersObj: c2, cookiesObj: s }, r = {}) => {
    const { closingTag: o = false, indent: i4 = "  ", noTags: l = false, shortTags: d = false } = r, { push: y2, blank: f3, join: h } = new G({ indent: i4 });
    switch (l || (y2(d ? "<?" : "<?php"), f3()), Yr.includes(e.toUpperCase()) || y2(`HttpRequest::methodRegister('${e}');`), y2("$request = new HttpRequest();"), y2(`$request->setUrl(${te2(n)});`), Yr.includes(e.toUpperCase()) ? y2(`$request->setMethod(HTTP_METH_${e.toUpperCase()});`) : y2(`$request->setMethod(HttpRequest::HTTP_METH_${e.toUpperCase()});`), f3(), Object.keys(a2).length && (y2(`$request->setQueryData(${te2(a2, i4)});`), f3()), Object.keys(c2).length && (y2(`$request->setHeaders(${te2(c2, i4)});`), f3()), Object.keys(s).length && (y2(`$request->setCookies(${te2(s, i4)});`), f3()), t2.mimeType) {
      case "application/x-www-form-urlencoded":
        y2(`$request->setContentType(${te2(t2.mimeType)});`), y2(`$request->setPostFields(${te2(t2.paramsObj, i4)});`), f3();
        break;
      case "application/json":
        y2(`$request->setContentType(${te2(t2.mimeType)});`), y2(`$request->setBody(json_encode(${te2(t2.jsonObj, i4)}));`), f3();
        break;
      default:
        t2.text && (y2(`$request->setBody(${te2(t2.text)});`), f3());
    }
    return y2("try {"), y2("$response = $request->send();", 1), f3(), y2("echo $response->getBody();", 1), y2("} catch (HttpException $ex) {"), y2("echo $ex;", 1), y2("}"), !l && o && (f3(), y2("?>")), h();
  }
};
var wi = {
  info: {
    key: "http2",
    title: "HTTP v2",
    link: "http://devel-m6w6.rhcloud.com/mdref/http",
    description: "PHP with pecl/http v2",
    extname: ".php"
  },
  convert: ({ postData: e, headersObj: n, method: t2, queryObj: a2, cookiesObj: c2, url: s }, r = {}) => {
    const { closingTag: o = false, indent: i4 = "  ", noTags: l = false, shortTags: d = false } = r, { push: y2, blank: f3, join: h } = new G({ indent: i4 });
    let x = false;
    switch (l || (y2(d ? "<?" : "<?php"), f3()), y2("$client = new http\\Client;"), y2("$request = new http\\Client\\Request;"), f3(), e.mimeType) {
      case "application/x-www-form-urlencoded":
        y2("$body = new http\\Message\\Body;"), y2(`$body->append(new http\\QueryString(${te2(e.paramsObj, i4)}));`), f3(), x = true;
        break;
      case "multipart/form-data": {
        if (!e.params)
          break;
        const $ = [], u2 = {};
        e.params.forEach(({ name: S, fileName: T, value: p, contentType: O }) => {
          if (T) {
            $.push({
              name: S,
              type: O,
              file: T,
              data: p
            });
            return;
          }
          p && (u2[S] = p);
        });
        const m = Object.keys(u2).length ? te2(u2, i4) : "null", v = $.length ? te2($, i4) : "null";
        if (y2("$body = new http\\Message\\Body;"), y2(`$body->addForm(${m}, ${v});`), tt(n, "content-type") && ve(n, "content-type")?.indexOf("boundary")) {
          const S = fe(n, "content-type");
          S && delete n[S];
        }
        f3(), x = true;
        break;
      }
      case "application/json":
        y2("$body = new http\\Message\\Body;"), y2(`$body->append(json_encode(${te2(e.jsonObj, i4)}));`), x = true;
        break;
      default:
        e.text && (y2("$body = new http\\Message\\Body;"), y2(`$body->append(${te2(e.text)});`), f3(), x = true);
    }
    return y2(`$request->setRequestUrl(${te2(s)});`), y2(`$request->setRequestMethod(${te2(t2)});`), x && (y2("$request->setBody($body);"), f3()), Object.keys(a2).length && (y2(`$request->setQuery(new http\\QueryString(${te2(a2, i4)}));`), f3()), Object.keys(n).length && (y2(`$request->setHeaders(${te2(n, i4)});`), f3()), Object.keys(c2).length && (f3(), y2(`$client->setCookies(${te2(c2, i4)});`), f3()), y2("$client->enqueue($request)->send();"), y2("$response = $client->getResponse();"), f3(), y2("echo $response->getBody();"), !l && o && (f3(), y2("?>")), h();
  }
};
var ji = {
  info: {
    key: "php",
    title: "PHP",
    default: "curl",
    cli: "php %s"
  },
  clientsById: {
    curl: bi,
    guzzle: xi,
    http1: $i,
    http2: wi
  }
};
var xa = (e) => ({ method: t2, headersObj: a2, cookies: c2, uriObj: s, fullUrl: r, postData: o, allHeaders: i4 }) => {
  const { push: l, join: d } = new G();
  if (!["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD", "OPTIONS"].includes(t2.toUpperCase()))
    return "Method not supported";
  const f3 = [], h = Object.keys(a2);
  return h.length && (l("$headers=@{}"), h.forEach((x) => {
    x !== "connection" && l(`$headers.Add("${x}", "${Be2(a2[x], { escapeChar: "`" })}")`);
  }), f3.push("-Headers $headers")), c2.length && (l("$session = New-Object Microsoft.PowerShell.Commands.WebRequestSession"), c2.forEach((x) => {
    l("$cookie = New-Object System.Net.Cookie"), l(`$cookie.Name = '${x.name}'`), l(`$cookie.Value = '${x.value}'`), l(`$cookie.Domain = '${s.host}'`), l("$session.Cookies.Add($cookie)");
  }), f3.push("-WebSession $session")), o.text && (f3.push(
    `-ContentType '${Be2(ve(i4, "content-type"), { delimiter: "'", escapeChar: "`" })}'`
  ), f3.push(`-Body '${o.text}'`)), l(`$response = ${e} -Uri '${r}' -Method ${t2} ${f3.join(" ")}`.trim()), d();
};
var Si = {
  info: {
    key: "restmethod",
    title: "Invoke-RestMethod",
    link: "https://docs.microsoft.com/en-us/powershell/module/Microsoft.PowerShell.Utility/Invoke-RestMethod",
    description: "Powershell Invoke-RestMethod client",
    extname: ".ps1"
  },
  convert: xa("Invoke-RestMethod")
};
var Oi = {
  info: {
    key: "webrequest",
    title: "Invoke-WebRequest",
    link: "https://docs.microsoft.com/en-us/powershell/module/Microsoft.PowerShell.Utility/Invoke-WebRequest",
    description: "Powershell Invoke-WebRequest client",
    extname: ".ps1"
  },
  convert: xa("Invoke-WebRequest")
};
var Ti = {
  info: {
    key: "powershell",
    title: "Powershell",
    default: "webrequest"
  },
  clientsById: {
    webrequest: Oi,
    restmethod: Si
  }
};
function Xr(e, n, t2, a2, c2) {
  const s = a2.repeat(c2), r = a2.repeat(c2 - 1), o = t2 ? `,
${s}` : ", ", i4 = e === "object" ? "{" : "[", l = e === "object" ? "}" : "]";
  return t2 ? `${i4}
${s}${n.join(o)}
${r}${l}` : e === "object" && n.length > 0 ? `${i4} ${n.join(o)} ${l}` : `${i4}${n.join(o)}${l}`;
}
var Ae = (e, n, t2) => {
  switch (t2 = t2 === void 0 ? 1 : t2 + 1, Object.prototype.toString.call(e)) {
    case "[object Number]":
      return e;
    case "[object Array]": {
      let a2 = false;
      const c2 = e.map((s) => (Object.prototype.toString.call(s) === "[object Object]" && (a2 = Object.keys(s).length > 1), Ae(s, n, t2)));
      return Xr("array", c2, a2, n.indent, t2);
    }
    case "[object Object]": {
      const a2 = [];
      for (const c2 in e)
        a2.push(`"${c2}": ${Ae(e[c2], n, t2)}`);
      return Xr("object", a2, n.pretty && a2.length > 1, n.indent, t2);
    }
    case "[object Null]":
      return "None";
    case "[object Boolean]":
      return e ? "True" : "False";
    default:
      return e == null ? "" : `"${e.toString().replace(/"/g, '\\"')}"`;
  }
};
var Pi = ["HEAD", "GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"];
var Ci = {
  info: {
    key: "requests",
    title: "Requests",
    link: "http://docs.python-requests.org/en/latest/api/#requests.request",
    description: "Requests HTTP library",
    extname: ".py",
    installation: "python -m pip install requests"
  },
  convert: ({ fullUrl: e, postData: n, allHeaders: t2, method: a2 }, c2) => {
    const s = {
      indent: "    ",
      pretty: true,
      ...c2
    }, { push: r, blank: o, join: i4, addPostProcessor: l } = new G({ indent: s.indent });
    r("import requests"), o(), r(`url = "${e}"`), o();
    const d = t2;
    let y2 = {};
    const f3 = {};
    let h = false, x = false, $ = false;
    switch (n.mimeType) {
      case "application/json":
        n.jsonObj && (r(`payload = ${Ae(n.jsonObj, s)}`), $ = true, x = true);
        break;
      case "multipart/form-data":
        if (!n.params)
          break;
        if (y2 = {}, n.params.forEach((v) => {
          v.fileName ? (v.contentType ? f3[v.name] = `('${v.fileName}', open('${v.fileName}', 'rb'), '${v.contentType}')` : f3[v.name] = `('${v.fileName}', open('${v.fileName}', 'rb'))`, h = true) : (y2[v.name] = v.value, x = true);
        }), h) {
          r(`files = ${Ae(f3, s)}`), x && r(`payload = ${Ae(y2, s)}`);
          const v = fe(d, "content-type");
          v && delete d[v];
        } else {
          const v = JSON.stringify(n.text);
          v && (r(`payload = ${v}`), x = true);
        }
        l(
          (v) => v.replace(/"\('(.+)', open\('(.+)', 'rb'\)\)"/g, '("$1", open("$2", "rb"))').replace(/"\('(.+)', open\('(.+)', 'rb'\), '(.+)'\)"/g, '("$1", open("$2", "rb"), "$3")')
        );
        break;
      default: {
        if (n.mimeType === "application/x-www-form-urlencoded" && n.paramsObj) {
          r(`payload = ${Ae(n.paramsObj, s)}`), x = true;
          break;
        }
        const v = JSON.stringify(n.text);
        v && (r(`payload = ${v}`), x = true);
      }
    }
    const u2 = Object.keys(d).length;
    if (u2 === 0 && (x || h))
      o();
    else if (u2 === 1)
      Object.keys(d).forEach((v) => {
        r(`headers = {"${v}": "${le(d[v])}"}`), o();
      });
    else if (u2 > 1) {
      let v = 1;
      r("headers = {"), Object.keys(d).forEach((S) => {
        r(v !== u2 ? `"${S}": "${le(d[S])}",` : `"${S}": "${le(d[S])}"`, 1), v += 1;
      }), r("}"), o();
    }
    let m = Pi.includes(a2) ? `response = requests.${a2.toLowerCase()}(url` : `response = requests.request("${a2}", url`;
    return x && ($ ? m += ", json=payload" : m += ", data=payload"), h && (m += ", files=files"), u2 > 0 && (m += ", headers=headers"), m += ")", r(m), o(), r("print(response.text)"), i4();
  }
};
var qi = {
  info: {
    key: "python",
    title: "Python",
    default: "requests",
    cli: "python3 %s"
  },
  clientsById: {
    requests: Ci
  }
};
var Ei = {
  info: {
    key: "httr",
    title: "httr",
    link: "https://cran.r-project.org/web/packages/httr/vignettes/quickstart.html",
    description: "httr: Tools for Working with URLs and HTTP",
    extname: ".r"
  },
  convert: ({ url: e, queryObj: n, queryString: t2, postData: a2, allHeaders: c2, method: s }) => {
    const { push: r, blank: o, join: i4 } = new G();
    r("library(httr)"), o(), r(`url <- "${e}"`), o();
    const l = n;
    delete n.key;
    const d = Object.keys(l).length;
    t2.length === 1 ? (r(`queryString <- list(${Object.keys(l)} = "${Object.values(l).toString()}")`), o()) : t2.length > 1 && (r("queryString <- list("), Object.keys(l).forEach((p, O) => {
      O !== d - 1 ? r(`  ${p} = "${l[p].toString()}",`) : r(`  ${p} = "${l[p].toString()}"`);
    }), r(")"), o());
    const y2 = JSON.stringify(a2.text);
    if (y2 && (r(`payload <- ${y2}`), o()), a2.text || a2.jsonObj || a2.params)
      switch (a2.mimeType) {
        case "application/x-www-form-urlencoded":
          r('encode <- "form"'), o();
          break;
        case "application/json":
          r('encode <- "json"'), o();
          break;
        case "multipart/form-data":
          r('encode <- "multipart"'), o();
          break;
        default:
          r('encode <- "raw"'), o();
          break;
      }
    const f3 = ve(c2, "cookie"), h = ve(c2, "accept"), x = f3 ? `set_cookies(\`${String(f3).replace(/;/g, '", `').replace(/` /g, "`").replace(/[=]/g, '` = "')}")` : void 0, $ = h ? `accept("${le(h)}")` : void 0, u2 = `content_type("${le(a2.mimeType)}")`, m = Object.entries(c2).filter(([p]) => !["cookie", "accept", "content-type"].includes(p.toLowerCase())).map(([p, O]) => `'${p}' = '${He(O)}'`).join(", "), v = m ? `add_headers(${m})` : void 0;
    let S = `response <- VERB("${s}", url`;
    y2 && (S += ", body = payload"), t2.length && (S += ", query = queryString");
    const T = [v, u2, $, x].filter((p) => !!p).join(", ");
    return T && (S += `, ${T}`), (a2.text || a2.jsonObj || a2.params) && (S += ", encode = encode"), S += ")", r(S), o(), r('content(response, "text")'), i4();
  }
};
var Ri = {
  info: {
    key: "r",
    title: "R",
    default: "httr"
  },
  clientsById: {
    httr: Ei
  }
};
var ki = {
  info: {
    key: "native",
    title: "net::http",
    link: "http://ruby-doc.org/stdlib-2.2.1/libdoc/net/http/rdoc/Net/HTTP.html",
    description: "Ruby HTTP client",
    extname: ".rb"
  },
  convert: ({ uriObj: e, method: n, fullUrl: t2, postData: a2, allHeaders: c2 }) => {
    const { push: s, blank: r, join: o } = new G();
    s("require 'uri'"), s("require 'net/http'"), r();
    const i4 = n.toUpperCase(), l = [
      "GET",
      "POST",
      "HEAD",
      "DELETE",
      "PATCH",
      "PUT",
      "OPTIONS",
      "COPY",
      "LOCK",
      "UNLOCK",
      "MOVE",
      "TRACE"
    ], d = i4.charAt(0) + i4.substring(1).toLowerCase();
    l.includes(i4) || (s(`class Net::HTTP::${d} < Net::HTTPRequest`), s(`  METHOD = '${i4.toUpperCase()}'`), s(`  REQUEST_HAS_BODY = '${a2.text ? "true" : "false"}'`), s("  RESPONSE_HAS_BODY = true"), s("end"), r()), s(`url = URI("${t2}")`), r(), s("http = Net::HTTP.new(url.host, url.port)"), e.protocol === "https:" && s("http.use_ssl = true"), r(), s(`request = Net::HTTP::${d}.new(url)`);
    const y2 = Object.keys(c2);
    return y2.length && y2.forEach((f3) => {
      s(`request["${f3}"] = '${He(c2[f3])}'`);
    }), a2.text && s(`request.body = ${JSON.stringify(a2.text)}`), r(), s("response = http.request(request)"), s("puts response.read_body"), o();
  }
};
var Ni = {
  info: {
    key: "ruby",
    title: "Ruby",
    default: "native"
  },
  clientsById: {
    native: ki
  }
};
var ce = (e = "") => /^[a-z0-9-_/.@%^=:]+$/i.test(e) ? e : `'${e.replace(/'/g, "'\\''")}'`;
var Ai = (e) => e.replace(/\r/g, "\\r").replace(/\n/g, "\\n");
var Ii = {
  "http1.0": "0",
  "url ": "",
  cookie: "b",
  data: "d",
  form: "F",
  globoff: "g",
  header: "H",
  insecure: "k",
  request: "X"
};
var Ui = (e) => (n) => {
  if (e) {
    const t2 = Ii[n];
    return t2 ? `-${t2}` : "";
  }
  return `--${n}`;
};
var Li = {
  info: {
    key: "curl",
    title: "cURL",
    link: "http://curl.haxx.se/",
    description: "cURL is a command line tool and library for transferring data with URL syntax",
    extname: ".sh"
  },
  convert: ({ fullUrl: e, method: n, httpVersion: t2, headersObj: a2, allHeaders: c2, postData: s }, r = {}) => {
    const { indent: o = "  ", short: i4 = false, binary: l = false, globOff: d = false } = r, y2 = "  ", { push: f3, join: h } = new G({
      ...typeof o == "string" ? { indent: o } : {},
      join: o !== false ? ` \\
${o}` : " "
    }), x = Ui(i4);
    let $ = ce(e);
    if (f3(`curl ${x("request")} ${n}`), d && ($ = unescape($), f3(x("globoff"))), f3(`${x("url ")}${$}`), t2 === "HTTP/1.0" && f3(x("http1.0")), ve(c2, "accept-encoding") && f3("--compressed"), s.mimeType === "multipart/form-data") {
      const u2 = fe(a2, "content-type");
      if (u2) {
        const m = a2[u2];
        if (u2 && m) {
          const v = String(m).replace(/; boundary.+?(?=(;|$))/, "");
          a2[u2] = v, c2[u2] = v;
        }
      }
    }
    switch (Object.keys(a2).sort().forEach((u2) => {
      const m = `${u2}: ${a2[u2]}`;
      f3(`${x("header")} ${ce(m)}`);
    }), c2.cookie && f3(`${x("cookie")} ${ce(c2.cookie)}`), s.mimeType) {
      case "multipart/form-data":
        s.params?.forEach((m) => {
          let v = "";
          m.fileName ? v = `${m.name}='@${m.fileName}'` : v = ce(`${m.name}=${m.value}`), f3(`${x("form")} ${v}`);
        });
        break;
      case "application/x-www-form-urlencoded":
        s.params ? s.params.forEach((m) => {
          const v = m.name, S = encodeURIComponent(m.name), T = S !== v, p = T ? S : v, O = l ? "--data-binary" : T ? "--data-urlencode" : x("data");
          f3(`${O} ${ce(`${p}=${m.value}`)}`);
        }) : f3(`${l ? "--data-binary" : x("data")} ${ce(s.text)}`);
        break;
      default:
        if (!s.text)
          break;
        let u2 = false;
        if (Uo(s.mimeType) && s.text.length > 20)
          try {
            const m = JSON.parse(s.text);
            u2 = true, s.text.indexOf("'") > 0 ? f3(
              `${l ? "--data-binary" : x("data")} @- <<EOF
${JSON.stringify(
                m,
                null,
                y2
              )}
EOF`
            ) : f3(
              `${l ? "--data-binary" : x("data")} '
${JSON.stringify(m, null, y2)}
'`
            );
          } catch {
          }
        u2 || f3(`${l ? "--data-binary" : x("data")} ${ce(s.text)}`);
    }
    return h();
  }
};
var Fi = {
  info: {
    key: "httpie",
    title: "HTTPie",
    link: "http://httpie.org/",
    description: "a CLI, cURL-like tool for humans",
    extname: ".sh",
    installation: "brew install httpie"
  },
  convert: ({ allHeaders: e, postData: n, queryObj: t2, fullUrl: a2, method: c2, url: s }, r) => {
    const o = {
      body: false,
      cert: false,
      headers: false,
      indent: "  ",
      pretty: false,
      print: false,
      queryParams: false,
      short: false,
      style: false,
      timeout: false,
      verbose: false,
      verify: false,
      ...r
    }, { push: i4, join: l, unshift: d } = new G({
      indent: o.indent,
      // @ts-expect-error SEEMS LEGIT
      join: o.indent !== false ? ` \\
${o.indent}` : " "
    });
    let y2 = false;
    const f3 = [];
    o.headers && f3.push(o.short ? "-h" : "--headers"), o.body && f3.push(o.short ? "-b" : "--body"), o.verbose && f3.push(o.short ? "-v" : "--verbose"), o.print && f3.push(`${o.short ? "-p" : "--print"}=${o.print}`), o.verify && f3.push(`--verify=${o.verify}`), o.cert && f3.push(`--cert=${o.cert}`), o.pretty && f3.push(`--pretty=${o.pretty}`), o.style && f3.push(`--style=${o.style}`), o.timeout && f3.push(`--timeout=${o.timeout}`), o.queryParams && Object.keys(t2).forEach((x) => {
      const $ = t2[x];
      Array.isArray($) ? $.forEach((u2) => {
        i4(`${x}==${ce(u2)}`);
      }) : i4(`${x}==${ce($)}`);
    }), Object.keys(e).sort().forEach((x) => {
      i4(`${x}:${ce(e[x])}`);
    }), n.mimeType === "application/x-www-form-urlencoded" ? n.params && n.params.length && (f3.push(o.short ? "-f" : "--form"), n.params.forEach((x) => {
      i4(`${x.name}=${ce(x.value)}`);
    })) : y2 = true;
    const h = f3.length ? `${f3.join(" ")} ` : "";
    if (s = ce(o.queryParams ? s : a2), d(`http ${h}${c2} ${s}`), y2 && n.text) {
      const x = ce(n.text);
      d(`echo ${x} | `);
    }
    return l();
  }
};
var _i = {
  info: {
    key: "wget",
    title: "Wget",
    link: "https://www.gnu.org/software/wget/",
    description: "a free software package for retrieving files using HTTP, HTTPS",
    extname: ".sh"
  },
  convert: ({ method: e, postData: n, allHeaders: t2, fullUrl: a2 }, c2) => {
    const s = {
      indent: "  ",
      short: false,
      verbose: false,
      ...c2
    }, { push: r, join: o } = new G({
      ...typeof s.indent == "string" ? { indent: s.indent } : {},
      join: s.indent !== false ? ` \\
${s.indent}` : " "
    });
    return s.verbose ? r(`wget ${s.short ? "-v" : "--verbose"}`) : r(`wget ${s.short ? "-q" : "--quiet"}`), r(`--method ${ce(e)}`), Object.keys(t2).forEach((i4) => {
      const l = `${i4}: ${t2[i4]}`;
      r(`--header ${ce(l)}`);
    }), n.text && r(`--body-data ${Ai(ce(n.text))}`), r(s.short ? "-O" : "--output-document"), r(`- ${ce(a2)}`), o();
  }
};
var Bi = {
  info: {
    key: "shell",
    title: "Shell",
    default: "curl",
    cli: "%s"
  },
  clientsById: {
    curl: Li,
    httpie: Fi,
    wget: _i
  }
};
var Zr = (e, n) => n.repeat(e);
var en = (e, n, t2, a2) => {
  const c2 = Zr(a2, t2), s = Zr(a2 - 1, t2), r = n ? `,
${c2}` : ", ";
  return n ? `[
${c2}${e.join(r)}
${s}]` : `[${e.join(r)}]`;
};
var tn = (e, n, t2) => `let ${e} = ${Ze(n, t2)}`;
var Ze = (e, n, t2) => {
  switch (t2 = t2 === void 0 ? 1 : t2 + 1, Object.prototype.toString.call(e)) {
    case "[object Number]":
      return e;
    case "[object Array]": {
      let a2 = false;
      const c2 = e.map((s) => (Object.prototype.toString.call(s) === "[object Object]" && (a2 = Object.keys(s).length > 1), Ze(s, n, t2)));
      return en(c2, a2, n.indent, t2);
    }
    case "[object Object]": {
      const a2 = [];
      for (const c2 in e)
        a2.push(`"${c2}": ${Ze(e[c2], n, t2)}`);
      return en(
        a2,
        // @ts-expect-error needs better types
        n.pretty && a2.length > 1,
        // @ts-expect-error needs better types
        n.indent,
        t2
      );
    }
    case "[object Boolean]":
      return e.toString();
    default:
      return e == null ? "nil" : `"${e.toString().replace(/"/g, '\\"')}"`;
  }
};
var Hi = {
  info: {
    key: "urlsession",
    title: "URLSession",
    link: "https://developer.apple.com/documentation/foundation/urlsession",
    description: "Foundation's URLSession request",
    extname: ".swift"
  },
  convert: ({ allHeaders: e, postData: n, uriObj: t2, queryObj: a2, method: c2 }, s) => {
    const r = {
      indent: "  ",
      pretty: true,
      timeout: 10,
      ...s
    }, { push: o, blank: i4, join: l } = new G({ indent: r.indent });
    o("import Foundation"), i4();
    const d = n.text || n.jsonObj || n.params;
    if (d)
      switch (n.mimeType) {
        case "application/x-www-form-urlencoded":
          if (n.params?.length) {
            const f3 = n.params.map((h) => `"${h.name}": "${h.value}"`);
            r.pretty ? (o("let parameters = ["), f3.forEach((h) => o(`${h},`, 1)), o("]")) : o(`let parameters = [${f3.join(", ")}]`), o('let joinedParameters = parameters.map { "\\($0.key)=\\($0.value)" }.joined(separator: "&")'), o("let postData = Data(joinedParameters.utf8)"), i4();
          }
          break;
        case "application/json":
          n.jsonObj && (o(`${tn("parameters", n.jsonObj, r)} as [String : Any?]`), i4(), o("let postData = try JSONSerialization.data(withJSONObject: parameters, options: [])"), i4());
          break;
        case "multipart/form-data":
          o(tn("parameters", n.params, r)), i4(), o(`let boundary = "${n.boundary}"`), i4(), o('var body = ""'), o("for param in parameters {"), o('let paramName = param["name"]!', 1), o('body += "--\\(boundary)\\r\\n"', 1), o('body += "Content-Disposition:form-data; name=\\"\\(paramName)\\""', 1), o('if let filename = param["fileName"] {', 1), o('let contentType = param["contentType"]!', 2), o("let fileContent = try String(contentsOfFile: filename, encoding: .utf8)", 2), o('body += "; filename=\\"\\(filename)\\"\\r\\n"', 2), o('body += "Content-Type: \\(contentType)\\r\\n\\r\\n"', 2), o("body += fileContent", 2), o('} else if let paramValue = param["value"] {', 1), o('body += "\\r\\n\\r\\n\\(paramValue)"', 2), o("}", 1), o("}"), i4(), o("let postData = Data(body.utf8)"), i4();
          break;
        default:
          o(`let postData = Data("${n.text}".utf8)`), i4();
      }
    o(`let url = URL(string: "${t2.href}")!`);
    const y2 = a2 ? Object.entries(a2) : [];
    return y2.length < 1 ? o("var request = URLRequest(url: url)") : (o("var components = URLComponents(url: url, resolvingAgainstBaseURL: true)!"), o("let queryItems: [URLQueryItem] = ["), y2.forEach((f3) => {
      const h = f3[0], x = f3[1];
      switch (Object.prototype.toString.call(x)) {
        case "[object String]":
          o(`URLQueryItem(name: "${h}", value: "${x}"),`, 1);
          break;
        case "[object Array]":
          x.forEach(($) => {
            o(`URLQueryItem(name: "${h}", value: "${$}"),`, 1);
          });
          break;
      }
    }), o("]"), o("components.queryItems = components.queryItems.map { $0 + queryItems } ?? queryItems"), i4(), o("var request = URLRequest(url: components.url!)")), o(`request.httpMethod = "${c2}"`), o(`request.timeoutInterval = ${r.timeout}`), Object.keys(e).length && o(`request.allHTTPHeaderFields = ${Ze(e, r)}`), d && o("request.httpBody = postData"), i4(), o("let (data, _) = try await URLSession.shared.data(for: request)"), o("print(String(decoding: data, as: UTF8.self))"), l();
  }
};
var Mi = {
  info: {
    key: "swift",
    title: "Swift",
    default: "urlsession"
  },
  clientsById: {
    urlsession: Hi
  }
};
var zi = {
  c: Io,
  clojure: _o,
  csharp: Wo,
  go: Vo,
  http: Do,
  java: Zo,
  javascript: ai,
  json: ii,
  kotlin: li,
  node: hi,
  objc: mi,
  ocaml: vi,
  php: ji,
  powershell: Ti,
  python: qi,
  r: Ri,
  ruby: Ni,
  shell: Bi,
  swift: Mi
};
var Ce = {};
var Le = { exports: {} };
var Wi = Le.exports;
var rn;
function Ji() {
  return rn || (rn = 1, (function(e, n) {
    (function(t2) {
      var a2 = n && !n.nodeType && n, c2 = e && !e.nodeType && e, s = typeof i == "object" && i;
      (s.global === s || s.window === s || s.self === s) && (t2 = s);
      var r, o = 2147483647, i4 = 36, l = 1, d = 26, y2 = 38, f3 = 700, h = 72, x = 128, $ = "-", u2 = /^xn--/, m = /[^\x20-\x7E]/, v = /[\x2E\u3002\uFF0E\uFF61]/g, S = {
        overflow: "Overflow: input needs wider integers to process",
        "not-basic": "Illegal input >= 0x80 (not a basic code point)",
        "invalid-input": "Invalid input"
      }, T = i4 - l, p = Math.floor, O = String.fromCharCode, g4;
      function R2(E2) {
        throw new RangeError(S[E2]);
      }
      function C(E2, k) {
        for (var L = E2.length, q2 = []; L--; )
          q2[L] = k(E2[L]);
        return q2;
      }
      function N(E2, k) {
        var L = E2.split("@"), q2 = "";
        L.length > 1 && (q2 = L[0] + "@", E2 = L[1]), E2 = E2.replace(v, ".");
        var U2 = E2.split("."), J = C(U2, k).join(".");
        return q2 + J;
      }
      function B(E2) {
        for (var k = [], L = 0, q2 = E2.length, U2, J; L < q2; )
          U2 = E2.charCodeAt(L++), U2 >= 55296 && U2 <= 56319 && L < q2 ? (J = E2.charCodeAt(L++), (J & 64512) == 56320 ? k.push(((U2 & 1023) << 10) + (J & 1023) + 65536) : (k.push(U2), L--)) : k.push(U2);
        return k;
      }
      function P2(E2) {
        return C(E2, function(k) {
          var L = "";
          return k > 65535 && (k -= 65536, L += O(k >>> 10 & 1023 | 55296), k = 56320 | k & 1023), L += O(k), L;
        }).join("");
      }
      function oe(E2) {
        return E2 - 48 < 10 ? E2 - 22 : E2 - 65 < 26 ? E2 - 65 : E2 - 97 < 26 ? E2 - 97 : i4;
      }
      function H2(E2, k) {
        return E2 + 22 + 75 * (E2 < 26) - ((k != 0) << 5);
      }
      function D(E2, k, L) {
        var q2 = 0;
        for (E2 = L ? p(E2 / f3) : E2 >> 1, E2 += p(E2 / k); E2 > T * d >> 1; q2 += i4)
          E2 = p(E2 / T);
        return p(q2 + (T + 1) * E2 / (E2 + y2));
      }
      function se(E2) {
        var k = [], L = E2.length, q2, U2 = 0, J = x, z = h, K, ne2, V2, I, F, _, Q, W, Y;
        for (K = E2.lastIndexOf($), K < 0 && (K = 0), ne2 = 0; ne2 < K; ++ne2)
          E2.charCodeAt(ne2) >= 128 && R2("not-basic"), k.push(E2.charCodeAt(ne2));
        for (V2 = K > 0 ? K + 1 : 0; V2 < L; ) {
          for (I = U2, F = 1, _ = i4; V2 >= L && R2("invalid-input"), Q = oe(E2.charCodeAt(V2++)), (Q >= i4 || Q > p((o - U2) / F)) && R2("overflow"), U2 += Q * F, W = _ <= z ? l : _ >= z + d ? d : _ - z, !(Q < W); _ += i4)
            Y = i4 - W, F > p(o / Y) && R2("overflow"), F *= Y;
          q2 = k.length + 1, z = D(U2 - I, q2, I == 0), p(U2 / q2) > o - J && R2("overflow"), J += p(U2 / q2), U2 %= q2, k.splice(U2++, 0, J);
        }
        return P2(k);
      }
      function Z(E2) {
        var k, L, q2, U2, J, z, K, ne2, V2, I, F, _ = [], Q, W, Y, ae2;
        for (E2 = B(E2), Q = E2.length, k = x, L = 0, J = h, z = 0; z < Q; ++z)
          F = E2[z], F < 128 && _.push(O(F));
        for (q2 = U2 = _.length, U2 && _.push($); q2 < Q; ) {
          for (K = o, z = 0; z < Q; ++z)
            F = E2[z], F >= k && F < K && (K = F);
          for (W = q2 + 1, K - k > p((o - L) / W) && R2("overflow"), L += (K - k) * W, k = K, z = 0; z < Q; ++z)
            if (F = E2[z], F < k && ++L > o && R2("overflow"), F == k) {
              for (ne2 = L, V2 = i4; I = V2 <= J ? l : V2 >= J + d ? d : V2 - J, !(ne2 < I); V2 += i4)
                ae2 = ne2 - I, Y = i4 - I, _.push(
                  O(H2(I + ae2 % Y, 0))
                ), ne2 = p(ae2 / Y);
              _.push(O(H2(ne2, 0))), J = D(L, W, q2 == U2), L = 0, ++q2;
            }
          ++L, ++k;
        }
        return _.join("");
      }
      function A2(E2) {
        return N(E2, function(k) {
          return u2.test(k) ? se(k.slice(4).toLowerCase()) : k;
        });
      }
      function re(E2) {
        return N(E2, function(k) {
          return m.test(k) ? "xn--" + Z(k) : k;
        });
      }
      if (r = {
        /**
         * A string representing the current Punycode.js version number.
         * @memberOf punycode
         * @type String
         */
        version: "1.4.1",
        /**
         * An object of methods to convert from JavaScript's internal character
         * representation (UCS-2) to Unicode code points, and back.
         * @see <https://mathiasbynens.be/notes/javascript-encoding>
         * @memberOf punycode
         * @type Object
         */
        ucs2: {
          decode: B,
          encode: P2
        },
        decode: se,
        encode: Z,
        toASCII: re,
        toUnicode: A2
      }, a2 && c2)
        if (e.exports == a2)
          c2.exports = r;
        else
          for (g4 in r)
            r.hasOwnProperty(g4) && (a2[g4] = r[g4]);
      else
        t2.punycode = r;
    })(Wi);
  })(Le, Le.exports)), Le.exports;
}
var vt;
var nn;
function $a() {
  return nn || (nn = 1, vt = Object), vt;
}
var bt2;
var an;
function Vi() {
  return an || (an = 1, bt2 = Error), bt2;
}
var xt;
var on;
function Gi() {
  return on || (on = 1, xt = EvalError), xt;
}
var $t;
var sn;
function Di() {
  return sn || (sn = 1, $t = RangeError), $t;
}
var wt;
var ln;
function Qi() {
  return ln || (ln = 1, wt = ReferenceError), wt;
}
var jt;
var cn;
function wa() {
  return cn || (cn = 1, jt = SyntaxError), jt;
}
var St;
var un;
function ze() {
  return un || (un = 1, St = TypeError), St;
}
var Ot2;
var fn;
function Ki() {
  return fn || (fn = 1, Ot2 = URIError), Ot2;
}
var Tt;
var pn;
function Yi() {
  return pn || (pn = 1, Tt = Math.abs), Tt;
}
var Pt;
var dn;
function Xi() {
  return dn || (dn = 1, Pt = Math.floor), Pt;
}
var Ct;
var hn;
function Zi() {
  return hn || (hn = 1, Ct = Math.max), Ct;
}
var qt2;
var yn;
function es() {
  return yn || (yn = 1, qt2 = Math.min), qt2;
}
var Et;
var mn;
function ts() {
  return mn || (mn = 1, Et = Math.pow), Et;
}
var Rt;
var gn;
function rs2() {
  return gn || (gn = 1, Rt = Math.round), Rt;
}
var kt;
var vn;
function ns() {
  return vn || (vn = 1, kt = Number.isNaN || function(n) {
    return n !== n;
  }), kt;
}
var Nt;
var bn;
function as() {
  if (bn) return Nt;
  bn = 1;
  var e = ns();
  return Nt = function(t2) {
    return e(t2) || t2 === 0 ? t2 : t2 < 0 ? -1 : 1;
  }, Nt;
}
var At;
var xn;
function os() {
  return xn || (xn = 1, At = Object.getOwnPropertyDescriptor), At;
}
var It;
var $n;
function rt() {
  if ($n) return It;
  $n = 1;
  var e = os();
  if (e)
    try {
      e([], "length");
    } catch {
      e = null;
    }
  return It = e, It;
}
var Ut;
var wn;
function nt() {
  if (wn) return Ut;
  wn = 1;
  var e = Object.defineProperty || false;
  if (e)
    try {
      e({}, "a", { value: 1 });
    } catch {
      e = false;
    }
  return Ut = e, Ut;
}
var Lt;
var jn;
function is() {
  return jn || (jn = 1, Lt = function() {
    if (typeof Symbol != "function" || typeof Object.getOwnPropertySymbols != "function")
      return false;
    if (typeof Symbol.iterator == "symbol")
      return true;
    var n = {}, t2 = Symbol("test"), a2 = Object(t2);
    if (typeof t2 == "string" || Object.prototype.toString.call(t2) !== "[object Symbol]" || Object.prototype.toString.call(a2) !== "[object Symbol]")
      return false;
    var c2 = 42;
    n[t2] = c2;
    for (var s in n)
      return false;
    if (typeof Object.keys == "function" && Object.keys(n).length !== 0 || typeof Object.getOwnPropertyNames == "function" && Object.getOwnPropertyNames(n).length !== 0)
      return false;
    var r = Object.getOwnPropertySymbols(n);
    if (r.length !== 1 || r[0] !== t2 || !Object.prototype.propertyIsEnumerable.call(n, t2))
      return false;
    if (typeof Object.getOwnPropertyDescriptor == "function") {
      var o = (
        /** @type {PropertyDescriptor} */
        Object.getOwnPropertyDescriptor(n, t2)
      );
      if (o.value !== c2 || o.enumerable !== true)
        return false;
    }
    return true;
  }), Lt;
}
var Ft2;
var Sn;
function ss() {
  if (Sn) return Ft2;
  Sn = 1;
  var e = typeof Symbol < "u" && Symbol, n = is();
  return Ft2 = function() {
    return typeof e != "function" || typeof Symbol != "function" || typeof e("foo") != "symbol" || typeof Symbol("bar") != "symbol" ? false : n();
  }, Ft2;
}
var _t;
var On;
function ja() {
  return On || (On = 1, _t = typeof Reflect < "u" && Reflect.getPrototypeOf || null), _t;
}
var Bt;
var Tn;
function Sa() {
  if (Tn) return Bt;
  Tn = 1;
  var e = $a();
  return Bt = e.getPrototypeOf || null, Bt;
}
var Ht;
var Pn;
function ls() {
  if (Pn) return Ht;
  Pn = 1;
  var e = "Function.prototype.bind called on incompatible ", n = Object.prototype.toString, t2 = Math.max, a2 = "[object Function]", c2 = function(i4, l) {
    for (var d = [], y2 = 0; y2 < i4.length; y2 += 1)
      d[y2] = i4[y2];
    for (var f3 = 0; f3 < l.length; f3 += 1)
      d[f3 + i4.length] = l[f3];
    return d;
  }, s = function(i4, l) {
    for (var d = [], y2 = l, f3 = 0; y2 < i4.length; y2 += 1, f3 += 1)
      d[f3] = i4[y2];
    return d;
  }, r = function(o, i4) {
    for (var l = "", d = 0; d < o.length; d += 1)
      l += o[d], d + 1 < o.length && (l += i4);
    return l;
  };
  return Ht = function(i4) {
    var l = this;
    if (typeof l != "function" || n.apply(l) !== a2)
      throw new TypeError(e + l);
    for (var d = s(arguments, 1), y2, f3 = function() {
      if (this instanceof y2) {
        var m = l.apply(
          this,
          c2(d, arguments)
        );
        return Object(m) === m ? m : this;
      }
      return l.apply(
        i4,
        c2(d, arguments)
      );
    }, h = t2(0, l.length - d.length), x = [], $ = 0; $ < h; $++)
      x[$] = "$" + $;
    if (y2 = Function("binder", "return function (" + r(x, ",") + "){ return binder.apply(this,arguments); }")(f3), l.prototype) {
      var u2 = function() {
      };
      u2.prototype = l.prototype, y2.prototype = new u2(), u2.prototype = null;
    }
    return y2;
  }, Ht;
}
var Mt;
var Cn;
function We() {
  if (Cn) return Mt;
  Cn = 1;
  var e = ls();
  return Mt = Function.prototype.bind || e, Mt;
}
var zt;
var qn;
function jr() {
  return qn || (qn = 1, zt = Function.prototype.call), zt;
}
var Wt;
var En;
function Sr() {
  return En || (En = 1, Wt = Function.prototype.apply), Wt;
}
var Jt;
var Rn;
function cs() {
  return Rn || (Rn = 1, Jt = typeof Reflect < "u" && Reflect && Reflect.apply), Jt;
}
var Vt2;
var kn;
function Oa() {
  if (kn) return Vt2;
  kn = 1;
  var e = We(), n = Sr(), t2 = jr(), a2 = cs();
  return Vt2 = a2 || e.call(t2, n), Vt2;
}
var Gt;
var Nn;
function Ta() {
  if (Nn) return Gt;
  Nn = 1;
  var e = We(), n = ze(), t2 = jr(), a2 = Oa();
  return Gt = function(s) {
    if (s.length < 1 || typeof s[0] != "function")
      throw new n("a function is required");
    return a2(e, t2, s);
  }, Gt;
}
var Dt;
var An;
function us() {
  if (An) return Dt;
  An = 1;
  var e = Ta(), n = rt(), t2;
  try {
    t2 = /** @type {{ __proto__?: typeof Array.prototype }} */
    [].__proto__ === Array.prototype;
  } catch (r) {
    if (!r || typeof r != "object" || !("code" in r) || r.code !== "ERR_PROTO_ACCESS")
      throw r;
  }
  var a2 = !!t2 && n && n(
    Object.prototype,
    /** @type {keyof typeof Object.prototype} */
    "__proto__"
  ), c2 = Object, s = c2.getPrototypeOf;
  return Dt = a2 && typeof a2.get == "function" ? e([a2.get]) : typeof s == "function" ? (
    /** @type {import('./get')} */
    (function(o) {
      return s(o == null ? o : c2(o));
    })
  ) : false, Dt;
}
var Qt;
var In;
function fs() {
  if (In) return Qt;
  In = 1;
  var e = ja(), n = Sa(), t2 = us();
  return Qt = e ? function(c2) {
    return e(c2);
  } : n ? function(c2) {
    if (!c2 || typeof c2 != "object" && typeof c2 != "function")
      throw new TypeError("getProto: not an object");
    return n(c2);
  } : t2 ? function(c2) {
    return t2(c2);
  } : null, Qt;
}
var Kt;
var Un;
function ps() {
  if (Un) return Kt;
  Un = 1;
  var e = Function.prototype.call, n = Object.prototype.hasOwnProperty, t2 = We();
  return Kt = t2.call(e, n), Kt;
}
var Yt3;
var Ln;
function Or() {
  if (Ln) return Yt3;
  Ln = 1;
  var e, n = $a(), t2 = Vi(), a2 = Gi(), c2 = Di(), s = Qi(), r = wa(), o = ze(), i4 = Ki(), l = Yi(), d = Xi(), y2 = Zi(), f3 = es(), h = ts(), x = rs2(), $ = as(), u2 = Function, m = function(V2) {
    try {
      return u2('"use strict"; return (' + V2 + ").constructor;")();
    } catch {
    }
  }, v = rt(), S = nt(), T = function() {
    throw new o();
  }, p = v ? (function() {
    try {
      return arguments.callee, T;
    } catch {
      try {
        return v(arguments, "callee").get;
      } catch {
        return T;
      }
    }
  })() : T, O = ss()(), g4 = fs(), R2 = Sa(), C = ja(), N = Sr(), B = jr(), P2 = {}, oe = typeof Uint8Array > "u" || !g4 ? e : g4(Uint8Array), H2 = {
    __proto__: null,
    "%AggregateError%": typeof AggregateError > "u" ? e : AggregateError,
    "%Array%": Array,
    "%ArrayBuffer%": typeof ArrayBuffer > "u" ? e : ArrayBuffer,
    "%ArrayIteratorPrototype%": O && g4 ? g4([][Symbol.iterator]()) : e,
    "%AsyncFromSyncIteratorPrototype%": e,
    "%AsyncFunction%": P2,
    "%AsyncGenerator%": P2,
    "%AsyncGeneratorFunction%": P2,
    "%AsyncIteratorPrototype%": P2,
    "%Atomics%": typeof Atomics > "u" ? e : Atomics,
    "%BigInt%": typeof BigInt > "u" ? e : BigInt,
    "%BigInt64Array%": typeof BigInt64Array > "u" ? e : BigInt64Array,
    "%BigUint64Array%": typeof BigUint64Array > "u" ? e : BigUint64Array,
    "%Boolean%": Boolean,
    "%DataView%": typeof DataView > "u" ? e : DataView,
    "%Date%": Date,
    "%decodeURI%": decodeURI,
    "%decodeURIComponent%": decodeURIComponent,
    "%encodeURI%": encodeURI,
    "%encodeURIComponent%": encodeURIComponent,
    "%Error%": t2,
    "%eval%": eval,
    // eslint-disable-line no-eval
    "%EvalError%": a2,
    "%Float16Array%": typeof Float16Array > "u" ? e : Float16Array,
    "%Float32Array%": typeof Float32Array > "u" ? e : Float32Array,
    "%Float64Array%": typeof Float64Array > "u" ? e : Float64Array,
    "%FinalizationRegistry%": typeof FinalizationRegistry > "u" ? e : FinalizationRegistry,
    "%Function%": u2,
    "%GeneratorFunction%": P2,
    "%Int8Array%": typeof Int8Array > "u" ? e : Int8Array,
    "%Int16Array%": typeof Int16Array > "u" ? e : Int16Array,
    "%Int32Array%": typeof Int32Array > "u" ? e : Int32Array,
    "%isFinite%": isFinite,
    "%isNaN%": isNaN,
    "%IteratorPrototype%": O && g4 ? g4(g4([][Symbol.iterator]())) : e,
    "%JSON%": typeof JSON == "object" ? JSON : e,
    "%Map%": typeof Map > "u" ? e : Map,
    "%MapIteratorPrototype%": typeof Map > "u" || !O || !g4 ? e : g4((/* @__PURE__ */ new Map())[Symbol.iterator]()),
    "%Math%": Math,
    "%Number%": Number,
    "%Object%": n,
    "%Object.getOwnPropertyDescriptor%": v,
    "%parseFloat%": parseFloat,
    "%parseInt%": parseInt,
    "%Promise%": typeof Promise > "u" ? e : Promise,
    "%Proxy%": typeof Proxy > "u" ? e : Proxy,
    "%RangeError%": c2,
    "%ReferenceError%": s,
    "%Reflect%": typeof Reflect > "u" ? e : Reflect,
    "%RegExp%": RegExp,
    "%Set%": typeof Set > "u" ? e : Set,
    "%SetIteratorPrototype%": typeof Set > "u" || !O || !g4 ? e : g4((/* @__PURE__ */ new Set())[Symbol.iterator]()),
    "%SharedArrayBuffer%": typeof SharedArrayBuffer > "u" ? e : SharedArrayBuffer,
    "%String%": String,
    "%StringIteratorPrototype%": O && g4 ? g4(""[Symbol.iterator]()) : e,
    "%Symbol%": O ? Symbol : e,
    "%SyntaxError%": r,
    "%ThrowTypeError%": p,
    "%TypedArray%": oe,
    "%TypeError%": o,
    "%Uint8Array%": typeof Uint8Array > "u" ? e : Uint8Array,
    "%Uint8ClampedArray%": typeof Uint8ClampedArray > "u" ? e : Uint8ClampedArray,
    "%Uint16Array%": typeof Uint16Array > "u" ? e : Uint16Array,
    "%Uint32Array%": typeof Uint32Array > "u" ? e : Uint32Array,
    "%URIError%": i4,
    "%WeakMap%": typeof WeakMap > "u" ? e : WeakMap,
    "%WeakRef%": typeof WeakRef > "u" ? e : WeakRef,
    "%WeakSet%": typeof WeakSet > "u" ? e : WeakSet,
    "%Function.prototype.call%": B,
    "%Function.prototype.apply%": N,
    "%Object.defineProperty%": S,
    "%Object.getPrototypeOf%": R2,
    "%Math.abs%": l,
    "%Math.floor%": d,
    "%Math.max%": y2,
    "%Math.min%": f3,
    "%Math.pow%": h,
    "%Math.round%": x,
    "%Math.sign%": $,
    "%Reflect.getPrototypeOf%": C
  };
  if (g4)
    try {
      null.error;
    } catch (V2) {
      var D = g4(g4(V2));
      H2["%Error.prototype%"] = D;
    }
  var se = function V2(I) {
    var F;
    if (I === "%AsyncFunction%")
      F = m("async function () {}");
    else if (I === "%GeneratorFunction%")
      F = m("function* () {}");
    else if (I === "%AsyncGeneratorFunction%")
      F = m("async function* () {}");
    else if (I === "%AsyncGenerator%") {
      var _ = V2("%AsyncGeneratorFunction%");
      _ && (F = _.prototype);
    } else if (I === "%AsyncIteratorPrototype%") {
      var Q = V2("%AsyncGenerator%");
      Q && g4 && (F = g4(Q.prototype));
    }
    return H2[I] = F, F;
  }, Z = {
    __proto__: null,
    "%ArrayBufferPrototype%": ["ArrayBuffer", "prototype"],
    "%ArrayPrototype%": ["Array", "prototype"],
    "%ArrayProto_entries%": ["Array", "prototype", "entries"],
    "%ArrayProto_forEach%": ["Array", "prototype", "forEach"],
    "%ArrayProto_keys%": ["Array", "prototype", "keys"],
    "%ArrayProto_values%": ["Array", "prototype", "values"],
    "%AsyncFunctionPrototype%": ["AsyncFunction", "prototype"],
    "%AsyncGenerator%": ["AsyncGeneratorFunction", "prototype"],
    "%AsyncGeneratorPrototype%": ["AsyncGeneratorFunction", "prototype", "prototype"],
    "%BooleanPrototype%": ["Boolean", "prototype"],
    "%DataViewPrototype%": ["DataView", "prototype"],
    "%DatePrototype%": ["Date", "prototype"],
    "%ErrorPrototype%": ["Error", "prototype"],
    "%EvalErrorPrototype%": ["EvalError", "prototype"],
    "%Float32ArrayPrototype%": ["Float32Array", "prototype"],
    "%Float64ArrayPrototype%": ["Float64Array", "prototype"],
    "%FunctionPrototype%": ["Function", "prototype"],
    "%Generator%": ["GeneratorFunction", "prototype"],
    "%GeneratorPrototype%": ["GeneratorFunction", "prototype", "prototype"],
    "%Int8ArrayPrototype%": ["Int8Array", "prototype"],
    "%Int16ArrayPrototype%": ["Int16Array", "prototype"],
    "%Int32ArrayPrototype%": ["Int32Array", "prototype"],
    "%JSONParse%": ["JSON", "parse"],
    "%JSONStringify%": ["JSON", "stringify"],
    "%MapPrototype%": ["Map", "prototype"],
    "%NumberPrototype%": ["Number", "prototype"],
    "%ObjectPrototype%": ["Object", "prototype"],
    "%ObjProto_toString%": ["Object", "prototype", "toString"],
    "%ObjProto_valueOf%": ["Object", "prototype", "valueOf"],
    "%PromisePrototype%": ["Promise", "prototype"],
    "%PromiseProto_then%": ["Promise", "prototype", "then"],
    "%Promise_all%": ["Promise", "all"],
    "%Promise_reject%": ["Promise", "reject"],
    "%Promise_resolve%": ["Promise", "resolve"],
    "%RangeErrorPrototype%": ["RangeError", "prototype"],
    "%ReferenceErrorPrototype%": ["ReferenceError", "prototype"],
    "%RegExpPrototype%": ["RegExp", "prototype"],
    "%SetPrototype%": ["Set", "prototype"],
    "%SharedArrayBufferPrototype%": ["SharedArrayBuffer", "prototype"],
    "%StringPrototype%": ["String", "prototype"],
    "%SymbolPrototype%": ["Symbol", "prototype"],
    "%SyntaxErrorPrototype%": ["SyntaxError", "prototype"],
    "%TypedArrayPrototype%": ["TypedArray", "prototype"],
    "%TypeErrorPrototype%": ["TypeError", "prototype"],
    "%Uint8ArrayPrototype%": ["Uint8Array", "prototype"],
    "%Uint8ClampedArrayPrototype%": ["Uint8ClampedArray", "prototype"],
    "%Uint16ArrayPrototype%": ["Uint16Array", "prototype"],
    "%Uint32ArrayPrototype%": ["Uint32Array", "prototype"],
    "%URIErrorPrototype%": ["URIError", "prototype"],
    "%WeakMapPrototype%": ["WeakMap", "prototype"],
    "%WeakSetPrototype%": ["WeakSet", "prototype"]
  }, A2 = We(), re = ps(), E2 = A2.call(B, Array.prototype.concat), k = A2.call(N, Array.prototype.splice), L = A2.call(B, String.prototype.replace), q2 = A2.call(B, String.prototype.slice), U2 = A2.call(B, RegExp.prototype.exec), J = /[^%.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|%$))/g, z = /\\(\\)?/g, K = function(I) {
    var F = q2(I, 0, 1), _ = q2(I, -1);
    if (F === "%" && _ !== "%")
      throw new r("invalid intrinsic syntax, expected closing `%`");
    if (_ === "%" && F !== "%")
      throw new r("invalid intrinsic syntax, expected opening `%`");
    var Q = [];
    return L(I, J, function(W, Y, ae2, he) {
      Q[Q.length] = ae2 ? L(he, z, "$1") : Y || W;
    }), Q;
  }, ne2 = function(I, F) {
    var _ = I, Q;
    if (re(Z, _) && (Q = Z[_], _ = "%" + Q[0] + "%"), re(H2, _)) {
      var W = H2[_];
      if (W === P2 && (W = se(_)), typeof W > "u" && !F)
        throw new o("intrinsic " + I + " exists, but is not available. Please file an issue!");
      return {
        alias: Q,
        name: _,
        value: W
      };
    }
    throw new r("intrinsic " + I + " does not exist!");
  };
  return Yt3 = function(I, F) {
    if (typeof I != "string" || I.length === 0)
      throw new o("intrinsic name must be a non-empty string");
    if (arguments.length > 1 && typeof F != "boolean")
      throw new o('"allowMissing" argument must be a boolean');
    if (U2(/^%?[^%]*%?$/, I) === null)
      throw new r("`%` may not be present anywhere but at the beginning and end of the intrinsic name");
    var _ = K(I), Q = _.length > 0 ? _[0] : "", W = ne2("%" + Q + "%", F), Y = W.name, ae2 = W.value, he = false, Se = W.alias;
    Se && (Q = Se[0], k(_, E2([0, 1], Se)));
    for (var qe = 1, Oe = true; qe < _.length; qe += 1) {
      var ye = _[qe], Ee = q2(ye, 0, 1), Re = q2(ye, -1);
      if ((Ee === '"' || Ee === "'" || Ee === "`" || Re === '"' || Re === "'" || Re === "`") && Ee !== Re)
        throw new r("property names with quotes must have matching quotes");
      if ((ye === "constructor" || !Oe) && (he = true), Q += "." + ye, Y = "%" + Q + "%", re(H2, Y))
        ae2 = H2[Y];
      else if (ae2 != null) {
        if (!(ye in ae2)) {
          if (!F)
            throw new o("base intrinsic for " + I + " exists, but the property is not available.");
          return;
        }
        if (v && qe + 1 >= _.length) {
          var Te = v(ae2, ye);
          Oe = !!Te, Oe && "get" in Te && !("originalValue" in Te.get) ? ae2 = Te.get : ae2 = ae2[ye];
        } else
          Oe = re(ae2, ye), ae2 = ae2[ye];
        Oe && !he && (H2[Y] = ae2);
      }
    }
    return ae2;
  }, Yt3;
}
var Xt2 = { exports: {} };
var Zt;
var Fn;
function ds() {
  if (Fn) return Zt;
  Fn = 1;
  var e = nt(), n = wa(), t2 = ze(), a2 = rt();
  return Zt = function(s, r, o) {
    if (!s || typeof s != "object" && typeof s != "function")
      throw new t2("`obj` must be an object or a function`");
    if (typeof r != "string" && typeof r != "symbol")
      throw new t2("`property` must be a string or a symbol`");
    if (arguments.length > 3 && typeof arguments[3] != "boolean" && arguments[3] !== null)
      throw new t2("`nonEnumerable`, if provided, must be a boolean or null");
    if (arguments.length > 4 && typeof arguments[4] != "boolean" && arguments[4] !== null)
      throw new t2("`nonWritable`, if provided, must be a boolean or null");
    if (arguments.length > 5 && typeof arguments[5] != "boolean" && arguments[5] !== null)
      throw new t2("`nonConfigurable`, if provided, must be a boolean or null");
    if (arguments.length > 6 && typeof arguments[6] != "boolean")
      throw new t2("`loose`, if provided, must be a boolean");
    var i4 = arguments.length > 3 ? arguments[3] : null, l = arguments.length > 4 ? arguments[4] : null, d = arguments.length > 5 ? arguments[5] : null, y2 = arguments.length > 6 ? arguments[6] : false, f3 = !!a2 && a2(s, r);
    if (e)
      e(s, r, {
        configurable: d === null && f3 ? f3.configurable : !d,
        enumerable: i4 === null && f3 ? f3.enumerable : !i4,
        value: o,
        writable: l === null && f3 ? f3.writable : !l
      });
    else if (y2 || !i4 && !l && !d)
      s[r] = o;
    else
      throw new n("This environment does not support defining a property as non-configurable, non-writable, or non-enumerable.");
  }, Zt;
}
var er;
var _n;
function hs() {
  if (_n) return er;
  _n = 1;
  var e = nt(), n = function() {
    return !!e;
  };
  return n.hasArrayLengthDefineBug = function() {
    if (!e)
      return null;
    try {
      return e([], "length", { value: 1 }).length !== 1;
    } catch {
      return true;
    }
  }, er = n, er;
}
var tr;
var Bn;
function ys() {
  if (Bn) return tr;
  Bn = 1;
  var e = Or(), n = ds(), t2 = hs()(), a2 = rt(), c2 = ze(), s = e("%Math.floor%");
  return tr = function(o, i4) {
    if (typeof o != "function")
      throw new c2("`fn` is not a function");
    if (typeof i4 != "number" || i4 < 0 || i4 > 4294967295 || s(i4) !== i4)
      throw new c2("`length` must be a positive 32-bit integer");
    var l = arguments.length > 2 && !!arguments[2], d = true, y2 = true;
    if ("length" in o && a2) {
      var f3 = a2(o, "length");
      f3 && !f3.configurable && (d = false), f3 && !f3.writable && (y2 = false);
    }
    return (d || y2 || !l) && (t2 ? n(
      /** @type {Parameters<define>[0]} */
      o,
      "length",
      i4,
      true,
      true
    ) : n(
      /** @type {Parameters<define>[0]} */
      o,
      "length",
      i4
    )), o;
  }, tr;
}
var rr;
var Hn;
function ms() {
  if (Hn) return rr;
  Hn = 1;
  var e = We(), n = Sr(), t2 = Oa();
  return rr = function() {
    return t2(e, n, arguments);
  }, rr;
}
var Mn;
function gs() {
  return Mn || (Mn = 1, (function(e) {
    var n = ys(), t2 = nt(), a2 = Ta(), c2 = ms();
    e.exports = function(r) {
      var o = a2(arguments), i4 = r.length - (arguments.length - 1);
      return n(
        o,
        1 + (i4 > 0 ? i4 : 0),
        true
      );
    }, t2 ? t2(e.exports, "apply", { value: c2 }) : e.exports.apply = c2;
  })(Xt2)), Xt2.exports;
}
var nr;
var zn;
function vs() {
  if (zn) return nr;
  zn = 1;
  var e = Or(), n = gs(), t2 = n(e("String.prototype.indexOf"));
  return nr = function(c2, s) {
    var r = e(c2, !!s);
    return typeof r == "function" && t2(c2, ".prototype.") > -1 ? n(r) : r;
  }, nr;
}
var bs = {};
var xs = Object.freeze(Object.defineProperty({
  __proto__: null,
  default: bs
}, Symbol.toStringTag, { value: "Module" }));
var $s = a(xs);
var ar;
var Wn;
function ws() {
  if (Wn) return ar;
  Wn = 1;
  var e = typeof Map == "function" && Map.prototype, n = Object.getOwnPropertyDescriptor && e ? Object.getOwnPropertyDescriptor(Map.prototype, "size") : null, t2 = e && n && typeof n.get == "function" ? n.get : null, a2 = e && Map.prototype.forEach, c2 = typeof Set == "function" && Set.prototype, s = Object.getOwnPropertyDescriptor && c2 ? Object.getOwnPropertyDescriptor(Set.prototype, "size") : null, r = c2 && s && typeof s.get == "function" ? s.get : null, o = c2 && Set.prototype.forEach, i4 = typeof WeakMap == "function" && WeakMap.prototype, l = i4 ? WeakMap.prototype.has : null, d = typeof WeakSet == "function" && WeakSet.prototype, y2 = d ? WeakSet.prototype.has : null, f3 = typeof WeakRef == "function" && WeakRef.prototype, h = f3 ? WeakRef.prototype.deref : null, x = Boolean.prototype.valueOf, $ = Object.prototype.toString, u2 = Function.prototype.toString, m = String.prototype.match, v = String.prototype.slice, S = String.prototype.replace, T = String.prototype.toUpperCase, p = String.prototype.toLowerCase, O = RegExp.prototype.test, g4 = Array.prototype.concat, R2 = Array.prototype.join, C = Array.prototype.slice, N = Math.floor, B = typeof BigInt == "function" ? BigInt.prototype.valueOf : null, P2 = Object.getOwnPropertySymbols, oe = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? Symbol.prototype.toString : null, H2 = typeof Symbol == "function" && typeof Symbol.iterator == "object", D = typeof Symbol == "function" && Symbol.toStringTag && (typeof Symbol.toStringTag === H2 || true) ? Symbol.toStringTag : null, se = Object.prototype.propertyIsEnumerable, Z = (typeof Reflect == "function" ? Reflect.getPrototypeOf : Object.getPrototypeOf) || ([].__proto__ === Array.prototype ? function(w) {
    return w.__proto__;
  } : null);
  function A2(w, j3) {
    if (w === 1 / 0 || w === -1 / 0 || w !== w || w && w > -1e3 && w < 1e3 || O.call(/e/, j3))
      return j3;
    var X2 = /[0-9](?=(?:[0-9]{3})+(?![0-9]))/g;
    if (typeof w == "number") {
      var ee = w < 0 ? -N(-w) : N(w);
      if (ee !== w) {
        var ie = String(ee), M = v.call(j3, ie.length + 1);
        return S.call(ie, X2, "$&_") + "." + S.call(S.call(M, /([0-9]{3})/g, "$&_"), /_$/, "");
      }
    }
    return S.call(j3, X2, "$&_");
  }
  var re = $s, E2 = re.custom, k = F(E2) ? E2 : null;
  ar = function w(j3, X2, ee, ie) {
    var M = X2 || {};
    if (W(M, "quoteStyle") && M.quoteStyle !== "single" && M.quoteStyle !== "double")
      throw new TypeError('option "quoteStyle" must be "single" or "double"');
    if (W(M, "maxStringLength") && (typeof M.maxStringLength == "number" ? M.maxStringLength < 0 && M.maxStringLength !== 1 / 0 : M.maxStringLength !== null))
      throw new TypeError('option "maxStringLength", if provided, must be a positive integer, Infinity, or `null`');
    var xe = W(M, "customInspect") ? M.customInspect : true;
    if (typeof xe != "boolean" && xe !== "symbol")
      throw new TypeError("option \"customInspect\", if provided, must be `true`, `false`, or `'symbol'`");
    if (W(M, "indent") && M.indent !== null && M.indent !== "	" && !(parseInt(M.indent, 10) === M.indent && M.indent > 0))
      throw new TypeError('option "indent" must be "\\t", an integer > 0, or `null`');
    if (W(M, "numericSeparator") && typeof M.numericSeparator != "boolean")
      throw new TypeError('option "numericSeparator", if provided, must be `true` or `false`');
    var Pe = M.numericSeparator;
    if (typeof j3 > "u")
      return "undefined";
    if (j3 === null)
      return "null";
    if (typeof j3 == "boolean")
      return j3 ? "true" : "false";
    if (typeof j3 == "string")
      return Te(j3, M);
    if (typeof j3 == "number") {
      if (j3 === 0)
        return 1 / 0 / j3 > 0 ? "0" : "-0";
      var de2 = String(j3);
      return Pe ? A2(j3, de2) : de2;
    }
    if (typeof j3 == "bigint") {
      var $e = String(j3) + "n";
      return Pe ? A2(j3, $e) : $e;
    }
    var it = typeof M.depth > "u" ? 5 : M.depth;
    if (typeof ee > "u" && (ee = 0), ee >= it && it > 0 && typeof j3 == "object")
      return U2(j3) ? "[Array]" : "[Object]";
    var ke = ka(M, ee);
    if (typeof ie > "u")
      ie = [];
    else if (he(ie, j3) >= 0)
      return "[Circular]";
    function me(Ne, Ge2, Aa) {
      if (Ge2 && (ie = C.call(ie), ie.push(Ge2)), Aa) {
        var Ir = {
          depth: M.depth
        };
        return W(M, "quoteStyle") && (Ir.quoteStyle = M.quoteStyle), w(Ne, Ir, ee + 1, ie);
      }
      return w(Ne, M, ee + 1, ie);
    }
    if (typeof j3 == "function" && !z(j3)) {
      var Cr2 = ae2(j3), qr = Je(j3, me);
      return "[Function" + (Cr2 ? ": " + Cr2 : " (anonymous)") + "]" + (qr.length > 0 ? " { " + R2.call(qr, ", ") + " }" : "");
    }
    if (F(j3)) {
      var Er = H2 ? S.call(String(j3), /^(Symbol\(.*\))_[^)]*$/, "$1") : oe.call(j3);
      return typeof j3 == "object" && !H2 ? Ie2(Er) : Er;
    }
    if (Re(j3)) {
      for (var Ue = "<" + p.call(String(j3.nodeName)), st = j3.attributes || [], Ve2 = 0; Ve2 < st.length; Ve2++)
        Ue += " " + st[Ve2].name + "=" + L(q2(st[Ve2].value), "double", M);
      return Ue += ">", j3.childNodes && j3.childNodes.length && (Ue += "..."), Ue += "</" + p.call(String(j3.nodeName)) + ">", Ue;
    }
    if (U2(j3)) {
      if (j3.length === 0)
        return "[]";
      var lt = Je(j3, me);
      return ke && !Ra(lt) ? "[" + ot(lt, ke) + "]" : "[ " + R2.call(lt, ", ") + " ]";
    }
    if (K(j3)) {
      var ct = Je(j3, me);
      return !("cause" in Error.prototype) && "cause" in j3 && !se.call(j3, "cause") ? "{ [" + String(j3) + "] " + R2.call(g4.call("[cause]: " + me(j3.cause), ct), ", ") + " }" : ct.length === 0 ? "[" + String(j3) + "]" : "{ [" + String(j3) + "] " + R2.call(ct, ", ") + " }";
    }
    if (typeof j3 == "object" && xe) {
      if (k && typeof j3[k] == "function" && re)
        return re(j3, { depth: it - ee });
      if (xe !== "symbol" && typeof j3.inspect == "function")
        return j3.inspect();
    }
    if (Se(j3)) {
      var Rr = [];
      return a2 && a2.call(j3, function(Ne, Ge2) {
        Rr.push(me(Ge2, j3, true) + " => " + me(Ne, j3));
      }), Pr("Map", t2.call(j3), Rr, ke);
    }
    if (ye(j3)) {
      var kr = [];
      return o && o.call(j3, function(Ne) {
        kr.push(me(Ne, j3));
      }), Pr("Set", r.call(j3), kr, ke);
    }
    if (qe(j3))
      return at("WeakMap");
    if (Ee(j3))
      return at("WeakSet");
    if (Oe(j3))
      return at("WeakRef");
    if (V2(j3))
      return Ie2(me(Number(j3)));
    if (_(j3))
      return Ie2(me(B.call(j3)));
    if (I(j3))
      return Ie2(x.call(j3));
    if (ne2(j3))
      return Ie2(me(String(j3)));
    if (typeof window < "u" && j3 === window)
      return "{ [object Window] }";
    if (typeof globalThis < "u" && j3 === globalThis || typeof i < "u" && j3 === i)
      return "{ [object globalThis] }";
    if (!J(j3) && !z(j3)) {
      var ut = Je(j3, me), Nr = Z ? Z(j3) === Object.prototype : j3 instanceof Object || j3.constructor === Object, ft = j3 instanceof Object ? "" : "null prototype", Ar = !Nr && D && Object(j3) === j3 && D in j3 ? v.call(Y(j3), 8, -1) : ft ? "Object" : "", Na = Nr || typeof j3.constructor != "function" ? "" : j3.constructor.name ? j3.constructor.name + " " : "", pt = Na + (Ar || ft ? "[" + R2.call(g4.call([], Ar || [], ft || []), ": ") + "] " : "");
      return ut.length === 0 ? pt + "{}" : ke ? pt + "{" + ot(ut, ke) + "}" : pt + "{ " + R2.call(ut, ", ") + " }";
    }
    return String(j3);
  };
  function L(w, j3, X2) {
    var ee = (X2.quoteStyle || j3) === "double" ? '"' : "'";
    return ee + w + ee;
  }
  function q2(w) {
    return S.call(String(w), /"/g, "&quot;");
  }
  function U2(w) {
    return Y(w) === "[object Array]" && (!D || !(typeof w == "object" && D in w));
  }
  function J(w) {
    return Y(w) === "[object Date]" && (!D || !(typeof w == "object" && D in w));
  }
  function z(w) {
    return Y(w) === "[object RegExp]" && (!D || !(typeof w == "object" && D in w));
  }
  function K(w) {
    return Y(w) === "[object Error]" && (!D || !(typeof w == "object" && D in w));
  }
  function ne2(w) {
    return Y(w) === "[object String]" && (!D || !(typeof w == "object" && D in w));
  }
  function V2(w) {
    return Y(w) === "[object Number]" && (!D || !(typeof w == "object" && D in w));
  }
  function I(w) {
    return Y(w) === "[object Boolean]" && (!D || !(typeof w == "object" && D in w));
  }
  function F(w) {
    if (H2)
      return w && typeof w == "object" && w instanceof Symbol;
    if (typeof w == "symbol")
      return true;
    if (!w || typeof w != "object" || !oe)
      return false;
    try {
      return oe.call(w), true;
    } catch {
    }
    return false;
  }
  function _(w) {
    if (!w || typeof w != "object" || !B)
      return false;
    try {
      return B.call(w), true;
    } catch {
    }
    return false;
  }
  var Q = Object.prototype.hasOwnProperty || function(w) {
    return w in this;
  };
  function W(w, j3) {
    return Q.call(w, j3);
  }
  function Y(w) {
    return $.call(w);
  }
  function ae2(w) {
    if (w.name)
      return w.name;
    var j3 = m.call(u2.call(w), /^function\s*([\w$]+)/);
    return j3 ? j3[1] : null;
  }
  function he(w, j3) {
    if (w.indexOf)
      return w.indexOf(j3);
    for (var X2 = 0, ee = w.length; X2 < ee; X2++)
      if (w[X2] === j3)
        return X2;
    return -1;
  }
  function Se(w) {
    if (!t2 || !w || typeof w != "object")
      return false;
    try {
      t2.call(w);
      try {
        r.call(w);
      } catch {
        return true;
      }
      return w instanceof Map;
    } catch {
    }
    return false;
  }
  function qe(w) {
    if (!l || !w || typeof w != "object")
      return false;
    try {
      l.call(w, l);
      try {
        y2.call(w, y2);
      } catch {
        return true;
      }
      return w instanceof WeakMap;
    } catch {
    }
    return false;
  }
  function Oe(w) {
    if (!h || !w || typeof w != "object")
      return false;
    try {
      return h.call(w), true;
    } catch {
    }
    return false;
  }
  function ye(w) {
    if (!r || !w || typeof w != "object")
      return false;
    try {
      r.call(w);
      try {
        t2.call(w);
      } catch {
        return true;
      }
      return w instanceof Set;
    } catch {
    }
    return false;
  }
  function Ee(w) {
    if (!y2 || !w || typeof w != "object")
      return false;
    try {
      y2.call(w, y2);
      try {
        l.call(w, l);
      } catch {
        return true;
      }
      return w instanceof WeakSet;
    } catch {
    }
    return false;
  }
  function Re(w) {
    return !w || typeof w != "object" ? false : typeof HTMLElement < "u" && w instanceof HTMLElement ? true : typeof w.nodeName == "string" && typeof w.getAttribute == "function";
  }
  function Te(w, j3) {
    if (w.length > j3.maxStringLength) {
      var X2 = w.length - j3.maxStringLength, ee = "... " + X2 + " more character" + (X2 > 1 ? "s" : "");
      return Te(v.call(w, 0, j3.maxStringLength), j3) + ee;
    }
    var ie = S.call(S.call(w, /(['\\])/g, "\\$1"), /[\x00-\x1f]/g, Ea);
    return L(ie, "single", j3);
  }
  function Ea(w) {
    var j3 = w.charCodeAt(0), X2 = {
      8: "b",
      9: "t",
      10: "n",
      12: "f",
      13: "r"
    }[j3];
    return X2 ? "\\" + X2 : "\\x" + (j3 < 16 ? "0" : "") + T.call(j3.toString(16));
  }
  function Ie2(w) {
    return "Object(" + w + ")";
  }
  function at(w) {
    return w + " { ? }";
  }
  function Pr(w, j3, X2, ee) {
    var ie = ee ? ot(X2, ee) : R2.call(X2, ", ");
    return w + " (" + j3 + ") {" + ie + "}";
  }
  function Ra(w) {
    for (var j3 = 0; j3 < w.length; j3++)
      if (he(w[j3], `
`) >= 0)
        return false;
    return true;
  }
  function ka(w, j3) {
    var X2;
    if (w.indent === "	")
      X2 = "	";
    else if (typeof w.indent == "number" && w.indent > 0)
      X2 = R2.call(Array(w.indent + 1), " ");
    else
      return null;
    return {
      base: X2,
      prev: R2.call(Array(j3 + 1), X2)
    };
  }
  function ot(w, j3) {
    if (w.length === 0)
      return "";
    var X2 = `
` + j3.prev + j3.base;
    return X2 + R2.call(w, "," + X2) + `
` + j3.prev;
  }
  function Je(w, j3) {
    var X2 = U2(w), ee = [];
    if (X2) {
      ee.length = w.length;
      for (var ie = 0; ie < w.length; ie++)
        ee[ie] = W(w, ie) ? j3(w[ie], w) : "";
    }
    var M = typeof P2 == "function" ? P2(w) : [], xe;
    if (H2) {
      xe = {};
      for (var Pe = 0; Pe < M.length; Pe++)
        xe["$" + M[Pe]] = M[Pe];
    }
    for (var de2 in w)
      W(w, de2) && (X2 && String(Number(de2)) === de2 && de2 < w.length || H2 && xe["$" + de2] instanceof Symbol || (O.call(/[^\w$]/, de2) ? ee.push(j3(de2, w) + ": " + j3(w[de2], w)) : ee.push(de2 + ": " + j3(w[de2], w))));
    if (typeof P2 == "function")
      for (var $e = 0; $e < M.length; $e++)
        se.call(w, M[$e]) && ee.push("[" + j3(M[$e]) + "]: " + j3(w[M[$e]], w));
    return ee;
  }
  return ar;
}
var or;
var Jn;
function js() {
  if (Jn) return or;
  Jn = 1;
  var e = Or(), n = vs(), t2 = ws(), a2 = ze(), c2 = e("%WeakMap%", true), s = e("%Map%", true), r = n("WeakMap.prototype.get", true), o = n("WeakMap.prototype.set", true), i4 = n("WeakMap.prototype.has", true), l = n("Map.prototype.get", true), d = n("Map.prototype.set", true), y2 = n("Map.prototype.has", true), f3 = function(u2, m) {
    for (var v = u2, S; (S = v.next) !== null; v = S)
      if (S.key === m)
        return v.next = S.next, S.next = /** @type {NonNullable<typeof list.next>} */
        u2.next, u2.next = S, S;
  }, h = function(u2, m) {
    var v = f3(u2, m);
    return v && v.value;
  }, x = function(u2, m, v) {
    var S = f3(u2, m);
    S ? S.value = v : u2.next = /** @type {import('.').ListNode<typeof value>} */
    {
      // eslint-disable-line no-param-reassign, no-extra-parens
      key: m,
      next: u2.next,
      value: v
    };
  }, $ = function(u2, m) {
    return !!f3(u2, m);
  };
  return or = function() {
    var m, v, S, T = {
      assert: function(p) {
        if (!T.has(p))
          throw new a2("Side channel does not contain " + t2(p));
      },
      get: function(p) {
        if (c2 && p && (typeof p == "object" || typeof p == "function")) {
          if (m)
            return r(m, p);
        } else if (s) {
          if (v)
            return l(v, p);
        } else if (S)
          return h(S, p);
      },
      has: function(p) {
        if (c2 && p && (typeof p == "object" || typeof p == "function")) {
          if (m)
            return i4(m, p);
        } else if (s) {
          if (v)
            return y2(v, p);
        } else if (S)
          return $(S, p);
        return false;
      },
      set: function(p, O) {
        c2 && p && (typeof p == "object" || typeof p == "function") ? (m || (m = new c2()), o(m, p, O)) : s ? (v || (v = new s()), d(v, p, O)) : (S || (S = { key: {}, next: null }), x(S, p, O));
      }
    };
    return T;
  }, or;
}
var ir2;
var Vn;
function Tr() {
  if (Vn) return ir2;
  Vn = 1;
  var e = String.prototype.replace, n = /%20/g, t2 = {
    RFC1738: "RFC1738",
    RFC3986: "RFC3986"
  };
  return ir2 = {
    default: t2.RFC3986,
    formatters: {
      RFC1738: function(a2) {
        return e.call(a2, n, "+");
      },
      RFC3986: function(a2) {
        return String(a2);
      }
    },
    RFC1738: t2.RFC1738,
    RFC3986: t2.RFC3986
  }, ir2;
}
var sr2;
var Gn;
function Pa() {
  if (Gn) return sr2;
  Gn = 1;
  var e = Tr(), n = Object.prototype.hasOwnProperty, t2 = Array.isArray, a2 = (function() {
    for (var u2 = [], m = 0; m < 256; ++m)
      u2.push("%" + ((m < 16 ? "0" : "") + m.toString(16)).toUpperCase());
    return u2;
  })(), c2 = function(m) {
    for (; m.length > 1; ) {
      var v = m.pop(), S = v.obj[v.prop];
      if (t2(S)) {
        for (var T = [], p = 0; p < S.length; ++p)
          typeof S[p] < "u" && T.push(S[p]);
        v.obj[v.prop] = T;
      }
    }
  }, s = function(m, v) {
    for (var S = v && v.plainObjects ? /* @__PURE__ */ Object.create(null) : {}, T = 0; T < m.length; ++T)
      typeof m[T] < "u" && (S[T] = m[T]);
    return S;
  }, r = function u2(m, v, S) {
    if (!v)
      return m;
    if (typeof v != "object") {
      if (t2(m))
        m.push(v);
      else if (m && typeof m == "object")
        (S && (S.plainObjects || S.allowPrototypes) || !n.call(Object.prototype, v)) && (m[v] = true);
      else
        return [m, v];
      return m;
    }
    if (!m || typeof m != "object")
      return [m].concat(v);
    var T = m;
    return t2(m) && !t2(v) && (T = s(m, S)), t2(m) && t2(v) ? (v.forEach(function(p, O) {
      if (n.call(m, O)) {
        var g4 = m[O];
        g4 && typeof g4 == "object" && p && typeof p == "object" ? m[O] = u2(g4, p, S) : m.push(p);
      } else
        m[O] = p;
    }), m) : Object.keys(v).reduce(function(p, O) {
      var g4 = v[O];
      return n.call(p, O) ? p[O] = u2(p[O], g4, S) : p[O] = g4, p;
    }, T);
  }, o = function(m, v) {
    return Object.keys(v).reduce(function(S, T) {
      return S[T] = v[T], S;
    }, m);
  }, i4 = function(u2, m, v) {
    var S = u2.replace(/\+/g, " ");
    if (v === "iso-8859-1")
      return S.replace(/%[0-9a-f]{2}/gi, unescape);
    try {
      return decodeURIComponent(S);
    } catch {
      return S;
    }
  }, l = 1024, d = function(m, v, S, T, p) {
    if (m.length === 0)
      return m;
    var O = m;
    if (typeof m == "symbol" ? O = Symbol.prototype.toString.call(m) : typeof m != "string" && (O = String(m)), S === "iso-8859-1")
      return escape(O).replace(/%u[0-9a-f]{4}/gi, function(oe) {
        return "%26%23" + parseInt(oe.slice(2), 16) + "%3B";
      });
    for (var g4 = "", R2 = 0; R2 < O.length; R2 += l) {
      for (var C = O.length >= l ? O.slice(R2, R2 + l) : O, N = [], B = 0; B < C.length; ++B) {
        var P2 = C.charCodeAt(B);
        if (P2 === 45 || P2 === 46 || P2 === 95 || P2 === 126 || P2 >= 48 && P2 <= 57 || P2 >= 65 && P2 <= 90 || P2 >= 97 && P2 <= 122 || p === e.RFC1738 && (P2 === 40 || P2 === 41)) {
          N[N.length] = C.charAt(B);
          continue;
        }
        if (P2 < 128) {
          N[N.length] = a2[P2];
          continue;
        }
        if (P2 < 2048) {
          N[N.length] = a2[192 | P2 >> 6] + a2[128 | P2 & 63];
          continue;
        }
        if (P2 < 55296 || P2 >= 57344) {
          N[N.length] = a2[224 | P2 >> 12] + a2[128 | P2 >> 6 & 63] + a2[128 | P2 & 63];
          continue;
        }
        B += 1, P2 = 65536 + ((P2 & 1023) << 10 | C.charCodeAt(B) & 1023), N[N.length] = a2[240 | P2 >> 18] + a2[128 | P2 >> 12 & 63] + a2[128 | P2 >> 6 & 63] + a2[128 | P2 & 63];
      }
      g4 += N.join("");
    }
    return g4;
  }, y2 = function(m) {
    for (var v = [{ obj: { o: m }, prop: "o" }], S = [], T = 0; T < v.length; ++T)
      for (var p = v[T], O = p.obj[p.prop], g4 = Object.keys(O), R2 = 0; R2 < g4.length; ++R2) {
        var C = g4[R2], N = O[C];
        typeof N == "object" && N !== null && S.indexOf(N) === -1 && (v.push({ obj: O, prop: C }), S.push(N));
      }
    return c2(v), m;
  }, f3 = function(m) {
    return Object.prototype.toString.call(m) === "[object RegExp]";
  }, h = function(m) {
    return !m || typeof m != "object" ? false : !!(m.constructor && m.constructor.isBuffer && m.constructor.isBuffer(m));
  }, x = function(m, v) {
    return [].concat(m, v);
  }, $ = function(m, v) {
    if (t2(m)) {
      for (var S = [], T = 0; T < m.length; T += 1)
        S.push(v(m[T]));
      return S;
    }
    return v(m);
  };
  return sr2 = {
    arrayToObject: s,
    assign: o,
    combine: x,
    compact: y2,
    decode: i4,
    encode: d,
    isBuffer: h,
    isRegExp: f3,
    maybeMap: $,
    merge: r
  }, sr2;
}
var lr;
var Dn;
function Ss() {
  if (Dn) return lr;
  Dn = 1;
  var e = js(), n = Pa(), t2 = Tr(), a2 = Object.prototype.hasOwnProperty, c2 = {
    brackets: function(u2) {
      return u2 + "[]";
    },
    comma: "comma",
    indices: function(u2, m) {
      return u2 + "[" + m + "]";
    },
    repeat: function(u2) {
      return u2;
    }
  }, s = Array.isArray, r = Array.prototype.push, o = function($, u2) {
    r.apply($, s(u2) ? u2 : [u2]);
  }, i4 = Date.prototype.toISOString, l = t2.default, d = {
    addQueryPrefix: false,
    allowDots: false,
    allowEmptyArrays: false,
    arrayFormat: "indices",
    charset: "utf-8",
    charsetSentinel: false,
    delimiter: "&",
    encode: true,
    encodeDotInKeys: false,
    encoder: n.encode,
    encodeValuesOnly: false,
    format: l,
    formatter: t2.formatters[l],
    // deprecated
    indices: false,
    serializeDate: function(u2) {
      return i4.call(u2);
    },
    skipNulls: false,
    strictNullHandling: false
  }, y2 = function(u2) {
    return typeof u2 == "string" || typeof u2 == "number" || typeof u2 == "boolean" || typeof u2 == "symbol" || typeof u2 == "bigint";
  }, f3 = {}, h = function $(u2, m, v, S, T, p, O, g4, R2, C, N, B, P2, oe, H2, D, se, Z) {
    for (var A2 = u2, re = Z, E2 = 0, k = false; (re = re.get(f3)) !== void 0 && !k; ) {
      var L = re.get(u2);
      if (E2 += 1, typeof L < "u") {
        if (L === E2)
          throw new RangeError("Cyclic object value");
        k = true;
      }
      typeof re.get(f3) > "u" && (E2 = 0);
    }
    if (typeof C == "function" ? A2 = C(m, A2) : A2 instanceof Date ? A2 = P2(A2) : v === "comma" && s(A2) && (A2 = n.maybeMap(A2, function(Y) {
      return Y instanceof Date ? P2(Y) : Y;
    })), A2 === null) {
      if (p)
        return R2 && !D ? R2(m, d.encoder, se, "key", oe) : m;
      A2 = "";
    }
    if (y2(A2) || n.isBuffer(A2)) {
      if (R2) {
        var q2 = D ? m : R2(m, d.encoder, se, "key", oe);
        return [H2(q2) + "=" + H2(R2(A2, d.encoder, se, "value", oe))];
      }
      return [H2(m) + "=" + H2(String(A2))];
    }
    var U2 = [];
    if (typeof A2 > "u")
      return U2;
    var J;
    if (v === "comma" && s(A2))
      D && R2 && (A2 = n.maybeMap(A2, R2)), J = [{ value: A2.length > 0 ? A2.join(",") || null : void 0 }];
    else if (s(C))
      J = C;
    else {
      var z = Object.keys(A2);
      J = N ? z.sort(N) : z;
    }
    var K = g4 ? m.replace(/\./g, "%2E") : m, ne2 = S && s(A2) && A2.length === 1 ? K + "[]" : K;
    if (T && s(A2) && A2.length === 0)
      return ne2 + "[]";
    for (var V2 = 0; V2 < J.length; ++V2) {
      var I = J[V2], F = typeof I == "object" && typeof I.value < "u" ? I.value : A2[I];
      if (!(O && F === null)) {
        var _ = B && g4 ? I.replace(/\./g, "%2E") : I, Q = s(A2) ? typeof v == "function" ? v(ne2, _) : ne2 : ne2 + (B ? "." + _ : "[" + _ + "]");
        Z.set(u2, E2);
        var W = e();
        W.set(f3, Z), o(U2, $(
          F,
          Q,
          v,
          S,
          T,
          p,
          O,
          g4,
          v === "comma" && D && s(A2) ? null : R2,
          C,
          N,
          B,
          P2,
          oe,
          H2,
          D,
          se,
          W
        ));
      }
    }
    return U2;
  }, x = function(u2) {
    if (!u2)
      return d;
    if (typeof u2.allowEmptyArrays < "u" && typeof u2.allowEmptyArrays != "boolean")
      throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
    if (typeof u2.encodeDotInKeys < "u" && typeof u2.encodeDotInKeys != "boolean")
      throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
    if (u2.encoder !== null && typeof u2.encoder < "u" && typeof u2.encoder != "function")
      throw new TypeError("Encoder has to be a function.");
    var m = u2.charset || d.charset;
    if (typeof u2.charset < "u" && u2.charset !== "utf-8" && u2.charset !== "iso-8859-1")
      throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
    var v = t2.default;
    if (typeof u2.format < "u") {
      if (!a2.call(t2.formatters, u2.format))
        throw new TypeError("Unknown format option provided.");
      v = u2.format;
    }
    var S = t2.formatters[v], T = d.filter;
    (typeof u2.filter == "function" || s(u2.filter)) && (T = u2.filter);
    var p;
    if (u2.arrayFormat in c2 ? p = u2.arrayFormat : "indices" in u2 ? p = u2.indices ? "indices" : "repeat" : p = d.arrayFormat, "commaRoundTrip" in u2 && typeof u2.commaRoundTrip != "boolean")
      throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
    var O = typeof u2.allowDots > "u" ? u2.encodeDotInKeys === true ? true : d.allowDots : !!u2.allowDots;
    return {
      addQueryPrefix: typeof u2.addQueryPrefix == "boolean" ? u2.addQueryPrefix : d.addQueryPrefix,
      allowDots: O,
      allowEmptyArrays: typeof u2.allowEmptyArrays == "boolean" ? !!u2.allowEmptyArrays : d.allowEmptyArrays,
      arrayFormat: p,
      charset: m,
      charsetSentinel: typeof u2.charsetSentinel == "boolean" ? u2.charsetSentinel : d.charsetSentinel,
      commaRoundTrip: u2.commaRoundTrip,
      delimiter: typeof u2.delimiter > "u" ? d.delimiter : u2.delimiter,
      encode: typeof u2.encode == "boolean" ? u2.encode : d.encode,
      encodeDotInKeys: typeof u2.encodeDotInKeys == "boolean" ? u2.encodeDotInKeys : d.encodeDotInKeys,
      encoder: typeof u2.encoder == "function" ? u2.encoder : d.encoder,
      encodeValuesOnly: typeof u2.encodeValuesOnly == "boolean" ? u2.encodeValuesOnly : d.encodeValuesOnly,
      filter: T,
      format: v,
      formatter: S,
      serializeDate: typeof u2.serializeDate == "function" ? u2.serializeDate : d.serializeDate,
      skipNulls: typeof u2.skipNulls == "boolean" ? u2.skipNulls : d.skipNulls,
      sort: typeof u2.sort == "function" ? u2.sort : null,
      strictNullHandling: typeof u2.strictNullHandling == "boolean" ? u2.strictNullHandling : d.strictNullHandling
    };
  };
  return lr = function($, u2) {
    var m = $, v = x(u2), S, T;
    typeof v.filter == "function" ? (T = v.filter, m = T("", m)) : s(v.filter) && (T = v.filter, S = T);
    var p = [];
    if (typeof m != "object" || m === null)
      return "";
    var O = c2[v.arrayFormat], g4 = O === "comma" && v.commaRoundTrip;
    S || (S = Object.keys(m)), v.sort && S.sort(v.sort);
    for (var R2 = e(), C = 0; C < S.length; ++C) {
      var N = S[C];
      v.skipNulls && m[N] === null || o(p, h(
        m[N],
        N,
        O,
        g4,
        v.allowEmptyArrays,
        v.strictNullHandling,
        v.skipNulls,
        v.encodeDotInKeys,
        v.encode ? v.encoder : null,
        v.filter,
        v.sort,
        v.allowDots,
        v.serializeDate,
        v.format,
        v.formatter,
        v.encodeValuesOnly,
        v.charset,
        R2
      ));
    }
    var B = p.join(v.delimiter), P2 = v.addQueryPrefix === true ? "?" : "";
    return v.charsetSentinel && (v.charset === "iso-8859-1" ? P2 += "utf8=%26%2310003%3B&" : P2 += "utf8=%E2%9C%93&"), B.length > 0 ? P2 + B : "";
  }, lr;
}
var cr;
var Qn;
function Os() {
  if (Qn) return cr;
  Qn = 1;
  var e = Pa(), n = Object.prototype.hasOwnProperty, t2 = Array.isArray, a2 = {
    allowDots: false,
    allowEmptyArrays: false,
    allowPrototypes: false,
    allowSparse: false,
    arrayLimit: 20,
    charset: "utf-8",
    charsetSentinel: false,
    comma: false,
    decodeDotInKeys: false,
    decoder: e.decode,
    delimiter: "&",
    depth: 5,
    duplicates: "combine",
    ignoreQueryPrefix: false,
    interpretNumericEntities: false,
    parameterLimit: 1e3,
    parseArrays: true,
    plainObjects: false,
    strictDepth: false,
    strictNullHandling: false
  }, c2 = function(f3) {
    return f3.replace(/&#(\d+);/g, function(h, x) {
      return String.fromCharCode(parseInt(x, 10));
    });
  }, s = function(f3, h) {
    return f3 && typeof f3 == "string" && h.comma && f3.indexOf(",") > -1 ? f3.split(",") : f3;
  }, r = "utf8=%26%2310003%3B", o = "utf8=%E2%9C%93", i4 = function(h, x) {
    var $ = { __proto__: null }, u2 = x.ignoreQueryPrefix ? h.replace(/^\?/, "") : h;
    u2 = u2.replace(/%5B/gi, "[").replace(/%5D/gi, "]");
    var m = x.parameterLimit === 1 / 0 ? void 0 : x.parameterLimit, v = u2.split(x.delimiter, m), S = -1, T, p = x.charset;
    if (x.charsetSentinel)
      for (T = 0; T < v.length; ++T)
        v[T].indexOf("utf8=") === 0 && (v[T] === o ? p = "utf-8" : v[T] === r && (p = "iso-8859-1"), S = T, T = v.length);
    for (T = 0; T < v.length; ++T)
      if (T !== S) {
        var O = v[T], g4 = O.indexOf("]="), R2 = g4 === -1 ? O.indexOf("=") : g4 + 1, C, N;
        R2 === -1 ? (C = x.decoder(O, a2.decoder, p, "key"), N = x.strictNullHandling ? null : "") : (C = x.decoder(O.slice(0, R2), a2.decoder, p, "key"), N = e.maybeMap(
          s(O.slice(R2 + 1), x),
          function(P2) {
            return x.decoder(P2, a2.decoder, p, "value");
          }
        )), N && x.interpretNumericEntities && p === "iso-8859-1" && (N = c2(N)), O.indexOf("[]=") > -1 && (N = t2(N) ? [N] : N);
        var B = n.call($, C);
        B && x.duplicates === "combine" ? $[C] = e.combine($[C], N) : (!B || x.duplicates === "last") && ($[C] = N);
      }
    return $;
  }, l = function(f3, h, x, $) {
    for (var u2 = $ ? h : s(h, x), m = f3.length - 1; m >= 0; --m) {
      var v, S = f3[m];
      if (S === "[]" && x.parseArrays)
        v = x.allowEmptyArrays && (u2 === "" || x.strictNullHandling && u2 === null) ? [] : [].concat(u2);
      else {
        v = x.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
        var T = S.charAt(0) === "[" && S.charAt(S.length - 1) === "]" ? S.slice(1, -1) : S, p = x.decodeDotInKeys ? T.replace(/%2E/g, ".") : T, O = parseInt(p, 10);
        !x.parseArrays && p === "" ? v = { 0: u2 } : !isNaN(O) && S !== p && String(O) === p && O >= 0 && x.parseArrays && O <= x.arrayLimit ? (v = [], v[O] = u2) : p !== "__proto__" && (v[p] = u2);
      }
      u2 = v;
    }
    return u2;
  }, d = function(h, x, $, u2) {
    if (h) {
      var m = $.allowDots ? h.replace(/\.([^.[]+)/g, "[$1]") : h, v = /(\[[^[\]]*])/, S = /(\[[^[\]]*])/g, T = $.depth > 0 && v.exec(m), p = T ? m.slice(0, T.index) : m, O = [];
      if (p) {
        if (!$.plainObjects && n.call(Object.prototype, p) && !$.allowPrototypes)
          return;
        O.push(p);
      }
      for (var g4 = 0; $.depth > 0 && (T = S.exec(m)) !== null && g4 < $.depth; ) {
        if (g4 += 1, !$.plainObjects && n.call(Object.prototype, T[1].slice(1, -1)) && !$.allowPrototypes)
          return;
        O.push(T[1]);
      }
      if (T) {
        if ($.strictDepth === true)
          throw new RangeError("Input depth exceeded depth option of " + $.depth + " and strictDepth is true");
        O.push("[" + m.slice(T.index) + "]");
      }
      return l(O, x, $, u2);
    }
  }, y2 = function(h) {
    if (!h)
      return a2;
    if (typeof h.allowEmptyArrays < "u" && typeof h.allowEmptyArrays != "boolean")
      throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
    if (typeof h.decodeDotInKeys < "u" && typeof h.decodeDotInKeys != "boolean")
      throw new TypeError("`decodeDotInKeys` option can only be `true` or `false`, when provided");
    if (h.decoder !== null && typeof h.decoder < "u" && typeof h.decoder != "function")
      throw new TypeError("Decoder has to be a function.");
    if (typeof h.charset < "u" && h.charset !== "utf-8" && h.charset !== "iso-8859-1")
      throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
    var x = typeof h.charset > "u" ? a2.charset : h.charset, $ = typeof h.duplicates > "u" ? a2.duplicates : h.duplicates;
    if ($ !== "combine" && $ !== "first" && $ !== "last")
      throw new TypeError("The duplicates option must be either combine, first, or last");
    var u2 = typeof h.allowDots > "u" ? h.decodeDotInKeys === true ? true : a2.allowDots : !!h.allowDots;
    return {
      allowDots: u2,
      allowEmptyArrays: typeof h.allowEmptyArrays == "boolean" ? !!h.allowEmptyArrays : a2.allowEmptyArrays,
      allowPrototypes: typeof h.allowPrototypes == "boolean" ? h.allowPrototypes : a2.allowPrototypes,
      allowSparse: typeof h.allowSparse == "boolean" ? h.allowSparse : a2.allowSparse,
      arrayLimit: typeof h.arrayLimit == "number" ? h.arrayLimit : a2.arrayLimit,
      charset: x,
      charsetSentinel: typeof h.charsetSentinel == "boolean" ? h.charsetSentinel : a2.charsetSentinel,
      comma: typeof h.comma == "boolean" ? h.comma : a2.comma,
      decodeDotInKeys: typeof h.decodeDotInKeys == "boolean" ? h.decodeDotInKeys : a2.decodeDotInKeys,
      decoder: typeof h.decoder == "function" ? h.decoder : a2.decoder,
      delimiter: typeof h.delimiter == "string" || e.isRegExp(h.delimiter) ? h.delimiter : a2.delimiter,
      // eslint-disable-next-line no-implicit-coercion, no-extra-parens
      depth: typeof h.depth == "number" || h.depth === false ? +h.depth : a2.depth,
      duplicates: $,
      ignoreQueryPrefix: h.ignoreQueryPrefix === true,
      interpretNumericEntities: typeof h.interpretNumericEntities == "boolean" ? h.interpretNumericEntities : a2.interpretNumericEntities,
      parameterLimit: typeof h.parameterLimit == "number" ? h.parameterLimit : a2.parameterLimit,
      parseArrays: h.parseArrays !== false,
      plainObjects: typeof h.plainObjects == "boolean" ? h.plainObjects : a2.plainObjects,
      strictDepth: typeof h.strictDepth == "boolean" ? !!h.strictDepth : a2.strictDepth,
      strictNullHandling: typeof h.strictNullHandling == "boolean" ? h.strictNullHandling : a2.strictNullHandling
    };
  };
  return cr = function(f3, h) {
    var x = y2(h);
    if (f3 === "" || f3 === null || typeof f3 > "u")
      return x.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
    for (var $ = typeof f3 == "string" ? i4(f3, x) : f3, u2 = x.plainObjects ? /* @__PURE__ */ Object.create(null) : {}, m = Object.keys($), v = 0; v < m.length; ++v) {
      var S = m[v], T = d(S, $[S], x, typeof f3 == "string");
      u2 = e.merge(u2, T, x);
    }
    return x.allowSparse === true ? u2 : e.compact(u2);
  }, cr;
}
var ur;
var Kn;
function Ca() {
  if (Kn) return ur;
  Kn = 1;
  var e = Ss(), n = Os(), t2 = Tr();
  return ur = {
    formats: t2,
    parse: n,
    stringify: e
  }, ur;
}
var Yn;
function Ts() {
  if (Yn) return Ce;
  Yn = 1;
  var e = Ji();
  function n() {
    this.protocol = null, this.slashes = null, this.auth = null, this.host = null, this.port = null, this.hostname = null, this.hash = null, this.search = null, this.query = null, this.pathname = null, this.path = null, this.href = null;
  }
  var t2 = /^([a-z0-9.+-]+:)/i, a2 = /:[0-9]*$/, c2 = /^(\/\/?(?!\/)[^?\s]*)(\?[^\s]*)?$/, s = [
    "<",
    ">",
    '"',
    "`",
    " ",
    "\r",
    `
`,
    "	"
  ], r = [
    "{",
    "}",
    "|",
    "\\",
    "^",
    "`"
  ].concat(s), o = ["'"].concat(r), i4 = [
    "%",
    "/",
    "?",
    ";",
    "#"
  ].concat(o), l = [
    "/",
    "?",
    "#"
  ], d = 255, y2 = /^[+a-z0-9A-Z_-]{0,63}$/, f3 = /^([+a-z0-9A-Z_-]{0,63})(.*)$/, h = {
    javascript: true,
    "javascript:": true
  }, x = {
    javascript: true,
    "javascript:": true
  }, $ = {
    http: true,
    https: true,
    ftp: true,
    gopher: true,
    file: true,
    "http:": true,
    "https:": true,
    "ftp:": true,
    "gopher:": true,
    "file:": true
  }, u2 = Ca();
  function m(p, O, g4) {
    if (p && typeof p == "object" && p instanceof n)
      return p;
    var R2 = new n();
    return R2.parse(p, O, g4), R2;
  }
  n.prototype.parse = function(p, O, g4) {
    if (typeof p != "string")
      throw new TypeError("Parameter 'url' must be a string, not " + typeof p);
    var R2 = p.indexOf("?"), C = R2 !== -1 && R2 < p.indexOf("#") ? "?" : "#", N = p.split(C), B = /\\/g;
    N[0] = N[0].replace(B, "/"), p = N.join(C);
    var P2 = p;
    if (P2 = P2.trim(), !g4 && p.split("#").length === 1) {
      var oe = c2.exec(P2);
      if (oe)
        return this.path = P2, this.href = P2, this.pathname = oe[1], oe[2] ? (this.search = oe[2], O ? this.query = u2.parse(this.search.substr(1)) : this.query = this.search.substr(1)) : O && (this.search = "", this.query = {}), this;
    }
    var H2 = t2.exec(P2);
    if (H2) {
      H2 = H2[0];
      var D = H2.toLowerCase();
      this.protocol = D, P2 = P2.substr(H2.length);
    }
    if (g4 || H2 || P2.match(/^\/\/[^@/]+@[^@/]+/)) {
      var se = P2.substr(0, 2) === "//";
      se && !(H2 && x[H2]) && (P2 = P2.substr(2), this.slashes = true);
    }
    if (!x[H2] && (se || H2 && !$[H2])) {
      for (var Z = -1, A2 = 0; A2 < l.length; A2++) {
        var re = P2.indexOf(l[A2]);
        re !== -1 && (Z === -1 || re < Z) && (Z = re);
      }
      var E2, k;
      Z === -1 ? k = P2.lastIndexOf("@") : k = P2.lastIndexOf("@", Z), k !== -1 && (E2 = P2.slice(0, k), P2 = P2.slice(k + 1), this.auth = decodeURIComponent(E2)), Z = -1;
      for (var A2 = 0; A2 < i4.length; A2++) {
        var re = P2.indexOf(i4[A2]);
        re !== -1 && (Z === -1 || re < Z) && (Z = re);
      }
      Z === -1 && (Z = P2.length), this.host = P2.slice(0, Z), P2 = P2.slice(Z), this.parseHost(), this.hostname = this.hostname || "";
      var L = this.hostname[0] === "[" && this.hostname[this.hostname.length - 1] === "]";
      if (!L)
        for (var q2 = this.hostname.split(/\./), A2 = 0, U2 = q2.length; A2 < U2; A2++) {
          var J = q2[A2];
          if (J && !J.match(y2)) {
            for (var z = "", K = 0, ne2 = J.length; K < ne2; K++)
              J.charCodeAt(K) > 127 ? z += "x" : z += J[K];
            if (!z.match(y2)) {
              var V2 = q2.slice(0, A2), I = q2.slice(A2 + 1), F = J.match(f3);
              F && (V2.push(F[1]), I.unshift(F[2])), I.length && (P2 = "/" + I.join(".") + P2), this.hostname = V2.join(".");
              break;
            }
          }
        }
      this.hostname.length > d ? this.hostname = "" : this.hostname = this.hostname.toLowerCase(), L || (this.hostname = e.toASCII(this.hostname));
      var _ = this.port ? ":" + this.port : "", Q = this.hostname || "";
      this.host = Q + _, this.href += this.host, L && (this.hostname = this.hostname.substr(1, this.hostname.length - 2), P2[0] !== "/" && (P2 = "/" + P2));
    }
    if (!h[D])
      for (var A2 = 0, U2 = o.length; A2 < U2; A2++) {
        var W = o[A2];
        if (P2.indexOf(W) !== -1) {
          var Y = encodeURIComponent(W);
          Y === W && (Y = escape(W)), P2 = P2.split(W).join(Y);
        }
      }
    var ae2 = P2.indexOf("#");
    ae2 !== -1 && (this.hash = P2.substr(ae2), P2 = P2.slice(0, ae2));
    var he = P2.indexOf("?");
    if (he !== -1 ? (this.search = P2.substr(he), this.query = P2.substr(he + 1), O && (this.query = u2.parse(this.query)), P2 = P2.slice(0, he)) : O && (this.search = "", this.query = {}), P2 && (this.pathname = P2), $[D] && this.hostname && !this.pathname && (this.pathname = "/"), this.pathname || this.search) {
      var _ = this.pathname || "", Se = this.search || "";
      this.path = _ + Se;
    }
    return this.href = this.format(), this;
  };
  function v(p) {
    return typeof p == "string" && (p = m(p)), p instanceof n ? p.format() : n.prototype.format.call(p);
  }
  n.prototype.format = function() {
    var p = this.auth || "";
    p && (p = encodeURIComponent(p), p = p.replace(/%3A/i, ":"), p += "@");
    var O = this.protocol || "", g4 = this.pathname || "", R2 = this.hash || "", C = false, N = "";
    this.host ? C = p + this.host : this.hostname && (C = p + (this.hostname.indexOf(":") === -1 ? this.hostname : "[" + this.hostname + "]"), this.port && (C += ":" + this.port)), this.query && typeof this.query == "object" && Object.keys(this.query).length && (N = u2.stringify(this.query, {
      arrayFormat: "repeat",
      addQueryPrefix: false
    }));
    var B = this.search || N && "?" + N || "";
    return O && O.substr(-1) !== ":" && (O += ":"), this.slashes || (!O || $[O]) && C !== false ? (C = "//" + (C || ""), g4 && g4.charAt(0) !== "/" && (g4 = "/" + g4)) : C || (C = ""), R2 && R2.charAt(0) !== "#" && (R2 = "#" + R2), B && B.charAt(0) !== "?" && (B = "?" + B), g4 = g4.replace(/[?#]/g, function(P2) {
      return encodeURIComponent(P2);
    }), B = B.replace("#", "%23"), O + C + g4 + B + R2;
  };
  function S(p, O) {
    return m(p, false, true).resolve(O);
  }
  n.prototype.resolve = function(p) {
    return this.resolveObject(m(p, false, true)).format();
  };
  function T(p, O) {
    return p ? m(p, false, true).resolveObject(O) : O;
  }
  return n.prototype.resolveObject = function(p) {
    if (typeof p == "string") {
      var O = new n();
      O.parse(p, false, true), p = O;
    }
    for (var g4 = new n(), R2 = Object.keys(this), C = 0; C < R2.length; C++) {
      var N = R2[C];
      g4[N] = this[N];
    }
    if (g4.hash = p.hash, p.href === "")
      return g4.href = g4.format(), g4;
    if (p.slashes && !p.protocol) {
      for (var B = Object.keys(p), P2 = 0; P2 < B.length; P2++) {
        var oe = B[P2];
        oe !== "protocol" && (g4[oe] = p[oe]);
      }
      return $[g4.protocol] && g4.hostname && !g4.pathname && (g4.pathname = "/", g4.path = g4.pathname), g4.href = g4.format(), g4;
    }
    if (p.protocol && p.protocol !== g4.protocol) {
      if (!$[p.protocol]) {
        for (var H2 = Object.keys(p), D = 0; D < H2.length; D++) {
          var se = H2[D];
          g4[se] = p[se];
        }
        return g4.href = g4.format(), g4;
      }
      if (g4.protocol = p.protocol, !p.host && !x[p.protocol]) {
        for (var U2 = (p.pathname || "").split("/"); U2.length && !(p.host = U2.shift()); )
          ;
        p.host || (p.host = ""), p.hostname || (p.hostname = ""), U2[0] !== "" && U2.unshift(""), U2.length < 2 && U2.unshift(""), g4.pathname = U2.join("/");
      } else
        g4.pathname = p.pathname;
      if (g4.search = p.search, g4.query = p.query, g4.host = p.host || "", g4.auth = p.auth, g4.hostname = p.hostname || p.host, g4.port = p.port, g4.pathname || g4.search) {
        var Z = g4.pathname || "", A2 = g4.search || "";
        g4.path = Z + A2;
      }
      return g4.slashes = g4.slashes || p.slashes, g4.href = g4.format(), g4;
    }
    var re = g4.pathname && g4.pathname.charAt(0) === "/", E2 = p.host || p.pathname && p.pathname.charAt(0) === "/", k = E2 || re || g4.host && p.pathname, L = k, q2 = g4.pathname && g4.pathname.split("/") || [], U2 = p.pathname && p.pathname.split("/") || [], J = g4.protocol && !$[g4.protocol];
    if (J && (g4.hostname = "", g4.port = null, g4.host && (q2[0] === "" ? q2[0] = g4.host : q2.unshift(g4.host)), g4.host = "", p.protocol && (p.hostname = null, p.port = null, p.host && (U2[0] === "" ? U2[0] = p.host : U2.unshift(p.host)), p.host = null), k = k && (U2[0] === "" || q2[0] === "")), E2)
      g4.host = p.host || p.host === "" ? p.host : g4.host, g4.hostname = p.hostname || p.hostname === "" ? p.hostname : g4.hostname, g4.search = p.search, g4.query = p.query, q2 = U2;
    else if (U2.length)
      q2 || (q2 = []), q2.pop(), q2 = q2.concat(U2), g4.search = p.search, g4.query = p.query;
    else if (p.search != null) {
      if (J) {
        g4.host = q2.shift(), g4.hostname = g4.host;
        var z = g4.host && g4.host.indexOf("@") > 0 ? g4.host.split("@") : false;
        z && (g4.auth = z.shift(), g4.hostname = z.shift(), g4.host = g4.hostname);
      }
      return g4.search = p.search, g4.query = p.query, (g4.pathname !== null || g4.search !== null) && (g4.path = (g4.pathname ? g4.pathname : "") + (g4.search ? g4.search : "")), g4.href = g4.format(), g4;
    }
    if (!q2.length)
      return g4.pathname = null, g4.search ? g4.path = "/" + g4.search : g4.path = null, g4.href = g4.format(), g4;
    for (var K = q2.slice(-1)[0], ne2 = (g4.host || p.host || q2.length > 1) && (K === "." || K === "..") || K === "", V2 = 0, I = q2.length; I >= 0; I--)
      K = q2[I], K === "." ? q2.splice(I, 1) : K === ".." ? (q2.splice(I, 1), V2++) : V2 && (q2.splice(I, 1), V2--);
    if (!k && !L)
      for (; V2--; V2)
        q2.unshift("..");
    k && q2[0] !== "" && (!q2[0] || q2[0].charAt(0) !== "/") && q2.unshift(""), ne2 && q2.join("/").substr(-1) !== "/" && q2.push("");
    var F = q2[0] === "" || q2[0] && q2[0].charAt(0) === "/";
    if (J) {
      g4.hostname = F ? "" : q2.length ? q2.shift() : "", g4.host = g4.hostname;
      var z = g4.host && g4.host.indexOf("@") > 0 ? g4.host.split("@") : false;
      z && (g4.auth = z.shift(), g4.hostname = z.shift(), g4.host = g4.hostname);
    }
    return k = k || g4.host && q2.length, k && !F && q2.unshift(""), q2.length > 0 ? g4.pathname = q2.join("/") : (g4.pathname = null, g4.path = null), (g4.pathname !== null || g4.search !== null) && (g4.path = (g4.pathname ? g4.pathname : "") + (g4.search ? g4.search : "")), g4.auth = p.auth || g4.auth, g4.slashes = g4.slashes || p.slashes, g4.href = g4.format(), g4;
  }, n.prototype.parseHost = function() {
    var p = this.host, O = a2.exec(p);
    O && (O = O[0], O !== ":" && (this.port = O.substr(1)), p = p.substr(0, p.length - O.length)), p && (this.hostname = p);
  }, Ce.parse = m, Ce.resolve = S, Ce.resolveObject = T, Ce.format = v, Ce.Url = n, Ce;
}
var fr = Ts();
var pr = Ca();
var Ps = (e) => typeof e == "object" && "log" in e && typeof e.log == "object" && "entries" in e.log && Array.isArray(e.log.entries);
var Cs = class {
  constructor(e, n = {}) {
    this.initCalled = false, this.entries = [], this.requests = [], this.options = {}, this.options = {
      harIsAlreadyEncoded: false,
      ...n
    }, this.requests = [], Ps(e) ? this.entries = e.log.entries : this.entries = [
      {
        request: e
      }
    ];
  }
  init() {
    return this.initCalled = true, this.requests = this.entries.map(({ request: e }) => {
      const n = {
        bodySize: 0,
        headersSize: 0,
        headers: [],
        cookies: [],
        httpVersion: "HTTP/1.1",
        queryString: [],
        postData: {
          mimeType: e.postData?.mimeType || "application/octet-stream"
        },
        ...e
      };
      return n.postData && !n.postData.mimeType && (n.postData.mimeType = "application/octet-stream"), this.prepare(n, this.options);
    }), this;
  }
  prepare(e, n) {
    const t2 = {
      ...e,
      fullUrl: "",
      uriObj: {},
      queryObj: {},
      headersObj: {},
      cookiesObj: {},
      allHeaders: {}
    };
    if (t2.queryString && t2.queryString.length && (t2.queryObj = t2.queryString.reduce(Hr, {})), t2.headers && t2.headers.length) {
      const d = /^HTTP\/2/;
      t2.headersObj = t2.headers.reduce((y2, { name: f3, value: h }) => {
        const x = d.exec(t2.httpVersion) ? f3.toLocaleLowerCase() : f3;
        return {
          ...y2,
          [x]: h
        };
      }, {});
    }
    t2.cookies && t2.cookies.length && (t2.cookiesObj = t2.cookies.reduceRight(
      (d, { name: y2, value: f3 }) => ({
        ...d,
        [y2]: f3
      }),
      {}
    ));
    const a2 = t2.cookies?.map(({ name: d, value: y2 }) => n.harIsAlreadyEncoded ? `${d}=${y2}` : `${encodeURIComponent(d)}=${encodeURIComponent(y2)}`);
    switch (a2?.length && (t2.allHeaders.cookie = a2.join("; ")), t2.postData.mimeType) {
      case "multipart/mixed":
      case "multipart/related":
      case "multipart/form-data":
      case "multipart/alternative":
        if (t2.postData.text = "", t2.postData.mimeType = "multipart/form-data", t2.postData?.params) {
          const d = "---011000010111000001101001", y2 = `${d}--`, f3 = `\r
`, h = (m) => m.replace(/\n/g, "%0A").replace(/\r/g, "%0D").replace(/"/g, "%22"), x = (m) => m.replace(/\r?\n|\r/g, `\r
`), $ = [`--${d}`];
          t2.postData?.params.forEach((m, v) => {
            const S = m.name, T = m.value || "", p = m.fileName || null, O = m.contentType || "application/octet-stream";
            p ? ($.push(
              `Content-Disposition: form-data; name="${h(x(S))}"; filename="${p}"`
            ), $.push(`Content-Type: ${O}`)) : $.push(`Content-Disposition: form-data; name="${h(x(S))}"`), $.push(""), $.push(x(T)), v !== t2.postData.params.length - 1 && $.push(`--${d}`);
          }), $.push(`--${y2}`), t2.postData.boundary = d, t2.postData.text = $.join(f3);
          const u2 = fe(t2.headersObj, "content-type") || "content-type";
          t2.headersObj[u2] = `multipart/form-data; boundary=${d}`;
        }
        break;
      case "application/x-www-form-urlencoded":
        t2.postData.params ? (t2.postData.paramsObj = t2.postData.params.reduce(Hr, {}), t2.postData.text = pr.stringify(t2.postData.paramsObj)) : t2.postData.text = "";
        break;
      case "text/json":
      case "text/x-json":
      case "application/json":
      case "application/x-json":
        if (t2.postData.mimeType = "application/json", t2.postData.text)
          try {
            t2.postData.jsonObj = JSON.parse(t2.postData.text);
          } catch {
            t2.postData.mimeType = "text/plain";
          }
        break;
    }
    const c2 = {
      ...t2.allHeaders,
      ...t2.headersObj
    }, s = fr.parse(t2.url, true, true);
    t2.queryObj = {
      ...t2.queryObj,
      ...s.query
    };
    let r;
    n.harIsAlreadyEncoded ? r = pr.stringify(t2.queryObj, {
      encode: false,
      indices: false
    }) : r = pr.stringify(t2.queryObj, {
      indices: false
    });
    const o = {
      ...s,
      query: t2.queryObj,
      search: r,
      path: r ? `${s.pathname}?${r}` : s.pathname
    }, i4 = fr.format({
      ...s,
      query: null,
      search: null
    }), l = fr.format({
      ...s,
      ...o
    });
    return {
      ...t2,
      allHeaders: c2,
      fullUrl: l,
      url: i4,
      uriObj: o
    };
  }
  convert(e, n, t2) {
    this.initCalled || this.init(), !t2 && n && (t2 = n);
    const a2 = zi[e];
    if (!a2)
      return false;
    const { convert: c2 } = a2.clientsById[n || a2.info.default];
    return this.requests.map((r) => c2(r, t2));
  }
};
var qs = ({
  operation: e,
  selectedServer: n,
  exampleBody: t2
}) => new Cs({
  method: e.method.toUpperCase(),
  url: n + e.path.replaceAll("{", ":").replaceAll("}", ""),
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  postData: t2,
  headers: [
    ...t2.text ? [{ name: "Content-Type", value: t2.mimeType }] : [],
    ...e.parameters?.filter((a2) => a2.in === "header" && a2.required === true).map((a2) => ({
      name: a2.name,
      value: a2.schema?.default ?? a2.examples?.find((c2) => c2.value)?.value ?? (a2.schema?.type === "string" ? "<string>" : a2.schema?.type === "number" || a2.schema?.type === "integer" ? "<number>" : a2.schema?.type === "boolean" ? "<bool>" : "<value>")
    })) ?? []
  ],
  queryString: e.parameters?.filter((a2) => a2.in === "query" && a2.required === true).map((a2) => ({
    name: a2.name,
    value: a2.schema?.default ?? a2.examples?.find((c2) => c2.value)?.value ?? (a2.schema?.type === "string" ? "<string>" : a2.schema?.type === "number" || a2.schema?.type === "integer" ? "<number>" : a2.schema?.type === "boolean" ? "<bool>" : "<value>")
  })) ?? [],
  httpVersion: "",
  cookies: [],
  headersSize: 0,
  bodySize: 0
});
var Es = (e, n) => {
  let t2;
  switch (n) {
    case "shell":
      t2 = e.convert("shell", "curl");
      break;
    case "js":
      t2 = e.convert("javascript", "fetch");
      break;
    case "python":
      t2 = e.convert("python", "requests");
      break;
    case "java":
      t2 = e.convert("java", "okhttp");
      break;
    case "go":
      t2 = e.convert("go", "native");
      break;
    case "csharp":
      t2 = e.convert("csharp", "httpclient");
      break;
    case "kotlin":
      t2 = e.convert("kotlin", "okhttp");
      break;
    case "objc":
      t2 = e.convert("objc", "nsurlsession");
      break;
    case "php":
      t2 = e.convert("php", "http2");
      break;
    case "ruby":
      t2 = e.convert("ruby");
      break;
    case "swift":
      t2 = e.convert("swift");
      break;
    default:
      t2 = e.convert("shell");
      break;
  }
  return t2 ? t2[0] : "";
};
var be = {
  green: "text-green-600",
  blue: "text-sky-600",
  yellow: "text-yellow-600",
  red: "text-red-600",
  purple: "text-purple-600",
  indigo: "text-indigo-600",
  gray: "text-gray-600"
};
var Rs = {
  get: be.green,
  post: be.blue,
  put: be.yellow,
  delete: be.red,
  patch: be.purple,
  options: be.indigo,
  head: be.gray,
  trace: be.gray
};
var qa = (e) => Rs[e.toLocaleLowerCase()] ?? be.gray;
var ks = xr(
  /* GraphQL */
  `
  query getServerQuery($input: JSON!, $type: SchemaType!) {
    schema(input: $input, type: $type) {
      url
      servers {
        url
      }
    }
  }
`
);
var Ns = [
  { value: "shell", label: "cURL" },
  { value: "js", label: "JavaScript" },
  { value: "python", label: "Python" },
  { value: "java", label: "Java" },
  { value: "go", label: "Go" },
  { value: "csharp", label: "C#" },
  { value: "kotlin", label: "Kotlin" },
  { value: "objc", label: "Objective-C" },
  { value: "php", label: "PHP" },
  { value: "ruby", label: "Ruby" },
  { value: "swift", label: "Swift" }
];
var As = ({
  operation: e,
  selectedResponse: n,
  onSelectResponse: t2
}) => {
  const { input: a2, type: c2, options: s } = pa(), r = Ot(), o = ir(ks, { input: a2, type: c2 }), i4 = Xt(o), l = P(), d = qa(e.method), [y2, f3] = hr(), [, h] = (0, import_react2.useTransition)(), [x, $] = (0, import_react2.useState)(), u2 = y2.get("lang") ?? s?.examplesLanguage ?? "shell", m = e.requestBody?.content, v = m && s?.transformExamples ? s.transformExamples({
    auth: r,
    type: "request",
    operation: e,
    content: m,
    context: l
  }) : m, S = ae.jsx(
    Qr,
    {
      path: e.path,
      renderParam: ({ name: C }) => ae.jsx(
        Yt2,
        {
          name: C,
          backgroundOpacity: "0",
          slug: `${e.slug}-${C}`,
          children: `{${C}}`
        }
      )
    }
  ), { selectedServer: T } = Cr(i4.data.schema.servers), p = (0, import_react2.useMemo)(() => {
    const C = x ?? (v?.[0]?.schema ? we(
      v[0].schema
    ) : void 0), N = qs({
      operation: e,
      selectedServer: T,
      exampleBody: C ? {
        mimeType: "application/json",
        text: JSON.stringify(C, null, 2)
      } : { mimeType: "application/json" }
    });
    return Es(N, u2);
  }, [
    x,
    v,
    e,
    T,
    u2
  ]), [O, g4] = wo({ rootMargin: "200px 0px 200px 0px" }), R2 = g4 && (e.extensions["x-explorer-enabled"] === true || e.extensions["x-zudoku-playground-enabled"] === true || e.extensions["x-explorer-enabled"] === void 0 && e.extensions["x-zudoku-playground-enabled"] === void 0 && !s?.disablePlayground);
  return ae.jsxs(
    "aside",
    {
      ref: O,
      className: "flex flex-col overflow-hidden sticky top-(--scroll-padding) gap-4",
      "data-pagefind-ignore": "all",
      children: [
        ae.jsxs($r, { children: [
          ae.jsxs(wr, { className: "flex justify-between items-center flex-nowrap py-2.5 gap-2 text-xs", children: [
            ae.jsxs("span", { className: "font-mono break-words leading-6", children: [
              ae.jsx("span", { className: mo("font-semibold", d), children: e.method.toUpperCase() }),
              " ",
              S
            ] }),
            R2 && ae.jsx(
              So,
              {
                servers: i4.data.schema.servers.map((C) => C.url),
                operation: e,
                examples: m ?? void 0
              }
            )
          ] }),
          g4 && ae.jsxs(ae.Fragment, { children: [
            ae.jsx(ya, { className: "p-0", children: ae.jsx(ha, { children: ae.jsx(
              Wc,
              {
                embedded: true,
                language: u2,
                noBackground: true,
                className: "[--scrollbar-color:gray] rounded-none text-xs max-h-[500px] p-2",
                code: p
              }
            ) }) }),
            ae.jsxs(ma, { className: "flex items-center text-xs gap-2 justify-end py-2.5", children: [
              ae.jsx("span", { children: "Show example in" }),
              ae.jsx(
                Xe,
                {
                  className: "self-start max-w-[150px]",
                  value: u2,
                  onChange: (C) => {
                    h(() => {
                      f3((N) => (N.set("lang", C.target.value), N));
                    });
                  },
                  options: Ns
                }
              )
            ] })
          ] })
        ] }),
        g4 && v && ae.jsx(
          Po,
          {
            content: v,
            onExampleChange: $
          }
        ),
        g4 && e.responses.length > 0 && ae.jsx(
          Co,
          {
            selectedResponse: n,
            onSelectResponse: t2,
            responses: e.responses.map((C) => ({
              ...C,
              content: C.content && s?.transformExamples ? s.transformExamples({
                auth: r,
                type: "response",
                context: l,
                operation: e,
                content: C.content
              }) : C.content
            }))
          }
        )
      ]
    }
  );
};
var Is = mo;
var Us = ({
  responses: e,
  selectedResponse: n,
  onSelectResponse: t2
}) => {
  const [a2, c2] = (0, import_react2.useState)(
    e[0]?.content?.[0]?.mediaType ?? ""
  ), s = e.find((o) => o.statusCode === n) ?? e[0], r = ae.jsxs("div", { className: "flex flex-col bg-muted text-muted-foreground", children: [
    ae.jsxs("div", { className: "flex flex-row items-center gap-2 justify-between px-4 py-2", children: [
      ae.jsx(List, { className: "flex flex-row font-medium text-sm gap-4", children: e.map((o) => ae.jsx(
        Trigger2,
        {
          value: o.statusCode,
          className: Is(
            "py-1 -mx-2 px-2 rounded-md",
            "data-[state=active]:dark:ring-1 data-[state=active]:dark:ring-border data-[state=active]:bg-background data-[state=active]:drop-shadow",
            "data-[state=active]:font-semibold data-[state=active]:text-foreground"
          ),
          children: o.statusCode
        },
        o.statusCode
      )) }),
      s?.content && s.content.length > 1 && ae.jsxs(
        qt,
        {
          value: a2,
          onValueChange: c2,
          children: [
            ae.jsx(Ve, { className: "h-8 mt-0 max-w-48 text-xs truncate", children: ae.jsx(Vt, { placeholder: "Select a type" }) }),
            ae.jsx(Be, { children: s.content.map((o) => ae.jsx(te, { value: o.mediaType, children: o.mediaType }, o.mediaType)) })
          ]
        }
      )
    ] }),
    s?.description && ae.jsx(
      Su,
      {
        className: "text-sm border-t px-4 py-2 text-muted-foreground",
        content: s.description
      }
    )
  ] });
  return ae.jsx("div", { className: "flex flex-col gap-2", children: ae.jsx(
    Root2,
    {
      value: n,
      onValueChange: (o) => {
        t2?.(o);
        const i4 = e.find((l) => l.statusCode === o);
        c2(i4?.content?.[0]?.mediaType ?? "");
      },
      children: e.map((o) => {
        const i4 = o.content?.find(
          (l) => l.mediaType === a2
        );
        return ae.jsx(Content2, { value: o.statusCode, children: ae.jsx(c, { schema: i4?.schema, cardHeader: r }) }, o.statusCode);
      })
    }
  ) });
};
var Ls = ["path", "query", "header", "cookie"];
var Fs = ({
  operationFragment: e,
  serverUrl: n
}) => {
  const t2 = yo(_s, e), a2 = H(
    t2.parameters ?? [],
    (i4) => i4.in
  ), { options: c2 } = pa(), s = t2.responses.at(0), [r, o] = (0, import_react2.useState)(s?.statusCode);
  return ae.jsxs("div", { children: [
    t2.deprecated && ae.jsx(_r, { variant: "muted", className: "text-xs mb-4", children: "deprecated" }),
    ae.jsxs(
      "div",
      {
        className: mo(
          "grid grid-cols-1 lg:grid-cols-[minmax(0,4fr)_minmax(0,3fr)] gap-x-8 gap-y-4 items-start",
          t2.deprecated && "opacity-50 transition hover:opacity-100"
        ),
        children: [
          ae.jsx(
            Ye2,
            {
              level: 2,
              id: t2.slug,
              registerNavigationAnchor: true,
              className: "break-all col-span-full",
              children: t2.summary
            }
          ),
          ae.jsxs("div", { className: "text-sm flex gap-2 font-mono col-span-full", children: [
            ae.jsx("span", { className: qa(t2.method), children: t2.method.toUpperCase() }),
            ae.jsxs(aE, { className: "max-w-full truncate flex cursor-pointer", children: [
              n && ae.jsx("div", { className: "text-neutral-400 dark:text-neutral-500 truncate", children: n.replace(/\/$/, "") }),
              ae.jsx("div", { className: "text-neutral-900 dark:text-neutral-200", children: t2.path })
            ] })
          ] }),
          ae.jsxs(
            "div",
            {
              className: mo(
                "flex flex-col gap-4",
                c2?.disableSidecar && "col-span-full"
              ),
              children: [
                t2.description && ae.jsx(
                  rs,
                  {
                    className: "max-w-full prose-img:max-w-prose",
                    content: t2.description
                  }
                ),
                t2.parameters && t2.parameters.length > 0 && ae.jsx(ae.Fragment, { children: Ls.flatMap(
                  (i4) => a2[i4]?.length ? ae.jsx(
                    $o,
                    {
                      summary: t2.summary ?? void 0,
                      id: t2.slug,
                      parameters: a2[i4],
                      group: i4
                    },
                    i4
                  ) : []
                ) }),
                Br(t2.requestBody?.content?.at(0)?.schema, (i4) => ae.jsxs("div", { className: "mt-4 flex flex-col gap-4", children: [
                  ae.jsxs(
                    Ye2,
                    {
                      level: 3,
                      className: "capitalize flex items-center gap-2",
                      id: `${t2.slug}/request-body`,
                      children: [
                        t2.summary && ae.jsxs(VisuallyHidden, { children: [
                          t2.summary,
                          " › "
                        ] }),
                        "Request Body",
                        " ",
                        t2.requestBody?.required === false ? ae.jsx(_r, { variant: "muted", children: "optional" }) : ""
                      ]
                    }
                  ),
                  ae.jsx(c, { schema: i4 })
                ] })),
                t2.responses.length > 0 && ae.jsxs(ae.Fragment, { children: [
                  ae.jsxs(
                    Ye2,
                    {
                      level: 3,
                      className: "capitalize mt-8 pt-8 border-t",
                      id: `${t2.slug}/responses`,
                      children: [
                        t2.summary && ae.jsxs(VisuallyHidden, { children: [
                          t2.summary,
                          " › "
                        ] }),
                        "Responses"
                      ]
                    }
                  ),
                  ae.jsx(
                    Us,
                    {
                      responses: t2.responses,
                      selectedResponse: r,
                      onSelectResponse: o
                    }
                  )
                ] })
              ]
            }
          ),
          Br(!c2?.disableSidecar, () => ae.jsx(
            As,
            {
              selectedResponse: r,
              onSelectResponse: o,
              operation: t2
            }
          ))
        ]
      },
      t2.operationId
    )
  ] });
};
function Xn(e, n = 160) {
  return e ? e.replace(/\[([^\]]+)\]\([^)]+\)/g, "$1").replace(/!\[.*?\]\(.*?\)/g, "").replace(/[_*`~]/g, "").replace(/^(?:>|\s*#+|-{3,}|\*{3,})/gm, "").replace(/[|>{}[\]]/g, "").replace(/\s+/g, " ").trim().substring(0, n).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;") : "";
}
var _s = xr(
  /* GraphQL */
  `
  fragment OperationsFragment on OperationItem {
    slug
    summary
    method
    description
    operationId
    contentTypes
    path
    deprecated
    extensions
    parameters {
      name
      in
      description
      required
      schema
      style
      explode
      examples {
        name
        description
        externalValue
        value
        summary
      }
    }
    requestBody {
      content {
        mediaType
        encoding {
          name
        }
        examples {
          name
          description
          externalValue
          value
          summary
        }
        schema
      }
      description
      required
    }
    responses {
      statusCode
      links
      description
      content {
        examples {
          name
          description
          externalValue
          value
          summary
        }
        mediaType
        encoding {
          name
        }
        schema
      }
    }
  }
`
);
var Bs = xr(
  /* GraphQL */
  `
  query SchemaWarmup($input: JSON!, $type: SchemaType!) {
    schema(input: $input, type: $type) {
      openapi
    }
  }
`
);
var Hs = xr(
  /* GraphQL */
  `
  query OperationsForTag(
    $input: JSON!
    $type: SchemaType!
    $tag: String
    $untagged: Boolean
  ) {
    schema(input: $input, type: $type) {
      servers {
        url
      }
      description
      summary
      title
      url
      version
      tag(slug: $tag, untagged: $untagged) {
        name
        description
        operations {
          slug
          ...OperationsFragment
        }
        next {
          name
          slug
        }
        prev {
          name
          slug
        }
      }
    }
  }
`
);
var ll = ({
  tag: e,
  untagged: n
}) => {
  const { input: t2, type: a2, versions: c2, version: s, options: r } = pa(), { tag: o } = sr(), i4 = ir(Hs, {
    input: t2,
    type: a2,
    tag: e ?? o,
    untagged: n
  }), l = Xt(i4), {
    data: { schema: d }
  } = l, { selectedServer: y2 } = Cr(d.servers), f3 = d.title, h = d.summary, x = d.description, $ = ne(), u2 = ir(Bs, { input: t2, type: a2 });
  if (Yt({
    ...u2,
    enabled: typeof window < "u",
    notifyOnChangeProps: []
  }), je(), !d.tag)
    return ae.jsxs("div", { className: "flex flex-col h-full items-center justify-center text-center", children: [
      ae.jsx("div", { className: "text-muted-foreground font-medium", children: "No operations found" }),
      ae.jsx("div", { className: "mt-2 text-sm text-muted-foreground", children: "This API doesn't have any operations defined yet." })
    ] });
  const { operations: m, next: v, prev: S, description: T } = d.tag, p = T ? Xn(T) : h || (x ? Xn(x) : void 0), O = Object.entries(c2).length > 1, g4 = r?.showVersionSelect === "always" || O && r?.showVersionSelect !== "hide", R2 = {
    prev: S?.name ? { to: `../${S.slug}`, label: S.name } : void 0,
    next: v ? {
      to: `../${v.slug ?? Ge}`,
      label: v.name ?? "Other endpoints"
    } : void 0
  };
  return ae.jsxs(
    "div",
    {
      className: "pt-(--padding-content-top)",
      "data-pagefind-filter": "section:openapi",
      "data-pagefind-meta": "section:openapi",
      children: [
        ae.jsxs(Ye, { children: [
          ae.jsx("title", { children: [d.tag.name, f3].filter(Boolean).join(" - ") }),
          p && ae.jsx("meta", { name: "description", content: p })
        ] }),
        ae.jsxs("div", { className: "mb-8", children: [
          ae.jsxs(q, { className: "w-full", children: [
            ae.jsxs("div", { className: "flex flex-col gap-y-4 sm:flex-row justify-around items-start sm:items-end", children: [
              ae.jsxs("div", { className: "flex flex-col flex-1 gap-2", children: [
                ae.jsx(ao, { children: f3 }),
                ae.jsxs(
                  Ye2,
                  {
                    level: 1,
                    id: "description",
                    registerNavigationAnchor: true,
                    className: "mb-0",
                    children: [
                      d.tag.name ?? "Documentation",
                      g4 && ae.jsxs("span", { className: "text-xl text-muted-foreground ms-1.5", children: [
                        " ",
                        "(",
                        s,
                        ")"
                      ] })
                    ]
                  }
                ),
                ae.jsx(go, {})
              ] }),
              ae.jsxs("div", { className: "flex flex-col gap-4 sm:items-end", children: [
                g4 && ae.jsxs(
                  qt,
                  {
                    onValueChange: (C) => $(c2[C]),
                    defaultValue: s,
                    disabled: !O,
                    children: [
                      ae.jsx(Ve, { className: "w-[180px]", children: ae.jsx(Vt, { placeholder: "Select version" }) }),
                      ae.jsx(Be, { children: Object.entries(c2).map(([C]) => ae.jsx(te, { value: C, children: C }, C)) })
                    ]
                  }
                ),
                d.description && ae.jsxs(Ft, { className: "flex items-center gap-1 text-sm font-medium text-muted-foreground group", children: [
                  ae.jsx("span", { children: "API information" }),
                  ae.jsx(
                    ChevronsUpDown,
                    {
                      className: "group-data-[state=open]:hidden translate-y-px",
                      size: 14
                    }
                  ),
                  ae.jsx(
                    ChevronsDownUp,
                    {
                      className: "group-data-[state=closed]:hidden translate-y-px",
                      size: 13
                    }
                  )
                ] })
              ] })
            ] }),
            d.description && ae.jsx(V, { className: "CollapsibleContent", children: ae.jsx("div", { className: "mt-4 max-w-full border rounded-sm bg-muted/25", children: ae.jsx(
              rs,
              {
                className: "max-w-full prose-img:max-w-prose border-border p-3 lg:p-5",
                content: d.description
              }
            ) }) })
          ] }),
          T && ae.jsx(
            rs,
            {
              className: "my-4 max-w-full prose-img:max-w-prose",
              content: T
            }
          )
        ] }),
        ae.jsx("hr", {}),
        ae.jsxs("div", { className: "px-6 mt-6 -mx-6 [content-visibility:auto]", children: [
          m.map((C) => ae.jsxs("div", { children: [
            ae.jsx(
              Fs,
              {
                serverUrl: y2,
                operationFragment: C
              }
            ),
            ae.jsx("hr", { className: "my-10" })
          ] }, C.slug)),
          ae.jsx(u, { className: "mb-4", ...R2 })
        ] })
      ]
    }
  );
};
export {
  ll as OperationList,
  _s as OperationsFragment
};
/*! Bundled license information:

zudoku/lib/OperationList-PnZbf3b2.js:
  (*! https://mths.be/punycode v1.4.1 by @mathias *)
*/
//# sourceMappingURL=OperationList-PnZbf3b2-4XAGMXYC.js.map
