import type { MarkdownFileInfo } from "./plugin-markdown-export.js";
import type { WorkerResult } from "./prerender/prerender.js";
export declare function generateLlmsTxtFiles({ markdownFileInfos, outputUrls, basePath, baseOutputDir, siteName, llmsTxt, llmsTxtFull, workerResults, }: {
    markdownFileInfos: MarkdownFileInfo[];
    basePath: string | undefined;
    outputUrls: string[];
    baseOutputDir: string;
    siteName?: string;
    llmsTxt?: boolean;
    llmsTxtFull?: boolean;
    workerResults: WorkerResult[];
}): Promise<void>;
