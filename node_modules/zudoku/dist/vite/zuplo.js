import bs58 from "bs58";
export const getZuploSystemConfigurations = (string) => {
    if (!string)
        return undefined;
    try {
        const zuploSystemConfigurations = JSON.parse(Buffer.from(bs58.decode(string)).toString("utf8"));
        return zuploSystemConfigurations;
    }
    catch (error) {
        // biome-ignore lint/suspicious/noConsole: Logging allowed here
        console.error(error);
        return undefined;
    }
};
//# sourceMappingURL=zuplo.js.map