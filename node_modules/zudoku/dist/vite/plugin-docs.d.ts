import type { Plugin } from "vite";
import { type ConfigWithMeta } from "../config/loader.js";
export declare const globMarkdownFiles: (config: ConfigWithMeta, options?: {
    absolute: boolean;
}) => Promise<Record<string, string>>;
/**
 * Resolves custom navigation paths and returns an updated file mapping.
 * Handles cases where navigation defines custom paths for documents.
 */
export declare const resolveCustomNavigationPaths: (config: ConfigWithMeta, fileMapping: Record<string, string>) => Promise<Record<string, string>>;
declare const viteDocsPlugin: () => Plugin;
export default viteDocsPlugin;
