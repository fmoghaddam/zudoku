import { type ViteDevServer } from "vite";
import type { LoadedConfig } from "../config/config.js";
export type IndexProgress = {
    type: "progress";
    total: number;
    current: number;
    path: string;
};
export type IndexComplete = {
    type: "complete";
    success: boolean;
    indexed: number;
    error?: string;
};
export type IndexEvent = IndexProgress | IndexComplete;
export declare function buildPagefindDevIndex(vite: ViteDevServer, config: LoadedConfig): AsyncGenerator<IndexEvent>;
