import { joinUrl } from "../../util/joinUrl.js";
export const getRelativeRedirectUrl = (redirectTo) => {
    if (!redirectTo) {
        return "/";
    }
    return redirectTo.replace(joinUrl(window.location.origin, import.meta.env.BASE_URL), "");
};
//# sourceMappingURL=relativeRedirectUrl.js.map