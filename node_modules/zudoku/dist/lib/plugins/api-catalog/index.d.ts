import type { AuthState } from "../../authentication/state.js";
import type { ZudokuPlugin } from "../../core/plugins.js";
export declare const getKey: (category: string, tag: string) => string;
export type ApiCatalogItem = {
    path: string;
    label: string;
    description: string;
    categories: CatalogCategory[];
};
export type CatalogCategory = {
    label: string;
    tags: string[];
};
export type ApiCatalogPluginOptions = {
    path: string;
    label: string;
    categories?: CatalogCategory[];
    items: ApiCatalogItem[];
    filterCatalogItems?: FilterCatalogItemsFn;
};
export type CatalogContext<ProviderData = unknown> = {
    auth: AuthState<ProviderData>;
};
export type FilterCatalogItemsFn<ProviderData = unknown> = (items: ApiCatalogItem[], { auth }: CatalogContext<ProviderData>) => ApiCatalogItem[];
export declare const apiCatalogPlugin: ({ path, items, label, categories, filterCatalogItems, }: {
    path: string;
    label: string;
    categories?: CatalogCategory[];
    items: ApiCatalogItem[];
    filterCatalogItems?: FilterCatalogItemsFn;
}) => ZudokuPlugin;
