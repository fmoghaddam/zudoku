import { useZudoku } from "../../../../components/context/ZudokuContext.js";
import { highlight } from "../../../../shiki.js";
import invariant from "../../../../util/invariant.js";
export const Highlight = ({ code, language, children, }) => {
    const { syntaxHighlighting } = useZudoku().options;
    invariant(syntaxHighlighting?.highlighter, "Highlighter not found");
    const highlightedCode = highlight(syntaxHighlighting.highlighter, code ?? children ?? "", language, syntaxHighlighting.themes);
    return highlightedCode;
};
Highlight.displayName = "Highlight";
//# sourceMappingURL=Highlight.js.map