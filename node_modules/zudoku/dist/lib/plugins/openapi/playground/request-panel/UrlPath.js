import { jsx as _jsx } from "react/jsx-runtime";
import { useFormContext } from "react-hook-form";
import { PathRenderer } from "../../../../components/PathRenderer.js";
import { ColorizedParam } from "../../ColorizedParam.js";
export const UrlPath = ({ url }) => {
    const { watch, setFocus } = useFormContext();
    const [pathParams] = watch(["pathParams"]);
    return (_jsx(PathRenderer, { path: url, renderParam: ({ name, originalValue, index }) => {
            const formValue = pathParams.find((param) => param.name === name)?.value;
            return (_jsx(ColorizedParam, { name: name, backgroundOpacity: "0", slug: name, onClick: () => setFocus(`pathParams.${index}.value`), children: formValue || originalValue }));
        } }));
};
//# sourceMappingURL=UrlPath.js.map